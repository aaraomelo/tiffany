# A cifra — o circuito autossimilar, e o que dele se colhe

Cada arquivo é **uma peça só, medida**: mede uma afirmação contra um oráculo externo e devolve
**resíduo 0** ou falha. Não se implementa a matemática — o circuito, sendo o corpo, já faz; aqui
apenas se **colhe** e se confere. C puro (libc + libm):

```
cc -O2 <arquivo>.c -lm -o <arquivo> && ./<arquivo>
```

Para rodar **tudo de uma vez** — a lista sai dos próprios papers, cada medidor roda sob teto de
memória e `timeout`, e a saída distingue três casos (verde, negativo-por-projeto, falha):

```
./tools/bateria.sh
```

Estado atual: **198 medidores — 196 verdes, 2 negativos por projeto, 0 falhas.** Os dois negativos são
`../tatoeba/ancora.c` e `../tatoeba/homogeneo.c`, que devolvem `1` porque **provam** que não existe
solução para o sistema da tradução — é o resultado documentado em `tiffany.tex` §6, não uma quebra.

As seções abaixo seguem a ordem de `teoria.tex`. Os três papers colhem daqui: `teoria.tex` (o corpo),
`microprocessador.tex` (o gabarito analógico), `tiffany.tex` (a fala) — todos na raiz.

---

## A peça, uma vez — os headers compartilhados

O gato é o mesmo em toda parte; para não copiá-lo, ele vive **uma vez** em cada header e é reusado
(`#include`). A auto-similaridade posta no próprio código — a peça, não a cópia.

| header | a peça | reusada por |
|---|---|---|
| `gp2.h`  | o gato em `GF(p²)=ℤ_p[σ]` (o plano ℂ finito) | `duais`, `navega`, `complexo`, `ordem` |
| `gf2n.h` | o corpo binário `GF(2ⁿ)` (`gfmul`, o inverso pelo dual) | `recursao`, `dimensoes` |
| `quat.h` | os quaternions `ℍ=M₂(ℤ_p)` (`mmul`, o salto) | `esquilo`, `saltos` |
| `pgm.h`  | o leitor de imagem PGM binária (`le_pgm`) | `completo`, `linear`, `ordem`, `venom` |

## §1 — a peça: o gato e o esquilo

O mesmo laço, `σ = m + 1/σ`, em toda escala.

| arquivo | o que colhe |
|---|---|
| `checkup.c`  | o gato `A_m` e o esquilo — o corpo fechado, reversível, contínuo, ordenado, completo, multidimensional |
| `esquilo.c`  | o esquilo (`det=+1`, `G⁴=I`) — o dual que traz de volta |
| `duais.c`    | os dois pontos fixos `σ` (negro/sorvedouro) e `σ'=−1/σ` (branco/fonte) — a dualidade |
| `espelho.c`| **quebrar o espelho não cria nem some espelho** — toda cisão é **partição**, e por isso conserva. **Sem um único float**: nos 256 bytes, `pop(b&0xAA) + pop(b&0x55) = pop(b)`, os cacos recompõem (`|`) e não se sobrepõem (`&` = 0), logo somar é unir. O gato é **permutação** mod `2^L` (todas as `2^2L` imagens distintas, `L=1..7`) — atravessar não perde. A mesma lei em 4, 8, 16 e 32 bits (**poucos bytes bastam**: o resto é cópia) e nos dois meios — no analógico a cisão é o **nó** de Kirchhoff, e é a mesma conta, sem casa decimal para concordar |
| `semente.c`| **o que não cabe na descrição é a SEMENTE** — e com ela a obra volta inteira. A assinatura diz *quantos*, a semente diz *quais*; o inverso reconstrói **256 de 256, bit a bit**, sem uma diferença. O espaço de sementes de cada classe é `C(4,p)·C(4,i)` e soma 256 — logo **a semente é maior onde a classe é maior**: o genérico não é o esvaziado, é onde cabe mais gente diferente. E o necrotério tem número: descartar a semente colapsa 256 em 25, **matando 231** — o limite (toda semente zero) é uma assinatura só para todos, que é o que a caixa exige para funcionar |
| `assinatura.c`| **a contagem não perde: ela ASSINA** — e o defeito era meu, de leitura. Inteiro, sem float, sem amostra, os 256 bytes varridos. Os dois papéis, que eu misturava, ficam separados: o **gato recupera** (bijeção, nada some) e a **contagem nomeia** (assinatura, agrupa por projeto). As fibras `C(4,p)·C(4,i)` somam 256 e dão **25 assinaturas**; **4 são próprias** (`0x00`, `0xAA`, `0x55`, `0xFF` — os puros, que por serem singulares não formam classe); e a **resolução** é exata: 2322 pares de 32640 assinam igual. Mesma assinatura no analógico — o que é força: uma que dependesse do meio não identificaria nada |
| `selo.c`| **o selo: uma chave emaranhada (o sistema) e uma dividida (os clientes)**. Critério exato de separabilidade — remodelando `|Ψ⟩` por uma bipartição, separável ⟺ **posto 1**: a dividida dá 1 em todo corte (um fator por cliente), a do sistema dá **2** (não se escreve como produto — por isso sela). A soma fecha em **zero**, e as fases do código (`φ = π·o/8`, raízes 16-ésimas) somam o centro — a ciclotomia. E **recupera-se por diferenças**: elas são invariantes sob deslocamento global, dando tudo menos a origem, que só a chave do sistema fixa. O selo mede-se por contagem: varrendo `Z_97`, a parte que falta tem **97** valores possíveis sem a chave do sistema e **1** com ela |
| `isserlis.c`| **o defeito `E_k` em forma fechada, ou não entra**: a esperança gaussiana é soma finita de emparelhamentos (Wick), e a maquinaria valida-se **de fora** — Isserlis contra Gauss–Hermite (exata para polinômios) em 9 momentos de uma gaussiana correlacionada, 13-14 casas. Achado que a tabela do capítulo não declara: **`N_k` é ele próprio um número de Wick** — `3,15,105,945` são os emparelhamentos de `k+1` pontos, isto é `k!!`, por força bruta. Logo `C_k² = 2·N_k·6^((k−1)/2)`, e as duas formas fechadas são **uma** identidade (`k!! = k(k−2)!!`, `a_k/b_k = 2·6^…`) |
| `prensor.c`| **o prensor é o cone que o chicote conserva** — sem ele a dinâmica não fecha (transporte sem invariante é agitação). `Q(p)=pᵗMp` com `M=[[−2,m],[m,2]]`, e `Q(Ap) = −Q(p)` **exato em inteiros** (2500 pontos, `m=1..4`): uma batida troca o sinal, duas devolvem. O cone `Q=0` **é** o par de atratores `(σ,1)` e `(σ′,1)`. E as **marcas são espirais**: quem começa fora nunca toca (`|Q|` constante em 40 batidas) e nunca escapa (`|c₂/c₁|` cai por `1/σ²` a **cada** batida) — nem chega, nem sai, enrola. Logarítmica: `Δ(log r) = log σ` a `1e-19`; com o esquilo, a áurea `r = φ^(2θ/π)` exata |
| `chicote.c`| **o chicote é o tensor**: compor gatos É a contração de Einstein `(A·B)^i_k = Σ_j A^i_j B^j_k` (exato em inteiros; para `m=1` as entradas são Fibonacci). A forma **k-ária não dá poder** — as parentizações são `Cat(k−1)` (`1,2,5,14,42,132,429`) e **todas concordam**, uma a uma: o ganho é não ter de inventar a ordem, que entre chaves simultâneas não existe. E o **mesmo flip nas duas formas bilineares**: `AᵗΩA = det(A)Ω` (o volume) e `AᵗMA = −M` (o cone, com `M = [[−2,m],[m,2]]` saindo **exato** porque `σσ'=−1` e `σ+σ'=m`) — uma só peça, dois retratos. Curvatura `N−1` conferida para `N=2..8` |
| `antissimetrico.c`| **a assimetria inicial é uma ANTIssimetria, e é a única que atravessa a dimensão**: `ω(u,u)=0` (nada, sobre um ponto) e não-degenerada (tudo, sobre pares), com `Sp` **transitiva** nos pontos (`6400/6400` transvecções construídas) — ela só fala de **diferença**. As alternantes não-degeneradas são `0` em toda dimensão ímpar e **uma só classe** em toda par (`28`, `468`, `12400` batendo `|GL_n|/|Sp_n|`; base simplética construída `468/468`), contra `≥2` classes da simétrica em `F_p` e `n+1` em `R`. O gato mora nela (`ω(Au,Av)=det(A)ω(u,v)`, `det=−1`: **anti**-conserva — a holonomia das folhas). Em `n=1` ela é **nula** e em `n=2` é `C` (`J²=−I`, `ω(u,v)=⟨u,Jv⟩`): os complexos são o primeiro lugar onde há algo. E a lei de potência sai daí: `det = Pf²` e o módulo `|x|^d` com `d=1,2,4` em `R,C,H` |
| `instrumento.c`| **o instrumento cria a assimetria**: sem lei, toda permutação preserva a estrutura (`S_N`, simetria máxima, nada distingue); com lei, só sobrevive quem **comuta** — o grupo cai de `N!` para `d^c·c!` (força bruta = fórmula; `40320→32`). **Observar é o que sobra quando a permutabilidade acaba.** E a realidade observada é produto do instrumento (`6,12,24,84,168,1` classes no mesmo corpo) — mas **coerente**: as partições formam um **retículo** de refinamentos, logo a medida escolhe a resolução e não fabrica o substrato |
| `significado.c`| **quando algo significa**: no repouso **tudo** é invariante, logo nada significa; uma lei particiona mas precisa **conservar** (`×σ` tem `N=−1` e a norma alterna — não nomeia; `×σ²` está na **borda** e nomeia); e se a lei alcança tudo, volta a não haver significado (uma classe só). O significado é uma **janela**: `1 < classes < pontos`, com lei conservativa |
| `venom.c`    | o `0` (Venom): a imagem inteira que se reparte, deixando o vértice |
| `rotaciona.c`| **rotaciona um polinômio pelo gato, desrotaciona pelo esquilo** — `σ⁻¹ = σⁿ⁻¹ − m·σⁿ⁻²` colhido da borda (sem Fermat); ida e volta exata em `n=2..8`, dado qualquer e prosa crua. E os **dois esquilos**: `σ⁻¹ = −σ'` — pelo inverso volta `A`, pelo conjugado volta `−A` (a folha) |
| `converte.c` | **converter dois polinômios quaisquer**: `C = B ⊛ A⁻¹` com `A⁻¹` do **dual** (os `n−1` conjugados de Frobenius sobre a norma, sem Fermat; em `n=4`, três batidas). Ida e volta, dado arbitrário e prosa |
| `transforma.c`| **escala · cisalhamento · rotação, e a composição**: `≤2n` cisalhamentos levam qualquer polinômio em qualquer outro (`SL_n` é transitivo — a escala só ajusta o volume), tudo reversível. E a diferença das rotas: em `GL_n` há `\|GL_n\|/(pⁿ−1)` que chegam (156 em `n=2,p=13`), no corpo há **uma** — a única que comuta com a convolução |

## §2 — os rótulos, e o gerador que já estava dado

| arquivo | o que colhe |
|---|---|
| `rotulos.c`  | **inteiro, racional e irracional são RÓTULOS** — trocam com a base: `1/φ` é irracional em `ℚ` e é `(−1,1)`, **inteiro**, em `ℤ[σ]`. Cada **dimensão é um irracional** que **colapsa em inteiro** na passagem (`σ_n = (0,1,0,…)`, grau `n` sobre `ℚ`), e um só gerador varre a base de toda reta. Logo **sem lista de primos**: o primo sai do inteiro por regra. E `√n` de todo não-quadrado tem fração contínua periódica — o período é a dobra; os quadrados não dobram (as partes **estéreis**) |
| `pi.c`       | **a reta é a órbita do 1, e até onde π comanda**: com "somar 1" + `×σ` alcança-se **todo** `GF(pⁿ)` (6 corpos); os metais são de π exatamente (`1/φ=2cos(2π/5)`, somas de Gauss `√5,√13,√17`); **π é o 0** — ciclotomia é *corte do círculo*, e `Σζ_n^k = 0` devolve o centro, com `ζⁿ=1` fechando. E o **corte**: raízes da unidade geram o **abeliano máximo** (Kronecker–Weber) — todo metal é quadrático logo está dentro, mas o **plástico** `x³−x−1` (`S₃`, disc −23) fica fora, e é o fator do furo do ouro em `n=5` |
| `estelar.c`  | **a base certa é `q=e^{−2π}`: π gera cada metal** (fonte: `broca-so/papers/corpo_estelar.tex`). Na fc **regular** `φ=[1;1,1,…]` é o pior aproximável (Hurwitz) — certo naquela base e contra o fluxo; na **base π** o ouro é **algébrico**: `R(e^{−2π}) = √(φ√5) − φ`, raiz de `x⁴+2x³−6x²−2x+1`, por **multiplicação complexa**. E há lei: `Q_m = x⁴+2mx³−6x²−2mx+1 = (x²+2σ_m x−1)(x²+2σ_m′x−1)`, com `v_m = √(σ_m²+1) − σ_m` (`m=1..8`). E o **dupolinômio é a COLISÃO** de `P_g=x²` com `P_a=mx+1` |
| `gerador.c`  | **o gerador do CORPO UNIVERSAL (enredo §150.1) — já estava dado**: a única propriedade usada é `χ_k(u+v)=χ_k(u)χ_k(v)` (o caractere leva `⊕` a `⊗`, 7680/7680), e a torção BASTA porque o dual do dual devolve o grupo — as inversas (transformada, convolução, flip) são consequências de **uma** razão, não três teoremas. O global: `p=40961, n=256, g=3` (o MENOR), `w=36043`, `r=16`, reconferidos; `ord(w)=n` exata, ida e volta em **inteiros**, três obras fundem e cada uma volta. **Um gerador e projeções**: `w_d = g^((p−1)/d)`, e os 16384 "outros geradores" são `g^j`. A cadeia **abre de baixo** nas ordens que a necessidade pede (`k=1..12`, o primo vem da ordem). E o aviso do enredo sai corrigido: fundir/abrir é **invariante** ao gerador; onde ele é areia é ao **ordenar** o dual (truncar: 256/256 discordam) |
| `gerador_analog.c`| **o mesmo gerador nos dois meios**: a torção discreta (`w=36043`, ordem 256) e a **rotação da malha LC** (`2π/n` de `ω₀=1/√(LC)`, na borda `\|λ\|=1`) — `n` passos voltam à identidade (`6e-15`), a torre **é** o zoom do §B.7, e a **convolução circular sai igual dos dois** (exata em `ℤ_p`, `8e-12` no LC, ambas batendo o oráculo `O(n²)`). Dente: errar o ângulo por 1/257 não fecha. As ordens **ímpares** (`k=3,5,7`) passam igual nos dois meios |

## §3 — as duas progressões, e o dupolinômio

| arquivo | o que colhe |
|---|---|
| `progressoes.c`| **PA e PG de ordem `k`**: a PA de ordem `k` **é** o polinômio de grau `k` (base binomial, Newton); a PG de ordem `k` é a sua imagem por `exp`, na base das potências; e a ponte `∏` (exp/log, exata em `ℤ_p`) atravessa **sem mudar a ordem**. `Δ` e `Σ` são gato e esquilo do lado `⊕` (`Δ∘Σ=id`, `Σ` sobe a ordem), Stirling é o dicionário das duas bases (`S·s=I`), e a peça `U_n(m)` **não é PA nem PG** de ordem finita — é o que fica entre as duas |
| `recursao.c` | **a multiplicação recursiva** — dim `n` pela `n−1`, a auto-similaridade posta como o produto |
| `dimensoes.c`| cada dimensão, par e ímpar, é um corpo — recursivo `=` direto |

## §4 e §5 — o corpo, e a dualidade

| arquivo | o que colhe |
|---|---|
| `ordem.c`    | o corpo é **ordenado** |
| `completo.c` | o corpo é **completo** |
| `complexo.c` | os atratores formam `ℂ` — `σ` e `σ'=σ̄`, um em cada eixo |
| `norma.c`    | os conjugados de Frobenius — o inverso pelo **dual** (`σ⁻¹` sem Fermat) |
| `sombras.c`  | a forma simplética, `k` gatos — o **lucro** do eixo ímpar |
| `saltos.c`   | os saltos entre órbitas quaisquer — os quaternions `ℍ = M₂(ℤ_p)` |
| `fermat.c`   | **Fermat cai no gato** — Wiles `ρ(Frob)` e Kummer `σ_n` são o gato `2×2` |
| `isomorfo.c` | **elétron e bóson são a mesma função de onda** — ponto a ponto (mesmo `C(n,N)`, mesma norma); a estatística está no *produto ordenado* (a string), não no estado. As **folhas**: `σσ'=−1` é a ida e volta que descola, holonomia `−1` em toda face, e a duplicidade é só das dimensões **pares** (`det A_n=(−1)^{n+1}`) |

## §6 — a dimensão é ancoragem; o contínuo é o corpo

A deformação como dinâmica, e o AGM como o invariante que a atravessa.

| arquivo | o que colhe |
|---|---|
| `quasi.c`     | **por que a dimensão 5 não tem ouro**: `2cos(2π/5)=φ−1 ∉ ℤ` e `φ(5)=4` proíbem o cinco em rede (as duas contas dão `{1,2,3,4,6}`); o furo fatora em `Φ₆` (giro) × `x³−x−1` (o menor Pisot, plástico) — a dim. 5 separa giro de crescimento; e a palavra do gato do ouro é aperiódica de complexidade `k+1` (sturmiana): o **quasicristal** |
| `entre.c`    | **há dimensão intermediária?** a variável contínua é a **inclinação** `α`: racional `p/q` → cristal de período exatamente `q`; irracional → quasicristal (`p(k)=k+1`). A transição é a escada dos convergentes (período `3→610`, concordância `6→1595`). E o intermediário é a **escala**: em toda janela finita há período local `≈L/φ` (sempre Fibonacci) — o rank só salta no limite |
| `deforma.c`  | **a deformação é a dinâmica; a simetria só ancora**: racionais densos e de **medida nula**; órbita racional fecha em `q` (cristalização sem dinâmica) vs `1/φ` que nunca fecha e tem só **3 comprimentos** de gap. A fração travada vai de **0,0000** (`K=0`) a `0,70` (`K=0,99`). E a robustez é da **classe modular**, não do número: os **nobres** (equivalentes a `φ` sob `SL(2,ℤ)`, sendo `φ` o ponto fixo do gato) ficam todos acima dos não-nobres |
| `deforma_d.c`| **a deformação em dimensões maiores (KAM)**: ressonâncias crescem como `Nᵈ` (contagem = fórmula de Delannoy), fração ressonante cresce com `d`, e no mapa **simplético** de Froeschlé o mesmo `K` destrói mais em dimensão maior (em `d=1` a virada cai sozinha no `K≈0,9716` de Greene). Entre irracionais, a curva **áurea** é a última a romper; racional não dá toro, dá **ilha** travada |
| `agm.c`      | **o AGM procura a âncora; o invariante é a integral**: `a←(a+b)/2` (⊕) e `b←√(ab)` (⊗) alternados, convergência quadrática (razão fixa `0,0858`), e `I(a,b)=I((a+b)/2,√(ab))` exata. `1/AGM = (2/π)I` — o invariante **é** o ponto de ancoragem. E a família é contínua: `τ=K′/K` varre `(0,∞)`, e as estruturas inteiras são os **singular values** `τ=√N` (CM), algébricos exatos |
| `agm_deforma.c`| **o invariante sobrevive à deformação?** Dissociação: a **velocidade** resiste (quadrática em todo `p≠1`, razão fechada `(1−p)/(8M)`) mas o **invariante** morre à primeira ordem (`\|ΔI\|/I ~ p`, expoente `0,9994`) — nenhum `p` crítico, o oposto de KAM. Sob a **isogenia** (Landen, `τ→2τ`) é indestrutível e leva âncora `τ=1` em âncora `τ=2`: sobrevive como **classe**, não como ponto |
| `agm_gerador.c`| **a dinâmica do AGM É a do gerador.** A **prova** são duas linhas: de `K(k₁)=((1+k′)/2)K(k)` e `K′(k₁)=(1+k′)K′(k)` sai `τ₁=2τ` por divisão (medi as duas separadamente, `≤5e-17`); e `(w_{2d})² = g^{2(p−1)/(2d)} = w_d`. A **forma forte**: nos dois lados a operação é um **homomorfismo de grau 2** — `x↦x²` leva `μ_{2d}` sobre `μ_d` com núcleo `{±1}` (todo degrau), e a 2-isogenia do toro tem grau 2 e núcleo 2. É a **multiplicação por 2 no grupo**, canônica. No expoente: `2·log√(ab) ≡ log a + log b` (261/261) — `⊕` é a média no **valor**, `⊗` a mesma média no **expoente** |
| `lemniscata.c`| **π se dobra, e o AGM é o fator da costura**: `ϖ = π/M(1,√2) = 2∫₀¹dt/√(1−t⁴)` pelas duas vias (erro **0**); e a costura é geral — `K(k) = π/(2M(1,k′))` para todo `k`. O círculo é `k=0` (π puro), a lemniscata é `k=1/√2` = a **primeira âncora τ=1**: a escada das curvas deformadas e a dos singular values são a mesma. Dobrar custa exatamente `M(1,√2)` |

## §7 — a colheita: os terminais, e o circuito nos dois meios

| arquivo | o que colhe |
|---|---|
| `analog.c` | as **10 colheitas** no circuito (§B.1–B.10): o translinear `ANTILOG(log a + s·log b − s·log ref)`, a convolução (`s=+1`, gato/×), a deconvolução (`s=−1`, esquilo/÷), a mult em `Rⁿ`, a interpolação — coordenadas contínuas |
| `agm_analog.c`| **o AGM colhido no circuito**: `⊕` é Kirchhoff + espelho 2:1 e `⊗` é o translinear com **somador em ganho ½** — que **dispensa a corrente de referência** (`I_S` ×10⁴ e `T` de 250–400 K não mudam nada). O laço de correntes dá o AGM dobrando os dígitos, o invariante `I(a,b)` fica fixo, e o dente (somador cheio = produto) **estoura** em 8 batidas |
| `tres_reconstroi.c` | as **três batidas** `ℱ³=ℱ⁻¹` — a prosa volta byte a byte, `16384` vetores semânticos, `0` erros |
| `interp.c` | a interpolação (`n` amostras **são** o polinômio de `Rⁿ`) — depois colhida no circuito em `analog.c` §B.9 |
| `neuronio.c` | o **digital** (bits): o gato `A_m` sobe as torres (temporal `Aᵏ→σ_m`, dimensional `Aₙ` em `Rⁿ`); o esquilo desce; os metais. *Pede um caminho de arquivo como argumento.* |
| `neuronio_analog.c` | o **analógico** (correntes): as mesmas etapas pelo translinear + Kirchhoff; alinha com o digital, resíduo 0. *Idem.* |
| `fractal.c` | valida os dois: o gato, as torres, os metais, a dualidade `gato∘esquilo=id` |
| `koch.c` | **a garrafa de Koch: completar É ficar reversível** (11 seções, 14 unidades). A garrafa tem **borda infinita em espaço finito** (o perímetro diverge por `4/3`, a área converge por `4/9` a cada nível — exato), e dimensão `log4/log3`, entre a linha e o plano. E **é essa a forma da assinatura**: informação finita nomeando alcance infinito. A escada: a assinatura sozinha colapsa 256 obras em 25 classes; **cada bit acrescentado devolve reversibilidade** (25→40→64→96→144→192→**256**), e com **6 bits** a obra volta inteira — *a assinatura vale 2 bits*. E a **mesma escada** já estava em dois sítios sem eu ver: casar harmónicos no `solar.c` e subir níveis no multinível — **uma lei, três balcões**. Depois **Julia**: sob `z↦z²`, `|z|<1` **extingue** (nilpotente, o glacial), `|z|>1` escapa, `|z|=1` **permanece** (idempotente, o tropical) — a tabela do `corpo_tropical.tex` cai no mapa de Julia sem forçar, e a **fronteira entre os dois É o conjunto de Julia**. **Cantor**: `c ∈ M` dá conexo, `c ∉ M` dá poeira — *a garrafa vista do avesso*, medida zero e cardinalidade contínua. E a lei do Aarão: **o que é reversível entra no circuito, o que não é fica na garrafa ATÉ TER DUAL** — o circuito só enche e a garrafa só esvazia (4→8→16→…→256, dobra a cada bit). *A garrafa não é cemitério, é sala de espera; completar é dar alta.* E o par **SOLAR** (tropical, quente, `e²=e`, estado) ⋈ **LUNAR** (glacial, frio, `n²=0`, fluxo), que fecham por **Peirce** — o corpo diferencial visto dos dois lados da sua dobra |
| `solar.c` | **o corpo solar: o eixo preditivo, a bateria-alfândega, e a eficiência áurea** (8 seções, 9 unidades). Sete peças que fecham numa: o **eixo preditivo** (`PA_m` de Lopes 2000 — a `m`-ésima diferença é constante, e o **Teorema da Unificação** dá predição por fórmula **fechada** `a(n+h) = Σ C(h,k)Δᵏa(n)`, exata contra a iteração até `h=20`); a **ALFÂNDEGA DIMENSIONAL** do enredo — *o que reverte passa; o que não tem dual fica retido, e o que fica retido ARDE — daí a luz*; e a **bateria É essa alfândega com terminais**: entra dual (a carga reverte), sai dual (a descarga reverte), e o **retido é exatamente `2I²Rt`** — o que não tem operação que o devolva, e por isso aquece. A **garrafa de Koch**: harmónicos de Fibonacci com amplitudes `φ^{-j}`, e daí `THD² = 1/φ` **fechado** (por `φ² = φ+1`). A eficiência é **áurea e AUTODUAL**: `FP = φ^{-1/2}` e `FP² = 1/φ = THD²` — *a perda e o ganho são o mesmo número, e encontram-se no vinco*. Casar `N` níveis da torre leva `η → 100%` (o multinível a limar, como no §M6). E as **estrelas irracionais**: `ν` racional **fecha**, `ν` irracional **nunca fecha** — e é esse o **ónus**. *Uma lei, três balcões*: o período que não existe, o algoritmo que não existe (§T2), e o `I²R` |
| `dtcn.c` | **o DTC hipercomplexo: a família ⋆_s em R^n** (8 seções, 13 unidades). Do paper do Aarão *DTC Hipercomplexo e Preditivo* (`hiper/aposentados/teoria/papers/paper_H_dtc_hipercomplexo.tex`). A família `z ⋆_s w = (a₀b₀ − ⟨a,b⟩) + (a₀b + b₀a + s·a×b)` **É** a fórmula das quatro peças, com um botão no **cruzado** — e mede-se que as outras três não veem o `s`: *mexer no s é mexer na ORDEM, não na medida*. Daí a peça nova, a **lei de conservação algébrica** `‖z⋆w‖² + (1−s²)‖a×b‖² = ‖z‖²‖w‖²`, que fecha para todo `s` e sai de **Lagrange**. O **imposto** `V(s) = (1−s²)m` anula **exatamente em s = ±1** — é o Hurwitz, mas agora a classificação não é uma lista: é o **conjunto de zeros de uma função**. Os **campos locais** `C_â` são fechados, isomorfos a `C`, e lá o imposto é **zero** — o preço está nas fronteiras. O **torque vetorial** tem 3 componentes em `n=4` (um motor esférico, SO(3)), a histerese vira **banda esférica** que em 1 componente **é** o comparador de sinal de Takahashi, e as **bandas derivam do imposto** (perto do campo local elas fecham). **ACHADO**: a forma compacta do paper `Im(conj(ψ)⋆i)` dá o **simétrico** da expandida — é a expandida que reduz ao clássico, e a redução é medida com resíduo 0 em 300 pontos. E o **ripple de torque** é ciclo limite, encolhendo `76×` com o passo — é por aqui que o multinível entra |
| `robo.c` | **a rede diferencial de motores** (6 seções, 7 unidades). O corpo de corpos posto a trabalhar: cada junta é um motor com o seu DTC, e o que os liga é o **JACOBIANO — que É o corpo diferencial**, porque `J` é a **derivada** da cinemática (medido pelos dois caminhos: forma fechada contra diferença finita). E a peça que fecha com o §B12: `ẋ = J·q̇` (a velocidade **sobe**, a torre branca), `τ = Jᵀ·F` (a força **desce**, a torre negra), e `⟨F,ẋ⟩ = ⟨τ,q̇⟩` — **a potência é a mesma dos dois lados**, resíduo 0 em 200 casos. *A adjunção do §B12 com unidade de watt, e chama-se conservação de energia.* A assimetria certa continua: `J` perde (N juntas → 2 coords), `Jᵀ` não é sobrejetiva — cada uma falha onde a outra fecha. A **singularidade É a degenerescência**: `√det(JJᵀ) = 0` no braço esticado **e** no dobrado, e ali duas direções colapsaram numa — é o `ε²=0`, a raiz dupla, e o robô avisa com o determinante a ir a zero. E o corpo de corpos **com preço**: 1 junta dá 1 grau, 2 dão 2, e a terceira **não dá 3 — dá REDUNDÂNCIA**, um núcleo de movimentos que não movem a ponta (é o que permite desviar sem largar o alvo). §R6 **controla**: a ponta chega ao alvo (erro `1e-6`) distribuindo por `Jᵀ`, e a potência fecha em todo o percurso |
| `motor.c` | **as máquinas: motor de indução, inversor e DTC** (7 seções, 9 unidades). Da **monografia do Aarão** — *Controle Direto de Torque do Motor de Indução Trifásico*, UFRR 2017, orientadora Profa. Dra. Susset Guerra Jiménez (`hiper/aposentados/teoria/papers/tcc_dtc_2017/`). E a peça central estava à espera desde o princípio: **T_e = (3/2)·P·ψ_s × i_s — O TORQUE É O PRODUTO CRUZADO**. Não é analogia: a parte **simétrica** devolve escalar e não gira nada (é a norma, é a reativa); a **antissimétrica** devolve vetor e É ela que roda o eixo — trocar `ψ` com `i` inverte o sentido de rotação. *Um motor é uma máquina de extrair a metade antissimétrica.* Medido: o cruzado é o `sen(γ)` e o direto é o `cos(γ)`; Clarke leva 3 fases pulsantes em **1 vetor girante** de módulo constante; os **8 vetores do inversor SÃO GF(2)³** (6 no hexágono a 60°, 2 nulos — as palavras constantes); `Δψ_s = v·Δt` move o fluxo e o ângulo `γ` dá o torque; e a **tabela de Takahashi sai da INTERSECÇÃO** de duas regras de histerese, única em cada caso — é a **amputação** do §A4, com o mesmo preço. O **multinível** alivia: mais níveis, mais vetores, e o erro angular nunca piora (30° com 2 níveis). E §M7 **simula a máquina com DTC**: 20000 passos, fluxo e torque nas bandas, **os 6 setores percorridos**, e os dois caminhos do torque concordam |
| `mcu.c` | **o microcontrolador multifractal: o circuito completo** (8 seções, 9 unidades). De `chess/sandbox/corpo_transistor.tex`: cada peça da corte é um bloco — **CLOCK** o Príncipe (o astável, `T = ln2·(R₁C₁+R₂C₂)`, medido pelos dois caminhos), **ALU** os Duques (JOAQUIM ⊕ o somador, YASMIN ⊗ o multiplicador), **MEMÓRIA** o grafo multifractal (`b^n`, e o **endereço É o caminho na árvore** — não há índice à parte do objeto, como a trie da cifra), **CONTROLE** o motor (o PC é o lance), **BARRAMENTO** casado ao metal (`Γ = 0 ⟺ Z = Z₀`, e o **ganho máximo cai no mesmo ponto** — otimizar é fazer o eco sumir). A ALU inteira sai do **NAND** e bate com a aritmética em **65536 pares**. O **HALT é o ponto fixo**: mil pulsos depois de parado não movem nada, `Δ = 0`. O **bustrofédon** tem passo sempre unitário (`N²−1` no total) contra o raster (`2N(N−1)`, com salto `N` a cada linha) — o boi arando, sem fio de retorno. E §U8 **RODA**: a máquina executa `1+2+…+n` e o fatorial, validados contra a conta direta. A cadeia de **oito elos** fecha — Shockley → chaveia → NAND → XOR → somador → multiplicador → ALU → ciclo → programa — e nenhum elo é postulado |
| `mmu.c` | **a MMU multifractal: o endereço `b^n` realizado EM DISCO** (6 seções, 8 unidades). O `mcu.c` §U4 mediu a bijeção endereço↔caminho *em memória*; aqui ela vira **suporte**, e o que lá era a tese é cá o problema. O motivo é físico e mede-se: a swap desta máquina é `/dev/zram0`, **8 GiB comprimidos dentro da RAM** — aqui *swap não é disco*, e quem pressiona a memória volta a pressioná-la ao transbordar. **Duas realizações** do mesmo endereço, cada uma medidor da outra: a **árvore** (o caminho na árvore É o caminho no sistema de ficheiros) e o **deslocamento** (o endereço É o offset num ficheiro esparso — 64 MiB endereçáveis por 4 KiB de disco, porque o buraco não custa). §M4 exige que os bytes batam, e foi assim que apareceu um defeito **estrutural**: sem sufixo na folha, o endereço curto cria o ficheiro que é o *diretório* de que o longo precisa — o deslocamento é auto-delimitado (o `n` entra na multiplicação), a árvore não era, porque **o caminho de um prefixo é um caminho válido**. 8 células em 586, e a conta fechou ao número exato antes de se tocar no código. E §M5 é o ponto: a RAM anónima **não cresce** com o corpus (`+0` kB enquanto as células ×64) — mas um zero é o resultado que eu queria ver, logo é suspeito, e o **controlo positivo** guarda o mesmo corpus em RAM e mede `+4096` kB com o mesmo ponteiro, batendo `4096×1024 B` **ao número**. É o controlo que transforma o zero em medida. A troca: a tabela acede em 1 passo e paga `N` células; a árvore paga `log_b N` passos e **zero**. Modo ferramenta (`mmu poe/le/topo`) serve de banco à `torre.sh` — a célula 0 é o topo, e nada fica residente entre invocações |
| `llm.c` | **a LLM mínima: montada, medida, e a ler os pesos do DISCO** (7 seções, 13 unidades). A arquitetura do Llama (= a do `qwen2.5` da torre): RMSNorm, atenção com RoPE, residual. Cada peça contra a **propriedade que a define**: o softmax soma 1 e é **invariante a deslocamento** (é isso que o torna bem-posto e justifica subtrair o máximo); o RMSNorm dá norma `√n` e é **idempotente**; o RoPE **preserva a norma** (logo é rotação) e o produto interno **só vê `m−n`** — é isso, não o rodar, que faz dele posição *relativa*. A régua também não é minha: `n·ε_float·escala`, o arredondamento acumulado. A **matmul lê linha a linha do disco** — em RAM nunca mais de uma linha, tenha a matriz mil linhas ou um milhão — validada contra o produto em RAM (§L3) e contra um bloco inteiro refeito pelo caminho direto (§L6). §L7 repete o `mmu.c` §M5: RAM plana com o modelo em disco a crescer 16×, **controlo positivo** a subir `+8192` kB batendo `8192×256×4 B` ao número. **E uma asserção minha era VAZIA**: a causalidade estragava `V` em posições que a função nunca lia (laço `t < T`, `T=corte`) — nenhuma entrada a fazia falhar. Agora mede a sequência inteira contra o prefixo, com **duas metades** (o passado não mexe **e** o presente mexe), e a mutação da máscara apanha-a: 80 componentes movem-se, falha. Falta o carregador GGUF — os pesos entram por **uma porta só**, `matmul_disco` |
| `gguf.c` | **o carregador: os pesos do llama, do disco, sem residir** (7 seções, 16 unidades). O GGUF do `qwen2.5:1.5b` — 940,4 MiB, 338 tensores, `Q4_K_M`. **Não carrega o modelo**: índice de 338 nomes (60 kB contra 940 MiB — 1:16000) e cada tensor por `pread` quando é preciso, largado a seguir. **Como validar um leitor de formato sem ser contra si próprio?** Um leitor testado contra um escritor meu partilharia o meu erro de leitura da spec — por isso os oráculos são de fora: metadados contra `ollama show` (qwen2, 1536, 32768, 28 camadas), parâmetros das formas = `1 543 714 304` ≈ os 1,5 B anunciados, e o mais duro — a soma dos 338 tensores põe o fim do último **exatamente** no fim do ficheiro, **diferença 0 B em 986 048 512**. Se o bloco `Q4_K` fosse 143 ou 145 em vez de 144, não fechava. §G5 desempacota Q4_K (escala+mínimo f16 por super-bloco, 8 pares de 6 bits, 4 bits por peso): média `−0,000167`, desvio `0,0206`, zero não-finitos. §G6 atravessa os 940 MiB e a RAM cresce `+0` kB — **lido, não residido**. §G7 projeta com `blk.0.attn_q.weight` real, RAM parada. **E diz o que NÃO prova**: linearidade e `W·e_j` apanham a máquina partir-se, não ela estar *coerentemente* errada (o zero exato é zero porque os dois caminhos partilham a aritmética de bloco). Falta o forward completo contra a saída do ollama |
| `telomero.c` | **o sistema é um computador autossimilar, e a cifra é o telómero** (7 seções, 11 unidades). O nome é exato e mede-se: a cifra é Euclides, cada passo deixa resto **estritamente** menor (logo consome-se e termina) e o que fica é o **período**, invariante por Lagrange. 79 800 pares: zero passos sem encurtar, e o mais longo tem 12 termos contra os 14,1 de **Lamé (1844)** — a régua é um teorema. §T2: **base e fração contínua são a MESMA função**, muda só qual o divisor do passo seguinte. §T3 é onde a tese fica de pé: a *mesma* NAND, sem laço **calcula** (somador, 8 de 8), com laço **guarda** (latch, o valor sobrevive à entrada) — não é analogia. Logo disco = porta fechada + telómero (§T4, 4096/4096), e memória = o mesmo sem o endereço. §T6: os metais têm período **um**, e o rei é o ouro. **E aqui errei duas vezes**: escrevi um segundo codificador em vírgula flutuante (o `cifra.h` já tem o exato, e avisa no cabeçalho contra isso) que diverge por arredondamento; e a tabela imprimia `[m; m,m,m,...]` com os `m` postos por mim no printf enquanto a asserção media outra coisa — **a tabela literária**. §T7 liga: o endereço sai do conteúdo, e o preço mede-se contra o aniversário `N(N−1)/2M` |
| `fita.c` | **a fita: o llama clonado para dentro, cada gene com o seu telómero** (4 seções, 6 unidades). Os 338 tensores **transcritos** para dentro do sistema — 934,7 MiB, sequenciais como o DNA. A **replicação** é o que faz disto medida e não cópia: 307,3 MiB relidos e comparados **byte a byte**, zero divergentes — um erro de um byte de deslocamento partia isto. Tudo com 64 kB de buffer, RAM `+128` kB para 1869 MiB de tráfego. **E os telómeros colidiam**: 472 pares em 338 genes com 6 termos, e o diagnóstico estava na tabela — **todos começam por `0 1`** (o primeiro quociente é 0 porque `255² ≫ 255·i`, o segundo é 1 pela mesma escala): dois dos seis termos eram constantes disfarçadas de cifra. Mediu-se a **lei** em vez de escolher: `4428 → 883 → 54 → 2 → 0` para 2,4,6,8,10 termos. **Bastam 10.** A informação está onde as escalas se aproximam, depois de Euclides trabalhar |
| `forward.c` · `oraculo.sh` | **o qwen a correr do disco, e o ollama como oráculo** (3 seções, 10 unidades). A rede inteira — 28 camadas, GQA 12Q/2KV, RoPE 1e6, SwiGLU, tied embeddings — contra o ollama no mesmo prompt: **texto idêntico, token a token**. É a prova que o `gguf.c` disse que faltava. **A tokenização é uma equação polinomial**: `P(texto) = Σ_j x^{d_j}·P(t_j)`, resolvida por DP (não gulosa) e verificada nas coordenadas de sempre — `R^16 = Z_p[x]/(x^16 − m·x^15 − 1)`, a borda dos metais — com os dois lados por caminhos independentes (esquerdo dos *bytes*, direito dos *tokens deslocados*). Fecha nos 3 metais × 2 primos, e a **derivada** fecha também (é ela que apanha troca de ORDEM, por pesar cada coeficiente pelo expoente). Com isso "Paris" passa de 2º a 1º. **O plugue alinhado**: desdobrar é tirar as escalas de fora do somatório, sem reconstruir o peso — mas a minha 1ª tentativa quantizou também a *ativação*, ficou 3,4× mais rápida e **desalinhou** (0,48% entre caminhos, a ordem das margens). Sem essa quantização: **0,0000%**, bate com o ollama, e ainda ~2× mais rápido. *O erro não vinha do desdobramento; vinha de eu ter quantizado o que não precisava.* **Armadilha do oráculo**: o ollama aplica `repeat_penalty 1.1` mesmo a temperatura 0 — dizia "The country" contra o nosso "The capital" porque *"capital" está no prompt* |
| `torres.c` · `assistente.sh` | **as duas torres, a convolução, e o ambiente reversível onde o llama corre** (5 seções, 6 unidades). O `base.c` §B11 tinha as torres e o `transformada.c` a transformada; faltava **a peça do meio — a CONVOLUÇÃO**, e é ela que liga as duas: `F(a ⊛ b) = F(a)·F(b)` no grupo `(Z/2)^m` (convolução do XOR), medida por dois caminhos independentes, 510 entradas, **zero divergências, tudo inteiro**. É o teorema que paga a transformada: a convolução custa `n²`, o produto ponto-a-ponto custa `n`. A **inversa** é `F∘F = n·id` — a transformada é a sua própria inversa a menos de escala, e é *isso* o ambiente reversível: a volta é exata, não aproximada. §R5 mede-o sobre material real — pesos do qwen lidos da fita, transformados e destransformados, **byte a byte sem perder um**. Isso não torna a rede reversível (uma rede não é), torna reversível o corpo onde ela mora. **E o §R1 era uma asserção VAZIA minha**: somava os mesmos quadrados em duas ordens e comparava — a soma é comutativa, nenhuma entrada a fazia falhar. O que a torre dual afirma é que a *descida não perde*: desce-se guardando saldos, sobe-se repondo, e o vetor volta exato — com **controlo positivo** (um andar perdido de propósito tem de ser detetado, e é). E `assistente.sh` liga tudo: o corpus responde o que sabe, o **decreto** ("não sei") passa a palavra ao `forward` local (disco+CPU, sem ollama nem GPU), e a resposta **volta para o corpus** — da segunda vez o modelo não é acordado |
| `teletransporte.c` | **o protocolo: mover sem copiar, com reversão garantida e resíduo 0** (6 seções, 10 unidades). **Este ficheiro autoriza a seção de teoria** — escrever o texto primeiro e medir depois é como se produz a *tabela literária*, e já aconteceu duas vezes aqui. Três cláusulas, todas medidas: entra num recipiente **finito**, atravessa por operação com **volta exata**, e o que sai é o que entrou com **resíduo 0** — não "quase". §X1 **o telómero é recipiente infinito**: a fração contínua de um irracional não termina e mesmo assim fecha num período finito (Lagrange, invariante completo) — 4 metais, período 2, irracional reconstruído com resíduo `0,00e+00`. *Não guarda as casas, guarda a regra que as gera.* §X2 **os plugues**: um polinómio de grau 200 desce a 16 coordenadas e **fica lá** (ponto fixo), 3 metais × 3 primos, resíduo 0 em todas. §X3 **a entrada na fita**: endereço e telómero **saem** do objeto, não se atribuem. §X4 **o protocolo etapa a etapa** — e mede-se cada uma porque um protocolo que só fecha no fim pode ter uma etapa que perde e outra que inventa. **E a etapa 1 era asserção vazia minha**: copiava um vetor para outro em memória e comparava; entrar na fita é entrar no **disco**. §X5 **o controlo**: um bit trocado no meio dá resíduo 128 num vetor de 128 — o bit espalha-se por todas as saídas porque a transformada é global, e é isso que a torna selo. §X6 **telemetria**: 8 tamanhos, de 4 a 512, resíduo total 0. *E o que não afirma*: a rede não fica reversível — reversível é o corpo onde ela mora |
| `ribossomo.c` | **o recipiente ribossómico: Cantor, Julia, Newton, e as duas fitas duais** (5 seções, 9 unidades). A especificação do Aarão medida peça a peça, porque cada uma tem nome próprio em matemática: **Cantor É o espaço das fitas** (um ponto seu em base 3 sem o dígito 1 *é* uma sequência binária — 4096 fitas, bijeção exata, zero dígitos 1); **`z → z²` é o DESLOCAMENTO** (para `c=0` o Julia é o círculo e dobrar o ângulo é comer um bit — 1758 passos, zero divergências): é o ribossomo a andar, e é *isso* que estica a fita, uma camada por passo; **Newton sobre `z^n − 1`** reparte o plano em `n` bacias, e a bacia é o codão que diz **qual dimensão** — as `n` raízes são as `n` dimensões, todas ocupadas para `n=2..6`. §Y4 mede que o enrolamento **desenrola sem perder**, com controlo (um bit mexido é detetado). **E o §Y5 tinha duas asserções vazias minhas e uma afirmação invertida**: escrevi que "nenhuma metade sozinha basta" quando é o oposto — no DNA cada fita **determina** a outra, e é por isso que a replicação funciona. O que a reversibilidade exige é que a complementação seja **involução**, e é isso que agora se mede, com uma regra que roda (não-involutiva) a servir de controlo |
| `furos.c` | **os furos entre as dimensões: Cantor é o direto, Julia é o cruzado** (5 seções, 8 unidades). A atribuição não é escolha — o `polar.c` já fixara que a **algébrica soma bem** e a **polar multiplica bem**: `x = Σ 2b_k/3^k` é uma *soma* de coordenadas independentes e `{0,2}^n` é o produto **direto**; `z→z²` é, na polar, `ρ²` com ângulo dobrado — *multiplicação*, o **cruzado**. **E o furo tem número**: `dim Cantor = log2/log3 = 0,630929754…`, que **não é inteira** — vive entre 0 e 1. Contada por caixas de duas formas independentes (recursão e varredura) que concordam, não pela fórmula. No direto as dimensões **somam** porque a contagem multiplica (o logaritmo é a ponte, a mesma do `∏ = exp∘Σ∘log`), e `k` cópias dão `0,63 · 1,26 · 1,89 · 2,52` — **atravessa 1 e 2 e nunca pousa**. **E eu pusera a descida como divisão**: o Aarão corrigiu — *no dual apenas troca o sinal da multiplicação* — e é literal, `σ·σ' = −1` exato em todo metal. Só a troca de sinal é **involução**, e é por isso que a torre dual volta sem resto; controlo não-involutivo incluído |
| `hurwitz.c` | **a torre de Hurwitz, e a dual que desce: o telómero é o lado de baixo** (5 seções, 7 unidades). Só há quatro álgebras de divisão normadas — `R(1) C(2) H(4) O(8)` — e o que a torna *a nossa* torre é a **meta-indução dos dois lados**: subindo cada dobra **perde** uma propriedade (limitada acima por Hurwitz), descendo cada metade **recupera** a que a de cima perdeu (limitada abaixo por `R`). As duas limitações **tocam-se**, e é esse toque — não uma escolha nossa — que fecha a torre em quatro. **O cristal é a norma**: `N(xy)=N(x)N(y)` vale em 1,2,4,8 com resíduo `0,000e+00` e **falha em 16** (`4,8e-01`) — testa-se *até* 16 de propósito, porque parar em 8 seria confirmar a metade que agrada. **A escada das perdas desencontra-se**: comuta até 2, associa até 4, sem divisores até 8 — se caíssem juntas a torre tinha um degrau só. **E eu tinha CITADO em vez de medir**: o par `(e3+e10)(e6+e15)` da literatura **não anula** nesta convenção de Cayley-Dickson; agora procura-se por varredura, e o achado é nosso — `(e1+e10)(e5+e14) = 0` em 16, nenhum em 8. **O telómero é a dual** porque é o lado **conjugado**: a conjugação é involução em todo andar e `x·conj(x)` dá a norma real e pura — a mesma involução de `furos.c` §F4 e `ribossomo.c` §Y5 |
| `dna.c` | **a fita biológica, e a lei que eu quase escrevi pela metade** (7 seções, 14 unidades). Do recado no `eval.txt`: genoma real (o início do *lacZ* de *E. coli* K-12; lê FASTA para escalar), duas fitas, replicação semiconservativa, transcrição, tradução, mutação, reparo, e **reversão byte a byte**. **E aqui eu errei a conclusão.** Previ antes de medir que a cadeia não podia ser reversível — o código é degenerado, 64 codões para 21 saídas — e medi `1,7819` bits perdidos por codão. Ia escrever isso como lei. O Aarão: *"todo código é reversível, inclusive Hurwitz, pois a torre é dual — nada é reversível só de um lado; você não pode observar metade do fenómeno e chamar de lei"*. **E a medida deu-lhe razão AO BIT**: `H(aa) = 4,2181` e `H(sinónimo|aa) = 1,7819`, e `4,2181 + 1,7819 = 6,0000 = H(codão)` — a regra da cadeia a fechar. Os bits estavam no **lado dual** (a terceira base), e com os dois lados a reconstrução dá **0 falhas em 64**. **A lei, na forma geral** (§N6): dada uma involução `s`, todo `x` parte-se em `S=(x+s(x))/2` com `s(S)=+S` e `A=(x−s(x))/2` com `s(A)=−A`, e `x = S+A` sempre — a involução separa, somar desdobra. No DNA a lei são os 6 bits; em Hurwitz é a norma; em `σσ'=−1` é o produto |
| `amplifica.c` | **amplificadores e portas: o transistor nos dois regimes** (8 seções, 11 unidades). Uma equação só — Shockley — e dela saem duas coisas que não se parecem. **DENTRO da janela ativa**: o amplificador, e `gm = dIc/dVbe = Ic/VT` é a **DERIVADA** — *amplificar É linearizar*, é a parte `ε` do `fisica.c` §P2 com `ε²=0`, e o ganho É o `f'(a)`. A janela mede-se e vale exatamente `VT·ln(99) = 118,8 mV`. Como `gm` depende do ponto de operação, a **realimentação** troca o ganho por uma **razão**: 50% de variação no dispositivo dá `<0,01%` na saída — o mesmo movimento da ponte de Wheatstone. **FORA da janela**: a porta lógica. O chaveamento **amputa** (801 níveis de entrada → 3 de saída, zona morta 1%) — o digital não é mais preciso que o analógico, é mais **surdo**, e é a surdez que o torna reproduzível. E as portas **SÃO GF(2)**: `AND` é o **produto**, `XOR` é a **soma**, `NOT` é a **dobra** de ordem 2. Em GF(2) vale `-x = x`, logo somar É subtrair — a característica 2 do `dual.c` §U8, onde o direto colapsa no dual. **De Morgan é a dualidade `∧ ⋈ ∨`**, e é involução — mais uma dobra. **NAND é universal**, medido *construindo* NOT/AND/OR/XOR tabela a tabela. E §A8 **valida pelos dois caminhos**: o ripple-carry de 8 bits contra `x+y`, **65536 pares, resíduo 0**. Ligado à assistente: `amplificador`, `ganho`, `logica`, `porta`, `somador` |
| `eletrico.c` · `eletrico.h` | **o corpo transistor: resolver, simular e validar circuitos** (7 seções, 11 unidades). Vem de `chess/sandbox/corpo_transistor.tex`, e o teorema é a **tríade encarnada**: a **SOMA** (⊕) é **Kirchhoff** (série soma `Z`, paralelo soma `Y` — o resistor, linear); o **PRODUTO** (⊗) é o **ganho** (o divisor, `α = R2/(R1+R2)`, e compor divisores multiplica); o **OPERADOR** (Π) é o **TRANSISTOR** — Shockley `I = Is·e^{V/VT}` leva **soma de tensões em produto de correntes**, `I(V₁+V₂) = I(V₁)·I(V₂)/Is`. *É onde vive o operador*: `Π = exp∘Σ∘log` **é** essa equação, e a Gilbert cell multiplica somando os logs. As **multiplicidades** medem-se pela inclinação log-log: `L` é `+1`, `R` é `0`, `C` é `-1` — e `L ⋈ C` soma 0 (o resistor) com média geométrica `√(L/C) = Z₀`, o metal. O **RLC cai na mesma borda das EDs** e o mesmo `Δ = R²-4L/C` dá as **três classes**, com o amortecimento crítico sendo a **raiz dupla** (`ε²=0`); a ressonância é o **casamento** (`Im Z = 0`, `FP = 1` — o cone nulo `σ=1` em circuito). **Wheatstone**: a medida por **anulação** — lê-se a razão no ponto de resíduo 0, e a razão é exata. E §E7 **valida pelos DOIS CAMINHOS**: forma fechada contra integração no tempo, nas três classes, resíduo `~1e-9` — incluindo o crítico, onde a fórmula das duas raízes distintas dividiria por zero. Ligado à assistente (`conversa.c`): `rlc`, `serie`, `paralelo`, `divisor`, `ressonancia`, `wheatstone`, `transistor` |
| `travessia.c` | **morto ≠ vivo, a indecidibilidade, e o circuito que fecha** (7 seções, 13 unidades). Vem do `\part{O Saco de Lixo}` do enredo: vai ao lixo o que **não tem dual**. Um objeto está **vivo** quando a identidade é um gerador (`U_t = e^{tL}`, e vale `U_{t+s} = U_t·U_s`) e **morto** quando foi reduzido a igualdade estática sem gerador — a **cristalização**, e a conjectura `G = A` é a lápide. **Morto ≠ vivo é DECIDÍVEL** (tem gerador ou não tem); **a travessia é INDECIDÍVEL** — com `a_M=1`, `b_M=1+2^{-t}` se `M` para, vem `a_M=b_M ⟺ M nunca para`, logo decidir a fronteira **é** decidir a parada. Executado: o decisor de orçamento `N` contra a máquina que para em `N+1` **erra 8/8**, e acerta 3/3 no controle — o que o derrota é o *futuro* da máquina, não o aparelho. E o **infinito não cabe no finito**: cada régua de `p` bits esgota-se num passo finito, mais bits adiam e não fecham. O **circuito**: fluido → lineariza (Parseval fecha) → resíduo `~1e-13` → **espelho ao contrário** → volta, exato. E a régua do espelho, com uma correção que a medida impôs: **Hodge preserva o Poynting e inverte a impedância; ν∘rev faz o oposto** — cada uma inverte *uma*, e a dualidade inteira é a **composição**. Não se promete chegar; promete-se **fechar**, e o resíduo é o certificado |
| `milenio.c` | **o teorema da linearização, generalizado, e o corpo diferencial como o máximo** (9 seções, 14 unidades). Vem de `chess/sandbox/solucoes_do_milenio.tex`: *Γ abeliano com medida invariante ⟹ os caracteres são base ortonormal de L²(Γ)*, e **as seis leituras são esse teorema com o Γ trocado**. Medidos: a ortogonalidade `δ_mk` exata e a completude **por contagem** (n vetores ortogonais em dimensão n — sem análise nenhuma); Parseval (nada vaza); os três corolários — **A** translação é diagonal (3816 coeficientes), **B** produto é soma no índice (6075 produtos), **C** o gato permuta os índices, e *permutação não perde*. A **generalização**: **Pontryagin é uma DOBRA** (`Γ̂̂ = Γ`, ordem 2) com **vinco nos auto-duais** — a mesma estrutura do `zeta.c` uma escala acima, e `F⁴ = n²·id` é a mesma ordem 4 do `dif.c`. **O corpo diferencial é a instância máxima** por três razões medidas: `R` é auto-dual (está no vinco), o log liga os dois eixos (**Mellin É Fourier no log**), e todo Γ finito é **quociente** de `R` — é corpo de corpos no sentido do `base.c` §B6–B7. A **zeta é o mesmo teorema com Γ = os primos**: a fatoração única É o `δ_ij`. E §M9, a **realização física**: oscilador (o gap é o passo), RLC (as três classes de `Δ` na bancada, e o crítico É `ε²=0`), corda (os modos normais **são** a base ortonormal), decaimento (a meia-vida é o passo do log). Quatro problemas, um teorema com Γ trocado quatro vezes |
| `zeta.c` | **a zeta desenrola nesses pontos?** (7 seções, 8 unidades). Três perguntas numa, e elas **não dão a mesma resposta**. (a) A equação funcional **É uma dobra**: `s → 1−conj(s)` tem ordem 2, e o seu conjunto fixo é a **RETA** `Re(s)=1/2` — a candidata holomorfa `s → 1−s` fixa só o **ponto** `1/2`. **A reta crítica é o vinco**, no sentido exato do `base.c` §B14. (b) `ξ(s) = ξ(1−s)` medido em 7 pontos, resíduo `<1e-8`: meia folha determina a outra, porque a dobra guardou. (c) O **desenrolar** `Σ → Π` é o Pontryagin do corpo diferencial, e a forma **exata** é a do crivo: `Π_{p≤P} = Σ` sobre os `P`-lisos, resíduo `~1e-15` — o teorema fundamental da aritmética a fazer de `δ_ij`, com os primos como base. (d) Mas nos **zeros ela NÃO degenera**: os dez primeiros são simples (`|ζ|<2e-14`, `|ζ'|>0,79`), e o ponto real onde `ζ'` anula (`σ=−2,7172628`) tem `ζ≠0` — é **extremo, não raiz dupla**. §Z7 separa o medido do conjeturado e **não empresta força de um ao outro**: nada aqui toca a hipótese de Riemann |
| `fisica.c` | **onde os duais se tocam: `ε²=0`, e o que é na física** (8 seções, 12 unidades). O "tocam-se" tem conta: o cone `N = a² - s·b² = 0` tem **0** direções nulas em `s=-1`, **duas retas** em `s=+1`, e em `s=0` elas colapsaram numa só — a raiz dupla, `Δ=-4s=0`, a mesma degenerescência da ressonância nas EDs. E a fronteira **É a derivada, exata**: `f(a+bε) = f(a) + f'(a)·b·ε`, sem `h`, sem limite, sem `O(h²)`. O **palpite do bra-ket está certo, com condição**: o projetor `|a><a|` não serve (`P²=P`); serve o de **transição**, e a lei é `(|a><b|)² = <b|a>·|a><b|` — anula **sse** `<b|a>=0` (200 pares, 0 discordâncias). Daí `σ⁺=|↑><↓|` com `(σ⁺)²=0`, e os férmions: `a²=(a†)²=0` com `{a,a†}=I`, sendo `(a†)²=0` **o princípio de exclusão** escrito em álgebra. Duas correspondências **exatas**: `N=a²-b²` **é** o intervalo de Minkowski e os divisores de zero **são** os vetores nulos (o fóton não tem inverso porque não tem repouso); e a **soma de velocidades de Einstein É a lei polar** — `v=tanh θ`, "ângulos somam" dá `(v₁+v₂)/(1+v₁v₂)`, resíduo 0, e `c` inatingível é o cone visto de dentro. E o **"acima do infinito"**: `ε_c = i*/c` dá `ε_c² = 1/c² → 0`, o boost converge para Galileu (medido até `c=10⁶`) — a contração de İnönü–Wigner. Os três casos `e² ∈ {-1,0,+1}` são as três cinemáticas: Euclides, Galileu, Lorentz. O §P8 separa o medido do citado |
| `dual.c` | **a notação algébrica dual, o `i*`, e a forma polar das duas** (8 seções, 15 unidades). A diferença é UM sinal e propaga-se até ao fim: `i²=-1`, ordem 4, `e^{iθ}=cos+i·sen`, norma `a²+b²`, CÍRCULO; `(i*)²=+1`, ordem 2, `e^{εθ}=cosh+ε·senh`, norma `a²-b²`, HIPÉRBOLE. A série exponencial é **uma só** — com `e²=-1` os termos alternam e dão cos/sen, com `e²=+1` não alternam e dão cosh/senh — e a lei polar é a mesma (raios multiplicam, ângulos somam). É a **ordem 2** que "garante a reversão": involução desdobra sempre. Três correções que a medição impôs: (i) em dim > 2 o dual **não** é trocar o sinal de todas as unidades — isso dá assinatura `(1,3)` e a norma deixa de ser multiplicativa (`N(e₁e₂)=-1` contra `N(e₁)N(e₂)=+1`); o que fecha é o sinal no **passo**, com assinatura `(2,2)`. (ii) "garante a reversão" vale para a **involução**, não para a inversão de todo elemento: no dual há um **cone** `a=±b` com `N=0` sem o número ser zero — anel, não corpo, e é o `0/0` no seu lugar. (iii) a tabela mista `i·i* = -1` **não é livre**: força `i*=i`, logo `2=0` — só fecha em característica 2, e lá o dual colapsa no direto. Em característica 0, `i·i*` é uma unidade **nova** `j` com `j²=-1`, e `R[i,i*]` tem dimensão 4 com `(1+i*)(1-i*)=0`. E o `i*` **está na assistente** (`expr.h`, `numerica.c` §X19): não há segunda máquina, a célula ganhou `sig` e o produto virou `(ac + S·bd) + (ad+bc)e` com `S = e²` |
| `base.c` | **a base ortonormal, as duas torres, e a geração** (14 seções, 35 unidades). A base é `n` pontos da esfera dois a dois perpendiculares; as duas recursões repartem a **mesma** partição — o direto **soma** as projeções (`⟨x,y⟩₂ₙ = ⟨π₁x,π₁y⟩ + ⟨π₂x,π₂y⟩`), o cruzado **cruza-as** com o conjugado, e a metade antissimétrica do Cayley–Dickson **é** o `a×b`. Correção do Aarão: a dimensão não vem sozinha, vem no par `(n,2n)` — o `J` está medido em cada um **inclusive em `R¹⁰`**, então a dim 5 não é buraco, é projeção do 10. E **a reversibilidade é do par**: sozinha toda `R^n` (`n≥2`) tem divisor de zero; com a dual, `x⁻¹ = conj(x)/N(x)`. **O corpo é a torre inteira**, não o andar: um andar perde (`E∘C ≠ id`), a torre com os saldos não perde; e há a **torre dual que desce** (o traço, sobrejetivo — a inclusão falha onde ele fecha). As duas são **adjuntas** (`Sᵀ = D`): `S−D` antissimétrica, `S+D` simétrica, e a conservação **é** a antissimetria (`⟨Ax,x⟩=0`, `exp(tA)` ortogonal — contra 100/100 de quebra no simétrico). O comutador `[S,D]` cancela em todo andar interior e só sobra nas **pontas** (traço 0); `D⁸=0` — a dual esvazia, e resta **um** elemento, `ker D = coker S`: a cifra, fora do jogo. E o **origami**: a dobra guarda a memória da simetria porque tem **ordem finita** (`conj²=id`, `J⁴=I`, `F⁴=id`) — escalar por `1,3` volta ao início em `0` de `100`. A geração, contada em `GF(2^6)`: fechar o par por `+` e `×` dá `2^lcm` exato; **só com o `+`** para em `16 = 2⁴` com `4∤6`, e nem chega a ser corpo |

## A aplicação — a fala (Tiffany)

| arquivo | o que colhe |
|---|---|
| `linear.c`   | lineariza o sinal — um texto é um sinal como outro qualquer. *Pede um `.pgm`.* |
| `orbitas.c`  | o corpus vira um **grafo de órbitas** — frases de mesma estrutura, mesmo atrator |
| `navega.c`   | o navegante percorre os caminhos que passam pela fala — cobertura `100%`, reversível |
| `navegante.c`| a busca **sem Metrópolis** — a recursão (a fração contínua desdobrada), backtrack, resíduo 0 |
| `traducao.c` · `rotacao.c` | a tradução como **rotação em classes**: um par fixa o multiplicador, e o dente (um `k` errado) quebra |
| `../tatoeba/regua.c`| **as duas réguas**: PT e EN não se contêm (`|V_pt|/|V_en| = 1,710` sobre o mesmo conteúdo) e o corpus **não é função** em direção nenhuma (21,7% das frases EN têm 2+ PT, até 52; 6,9% das PT têm 2+ EN). A **amputação** tem preço: 41 palavras EN cobrem 50% dos tokens, e 30% do léxico aparece uma vez e paga 0,32%. A **âncora é órbita**: 758 verbos validados pela família de 1287 candidatos, em 77,7% dos pares. *Pede `pares.tsv`.* |
| `../tatoeba/centro.c`| **o centro é o interlocutor**: PT→centro→EN, e no centro o verbo é invariante — 55,4% das formas chegam ao mesmo parceiro da órbita contra 34,6% do acaso. Duas hipóteses minhas caíram medidas (massa ≠ ambiguidade; Dice não compara granularidades). O significado é da palavra **com a vizinhança**: pureza `0,629 → 0,701`, e em 54,5% das ocorrências a vizinhança muda a resposta (`vendo`→sell/see, `saia`→skirt/sair, `que`→that/why). E o **teste cego** (10% fora do aprendizado): 65,8% cascata > 62,7% forma > 54,9% órbita > 37,4% base. §C7 instancia a **BAI** (`broca-so/papers/casl-propagation.tex`): a entrada de dicionário é `κ=(σ,c,δ,ω,o)`, `δ` é o raio da amputação, `Ψ=Collapse` com **fail-closed** — e o dado justifica recusar (onde o estrito aceita, acerta 68,5%; onde recusa, 58,0%) com **0 escaladas** (`thm:noesc` exato). *Pede `pares.tsv`.* |
| `../tatoeba/bairro.c`| **a vizinhança É a órbita**: o bairro inteiro entra e quem escolhe é a contração `s(e)=a(e)·(m+Σ w(f)c(e,f))` — `σ=m+1/σ` iterado ao ponto fixo. Converge em **todas** as frases do cego; iteração 0 É a marginal, e o bairro paga `+0,4` ponto (66,5→66,9%) com a **mesma população** (115.871 decisões). Onde mexeu: troca 4,10% das escolhas e **ganha 1219 contra 772** (61,2%) — fiação **difusa**, não inerte. E o mesmo ponto fixo pelas duas vias: digital × translinear, `4000/4000` mesma escolha, `3,3e-16` de diferença. *Pede `pares.tsv`.* |
| `../tatoeba/`| o pipeline real sobre o Tatoeba: `ingestor`, `convtexto`, `caminha`, `dual`, `ciclo`, `ciclo_analog`, `tiffany` — e a série da tradução (`embedding`, `ancora`, `dente`, `homogeneo`, `operador`), cujo resultado é o §6 de `tiffany.tex` |

## Base histórica

| arquivo | nota |
|---|---|
| `micro.c` | o corpo no **discreto** (10 seções). O gabarito é hoje o analógico (`analog.c`); "discretização é inútil" — útil só como oráculo. |
