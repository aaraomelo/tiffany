---
name: project-assistente-cone-espiral
description: "A assistente — cone/espiral realizados; a CAIXA (eixo de segmentação do Controle de Histerese) é a admissão das quatro réguas; corpus curado do lixo do bench"
metadata:
  node_type: memory
  type: project
  originSessionId: c1a01a83-ee3b-4229-965f-a0acb8476ee5
  modified: 2026-08-15T03:30:41.777Z
---

**O desenho, palavras dele (05/08):** *«a saída da assistente é a ESPIRAL e a entrada é o CONE; o cone precisa DESDOBRAR — involução na entrada e evolução no banco.»* — REALIZADO (commits 1344126, d97b5ce, 7ab3c82) e estendido a 14/08.

## O estado a 14/08 (noite) — funcional e medida

- **Pipeline completo**: `banco/conversa.c` (motor, teste interno **45:0**) → `banco/fala` (protocolo TFAL, `-teste` 4:0) → plugin `/antena` no Vite → `app/src/assistente.js`. Bandas 1–9: `bench_vizinhanca` **35:0**.
- **A banda é a soma do corpo**: base = `.fala/hex16(sha256(assinatura))`, assinatura = `Tiffany-Assinatura/1\nsoma=<soma do corpo.json>\n`. O corpus curado vive em `9dcbce70…` (soma `d0feb8920c508c94`, commit b5c7cf6). **Cada rebuild do corpo muda a banda → base virgem**; o BARRAMENTO (outras bases na mesma pasta respondem e a nova aprende) faz a migração orgânica. CUIDADO: `$(…)` do bash engole o `\n` final da assinatura → banda errada (avisado no próprio FALA.txt; caí nela).

## A admissão pela teoria — a CAIXA nas quatro réguas

Sessão de 14/08: li `corpo_universal.tex` e `corpo_topologico.tex` COMPLETOS por ordem dele, e a régua saiu da teoria, **sem constante numérica**: o eixo *caixa* (segmentação) da vizinhança admissível 𝒱 do Controle de Histerese.

- **Dilatação** (re-ancorada — conserto meu para a gulosa que morria no ramo de outra fala do mesmo banco): I₁ contagem (consumido > saltado interno) ∧ I₂ caixa (palavra da consulta ou consumida inteira ou saltada inteira; partida mata a âncora, a re-âncora tenta limpa) ∧ **peek** (o registo só vale em FIM de palavra da consulta — o «q» a meio de «quanto» não é a abreviação «q»).
- **Erosão, torção e desce_daqui**: o mesmo peek — prefixo/troço só vale se FECHA em fronteira de palavra; a torção também só ANCORA em fronteira.
- A escada do observador realizada: o degrau grosso (contagem) resolve o natural, o fino (caixa) o adversário denso («é racional» contém «e ai» com acento despido — `prox_simb` despe acentos POR DESENHO).
- **O cone ganhou «menos»** (`desdobra_entrada`: vezes→x, mais→+, menos→−). «quanto é 1 mais 1» fica no corpus **DELIBERADAMENTE** (comentário no ficheiro — não alargar a porta).

## O corpus — a cura do bench

`tools/bench_histerese.sh` semeado na base de produção deixou 31 pares técnicos («música»→«suporte=musica borda=1 I=0»). Curados PELO TRADUTOR: pares banais em `papers/conversa.tex` (música, maestro, computador, janela… + «gosto de conversar contigo») re-ingeridos por `casual.sh`; typos/abreviações em `tools/vizinhanca.sh` (bandas 1–2). O `aprende` substitui por cima. **O bench re-suja se correr na base de produção — correr em cópia.**

## O corte e as tarefas (14/08 noite, com o Dual Sort lido completo)

- **O corte moldura→parâmetro**: a erosão escolhe a moldura (Dual Sort: o corte é a seleção de suporte), o resto da fala é o parâmetro, e escreve-se no buraco **`___`** da resposta aprendida («erode-se para escolher, dilata-se para escrever»). Um `_` só NÃO serve de marcador — o corpus cita `torre_fundacao.tex` (nu) e `\_` (LaTeX): duas regressões medidas até chegar ao `___`. A moldura crua é um funcional sem argumento: não responde por via não-prefixal nem fecha troço de torção. Molduras semeadas no `vizinhanca.sh` («gosto de», «fala-me de», «o que achas de»).
- **As tarefas** (Dual Sort §assistente: «o agente AUMENTA A MASSA — não finge que ela já estava lá»): portas `e_X/resolve_X` como as contas — «lembra que X = Y» (o ensino pela fala; o `=` da casa), «esquece X» (o inverso: zera o `.b` do nó, o caminho fica), «que horas/tem horas/que dia» (relógio/calendário), «mostra X» (slug→`papers/*.tex`, senão o corpus decide).
- **A erosão re-ancorada**: o prefixo com ruído à frente (come até fronteira de palavra e tenta de novo) — dá o corte às molduras no MEIO da fala: «que estilo? gosto de rock» → moldura preenchida. O cone ganhou «dividido por»→`/`. Teste interno **60:0**.
- **bench_histerese corre em CÓPIA fora da pasta** (mktemp+trap): dentro de `.fala/` a cópia suja entrava no BARRAMENTO e as outras assistentes aprendiam o diagnóstico. O par mentiroso «q horas sao»→«não tenho relógio» foi curado no vizinhanca.sh.
- **As funções nomeadas** (a fita recuperada para a fala): a fita `lib/expr.h` já sabia raiz, `!`, `^`, mod, `%` com «de», frações com o porquê decimal, o i e o i* — só entrava quem escrevesse símbolos. `funcao_monta` (testável, separada da resolução) + `resolve_funcao`: «raiz de X», «fatorial de», «dobro/triplo/metade/quadrado/cubo de» — o parâmetro passa pelo cone («dobro de 7 vezes 3»=42) e a guarda é `e_conta` puro («a raiz de 2 é racional» fica no corpus, medido). Cone também ganhou «por cento»→`%`.
- **A MEMBRANA TEXTUAL — o LaTeX desdobra-se na entrada** (ordem dele, 14/08: «o tradutor já é, só ensinar»). O tradutor COMPÕE, não avalia: faltava a mesma lei do cone. `lx_passo` (uma passagem: `\frac{A}{B}`→`(A)/(B)`, `\sqrt`, `\cdot/\times`, `\div`, `\bmod`, `\left/\right`, `$`, `^{2}`→`^2`, `{}`→`()`) iterada até ao **PONTO FIXO** — o aninhamento fecha em voltas, sem recursão. `tem_membrana` é a marca (`\letra`, `$`, `^{`); sem marca a fala sai **byte a byte**.
- **6ª versão (23:56): OS INTEIROS — «o que eles acrescentam é a REVERSIBILIDADE: todo a tem uma folha (−a) que retorna ao zero»**, e daí «divisibilidade/MDC/Bézout transformam essa reversibilidade numa MÁQUINA DE CORTES». `lib/inteiros.h`: ℤ por **pares** ((a,b)~(c,d) ⟺ a+d=b+c) com a soma **bem definida** (não depende do representante); o oposto **único**; **«menos vezes menos» PROVADO** elo a elo pela distributividade («não vale dizer: PROVE»); triangular com **igualdade no mesmo sinal e estrita quando diferem**; a combinação linear como «motor central»; **Bézout = o MENOR positivo de {ax+by}** (boa ordenação); diofantinas pelo critério gcd|c; congruências compatíveis com + e ×; **37⁴ mod 7 pela DOBRA** contra a conta. **E o RELÓGIO DE SEIS TICKS que ele desenhou** (definição·tradução·operação·demonstração·contraexemplo·volta) na fala «prova divisibilidade 3 12 18» — e a prova é a **CADEIA**, não a varredura («a varredura seria controlo»). Falas: «bezout 35 e 22» (x=−5,y=8), «diofantina 35x + 22y = 7».
- **5ª versão (23:46): OS NATURAIS — «o motor demonstra os PRÓPRIOS INSTRUMENTOS que depois usa em todo o resto»**: 0 → S → + → × → ≤ → | → gcd → primos → fatoração. `lib/naturais.h`: soma e produto definidos por **recursão de Peano** e medidos contra o `+`/`×` da máquina (**dois caminhos**, 676 pares); a ordem é a **testemunha** (a≤b ⟺ ∃c: a+c=b, e o c exibe-se); **boa ordenação exaustiva** nos 1024 subconjuntos (e o vazio NÃO tem menor — a hipótese mede-se); divisão com resto **existe e é única** (a unicidade varre-se); **Bézout exibe x,y e substitui-se** (1600 pares); **TFA com a VOLTA** (o produto refaz o número, 299 casos); **Lema de Euclides vale para primo e CAI para composto** (6|4·9); **infinitos primos CONSTRUTIVO** (a fatoração de N=p₁…pₖ+1 exibe o primo novo); indução com o **passo em álgebra exata**. Fala: «fatora 60» (=2²·3·5), «mdc de 18 e 12» (Bézout x=1,y=−1 — os números DELE), «divide 17 por 5».
- **4ª versão (23:35): RELAÇÕES → FUNÇÕES → BIJEÇÕES**, e o fecho é «**o sino inteiro em miniatura**»: *A×B → pares → relação → função → bijetividade → inversa → volta*. `lib/relacao.h`: a relação É a **tabela booleana sobre os pares** (como ele diz), as propriedades varrem-se inteiras (n² e n³), a composição é o ⊗ e a inversa é o †. **«f bijetiva ⟺ f⁻¹ existe»** medido nas **256 funções** de um conjunto de 4 — coincidem uma a uma, e as bijeções são exatamente **24 = 4!**; a composição preservada em **65 536 pares**; a congruência mod n dá exatamente n classes que **particionam**; e o exemplo dele (f=3x−7, f⁻¹=(y+7)/3) fecha **em ℚ, sem um decimal**. Fala: «relacao … em 3» e «funcao … em 3».
- **O `eval.txt` MUDA A CADA VEZ — ele reescreve-o e diz só «eval»**. Terceira versão (23:26): **teoria dos conjuntos**, «no mesmo padrão do motor: definição → operação → demonstração → volta», com a ponte que ele nomeia: **conjuntos ↔ Booleano ↔ árvore ↔ prova**. Implementado: `conj_traduz` (a pertença É a variável — A∪B↦a+b, A∩B↦a*b, A\B↦a*¬b, Aᶜ↦¬a, **A△B↦XOR**, A⊆B↦a→b), a fala «prova A menos (B uniao C) = …» com os ticks **que o próprio §4 desenha**, e a prova pela **varredura dos ÁTOMOS** (as 2ⁿ regiões do diagrama) — «igualdade de conjuntos é igualdade das folhas de pertencimento». **15 exercícios dele** medidos, e onde a identidade é falsa **exibe-se o contraexemplo concreto** (a região). O §6 fecha: △ É o XOR, e as 8 folhas de Shannon são as 8 atribuições.
- **DEFEITO MEU: números escritos à mão** na linha do contraexemplo («a esquerda dá 0 e a direita 1») — calhavam certos e eram inventados na mesma; bastava o contraexemplo cair do outro lado para a frase mentir. Agora saem da medição, e verifiquei trocando os lados (1 e 0).
- **O `eval.txt` (2ª versão, 23:16): LÓGICA E DEMONSTRAÇÃO** — e a regra que ele põe no fim muda a natureza da coisa: «**toda demonstração precisa carregar a JUSTIFICATIVA DE CADA TRANSIÇÃO** … passa a produzir o **rastro verificável**». Implementado: a implicação entra **pela definição do ficheiro** (P→Q ≡ ¬P∨Q, não por tabela minha); **12 regras de inferência VERIFICADAS** (cada uma é tautologia na tabela inteira — Modus Ponens/Tollens, silogismos, exportação, absurdo, casos), com o gume da falácia («afirmar o consequente» cai); a **negação dos quantificadores** exaustiva em domínios até 4; e a fala «**prova …**» dá o **rastro**: classificação (tautologia/contradição/**contingência recusa-se**), cada transição com a regra nomeada **e verificada na hora**, e a **VOLTA** (a fórmula do fim tem a mesma tabela) — «um rastro sem volta seria uma história bem contada».
- **O `eval.txt` (versão anterior) IMPLEMENTADO** (ordem dele): é a caixa booleana inteira e acaba com a regra da casa — «cada um com **VOLTA OBRIGATÓRIA**: transformação → resultado → reconstrução». **21 problemas do ficheiro** viraram medidor, com **a resposta que o FICHEIRO publica** (a referência não é minha) e a igualdade verificada **pela tabela exaustiva**. A simplificação nomeia **a lei em cada transição** (a adjacência é distributividade + complemento + identidade: `xy + xy' = x(y+y') = x·1 = x`), sai em **LaTeX**, e só se afirma com resíduo 0. **Shannon** entrou como o que o ficheiro diz que é: **o corte da árvore** («árvore → corte → folha», e o caminho guarda a proveniência).
- **A NOTAÇÃO É A DO FICHEIRO, e foi defeito medido**: eu lia `+` como XOR (GF(2)) e o `eval.txt` usa `+` como **OU** (`A+AB=A`) — dava «a + ab = a·b̄», certo no corpo e errado no ficheiro que manda. Agora `+`/`|`/`ou` = OU, `^`/`xor` = XOR, dito na saída; e a redução da equação usa o `^` explícito.
- **A LÓGICA É O CORPO GF(2)** (`lib/booleana.h`), não máquina nova: o ramo do transístor já o dizia («AND é a multiplicação, XOR é a soma, −x = x»). A equação reduz-se como a polinomial: **A = B ⟺ A ⊕ B = 0**. A forma canónica é a **ANF (Zhegalkin) pela transformada de Möbius, que é INVOLUÇÃO** — a mesma leva de volta (o Dual das cinco, de graça porque somar é subtrair). Medido **exaustivo**: nas 256 funções de 3 variáveis, ν∘ν = id e a ANF avaliada devolve a tabela. `a∨b = a+b+ab` sai sozinho.
- **AS EXPLICAÇÕES VÃO DENTRO DO TICK** (ordem dele): cada passo é um tick que diz **o que fez e porquê** — leitura (2ⁿ, o corpo é finito), equação (o XOR a zerar), **fibra** (quais entradas dão a saída — a divisão das cinco), **dual** (a ANF), **volta** (resíduo 0 ou não se afirma). A granularidade continua do Quantizador.
- **A INTEGRAÇÃO é a derivada com o sinal trocado** — `pol_calculo(p, ±1)`: +1 desce o grau, −1 sobe-o, exato em frações (∫x² = x³/3, e 1/3 é 1/3). **O «+C» é MEDIDO, não convencionado**: integrar→derivar devolve a IDENTIDADE, derivar→integrar devolve **p − p(0)** — a constante é o NÚCLEO da derivada, e o gume é a derivada da constante ser zero e a volta não a inventar. A definida é F(b)−F(a) exata («integra x^2 de 0 a 3» → 9). E a **lei de Gentil** (Teorema Central) mede-se exata em ℚ: ∫f + ∫f⁻¹ = b·f(b) para f=x^m, 20 casos.
- **DEFEITO MEU, do escritor**: sete `frac2` num só `printf` — o buffer rotativo dele tem QUATRO, e as três primeiras foram pisadas («F(3)−F(0) = 0 − 3 = 3»). Cura: uma fração viva por `printf`, e o valor da definida virou asserção. E o `ingere.py` levou a segunda volta do mesmo defeito do `\_`: o `\^{}` — com a **ORDEM** a ser o erro (as chavetas viravam espaço antes do escape ser tratado).
- **O PADRÃO OURO SÃO AS CINCO** (correção dele: «o padrão ouro são as 5 operações do universal que listei»; NÃO é o MOVE do commit d7996e3, que é outro padrão, mais antigo, da E/S). Migrado: três pares meus que eram duas funções passaram a **UMA operação com sinal** — `MEMBRANA(±1)` (Dual: +1 desdobra a entrada, −1 veste a saída), `CORPUS(±1)` (Soma: +1 aprende/a dobra, −1 esquece/a retração), `CONV(±1)` (⊗ e a fibra: +1 convolve, −1 deconvolve). Os nomes antigos ficam como invólucro de uma linha — **o padrão que a casa já usara** com `le`/`grava` sobre o `MOVE`. Medido o que os faz UM: os dois sentidos **compõem na identidade**, e o gume é `CONV(−1)` a RECUSAR onde a fibra não existe. Fala «as cinco operações» dá a vista com cada peça debaixo da sua.
- **O MDC é a FOLHA DA ÓRBITA DO INVERSOR** — «Euclides é a dinâmica do inversor, e a folha é o gcd». A mesma cadeia corre no inteiro e no polinómio (medido nos dois: mdc(x³−1,x²−1)=x−1 em 2 passos; mdc(1071,462)=21 em 3), e a **volta confere** — ele divide os dois com resto 0. Primos entre si descem até à unidade. Fala: «mdc de … e …».
- **AS CINCO OPERAÇÕES NO POLINÓMIO, cada uma com a SUA conservação** (ordem dele: «vê no universal soma, multiplicação, dual, inversão, Pontryagin — padrão ouro»). Soma = a retração devolve; Multiplicação = a norma multiplica (`eval(u⊗v)=eval(u)·eval(v)`); Divisão = a **FIBRA** `fator·a = q·b + r` com a volta a reconstruir; **Dual = o RECÍPROCO** `ν(p)=xⁿp(1/x)`, involução `ν∘ν=id` — e é o **mesmo ν da prova de Pisot** (`ν(β)=β*`); Inversão = a volta, e dividir por zero é **recusado**. **Pontryagin**: o caráter leva SOMA em PRODUTO (`x^(a+b)=x^a·x^b`), a troca ⊕→⊗ do grupo dual.
- **DIVISÃO DE POLINÓMIOS na fala**: «divide x^3 - 1 por x - 1» → quociente, resto, e **a volta obrigatória** (q·b+r reconstrói, ou não se afirma). Em ℤ o **pseudo-fator é DITO**, nunca escondido («em Z multiplica-se por 4 — o líder de b não é 1»).
- **O RELÓGIO NA MÃO: o tick é o quantum** (ordem dele: «passo a passo, e a velocidade é escolher os ticks — lê o Metrónomo/Quantizador»). `conversa <base> tick "conta"` dá um tick e **guarda a fita no disco** (`.tick`), `tick` continua, `tick fim` fecha. A velocidade é `TICKS=` e **só sobe por DOBRA** (1,2,4,8 — a torre `ω_{2M}²=ω_M`); 3 é recusado com o motivo, «nunca por salto». O que o Quantizador promete está **medido**: mudar a velocidade muda a LEITURA e não o objeto — mesmo valor e **mesmo número de dobras** em 1,2,4,8.
- **E TEM NOME: é CONVOLUÇÃO e DECONVOLUÇÃO** (correção dele). O `pz_mul` é a `estrela` do `convolucao_universal.js` e o `pz_div_exata` é a divisão espectral. Medido contra a LEI, não sozinho: `eval_σ(a⊛b) = eval_σ(a)·eval_σ(b)` nas **duas folhas** (σ=61, σ†=198 em q=257), 16 pares; e a divisão espectral bate com a exata, com o divisor de zero exibido quando `eval(b)=0`.
- **A FATORAÇÃO: fatorar é a VOLTA da convolução** (ordem dele: «é a especialidade da casa; vê Pascal para distribuir as multiplicações; dominamos a família metálica e Pisot»). O produto de polinómios É convolução (já teorema), e **a linha de Pascal é o caso (x+1)ⁿ** — medido por dois caminhos (convolução vs binomial, 10 graus). `pz_fatora` em poli.h: conteúdo → fatores lineares por raiz racional → busca limitada, **sempre com divisão de resto ZERO**, e `pz_confere` obriga o produto dos fatores a reconstruir o original ou não se escreve nada. **A irredutibilidade da família de Pisot sai sem calcular raiz**: β(n,m)=xⁿ−mx^{n−1}−1 (m≥2) tem n−1 raízes dentro do círculo por **Rouché NO DUAL**, logo um fator próprio teria |c₀|=∏|raízes|<1 com c₀ inteiro≠0 — impossível. O m=1 fica de fora (é onde a prova falha: as raízes de Selmer caem SOBRE a circunferência).
- **ZERO DOUBLES — a álgebra é PLENA** (ordem dele, 14/08, e é a minha regra [[feedback-inteiro-primeiro]] que eu próprio quebrei). Eram **54** em conversa.c; ficaram **0** (as ocorrências restantes são comentários a dizer onde estavam). O que saiu e por quê: `sqrt(D)` a dar decimal (o próprio texto do polinómio já dizia que aproximar é SAIR do corpo); `poli.h` guardava coeficientes em double e **adivinhava as frações de volta** com um laço até 64 — agora `Pol` é p/q e Sturm corre por **pseudo-divisão inteira** com a contagem em **±∞ pelos líderes** (sem cota, sem avaliação, sem limiar); `bairro_escolhe` foi para **mantissa inteira** (S=2^20, a régua «S=2^30 fixo» do corpo_topologico); o circuito lê a bancada em **frações** (1u = 1/1000000 exato) e a ressonância anula-se por **identidade**, não por arredondamento; `edo.h` dividia as suas próprias frações em double na porta.
- **A BASE JÁ EXISTIA e eu duplicava**: `raizi` (raiz inteira) e `lado` (a **FC por PQa em inteiros**, que pára no período — Lagrange) estão em `lib/cifra.h`; `ct_reduz`/`ct_escreve` em `expr.h`. Eu tinha escrito `lsqrt` e `frac2` do zero. **A raiz normaliza-se só no fim, em caracteres, pela FC**: o ouro sai `σ = [1; 1]`.
- **Os medidores mediam a régua errada**: a derivada do transístor por diferenças finitas (h=1e-7) virou a **parte ε** do número dual (exata, dois caminhos contra a derivada simbólica); a lei soma→produto virou **σ^(a+b) = σ^a·σ^b em ℤ[σ]** (81 pares, exato); e as nove particulares da ED, que se aceitavam com «resíduo < 1e-5», passaram a cumprir a **identidade que as define** com **resíduo ZERO EXATO**. O `edo_residuo` (nunca chamado, em doubles) foi removido: o medidor que nunca mediu.
- **O MECANISMO DE RESOLVER: as folhas no relógio** (ordem dele: juntar o tradutor com a teoria discreta). A régua já declarava ℚ[x]/(p) — o passo que torna a raiz NÚMERO é a escada de Fermat: Δ quadrado ⇒ SEPARADO (as duas folhas inteiras), senão INERTE (sobem um andar, e lá o Frobenius É a estaca). Dois caminhos (Euler + varredura) e nenhuma folha se escreve sem voltar à equação com **resíduo 0**. `folhas_no_relogio` em conversa.c, chamada no grau 2. **A referência não é minha**: o paper PUBLICA σ=4081/σ†=61458 para a prata em 𝔽₆₅₅₃₇ e o código dá esses números — teoria e código são a mesma casa. E o bit i (x²=−1) devolve a própria escada: **4, 16, 256**.
- **O TRADUTOR É O BACKEND** (`tools/compoe.sh`): fala→resolução→membrana(.tex)→`tests/tex` compõe o PDF→`-volta` lê de volta e o valor declarado atravessa. O desenho já estava escrito no `tex.c`: «ABRE/STORE/LOAD — o PDF é mais um backend». Três roupas (prosa/ASCII/LaTeX) na mesma cascata; ambientes só os que o tradutor compõe (`align`/`aligned`/`equation`; `cases` NÃO), e o `\\` vira o `;` do sistema.
- **A MEMBRANA TEM DOIS MEMBROS** (a regra [[feedback-dual-exige-dois]] aplicada a mim): só tinha feito a entrada. A volta é `veste_valor` (fração→`\frac{v}{q}`, inteiro→ele próprio) com o flag `MEMBRANA_ENTRADA` — quem fala vestido ouve vestido. E é MEDIDA, não afirmada: veste→desdobra→a fita devolve o mesmo número; ponta a ponta, a resposta vestida re-entregue fecha no mesmo racional.
- **O DIALECTO É DO TRADUTOR, e o medidor são DOIS CAMINHOS**: li `tests/tex_core.c` (6194 linhas) — ele **compõe** (a pilha do \frac emite '/' ao fechar o numerador, o radicando, o expoente ±6/±7) e **não avalia**, logo o desdobrador não era redundante; mas a LISTA de comandos é dele (tabela de símbolos linha ~383 + operadores nomeados ~5645). Eu tinha inventado `\ast` e `\pmod` — o LaTeX de fora tem-nos, a casa NÃO. `tools/bench_membrana.sh` compara os dois caminhos (13:0, e com um comando inventado dá FAIL — o gume provado). `\pm` fica FORA de propósito: ± não é um valor, são dois.
- **A ordem é NA ENTRADA, e foi defeito medido**: vestida, `x^{2} = 4` passa na porta da EQUAÇÃO (que aceita chavetas) e não na do POLINÓMIO — respondia «isto não é do primeiro grau». Desdobrar só depois não chega: `resolve_vestido` desdobra ANTES da cascata.
- **`ingere.py` curado**: o `\_` do LaTeX vazava para a fala («papers/torre\_fundacao.tex» dito à letra) — o `\[a-zA-Z]+` não o apanha porque `_` não é letra. 11→0 no mesmo ficheiro.
- **`resolve_simbolico`** — a cascata inteira (circuito, poli, sistema, ED, álgebra, equação, lei, conta, cone, funções) extraída numa função só, com DUAS portas: a fala nua (o responde) e «resolve/resolva/calcula/calcule X» (`pedido_nu` tira a roupa; «resolvemos tudo» não casa pela fronteira do espaço; «quanto é» continua no corpus POR DECISÃO). Teste interno **65:0**, bateria **468:468**.

## Pendente

- Conversa com CONTEXTO (fase 2: «que estilo? gosto de rock» → não sei — corpus, não mecanismo; a ordem do FALA.txt: gabarito → árvore → casual).
- A fragilidade banda↔soma a cada deploy (decisão dele).
- (commitado em `8dfb094`: a escada aritmética inteira, bateria 468:468.)

A escada que os `eval.txt` construíram — booleana → conjuntos → relação → ℕ → ℤ → ℚ — está em [[project-escada-aritmetica-n-z-q]], com o gume de ℚ («dividir por zero é uma operação SEM FIBRA»).

Ver [[project-checkpoint-2026-08-14-curadoria]], [[feedback-a-base-ja-existe]] (o gume «maioria» era régua minha; a da casa estava no Controle de Histerese), [[feedback-a-ausencia-e-deliberada]].
