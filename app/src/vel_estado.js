// O estado do CIRCUITO — a frequência do reino (o tempo backend), compartilhada por todos os players.
// v = o fator de velocidade (o slider); tocando = play/pause. O tempo real lê daqui.
export const velEstado = { v: 2.0, tocando: true }   // default 2× (máx 4×) — a frequência-base do reino
