CURSOR.TXT — ONDE ESTÁ O TRABALHO, E COMO SE SEGUE
==================================================
Não é histórico. É o que precisas para pegar e continuar.

ESTADO
------
20/08/2026. Fase A Trios 1–10 fechados. Fase D FECHADA: Qz em E₁₆.
Fase E (descida 8 bits): Trios 1–3 FECHADOS; reducao §DW liga W_8²→𝔽₁₂₇.
Fase PG: Trios PG1–PG4 FECHADOS. A PROMOÇÃO (saturo→promove) está PROPAGADA.

    inteiros    §NT1–§NT11b (14/14) · cadeia §KW (10/10) · migracao §GW (7/7)
    reducao     §DW (8/8) · dual16 §EW (6/6) · transformada §TW (26/26) · palavra §PW (34/34)
    bateria     528 : 528 verdes, 0 falhas
    doubles     0 do TIPO e 0 LITERAIS de vírgula — trava declarada: tools/libc.c (§18)
    limiares    58 → 0 régua          (+4 valor físico JS, não alvo)
    long/64     0 ll · 16 i128 rest.   python3 tools/longs_todos.py  (ll FECHADO; i128 só dual32.c)
    formatos    0 avisos -Wformat no repo (eram 216; ver §17)
    git         0 includes fora do repositório  tests/refs.c §R5  (eram 3 headers)
    js          5 medidores .js passaram a emitir #UNIT: 26 unidades finas (§22)
    selo        8aa1a8468eca3cf0      bash tools/bateria.sh

Verificar SEMPRE com:  bash tools/bateria.sh   (lê AS DUAS linhas: total e unidades)

Fase A concluída nesta sessão:
  Trio 1  traduz.c · le_num.h · dualcifra.c     (commit 6109d64)
  Trio 2  campomedio.c · liquida_doador.c · icecube.c
  Trio 3  pinos.c · gguf.c · palavra.c + lib/i128.h
  Trio 4  prensor.c · rn.c · selo.c
  Trio 5  corpo_topologico.c · isserlis.c · evolutivo.c
  Trio 6  migracao.c · qmd.h · transfusao_real.c
  Trio 7  agentes.c · protocolo.c · le_emb.h
  Trio 8  fita.c · chicote.c · folhas.c
  Trio 9  dif.c · microfluidica.c · antissimetrico.c
  Trio 10 cifrando.c · completo.c · po_corpo.c   (int32_t; long só interface)
  Trio 11 lib/racionais.h → E₁₆ (int16_t + dual16/dual32) + testes dependentes
  Trio 12 lib/medida.h mod p em long (fora de Qz) · RtOp int16_t · conservacao_metrica
  Trio 13 lib/calculo.h fn_quociente binomial · corpo_fisico §H1 E₁₆ · reta.h mod int32
  Trio 14 fn_desloca binomial · rt_opera_i32/inverte_i32 · rt_fixo_* int32 · reta §R27
  Trio 15 rt_dir/cruz/cruz3_i32 · rt_ciclo_i32 · rt_cf_slot.h (FC nos slots ISA)
  Trio 16 slot_mem.h · rt_cf_slot fd (.mem) · reta §R12e persistência
  Trio 17 slot_map.h S_CF=2048 · sql.c mem_le→slot_mem · reta §R12f
  Trio 18 entrega.c · fator.c → rt_cf_slot no .mem (S_CF)
  Trio 19 descida_mobius.c · telomero.c · calculo2.c → rt_cf_slot (.mem)
  Trio 20 dif.c · racionais_fixos.c → rt_cf_slot (.mem)
  Trio 21 slot_map cf_slot_base · sql.c cf_le/grava · reta §R12g (mem_le ≡ slot)
  Trio 22 antissim.c · ouro_base.c · veste.c (ll→long) + rt_cf_slot_word nos medidores FC
  Trio 23 serie_phi.c · curvatura.c · mcu.c (ll→long)
  Trio 24 lambert.c · pidual.c (L→long) · bench_pares.c (timing→int64_t)
  Trio 25 bench_dualsort.c · relogio.c · koch.c (ll→long/int64_t)
  Trio 26 bench_dissipa.c · sql.c · transformada_universal.c (ll→int64_t/uint64_t)
  Trio 27 disco.h · borda_sem_teto.c · bidual.c (ll→int64_t/long)
  Trio 28 banda.h · canal.c · zeta.c (ull/ll→uint64_t/long)
  Trio 29 dtcn.c · gitb.c · fatia.c (ll/ull→int64_t/uint64_t)
  Trio 30 str2dbl_dual.c · dual32.c · graduacao_realiza.c (ull/ll→uint64_t/long)
  Trio 31 tex_core.c (π) · eletrico.h · erg.c (ll/ull→int64_t/uint64_t)
  Trio 32 guarda.c · torres.c · cruza.c (L→long, off→int64_t)
  Trio 33 escada.c · dna.c · kernelb.c (L→long, stat→int64_t)
  Trio 34 pisotbase.c · selberg.c · dourada.c (L→long)
  Trio 35 medida.c · peano_dual.c · gauss.c (L→long)
  Trio 36 bit_metropolis.c · disco_wasm.c · assinatura_tradutor.c (L→long)
  Trio 37 config_corpo.c · metalica.c · aurea.c (L→long)
  Trio 38 dualreta.c · ortogonal.c · cantor.c (L→long)
  Trio 39 estado_caos.c · leis_no_tradutor.c · nove_periodico.c (L→long)
  Trio 40 milenio_trial.c · estrela_pontofixo.c · pimonte.c (L→long)
  Trio 41 cantor_julia_dual.c · lyapunov_refletido.c · xx.c (L→long)
  Trio 42 natural.c · continua.c · dtc_viveiro.c (L→long)
  Trio 43 pontofixo.c · nomeia.c · estelar_completo.c (L→long)
  Trio 44 zetadin.c (L→long)
  Trio 45 dominios.c · relogio_curva.c · tres_reconstroi.c (LL/i64→int64_t)
  Trio 46 cadeia.c · unico.c · koch.c (§KK W_8 + eletrico I128)   FECHADO
  Trio 47 corpos.h · medida.h · calculo2.h (i128; sem __int128 no código)
  Trio 48 eletrico.h · koch.c · pascal.c (I128 Verlet; pascal sem __int128)
  Trio 49 reais.h · dominios.c (cresc→i128.h)                    FECHADO
  Trio 50 borda_sem_teto.c · graduacao_realiza.c · relogio_curva.c FECHADO
  Trio 51 relogio.c · pidual.c · lambert.c                        FECHADO
  Trio 52 zeta.c · pi_rei.c · cosmico.c                           FECHADO
  Trio 53 simula.c · xx.c · tres_regimes.c                        FECHADO
  Trio 54 banco/conversa.c (monotonia x^n §12)                    FECHADO
          i128 FECHADO fora de tests/dual32.c (trava intencional)

Fase E — FIRMA DA TORRE (coordenador 19/08) — FECHADA nesta etapa:
  lib/torre_alg.h     contrato: T_{k+1}=T_k+T_k*, três pipelines, mapa andares
  dual16/32/i128      d*_par_dual, d*_dual (involução por andar)
  slot_mem/rt_cf_slot ancoragem retração Σ∘Π nos slots S_CF=2048
  tests/torre_alg.c   medidor §T1–§T4 (4:0) — BATERIA 509, atestado c92f2db95f4566b5
  Papers (ponte formal, sem código):
    racionais.tex     subsec. firma da torre + inventário §T1–§T4
    corpo_algebrico.tex  thm:torre + def:cone → torre_alg.h / torre_alg.c
  Bateria: 509 medidores, 495 verdes, 14 falhas pré-existentes; selo 137cfad6a8c6c872


0b. FIRMA DA TORRE — pós-Fase-A (coordenador)
==========================================
A Fase A matou tipos largos; a matemática já estava noutro sítio. Agora tornar
visível na arquitetura:

  álgebra → operação → dual → largura necessária → slot     (NÃO: tipo C → op)

DOBRA:  T_{k+1} = T_k + T_k*   (corpo_algebrico thm:torre · topológico thm:rn)

TRÊS PIPELINES:
  INVOLUÇÃO  ν∘ν=id   ι swap · hip_conj · d*_par_dual
  RETRAÇÃO   Σ∘Π=Id   FC → slots S_CF (rt_cf_slot · slot_mem)
  SUBIDA     T_k→T_{k+1}   torre.h tr_mult

ANDARES:  E₁₆/Qz → D32/dual16 → D64/dual32 → I128/i128 → Hip/torre.h
Contrato: lib/torre_alg.h


0. A LEI — a construção que fundamenta a migração
==================================================
papers/inteiros.tex — degrau N → Z, construção formal (FECHADO):

    N² → N²/∼ ≅ Z       (a,b)∼(c,d) ⟺ a+d=b+c   (soma cruzada; SEM a-b antes do ∼)

    Φ([(a,b)]) = a-b    Cruz(x) := Φ(x) só APÓS o quociente
    D(a,b) = a+b        Dir — não invariante de classe (§NT10)

    oposto = swap ι(a,b)=(b,a)     ν = -id_Z,  Fix(ν)={0}
    G_alg(z) = ℵ₀  ≠  G_geom(x) = |p⁻¹(x)|   (fibra estrutural vs multiplicidade)

    métrica: d(A,B)=µ(A△B) isometria discreta; |det T|=1 conserva células em Z²
             (algebrico thm:metrica · metrica.c §M3–§M8 · dragão §AG2)

    palavra W_8 = {0..255}: N ≠ W_8 — envelope finito, NÃO redefine ℕ
    equivalência: uint16 exacto (a+d=b+c); wrap/sat só na ESCRITA
    descida degrau: W_8 → (uint16 cruz) → E_16 → … → GF(2)
    medidor: tests/inteiros.c §NT1–§NT11 (14/14); Peano: aritmetica.c §NP
    refcruz: chave inteiros → papers/inteiros.tex
    arquitetura: papers/arquitetura.tex §sec:descida (degrau k=0 ligado à cadeia)

papers/racionais.tex fecha a construção Z → Q pelo molde dual:

    Z² ⊃ V_rat → V_rat/∼ ≅ Q      (a,b)∼(c,d) ⟺ ad=bc

O racional é a CLASSE DE EQUIVALÊNCIA de pares de inteiros. A involução é o
swap ι(a,b)=(b,a), que induz x⁻¹ no quociente (Thm swap-inverso). As
operações são inteiras no representante:

    soma:     (a,b)⊕(c,d) = (ad+bc, bd)
    produto:  (a,b)⊗(c,d) = (ac, bd)
    oposto:   −(a,b)       = (−a, b)
    inverso:  (a,b)⁻¹      = (b, a)       a≠0

A equivalência é decidida por dois produtos inteiros, sem divisão:
    Cruz(a,b,c,d) = ad−bc = 0  ⟺  [(a,b)] = [(c,d)]
    Cruz < 0  ⟺  a/b < c/d   (b,d>0)

Dir = ad+bc e Cruz = ad−bc são os numeradores inteiros das leituras simétrica
e antissimétrica sobre o denominador comum bd, com B((a,b),(c,d))=ad escolhida.

O swap NÃO preserva V_rat (ι(0,b)=(b,0)), mas preserva V× = {(a,b):ab≠0}.
A extensão a P¹(Q) (incluindo [1:0]=∞) é interface herdada da torre, não
construção deste degrau.

Conjugação de campo (a+b√D ↦ a−b√D) tem Fix=Q.
Dobra s†=−1/s tem Fix={s:s²=−1}. NÃO confundir.


1. ARQUITECTURA DE 16 BITS — o alvo
====================================
O envelope computacional (racionais.tex §sec:ja):

    E₁₆ = {(p,q) ∈ Z² : −32768 ≤ p ≤ 32767, 1 ≤ q ≤ 32767, gcd(|p|,q)=1}

    typedef struct { int16_t p, q; } Qz;    /* p/q em par de 16 bits, q≠0 */

E₁₆ NÃO é Q, nem subcorpo finito. É um envelope de representantes canónicos.
Cada operação retorna elemento de E₁₆ ou sinaliza saturação (qz_saturou).
red(0,b) = (0,1) para todo b≠0.

Produto exacto em pares de 16 bits (lib/dual16.h):

    typedef struct { uint16_t alto, baixo; } D32;

O par (alto,baixo) representa exactamente os 32 bits do resultado em
complemento de dois. A interpretação assinada ocorre após recomposição.
Comparação: alto (signed 16) primeiro; em igualdade, baixo (unsigned).

    d16_mult(a,b) → D32           produto 16×16 → par de 16
    d16_cmp_prod(a,b,c,d) → int   sinal(ab−cd) sem materializar Qz

Todas as operações: pares com pares resultam em pares. Os produtos
intermediários usam largura dupla (D32), reduzidos depois ao domínio E₁₆.


2. O CICLO UNIVERSAL — quatro passos, nenhum normaliza no meio
===============================================================
algebrico thm:universal · lib/reta.h · tests/reta.c

    DESCODIFICA   (p,q) := texto → par EXACTO em Z² (ou Qz)
    OPERA         |det T| = 1;  [p:q] → [T₀₀p+T₀₁q : T₁₀p+T₁₁q]
    INVERTE       T⁻¹ = adj(T)/det T;  α⁻¹ = α*/N(α) nas unidades
    CODIFICA      w := palavra (FC) ou dígitos
    FECHA         descodifica(w) = (p,q)

A "transformada" é terminologia interna da série; matematicamente, eval é a
aplicação canónica de representantes para o corpo de fracções. Convolução/
deconvolução (gato/esquilo) = ciclo produto/inversão neste degrau.


3. PLANO DE MIGRAÇÃO — quatro fases em trios
=============================================

─── FASE A — MATAR O 64 ──────────────────────────────────────────────────
Objectivo: 0 __int128 fora de travas; 0 long long; produtos via d16_mult/
d16_cmp_prod.

  Trio 1:  tools/traduz.c · lib/le_num.h · integration/dualcifra.c   FECHADO
  Trio 2:  integration/campomedio.c · integration/liquida_doador.c · tools/icecube.c   FECHADO
  Trio 6:  tests/migracao.c · lib/qmd.h · integration/transfusao_real.c   FECHADO
  Trio 7:  banco/agentes.c · integration/protocolo.c · lib/le_emb.h   FECHADO
  Trio 8:  banco/fita.c · tests/chicote.c · integration/folhas.c   FECHADO
  Trio 9:  tests/dif.c · tests/microfluidica.c · tests/antissimetrico.c   FECHADO
  Trio 10: integration/cifrando.c · tests/completo.c · tests/po_corpo.c   FECHADO

─── FASE B — long → int32 (+ par onde a ordem exige) ─────────────────────
Objectivo: lib/reta.h e dependentes passam a int/Qz; long só onde ABI exige.

  Trio 1:  lib/reta.h · tests/reta.c · tests/transformada_universal.c   FECHADO
    rt_mdc_i32 · rt_inv_mod_i32 · rt_op_det_i32 · rt_ordem_*_i32
    rt_orbita_i32 · rt_dobra_i32 · rt_forma_i32 · rt_reverte_i32
    rt_companheira_i32 · rt_tracos_i32 (int64)
    §R1 §R5 §R6 §R10 §R11 §R12b §R27 reta.c · §T1–§T7b transformada_universal (int32/int64)
  Trio 2:  banco/sql.c · banco/conversa.c · tests/tex_core.c   FECHADO (migração)
    sql.c: átomo Word_8; Word ISA = Word_8² (Lei 7); GOLD=×σ σ²=σ+1; blobs atomos_*
    erg.c: Word ISA = Word_8² (stride 2); sem long semântico — envelope, não C long
    conversa.c: mod_p → rt_mod; inv_mod → rt_inv_mod_i32 (p≤2³¹−1); chaves } em teste (19290, 20380)
    tex_core.c: fixo_mil acumula int64; fixo_mil_i32; §X19 tex.c (9 casos)
    Nota: conversa compila e corre (331/331 ok — bloco analítico/completude em ℚ FECHADO)
  Trio 3:  tests/geometria_real.c · tests/tex.c · lib/expr.h   FECHADO (migração)
    expr.h: Cel long (ABI 48B); ct_mdc → rt_mdc
    geometria_real.c: g_cmp_i32 (int64); g_ordem via rt_mod; §L7b cross-check (720 800 casos)
    tex.c: agora_ms/timing → int64_t; §X19 já em Trio 2
  Regra: comparar com d16_cmp_prod; guardar com qz_cabe; saturação explícita.

─── FASE C — SOLTAR A ESCALA ─────────────────────────────────────────────
Objectivo: SCALE/fixo_mil/parse_dec6 só leitura/escrita; interior em Qz ou
int puro.

  Trio 1:  integration/protocolo.c · integration/antissim.c · lib/le_emb.h   FECHADO (início)
    le_emb.h: emb_parse_dec6 (EMB_S6=10⁶); EMB_S=10⁴ mantido
    protocolo.c · antissim.c: parse_dec6 duplicado removido; RES/raz/def → int64_t
  Trio 2:  tests/tex_core.c (fixo_mil → rt_le_decimal + unidade)   FECHADO (início)
    reta.h: rt_le_decimal_end (ponteiro de consumo)
    tex_core.c: fixo_mil_de_pq — rt_le_decimal_end + escala 10^-3; §X20 tex.c
  Trio 3:  medidores com S=10⁴ disperso (palavra.c, icecube, transfusao_real)   FECHADO (início)
    palavra.c: EMB_S (raiz β_{n,m}, convergência p.u.)
    transfusao_real.c: emb_f32_bits_para_z; quantiza via EMB_S
    icecube.c: BAND ±EMB_S; parse_fp → rt_le_decimal_end; ICE_SCALE=10⁹

─── FASE D — 32 → int16 (aí sim Z) ──────────────────────────────────────   FECHADO
Objectivo: cada valor cabe em int16 OU em par (alto,baixo) de int16 — mesmo
padrão dual16. Escrever lib/dual16.h (espelho de dual32: 16×16 → par int16).

  Trio 1:  lib/dual16.h · tests/dual16.c · lib/racionais.h (Qz com int16_t)   FECHADO
  Trio 2:  lib/reta.h (coordenadas int16) · tests/cadeia.c §K2b   FECHADO (início)
    rt_opera_i16 · rt_inverte_i16 · rt_ordem_*_i16 · rt_ciclo_i16
    cadeia §K2b: Qz E₁₆ + RtOp int16; reta §R27 cross-check i16/i32/long
  Trio 3:  propagação pelos dependentes   FECHADO (início)
    migracao.c §G5: envelope Qz + d16_mult
    reducao §DW: W_8² → Qz E₁₆ → 𝔽₁₂₇ — 8/8 asserções
    transformada_universal.c §T21: rt_opera_i16 / rt_ciclo_i16

  Nota: a transformada dourada discreta (tests/gerador_analog.c, GA1/GA2) usa
  módulo p=40961, que NÃO cabe em int16 assinado (limite 32767) mas cabe em
  uint16. Aritmética em F_p usa resíduos uint16; intermediários via D32.

Ordem de ataque: A → C (paralelo com B) → B → D → E.
Motivo: matar 64 e escala primeiro; reta.h depende de int32 estável; depois
fechar descida 8 bits do degrau inteiros.


─── FASE E — DESCIDA 8 BITS (degrau inteiros) ──────────────── FECHADA ───
Objectivo: propagar W_8 / uint16-cruz / G_geom pela torre sem contaminar ∼.

  Princípios (inteiros.tex + arquitetura.tex §sec:descida):
    · N ≠ W_8 — byte é envelope, não definição de ℕ
    · (a,b)∼(c,d) testado em uint16 (a+d=b+c) — NUNCA no byte projectado
    · π_wrap / π_sat só na escrita; contadores w8_wrap / w8_saturou à parte
    · G_alg = ℵ₀ (cardinal) ≠ G_geom = |p⁻¹| (operacional); dragão testa dobra
    · G mod 2 → GF(2) — paridade sobrevive (aranha_g §AG6–§AG7)

  Trio 1:  lib/naturais.h · tests/inteiros.c · papers/inteiros.tex   FECHADO
    w8_equiv (uint16 cruz) · w8_proj_wrap/sat · nt_wrap contado
    §NT1–§NT11b (14/14)

  Trio 2:  tests/aranha_g.c · tests/metrica.c · papers/arquitetura.tex   FECHADO (início)
    aranha_g §AG1–§AG11 (11/11) · metrica §M1–§M8b (9/9)
    arquitetura §sec:descida: W_8 ligado a E_16 e cadeia W_8²→long→GF(2)

  Trio 3:  ligação W_8 → E_16 → cadeia.c   FECHADO (início)
    cadeia §KW: W_8² antes de long — 10/10 asserções
    migracao §GW: envelope W_8² → Qz E₁₆ — 7/7 asserções
    reducao §DW: W_8² → 𝔽₁₂₇ via E₁₆ — 8/8 asserções
    dual16 §EW: W_8 → E₁₆ d16_mult — 6/6 asserções
    transformada §TW: RtOp coord. byte — 26/26 asserções
    palavra §PW: fronteira W_8 vs EMB_S interior — 34/34 asserções

  eval.txt (torre/unfold): (a)(b)(c) FECHADOS
    (a) sec:torre — unfold/fold = PROMOVE/DESCE; subida estrita (λ_{k+1}=busca(F_k))
    (b) lib/largura.h + tests/largura.c §LG0–§LG5 = dual16/dual32 bit a bit
    (c) saturo→promove NO HOT PATH (20/08):
        · Qz = {int64_t p,q}; fora de E₁₆ conta e GUARDA exacto (não ±32767)
        · w8_proj_sat → uint16 exacto (não min(,255))
        · medido: promocao.c §SP + promocao_qz.c §SQ + calculo2 + migracao

  Medidores âncora:
    §NT8   equivalência uint16 vs wrap
    §NT11  wrap≠promove; contadores
    §AG2   dragão dobra (G>1)
    §AG6   G=0 e G=2 colidem em B=0 (perda profundidade GF(2))
    §M8    [Z²:T(Z²)] = |det T|

  NÃO fazer:
    · definir ℤ ou ∼ via mod 256
    · confundir G_alg com memória finita
    · chamar homeomorfismo o que é isometria discreta d=µ(△)


─── FASE F — SELAGEM (pós-descida) ─────────────────────────── FECHADA ───
  bateria 513 : 500 verdes · selo 4e94e51a8af9a844
  atestados actualizados: dual16 · palavra · reducao · transformada_universal
  refcruz: inteiros.tex alinhado (dívida sec:dourada pré-existente)


4. O MÉTODO — como se tira um tipo largo ou uma escala
======================================================
Ver o que a asserção DECIDE. Para a migração:

  ORDEM em Q/Z     d16_cmp_prod(a,b,c,d) — produto cruzado, sem formar fração
  ORDEM em Z       comparar int16 directamente, ou par (alto,baixo) se transbordar
  COORDENADAS      (a,b) ∈ Z² — mesma construção, base Z
  RAIZ / CORTE     enquadramento por racionais; rt_raiz_exacta + piso
  ESCALA           NÃO CARREGAR denominador — usar par ou produto cruzado
  64 → 32          long vira par D64; __int128 só na TRAVA do medidor
  32 → 16          partir cada int em dois int16 (mesmo movimento: 16×16 → D32)

REGRAS DURAS (actualizadas 20/08 — bit-level):
  · A MATEMÁTICA MANDA: σ²=σ+1 (ouro/GOLD); ∞+1=−1 (Möbius T_{−1}∘S, §M8 / thm:mediante).
  · NÃO inventar “carry” de CPU. Transporte = thm:transporte a+b=(a⊕b)+2(a∧b) — é ℕ.
  · Largura semântica = Word_8 / torre (F_1→…→F_8→ℕ→ℤ→…). NUNCA long do C como envelope.
  · long SÓ em I/O de sistema (printf, stat, off_t) — cast explícito; não é tipo da ISA.
  · O 64 SAI do código de produção. A trava pode usar __int128; o resto usa D32/I128.
  · long long → int64_t (Fase A); onde ainda há long interior → Word_8² ou subir a torre.
  · SCALE/fixo_mil SÓ na fronteira I/O — nunca dentro de ok().
  · REDUZIR antes de multiplicar (qz de racionais.h; rt_para_unidade na reta).
  · qz_saturou / d16 recusa — contar à parte, não truncar em silêncio.
  · Overflow em 16 bits É rotação; a topologia preserva o invariante.


5. LEVANTAMENTO (git ls-files *.c *.h, 19/08/2026)
===================================================
Contagem bruta de tokens (código, inclui comentários):

    long (bare)              ~19 513
    long long                  ~836
    unsigned long long         ~121
    __int128                   ~151
    int32/uint32 (explícito)    ~30
    int16/uint16 (explícito)     ~2
    unsigned (palavra 32)      ~1 972

Ficheiros que JÁ usam dual32/racionais (17):
    lib/dual32.h lib/racionais.h lib/reta.h lib/analise.h lib/metrico.h
    lib/projetiva.h lib/ramos.h banco/conversa.c
    tests/dual32.c tests/cadeia.c tests/calculo2.c tests/continua.c
    tests/migracao.c tests/ordenado.c tests/primitivas.c tests/reducao.c
    tests/supremo.c

TOP por 64-bit:
    59  tests/gguf.c         57  tools/icecube.c      57  integration/campomedio.c
    50  tools/traduz.c       47  integration/dualcifra.c  37  tests/prensor.c
    34  tests/rn.c           34  tests/palavra.c      32  tests/dual32.c (trava)

TOP por long:
  1977  banco/conversa.c    835  tests/tex_core.c    415  tools/traduz.c
   395  lib/reta.h           360  tests/geometria_real.c
   324  tests/reta.c         316  tests/transformada_universal.c

Escalas a eliminar do interior:
    SCALE=10⁶     integration/protocolo.c, integration/antissim.c
    EMB_S=10⁴     lib/le_emb.h
    fixo_mil      tests/tex_core.c (10³ de ponto)
    S=10⁴         tests/palavra.c, disperso noutros medidores


6. FECHADO — migração double/float (b2e2ad8)
============================================
3906 → 0 ocorrências double/float. 0 régua. Ferramentas: tools/doubles_todos.py.

Patrões que funcionaram:
  · produto cruzado __int128 → d32_cmp_prod (depois o __int128 também sai)
  · IEEE bits em ull / le_f64_bits_pq — sem tipo double no compilador
  · tempos em long long ns, printf em inteiros
  · str2dbl → bits; libc via __typeof__(strtod) na trava


7. FUNDAMENTO TEÓRICO (papers/racionais.tex)
=============================================
O paper formaliza:

  Thm (Construção inteira de Q):
    V_rat/∼ ≅ Q, com prova explícita (equivalência, boa definição, neutros,
    oposto, inverso, distributividade).

  Thm (O swap é a inversão multiplicativa):
    ι:V×→V× preserva ∼, induz ῑ:Q×→Q×, ῑ(x)=x⁻¹, Fix(ῑ)={±1}.

  Oito leis realizadas neste andar:
    Lei 0 — mista (Q + P¹); Lei 1,3,5 — internas; Lei 2 — canónica;
    Lei 4,6,7 — herdadas da torre.

  Envelope E₁₆ definido; saturação explícita; red(0,b)=(0,1).

  Distinção: conjugação de campo ≠ dobra s†=−1/s.

  Hurwitz/Gentil/Lebesgue: interpretação herdada, não usada na prova de Q.


8. O GUME — muta e confirma que CAI
====================================
    S=/tmp/gume; mkdir -p $S
    g(){ rm -f $S/g $S/g.c
         sed "$2" tests/X.c > $S/g.c
         cc -O2 -std=c99 -I tests -I lib -o $S/g $S/g.c -lm 2>/dev/null || return
         (cd tests && timeout 300 $S/g >/dev/null 2>&1; echo "exit=$?"); }

Cinco modos de mentir: binário velho, sed que não aplica, gume relaxado,
mensagem que não pode falhar, exit sombreado. NÃO declares `falhas` local.


9. ARMADILHAS DO ESCOPO
========================
  (1) integration/*.c compila mas NEM SEMPRE corre na bateria.
  (2) doubles_todos.py conta float; longs_todos.py conta long long/__int128/ull.
  (3) UM LIMIAR SÓ CONTA SE DECIDIR — fechado em double; vigiar overflow silencioso.
  (4) Números nos papers desactualizados — recontar com ferramenta certa.


10. FORMAS DA ASSERÇÃO QUE NÃO PODE FALHAR
============================================
  python3 tools/tautologia.py   releitura (forma 13)
  python3 tools/tectos.py       literal nu, recíproca, f∘f⁻¹, resto
  python3 tools/residuo_zero.py resíduo 0 com limiar
  python3 tools/orfas.py        contador que não conta

Pergunta sempre: QUE ENTRADA FARIA ISTO FALHAR?


11. O QUE NÃO FAZER
====================
  · replace global — editar por linha
  · inventar peça — está em lib/reta.h, dual16.h, racionais.h
  · carregar SCALE «porque o printf era mais fácil»
  · __int128 «só aqui» — é sempre o próximo long long
  · commitar sem git status — artefactos ELF na raiz são lixo de cc
  · confundir conjugação de campo com dobra s†=−1/s


12. PEÇAS DA LIB
=================
  lib/dual16.h      D32, d16_mult, d16_cmp_prod, d16_par_soma/mult — FEITO
  lib/dual32.h      D64, d64_*, d32_par_soma/mult; inclui dual16.h
  lib/racionais.h   Qz {int16_t p,q}, qz_add/mul/cmp, qz_saturou — Q em 16 bits
  lib/reta.h        RtOp int16_t; rt_ipow/sinal/mod int32_t; rt_inv_mod long; rt_opera_i32;
                    rt_dir/cruz/cruz3_i32; rt_ciclo_i32; RtCf RAM = vista; FC canónica → slot
  lib/rt_cf_slot.h  acumulador FC: slots; fd≥0 → .mem; rt_cf_slot_mem_abre;
                    rt_cf_slot_word(ix,fd); cf_slot_base em slot_map.h
  lib/slot_map.h    S_CF=2048, S_CF_STRIDE, S_CF_WORDS, S_CF_MEM_BYTES, cf_slot_base
  lib/slot_mem.h    SlotWord = Word_8 = 1 B/slot; par (a,b) = dois slots (Lei 7)
  lib/palavra8.h    Word_8 bit a bit (⊕∧); F_8; ℕ; largura 8→16→32; W8Par ~
  lib/isa_disk.h    registos A,B,R = Word_8²; ALU 8-bit; MOVE; sem long
  banco/conversa.c  átomo Word_8; par lógico = 8 átomos (dois u32 LE); texto 1 B/átomo
                    ABI nova — re-semear bases .fala (layout antigo 16 B incompatível)
  lib/calculo.h     fn_quociente e fn_desloca binomial (E₁₆)
  lib/medida.h      md_det_potencia_mod em long[2][2] — mod p fora de E₁₆
  lib/isa_disk.h    rotação ordem 4/2 — sem trig float
  lib/le_num.h      str2dbl → bits IEEE (__int128 a eliminar na Fase A)


13. FERRAMENTAS
================
  tools/doubles_todos.py    FECHADO — 0 double/float
  tools/longs_todos.py      FEITO — ver §5
  tools/tautologia.py       candidatos releitura
  tools/tectos.py           janela, literal, recíproca


14. TRÍADE E CHECKPOINT
========================
  tau=−1  corpo_topologico · tau=0  corpo_algebrico · tau=+1  corpo_analitico
  Checkpoint: memória → sincroniza.sh → git commit (só quando pedido)


15. A FRENTE AGORA
==================
  20/08/2026 — metal bit-level: Word = Word_8²; σ²=σ+1; ∞+1=−1. Sem long C na ISA.

  DIRETRIZ (não negociar):
    · Envelope = Word_8. Par ℤ = dois átomos (Lei 7). GOLD = ×σ, σ²=σ+1.
    · ∞+1=−1 PROVADO (descida_mobius §M8 · corpo_algebrico · inteiros thm:mediante).
    · Não “carry”: transporte é thm:transporte. Não alargar com long “porque o C tem”.
    · Faixas/endereços largos (nonce, merkle base) = átomos u32 LE, não campos long da Word.
    · Medir GOLD/volta no envelope Word_8; coef. que crescem sobem a torre (não long C).
    · Verif.: sql teste 86/86 ; erg RESÍDUO 0 ; barramento 5/5

  FECHADO metal (20/08, continua):
    · lib/word_isa.h — Word = Word_8² partilhado
    · lib/palavra8.h + tests/palavra8.c §P0–§P4 (5/5) — citado em inteiros/arquitetura
    · sql.c + erg.c — Word_8²; GOLD σ²=σ+1
    · barramento.c — Slot = Word_8² (2 átomos; cifra no envelope)
    · isa_disk.h — API uint8_t/int8 envelope; sem long semântico
    · traduz.c — multi-átomo (PAR_ATOMS=8); Slot.a/.b = uint32_t; 7/7 UNIT
    · conversa.c — Slot lógico uint32_t (8 átomos u32 LE); ABI disco inalterada

  PENDENTE metal:
    · (vazio — envelope Word_8 + pares lógicos tipados)

  FECHADO TeX-vivo:
    · DF21 + TP20 + MT16 + HM14 + PN10 + HI9 + UN23 + TR16 + EX15 + TS16 + FQ16
    · LI16 + DU14 + EL12 + TN17 + EX20
    · CM…C3 + AR + CL + CP + AL
    · corpus/docs/*_exercicios.tex + semear

  Uso:
    TIFFANY_ROOT=$PWD banco/bin/conversa banco/.fala/vivo responde "espaco vetorial oito leis"
    TIFFANY_ROOT=$PWD banco/bin/conversa banco/.fala/vivo responde "divisibilidade transitiva"
    TIFFANY_ROOT=$PWD banco/bin/conversa banco/.fala/vivo responde "ponto no infinito neutro"

  Pendente natural:
    · FASE PG — Trios PG1–PG4 FECHADOS; próximo = Trio PG5 (pqlike opcional)

  Verificação (feita 20/08):
    bash tools/semear.sh banco/.fala/vivo          → ok
    bash tools/indexa_tex_vivo.sh banco/.fala/vivo → pronto
    smoke: espaco vetorial / divisibilidade / ponto infinito / cauchy / nucleo dual
    limpeza TeX: ≠ √ ∘ nos folhas exterior/tensor/forma/linear/dual
    hot path: promocao + promocao_qz + calculo2 + migracao + sql

  IMPORT CORPO: ./tiffany_sql .torre/reino "IMPORT CORPO 'app/src/corpo.json' '.'"


16. FASE PG — INTERFACE POSTGRES DO ZERO (entrada das aplicações)
=================================================================
ORDEM DO COORDENADOR (20/08): Postgres como *interface* de referência —
mapear a API/protocolo e implementar AQUI do zero. NÃO baixar o servidor
Postgres, NÃO meter o binário oficial, NÃO “embrulhar” um postgres instalado.
O motor continua a ser o metal da casa: sql.c → ISA → .mem (Word_8).

PORQUÊ
------
  · Hoje a porta comercial é SQL textual (`banco/sql.c`, HTTP `/sql`,
    `interface_padrao=sql`). Apps de fora (ORM, drivers, painéis) falam
    o *wire* Postgres (FEBE) ou a superfície libpq — não o nosso argv.
  · Implementar essa face = entrada estável para aplicações sem ceder o
    núcleo. A forma dos bits vem da documentação Postgres; a realização
    é Tiffany.

O QUE É / O QUE NÃO É
---------------------
  É:
    · Protocolo FEBE (Frontend/Backend) implementado em C da casa
    · Listener TCP que fala as mensagens do protocolo (referência:
      docs.postgresql.org — Protocol; Message Formats; Message Flow)
    · Tradução: mensagem FEBE → string/AST que `sql.c` já executa →
      resposta FEBE (RowDescription / DataRow / CommandComplete / …)
    · Mais tarde: fachada C estilo libpq (`PQconnectdb`, `PQexec`, …)
      que fala com O NOSSO listener (código nosso, não libpq do SO)

  Não é:
    · apt/dnf install postgresql / docker postgres / libpq-server
    · Copiar código-fonte do postgres para o repo
    · Substituir o motor `.mem`/ISA por MVCC/WAL alheio
    · Compatibilidade 100% dialecto no dia 1 (só o que o metal já mede)

REFERÊNCIA CANÓNICA (ler, não vender)
-------------------------------------
  Docs oficiais (só mapa de bits / fluxo):
    · Overview + Message Flow + Message Formats (FEBE)
    · Startup sem type-byte; resto: 1 byte tipo + int32 len + payload
    · Simple Query: Query('Q') → RowDescription('T')* DataRow('D')*
      CommandComplete('C') ReadyForQuery('Z') | ErrorResponse('E')
    · Extended Query: Parse('P') Bind('B') Describe('D') Execute('E')
      Sync('S') → ParseComplete/BindComplete/… / ReadyForQuery
    · Startup: AuthenticationOk / ParameterStatus / BackendKeyData /
      ReadyForQuery; Terminate('X'); SSLRequest (recusar ou stub)
  Versão-alvo do protocolo: 3.0 (o que os clientes modernos pedem).
  Catálogo/pg_*: FASE posterior (fachada só do que apps pedem ao ligar).

ARQUITECTURA NA CASA
--------------------
                    ┌─────────────────────────────┐
   app / driver ───►│  pgwire (Tiffany, do zero)  │
   (psql, JDBC,     │  FEBE encode/decode         │
    libpq-cliente,  └──────────┬──────────────────┘
    ORM, …)                    │ Query text / params
                               ▼
                    ┌─────────────────────────────┐
                    │  banco/sql.c  (já existe)   │
                    │  SQL → bytecode ISA         │
                    │  memória = .mem (Word_8)    │
                    └─────────────────────────────┘

  Peças novas (nomes provisórios — criar quando o Trio 1 arrancar):
    lib/pgwire.h          frames FEBE: le/escreve tipo+len+payload
    lib/pgmsg.h           constantes dos type-bytes + builders/parsers
    banco/pgwire.c        listener TCP; sessão; despacho Simple Query
    tests/pgwire.c        medidor bit-a-bit das mensagens (sem rede real
                          se possível: buffer in-memory)
    tests/pgwire_cli.c    cliente mínimo nosso (não libpq do SO) contra
                          o listener — equivalência ponta a ponta
  Opcional depois:
    lib/pqlike.h          API estilo PQconnectdb/PQexec → nosso wire
    papers/arquitetura    §sec:api-sql: “porta FEBE = face Postgres”

MÉTODO (o da casa — não atalhar)
--------------------------------
  1. Caminho novo primeiro (pgwire) — sql.c intacto
  2. Equivalência medida: mesma query via argv sql.c vs via FEBE
     → mesmas linhas / mesmo CommandComplete tag
  3. Só depois: `/sql` HTTP ou manifesto a apontar o listener como
     entrada preferida das apps
  4. Dialectos e pg_catalog: um comando de cada vez, com medidor

SUBCONJUNTOS SQL (o que o metal já tem → prioridade FEBE)
---------------------------------------------------------
  Já em sql.c (entrada imediata do Simple Query):
    CREATE TABLE · INSERT · SELECT · WHERE · UPDATE · DELETE
    INSERT TEXTO / BUSCA / ACHA · IMPORT CORPO / GET CORPO · …
  Fora do dia 1 (recusar com ErrorResponse claro, não silêncio):
    transactions completas, COPY binário, LISTEN/NOTIFY, cursores
    declarados, funções PL, tipos geométricos PG, extensões

TRIOS (ordem de trabalho — um de cada vez)
------------------------------------------
  Trio PG1 — FRAME + STARTUP + AUTH TRUST          FECHADO (20/08)
    · lib/pgmsg.h + lib/pgwire.h + tests/pgwire.c §W0–§W2b (4/4)
    · AuthOk, ReadyForQuery, BackendKeyData, Terminate, Startup↔parse,
      SSLRequest→'N', handshake trust, ErrorResponse, Query encode
    · Sem TCP

  Trio PG2 — SIMPLE QUERY → sql.c                  FECHADO (20/08)
    · banco/sql_api.h + sql_abrir/fechar/executa (captura SELECT/tags)
    · RowDescription / DataRow / CommandComplete / ReadyForQuery
    · tests/pgwire.c §W3; link -DSQL_NO_MAIN banco/sql.c
    · Equivalência: CREATE/INSERT/SELECT → FEBE no buffer

  Trio PG3 — LISTENER + BASE + MANIFESTO           FECHADO (20/08)
    · banco/pgwire.c: listen 127.0.0.1 (5432 se livre, senão 55432)
    · SSL→'N', Startup trust, Simple Query → sql_api → FEBE
    · conecthus/backends/manifesto.json: entrada_apps=pgwire
      (interface_padrao continua sql)
    · tests/pgwire.c §W4; tools/pgwire_smoke.sh
    · papers/arquitetura.tex §sec:api-sql: parágrafo FEBE

  Trio PG4 — EXTENDED QUERY (Parse/Bind/Execute/Sync) FECHADO (20/08)
    · banco/pgwire_sess.h: statement/portal em RAM; $n → texto no Bind
    · ParseComplete / BindComplete / Execute(rows) / Sync→Ready
    · Describe(S)→ParamDesc+NoData; Close
    · tests/pgwire.c §W5–§W6: Bind $1=7 ≡ Simple WHERE a=7

  Trio PG5 — FACHADA libpq-LIKE                     FECHADO (20/08)
    · lib/pqlike.h: PQconnectdb/PQexec/PQntuples/PQgetvalue/PQcmdStatus/
      PQresultStatus/PQerrorMessage/PQfinish — sobre o FEBE da casa, sem -lpq
    · SEM malloc: um PGresult por ligação. Consequência DITA no cabeçalho —
      dois PQexec seguidos sobrescrevem o primeiro resultado
    · tests/pgwire.c §W7: a MESMA consulta por sql_api e por PQexec dá as
      mesmas linhas, células e tag (0 divergências) — dois caminhos, não um
    · e o GUME apanhou um defeito REAL do motor: ver §20

  Trio PG6 — CATÁLOGO MÍNIMO (quando uma app real exigir)   ← PRÓXIMO
    · Respostas stub a `SELECT version()`, `SHOW`, `pg_catalog` mínimo
      que o driver pede no connect — cada um medido, sem fingir PG completo

NÃO FAZER
---------
  · Instalar ou vendorar o servidor PostgreSQL
  · “Compatível com Postgres” como slogan sem medidor FEBE
  · Meter WAL/MVCC/processos do PG no metal
  · Alargar Word/ISA “porque o driver pediu int8” — promove pela torre
  · Misturar o canal UDP do sql.c com FEBE sem contrato

PRÓXIMO PASSO OPERACIONAL
-------------------------
  Trio PG5 (opcional): fachada pqlike. Ou PG6 se uma app real pedir catálogo.

COMANDOS ÚTEIS
-------------------------------
  cc -O2 -std=c99 -w -Ilib -Ibanco -DSQL_NO_MAIN -DPGWIRE_NO_MAIN -o /tmp/pgwire \
     tests/pgwire.c banco/sql.c banco/pgwire.c -lm && /tmp/pgwire
  cc -O2 -std=c99 -w -Ilib -Ibanco -DSQL_NO_MAIN -o /tmp/pgwire_srv \
     banco/pgwire.c banco/sql.c -lm
  /tmp/pgwire_srv /tmp/reino 55432
  bash tools/pgwire_smoke.sh
  bash tools/bateria.sh --reatesta pgwire
  # cliente nosso — nunca exigir psql do sistema para fechar o Trio


17. A PROMOÇÃO PROPAGADA — a metade que a decisão de 20/08 deixou por fazer
===========================================================================
A decisão «saturo→promove» (§eval.txt (c)) trocou o Qz de int16 para int64 e
tirou o grampo dos ±32767. O que ela mudou por baixo, e ninguém propagou, foi o
SIGNIFICADO de `qz_saturou`: antes «saiu de E₁₆» era «truncou»; depois passou a
ser «subiu de andar, e o valor está INTACTO». Seis medidores caíram por isso, e
nenhum deles falava de saturação — falavam de Cauchy, de retração e de séries.

  bateria antes: 528 : 522 verdes, 6 falhas   ·  depois: 528 : 528, 0 falhas

O QUE CAIU, E PORQUÊ
--------------------
  dourada_bidual  NÃO COMPILOU — `isa_norma2` saiu do isa_disk.h na migração
                  Word_8 e o medidor que a chama ficou sem ela. Um medidor que
                  não compila não falha: DESAPARECE do total.
  edo             NÃO COMPILOU — `unsigned int ae[12]` contra a assinatura
                  `const unsigned long *` de `nt_convergentes`.
  torre_alg §T2   a FC no disco tinha o termo em UM átomo e o sinal como byte
                  ASSINADO num envelope sem sinal: −1/3 voltava 255/3 e o
                  quociente 292 enrolava para 36. Silêncio nos dois casos.
  entrega §E2/§E4 o mesmo defeito, visto de outro lado: 80 de 10000 decimais
                  não voltavam, e «-0.1» reconstruía-se como «25.500».
  tres_caminhos   `cy_teto_honesto` lia `qz_saturou` para saber até onde a
                  sucessão ainda é a sucessão. Com a promoção deixou de ver o
                  que congela (o encaixe a desistir) e passou a cortar o que
                  não congela (a promoção): tecto 30 onde o encaixe parara no 14.
  conversa §C43   `c2_soma_segura` tirou o teste `x.saturo` e não pôs nada no
                  lugar: passou a devolver 1 SEMPRE. Σ1/n³ até 40 pede
                  lcm(1..40)³ ≈ 1,5·10⁴⁷, perdia-se, e a asserção cujo texto diz
                  «uma soma negativa passou por resultado» voltou a deixá-lo passar.

AS PEÇAS NOVAS
--------------
  qz_perdeu       racionais.h — a PERDA (nem no int64 coube; valor descartado).
                  É gaveta separada de `qz_saturou`, que conta a PROMOÇÃO.
  rz_parou        reais.h — o passo do encaixe que NÃO se deu.
  cy_congelou     cauchy.h — o tecto próprio da sucessão a repetir o último termo.
  cy_desonesto()  a soma dos três: um termo é honesto enquanto NENHUM se mexe.
  rz_passo        ganhou guarda i128 — a órbita cresce ×1,85/passo e transbordava
                  em silêncio (antes não chegava lá porque `qz()` grampeava).
  rz_encaixota    já não trava em E₁₆ (isso era truncar); trava no tecto
                  DECLARADO do andar (rz_cmp, RZ_TETO=2³⁰) e conta a paragem.
  FC nos slots    a_k = Word_8² (dois átomos, baixo+256·alto, 0..65535); sinal =
                  um BIT; acima de RT_CF_TERMO_MAX RECUSA-SE com `saturou`.
                  S_CF_STRIDE = 2·RT_CF_MAX+3.

MEDIDORES NOVOS / ENDURECIDOS
-----------------------------
  tres_caminhos §RE6b   as três bandeiras, cada uma pelos DOIS lados
  torre_alg §T2 (tecto) 65535 encosta e volta · 65536 é recusado
  reta §R12f            era TAUTOLOGIA (repetia a macro); agora escreve a
                        palavra 1 e verifica que a 0 volta inteira
  reta §R12g            o caso era 22/7 (termos 3 e 7): não exercitava o átomo
                        alto. Passou a 1000/3, cujo quociente 333 obriga os dois.

O GUME (dez mutações, todas caem)
---------------------------------
  sinal→byte assinado · termo→um átomo · recusa do tecto desligada · stride
  curta · tecto honesto a ler qz_saturou · rz_parou mudo · cy_congelou mudo ·
  rz_passo sem guarda · c2_soma_segura sem qz_perdeu           (torre_alg, reta,
  entrega, tres_caminhos, conversa)

FORMATOS: 216 → 0 avisos -Wformat no repo
------------------------------------------
A mesma metade por propagar, no outro sítio. O Qz alargou para int64 e os
`printf` ficaram em `%d`; a Fase A estreitou `long long`→`long` e `int` e os
formatos ficaram em `%lld` e `%ld`. Nenhuma asserção o via — em x86-64 o valor
sai certo por acidente de ABI —, mas trunca acima de 2³¹, que são exactamente
os valores que a promoção existe para guardar.

  · 78 sítios %d  ← int64_t   · 31 sítios %lld ← long   · 53 sítios %ld ← int
  · 24 à mão, onde um %d legítimo convivia com um Qz
  Critério: só se corrige a linha quando o nº de specs = nº de avisos; o resto
  é à mão. Verificação: `cc -Wformat` a zero, e a bateria a 528:528.

  E havia GARBAGE a sério, não só UB: `tests/sshb.c` tinha `long rtt = 0.5`
  (a Fase A trocou o tipo e deixou a constante em vírgula) — o 0,5 ms virava 0,
  a tabela de latências inteira saía de uma conta em double impressa com %ld, e
  nenhuma asserção lia o que estava escrito. Passou a inteiros, em DÉCIMOS de ms.

O QUE ISTO DESTAPOU — e NÃO está fechado
-----------------------------------------
  `tools/doubles_todos.py` conta o TIPO `double|float`. Ele está em 0, e isso é
  verdade. Mas o TIPO não é a aritmética: um literal `2.0` faz uma conta em
  vírgula flutuante sem escrever a palavra. São 81 literais em código, em 28
  ficheiros — tools/libc.c 15, tests/sshb.c 11 (agora 0), banco_relogio.c 9,
  prisma.c 5, analog.c 4. A régua não vê o alvo, e «0 doubles» é a definição da
  consulta, não um facto sobre o repo. Medir com:

      grep -nE '(^|[^A-Za-z0-9_.])([0-9]+\.[0-9]*|\.[0-9]+|[0-9]+[eE][-+]?[0-9]+)' \
           $(git ls-files '*.c' '*.h')     # depois de tirar comentários e strings

  Decisão do coordenador: migrar os 81 é trabalho próprio, por trios, e não
  entrou aqui.

PRÓXIMO PASSO OPERACIONAL
-------------------------
  Ver §18 e §20: literais e PG5 fechados. Segue Trio PG6 (catálogo mínimo:
  version(), SHOW, o pg_catalog que um driver pede ao ligar) quando uma app
  real o exigir — cada resposta com medidor, sem fingir PG completo.


18. A ARITMÉTICA DE VÍRGULA — 67 → 0, e o que ela escondia
===========================================================
Fechado nesta etapa. `doubles_todos.py` contava o TIPO `double|float` e estava em 0;
o `2.0` faz a conta sem escrever a palavra. Não era estilo: CINCO asserções eram
TAUTOLOGIAS por causa dele, e nenhuma podia cair.

O QUE ESTAVA A PASSAR SEM MEDIR
-------------------------------
  balanco §B3     `long f = 0.1 + 0.2` truncava para 0, e o exemplo canónico do
                  ponto flutuante comparava «0 ≠ 0.3». Passava sempre.
                  → agora nos BITS: `le_f64_bits_pq(1,10)` e `(2,10)`, soma exacta
                    sobre o denominador diádico comum, e o arredondamento dá
                    3fd3…34 contra 3fd3…33 — um ULP, em inteiros. Com o controlo
                    diádico 1/4 + 1/4 = 1/2, que tem de dar IGUAL.
  prisma §S3      `long med = 1.0; med*2.0/3.0` zerava no 1.º nível; a tabela
                  imprimia 1,0,0,0,0,0,0 e «a medida vai a zero» era 0 < 0.1.
                  → a medida é 2^k/3^k em ℚ, confere contra pedaços/3^k, encolhe
                    ESTRITAMENTE nos 6 passos, e «vai a zero» traz o ε = 1/100 com
                    a testemunha k = 12.
  base §B5        `long t = 0.7` → cada termo da série `s*t/p` dava 0, exp(tA)
                  ficava a IDENTIDADE, e «o fluxo é ortogonal» era ‖I·x‖ = ‖x‖.
                  → e não se põe o 0,7 de volta: exp TRUNCADA não é ortogonal nem
                    em ℝ. Mede-se por CAYLEY, que é exacto em ℤ:
                    ‖(I−A)x‖ = ‖(I+A)x‖ ⟺ (I+A)(I−A) = I − A² dos dois lados.
                    Gume: com M = S+D (SIMÉTRICA) as normas separam-se, 100/100.
  dissipa §D3     kT·ln2 em `long` a partir de 1,38·10⁻²³ = ZERO, e os DOIS lados
                  de Landauer imprimiam 0 — a distinção que a secção existe para
                  mostrar não aparecia.
                  → em rJ (10⁻²⁷ J): k_B·T = 4 141 947 rJ EXACTO (k_B é exacto no
                    SI), ln2 entra como 6931471805599453/10¹⁶ e é o único factor
                    arredondado. Destrutiva 376 304 828 416 rJ, involutiva 0.
  banco_relogio   `alfa = M/nslot` era divisão INTEIRA (= 0), e a «forma fechada»
                  (1 + 1/(1−alfa))/2 avaliava-se sempre em 0 devolvendo 1.
                  → alfa = M/n em ℚ, fechada = (2n−M)/(2(n−M)), comparação por
                    produto cruzado. E ela não discriminava: a 3000/65536 vale ~1
                    e a folga de +1/2 engolia qualquer coisa. Varre-se alfa =
                    1/8, 1/4, 1/3, 1/2, 7/10 numa tabela sintética, e sem
                    tolerância inventada: a fechada é ASSINTÓTICA sob hashing
                    uniforme e o contador fica sempre ABAIXO dela — um hash bom
                    agrupa menos que o acaso —, logo afirma-se MAJORANTE +
                    CRESCIMENTO. A constante 1 respeita o majorante e não cresce.
  linear §1       `r = (px[i]+2.0) + 1.0/r` num long: 1/r cai em (0,1/2] e some no
                  truncamento. «A imagem inteira como uma fracção contínua» valia
                  o PRIMEIRO PIXEL mais dois — e o ficheiro não tinha asserção
                  nenhuma para o dizer.
                  → o representante é o PAR p/q, e diz-se onde deixa de caber: dos
                    1024 pixels só os últimos 9 cabem em 64 bits. Ganhou asserção
                    (|p·q'−p'·q| = 1 e a paragem verificada no pixel seguinte) e o
                    exit passou a seguir as unidades.
  operador_dual   `long F = i*0.37` colapsava 17 índices em 5 valores, 7 deles 0.
                  → `i*37`: 17 forças DISTINTAS, e a contagem diz que viu 17.

O RESTO, MECÂNICO
-----------------
  tectos    (long)9e18 · 1e15 · 1e18 → literais inteiros (deforma, matricial, navegante)
  tempos    (t.tv_sec)*1e9 + … → 1000000000L (banco_relogio, canal, grupo)
  %        100.0*x/y com %.1f → 1000*x/y em /10 e %10 (gauss, koch, largura,
            palavra, promocao, promocao_qz, tex, pontryagin)
  mortos    #define M_PI / PI / Q_E / K_B / T_AMB / VT e o próprio <math.h>:
            NENHUM era usado. As chamadas a sin/cos/exp que estes ficheiros citam
            vivem todas em COMENTÁRIOS. (eletrico.h, analog, deforma,
            gerador_analog, microfluidica)

A RÉGUA — e é esta a correcção de raiz
--------------------------------------
  `tools/doubles_todos.py` passou a contar LITERAIS além do tipo, com a trava
  DECLARADA e com motivo: `tools/libc.c` não é compilado por cc — é fonte para o
  `traduz.c` subir a wasm, onde `f64` é o tipo NATIVO, e ele IMPLEMENTA o atof.
  São os 15 que sobram, e agora aparecem como trava e não como silêncio.

      python3 tools/doubles_todos.py    → 0 tipo · 0 literais · 15 na trava

O GUME (doze mutações)
----------------------
  a soma dos bits vira o bit de 3/10 · o controlo diádico mente · a medida de
  Cantor não encolhe · a testemunha k desaparece · Cayley com o SIMÉTRICO · o
  controlo simétrico deixa de separar · kT·ln2 volta a zero · a força colapsa ·
  o majorante vira a constante 1 · as densidades ficam todas iguais · as DUAS
  redes do linear saem.  Todas caem.

  E DUAS SOBREVIVERAM PRIMEIRO, com causas diferentes — vale registar:
   · tirar UMA das redes do `linear` não faz cair, porque a outra apanha: guarda
     REDUNDANTE, e o TECTO=2⁶²−1 é conservador (o passo ainda cabia). E o
     |det| = ±1 NÃO detecta transbordo: a recorrência preserva-o MÓDULO 2⁶⁴.
   · a asserção do `linear` tinha uma escapatória minha — «consumiu tudo» —, e a
     mutação entrava por ela. Só é aceitável onde CABE: com a_k ≥ 2 o par cresce
     como Fibonacci e passado o 90.º termo nenhum par de 64 bits o segura.


19. O HEADER FORA DO REPOSITÓRIO — a órfã do C
================================================
Ao preparar o checkpoint: `banco/sql.c` está na bateria, inclui `banco/sql_api.h`,
e esse ficheiro NÃO EXISTIA no git. O `.gitignore` tinha

    banco/*
    !banco/*.c   !banco/*.sql   !banco/*.sh   !banco/*.txt   …      (sem `.h`)

logo `sql_api.h`, `pgwire_api.h` e `pgwire_sess.h` — os três headers dos Trios
PG1–PG4, declarados FECHADOS — viviam só no disco desta máquina. Faltavam também
`lib/palavra8.h` e `lib/word_isa.h`, por estarem simplesmente por adicionar.

A BATERIA ESTAVA VERDE SOBRE UMA ÁRVORE QUE O GIT NÃO REPRODUZ. É a mesma
assinatura do `refs.c`: nada falha deste lado. Aqui compila, num clone não existe.

  correcção   `!banco/*.h` no .gitignore + os cinco ficheiros adicionados
  medida      tests/refs.c §R5 — todo `#include "x"` de um ficheiro RASTREADO
              aponta para um ficheiro que o git também tem, com controlo negativo
  verificação `git ls-files | tar` para uma árvore limpa; `banco/sql.c` e o
              medidor `pgwire` compilam e correm lá (§W0–§W6 verdes)

  E O RUÍDO PRIMEIRO: a §R5 nasceu a acusar 88 e 85 eram falso positivo meu —
  `banco/../lib/disco.h` não é `lib/disco.h` sem colapsar os `X/..`. Normalizado,
  são 5 (três headers). Duas réguas independentes (C e Python) deram o mesmo.

  Os ELF de `cc` soltos em tests/ (calculo2, dominios, koch, refs, reta, …)
  entraram na lista do .gitignore, que já os enumerava um a um.


20. A TABELA NÃO TINHA NOME — e o gume do PG5 é que o apanhou
===============================================================
O §W7 mede a fachada pelos DOIS caminhos (sql_api directo vs PQexec pelo socket) e
traz o gume do lado que tem de falhar: uma consulta inválida tem de chegar como
`PGRES_FATAL_ERROR` com mensagem. Ela chegava como **PGRES_TUPLES_OK**.

A causa não era da fachada. Era do motor:

    SELECT * FROM tabela_que_nao_existe   →   devolvia as linhas da tabela que lá está

O catálogo (`S_CAT`) guardava só `{ncols, nrows}`: a relação NÃO TINHA NOME. O
`varre()` lia o `FROM x` para uma variável e nunca a usava — nos TRÊS ramos
(SELECT, UPDATE, DELETE) —, e o `insere()` o mesmo com o `INSERT INTO x`. Uma
aplicação que se enganasse no nome recebia dados de outra tabela e não tinha como
saber; pela porta FEBE isso chega ao driver como um SELECT bem sucedido.

  correcção   S_NOME = 192..207 (16 Words, dois caracteres por Word — Lei 7),
              escrito no CREATE TABLE, conferido no SELECT/UPDATE/DELETE/INSERT
  normalizar  minúsculas: `FROM T` depois de `CREATE TABLE t` tem de passar, e
              recusar isso era uma recusa ERRADA — das que só aparecem em produção
  compat      base antiga lê nome vazio → aceita qualquer nome (a mesma porta que
              o CORPO_INTEIRO tem por ser o código 0)
  mensagem    `relation "x" does not exist` — a redacção que um driver espera

  Verificação: sql 86/86 · pgwire §W0–§W7 · pgwire_smoke ok · bateria 528:528

  GUME (cinco mutações, todas caem): a recusa desligada · o CREATE sem guardar o
  nome · PQgetvalue a devolver sempre a mesma célula · a tag do lado FEBE mudada ·
  o PQexec sem ler DataRow.
  E uma sobreviveu primeiro por gume mal feito meu: apagar a COMPARAÇÃO da tag não
  faz cair, porque a tag batia à mesma — mutar o DETECTOR remove a medida em vez de
  partir o código. A mutação certa é no DADO.


21. A ÁRVORE DA EXPRESSÃO PISAVA O CORPO DA COLUNA
====================================================
Achado a seguir ao §20, a ler o mapa de slots do `sql.c`:

    S_CORPO   60..67    o corpo declarado de cada coluna {código, parâmetro}
    S_EXPR    64..191   os temporários da árvore do WHERE

As duas regiões **sobrepunham-se em 64..67**, que são as colunas 4 a 7. Numa
tabela com CINCO colunas ou mais, o primeiro SELECT com expressão escrevia o
temporário por cima da declaração — e ficava no disco. Medido: uma coluna
`MORFICO(6)`, o par (3,6), voltava (1,0), que é RACIONAL. A partir daí a
aritmética daquela coluna é a álgebra errada, para sempre, e nada o diz.

Ninguém lá chegou porque TODAS as tabelas dos medidores têm três colunas —
«varrer onde o defeito não vive», e desta vez o regime era o número de colunas.

  correcção  S_EXPR 64→72 (72..199) e S_NOME 192→224, com folga declarada entre
             as três regiões em vez de encostadas. S_CORPO fica onde está, para
             as bases antigas não perderem a declaração.
  medida     banco/sql.c, no bloco do CORPO DA COLUNA: uma tabela de SEIS colunas,
             o par (corpo, parâmetro) lido ANTES e DEPOIS de um SELECT com WHERE
  gume       repor S_EXPR=64 → cai; e usar três colunas no medidor → cai também
             (a escolha do regime é parte da medida)
  e o exit   `sql teste` tinha `return 0` FIXO: dezenas de `ok()` e o exit sempre
             verde. Agora segue `falhas`. Quem o apanhava era a rede da bateria,
             e uma rede não substitui o medidor dizer a verdade sobre si.


22. AS DUAS FRENTES EM PARALELO — o que elas acharam, e o que eu verifiquei
============================================================================
Enquanto o PG5 corria, duas varreduras em paralelo: a CADEIA DOS PAPERS e o lado
JAVASCRIPT. Nada entrou sem eu correr o medidor citado.

PAPERS — números desactualizados, e limiares de vírgula em prosa
  · naturais.tex     §NB0--§NB11 (12:0) → §NB0--§NB13 (14:0)   [medido: 14 ok]
  · reais.tex        tres_caminhos 12:0 e 14:0 → 15:0 (o §RE6b de hoje);
                     fractais §FR0--§FR6 (7:0) → §FR0--§FR8 (9:0)  [medido: 9]
  · corpo_algebrico  cuspide (8:0) → (15:0)  [medido: 15]
  · analitico/fisica/milenio: o «|geo−dobra| ≤ 6×10⁻¹²» e o «erro_n·c_n < 1/2»
    SAÍRAM — o luz_periodo.c já não os tem. A lei da torre escreve-se sem π
    (q_{2n} = q_n·2^{n+1}, exacto em 3 passos) e o teste decisivo corre em dois
    instrumentos EXACTOS (G^{q/2} = −I e D² = 2 no passo q/2). §L6a--§L6b.
  · torre_fundacao   caminho para um `corpo_computacional.tex` que não existe
  · E UM QUE EU RECUSEI: a frase «restam 54 literais decimais no código» é a
    DEFINIÇÃO DA CONSULTA, não um facto — o `genealogia.sh` varre o ficheiro
    inteiro, comentários e cadeias INCLUÍDOS. O `115.871` é «115 871 decisões»
    e o `1.618` é a entrada de um caso de leitura. Em código são ZERO. A frase
    ficou a dizer o que cada régua mede.

JAVASCRIPT — o limiar escrito à mão, nos dois medidores onde ele decidia
  · escala.js        `|r − φ^(1/3)| > 0,002` era régua minha, em doubles. Agora a
                     caixa vem da FONTE: o estilo.tex escreve duas casas, logo
                     cada degrau é inteiro ± meia centésima, e a razão vive em
                     [(2b−1)/(2a+1), (2b+1)/(2a−1)] — decidida em BigInt por
                     2p³ vs (1+√5)q³, sem raiz cúbica e sem vírgula. E o gume é
                     MEDIDO: quantas unidades da última casa é preciso mexer para
                     a lei cair (1 centésima chega).
  · traduz_pi_dim.js `|piMotor − piHarness| > 0.001` → IGUALDADE EXACTA em nanos
                     inteiros, os dois lados em BigInt. 42 de 42 linhas comparadas
                     e 42 iguais; um nano de perturbação derruba 40.

E O SEGUNDO QUE FICOU POR CORRIGIR — no MESMO ficheiro que a frente arrumou:
`escala.js` §T3 tinha `Math.abs(t − m/1000) <= 0,02`, outra régua escrita à mão.
Medido, a folga não era precisa: dos tamanhos que o PDF traz, 14 batem um degrau
com desvio ZERO e só 3 são derivados (produto cruzado de dois degraus), com pior
desvio de 4 milésimos. Vinte milésimos é cinco vezes isso — absorve em vez de
medir. Agora a comparação é INTEIRA (o PDF escreve a escala com 6 casas, logo o
corpo é exacto ao milésimo) e a folga DERIVA-SE das casas que o estilo escreve:
1000/10^casas/2, que dá 5. Se o estilo passar a três decimais, a folga aperta
sozinha e a frase da asserção continua verdadeira.
  gume: folga 3 (abaixo do pior desvio) → cai · a base deslocada 1 milésimo → cai
        (e a primeira tentativa sobreviveu: eu tinha deslocado só `mil`, e o corpo
        da CLASSE é acrescentado depois e continuava a bater — guarda parcial)

E O QUE EU ACHEI AO VERIFICAR: cinco medidores `.js` chamavam `ok()` e NÃO
emitiam `#UNIT` — app_arranca (6), design_no_pdf (4), dois_streams (5),
escala (4), sem_chute (7). A bateria contava UMA unidade grossa (o exit) por
ficheiro: 26 asserções escondidas atrás de 5. Corrigido; é a mesma lição que o
`bateria.sh` já tinha escrita em comentário sobre todo o .py e .js.


23. OS LIMIARES QUE FICARAM, E DUAS TAUTOLOGIAS (uma escrita por mim)
=======================================================================
Varridos os 104 `.js` à procura de literal de vírgula DENTRO de uma comparação:
41 ocorrências, e a maioria não é régua — são cotas de magnitude sobre tamanhos
de ficheiro (`tam > 1e5`), conversões de visualização (`/1e6`) e entradas de
funções. Três decidiam mesmo:

  escala.js §T3     `|t − m/1000| <= 0,02` — ver §22.
  rede_dual.js §R3  `bestOv > 0.3`. MEDIDO: o overlap certo é 32/32 = 1 EXACTO e
                    o outro é −2/32. O limiar escondia as duas coisas — a
                    exactidão de um lado e a MARGEM (34) do outro. Os bits são
                    ±1, logo o overlap é inteiro sobre inteiro: afirma-se s = n
                    e a separação estrita, sem número escolhido.
                    gume: bit invertido → cai · escolher o pior → cai · dois
                    padrões com o MESMO overlap (margem 0) → cai
  sem_chute.js §N6  `pior < 1.0` era TAUTOLOGIA: `pior` é o máximo entre as
                    linhas que JÁ cumprem `dif < 1.0` — a condição da classe
                    repetida como se fosse medida.

E A SEGUNDA TAUTOLOGIA FOI MINHA, dentro da correcção. Escrevi
`menorFora > pior` e ela sai de graça de `pior < CORTE ≤ menorFora`. Trocar a
comparação não chega: o CORTE tem de sair da conta.
  O que ficou: a LARGURA do vazio contra a escala que o objecto dá — a dispersão
  do próprio aglomerado. Medido: 400 linhas até 0,569pt, a de fora mais próxima
  a 1,674pt, 1,105pt sem uma única borda = 1,9× o espalhamento inteiro. Mover o
  corte dentro do vazio dá a MESMA partição, logo ele é um nome e não uma régua.
  gume: uma borda a 0,9pt (dentro do vazio) → CAI. Uma a 1,2pt e o corte a 0,3pt
  sobrevivem, e é correcto: a tese é sobre a FORMA da distribuição, não sobre o
  1,0 — um ponto isolado não destrói a bimodalidade.

E UMA ARMADILHA DO PRÓPRIO GUME, que custou uma leitura errada: o `sem_chute`
MUTA `tests/tex_core.c` e repõe-no num `finally`. O meu `timeout` matou um
mutante a meio e deixou o ficheiro trocado — as três corridas seguintes leram um
binário `tex` construído do fonte mutado, e o ficheiro INTACTO passou a falhar
duas asserções que nada tinham a ver. Correr mutantes de um medidor que mexe em
fontes exige repor E RECONSTRUIR entre cada um; um a um, e não em lote.


24. O DB FUNCIONAL — o que faltava, medido antes de mexer
===========================================================
«O PG deve ficar um db funcional». Medido, e não suposto:

  1. UMA TABELA POR BASE. `CREATE TABLE beta` escrevia por cima da `alfa` — o
     catálogo era `{ncols, nrows}` no slot 0 e mais nada.          RESOLVIDO
  2. A CÉLULA ENROLAVA CALADA. 256→0, 500→244, −1→255, 70000→112, no disco e
     sem uma palavra.                                              RESOLVIDO
  3. O SELECT LIA SEM SINAL. `−2/6` fica `−1/3` no slot (o medidor confirma-o
     com um `(int8_t)` à mão) e o SELECT mostrava **85**.           RESOLVIDO
  4. OS NOMES DE COLUNA SÃO DESCARTADOS. `CREATE TABLE cliente
     (nome,idade,saldo)` → o SELECT devolve `a`,`b`,`c`, e
     `WHERE idade > 20` é «comando recusado».                      RESOLVIDO
  5. O ALCANCE DE UMA CÉLULA. DADO: 16 bits (§27). AVALIADOR: 16 bits, com o
     PRODUTO incluído (§30). O andar escolhe-se pela FORMA.
  6. Catálogo mínimo para um driver ligar (PG6).                   POR FAZER

A TABELA É UM FICHEIRO .mem
---------------------------
O motor lê UMA relação e sempre leu bem. O que faltava não era motor novo: era
outro FICHEIRO. `<base>.mem` continua a ser a tabela sem nome (as bases antigas
não mexem) e cada tabela nomeada é `<base>__<nome>.mem`; trocar de tabela é
trocar o descritor, que é o que o banco já faz com o canal e o pool. O mapa de
slots não muda uma linha e o `varre` não sabe que há mais tabelas.

  · CREATE abre com O_TRUNC — a tabela começa LIMPA, como sempre começou quando
    era uma só. Sem isso o medidor do sql deixou de ser idempotente: primeira
    corrida 0 falhas, segunda 3. Um medidor que depende do que ficou de antes
    não mede.
  · compatibilidade: base antiga sem ficheiro por tabela cai no `<base>.mem`
    quando o nome guardado bate (ou quando ela é tão antiga que nem nome tem)

O ENVELOPE DIZ-SE, E RECUSA
---------------------------
A célula é uma Word_8²: um átomo para o numerador, outro para o denominador (ou
o coeficiente de σ). O que não cabe é RECUSADO com erro — `value out of range
for Word_8 cell` — e contado em `cel_recusadas`. E o alcance é o do CORPO: numa
coluna inteira 0..255, num racional/áureo/cristal o componente é assinado.

E o leitor passou a ler com o sinal que o corpo declara. Eram DUAS RÉGUAS para a
mesma célula: o medidor lia o slot com `(int8_t)` e passava, o SELECT lia sem
sinal e mostrava outro número.

  medidores  banco/sql.c: «UMA BASE TEM VÁRIAS TABELAS» (alfa 2x2 e beta 1x3
             coexistem, o WHERE na certa, `gama` recusada) e «O ENVELOPE DA
             CÉLULA DIZ-SE E RECUSA» (255 encosta, 256 e −1 recusados)
  gume       CREATE a ignorar o nome → cai · o SELECT sem trocar de tabela →
             cai (alfa devolve 1x3, as linhas da beta) · o envelope a aceitar
             tudo → cai · CREATE sem O_TRUNC → cai À SEGUNDA CORRIDA, e a
             primeira versão do meu gume não o via porque limpava o /tmp antes


25. A COLUNA TEM NOME
=======================
`CREATE TABLE cliente (nome,idade,saldo)` lia os três identificadores e
DEITAVA-OS FORA: só o corpo de cada coluna era guardado. A coluna era decidida
por `nome[0] - 'a'` em três sítios, e a consequência não é cosmética —

    SELECT * FROM cliente          →  colunas `a`, `b`, `c`
    WHERE idade > 20               →  «comando recusado ou nao entendido»
    UPDATE ... SET saldo = 99      →  pedia a coluna 's'−'a' = 18

  região     S_COLNOME = 36864, 16 Words (32 caracteres) por coluna, 8 colunas —
             acima das linhas (3024) e dos denominadores (35792), longe do mapa
             que a ISA percorre. Dois caracteres por Word, como o nome da tabela.
  ligado em  o CREATE guarda · o WHERE resolve · o SET resolve e RECUSA o que não
             existe · o SELECT devolve o nome guardado
  a LETRA    continua a valer onde NÃO há nomes guardados — as bases antigas e
             todos os medidores que escrevem `(a,b,c)`. É recurso, não regra:
             havendo nomes, `WHERE a` numa tabela `nome/idade/saldo` é RECUSADO.

E A TABELA VAZIA TAMBÉM TEM COLUNAS. A saída antecipada de `varre` («ainda não
há uma linha») devolvia `ncols = 0` e tag vazia: pela porta FEBE o driver ficava
sem RowDescription e sem CommandComplete, e não distinguia «está vazia» de «a
resposta perdeu-se». O caminho com WHERE já o fazia bem — a divergência era só
aqui. Os DOIS caminhos até zero linhas dizem agora o mesmo.

O DEFEITO QUE SÓ A LIGAÇÃO REAL MOSTROU
---------------------------------------
O `varre` resolvia a coluna do SET ANTES de abrir a tabela do FROM. Logo

    SELECT * FROM conta            (a conta fica aberta)
    UPDATE cliente SET saldo = 111 →  «column "saldo" does not exist»

porque «saldo» era procurada na `conta`. Nenhuma asserção o via: nos testes a
tabela do UPDATE era sempre a que já estava aberta. Foi uma sessão ponta a ponta
pelo FEBE — CREATE, INSERT, SELECT noutra tabela, UPDATE de volta — que o
mostrou. Abrir a tabela passou a vir PRIMEIRO, antes de resolver coluna nenhuma:
uma coluna só existe DENTRO de uma tabela.

  medidores  banco/sql.c (92 unidades): «A COLUNA TEM NOME», «A BASE ANTIGA
             CONTINUA A ANDAR» (nomes apagados = base antiga, a letra volta a
             valer), «UMA TABELA VAZIA TAMBÉM TEM COLUNAS»
             tests/pgwire.c §W7: duas tabelas pela MESMA ligação, 5 passos
  gume       8 mutações: o CREATE sem guardar · o WHERE pela letra · a saída a
             devolver a,b,c · o SET pela letra · a letra a valer havendo nomes ·
             a letra a NÃO valer sem nomes · col_tem_nomes a mentir · a ordem
             trocada (3 de 5 passos). Todas caem.
             E uma sobreviveu primeiro: tirar o recurso da letra não derrubava
             nada, porque nenhuma tabela do medidor era antiga — o ramo da
             compatibilidade nunca era visitado. Mede-se agora apagando os slots.

PRÓXIMO
-------
  · o alcance de uma célula é UM átomo (0..255) — alargar é subir a torre, uma
    célula de Word_8⁴. É o que falta para «funcional» a sério.
  · catálogo mínimo do PG6 (version(), SHOW, o que um driver pede ao ligar)


26. O ANDAR SEGUINTE DA TORRE — o transporte atravessa o átomo
================================================================
Porque é que uma célula segura 0..255: a ULA soma COMPONENTE A COMPONENTE e o
vai-um não passa de um átomo para o outro.

    ula_add(a,b) = { soma8(a.total,b.total), soma8(a.e,b.e) }

E isso está CERTO onde está: a Word é um PAR (numerador, denominador; ou a+bσ),
não um número de dezasseis bits. O par não é um número por acaso — é-o quando o
transporte atravessa, e é essa a dobra: T_{k+1} = T_k + T_k*.

  dentro do átomo   a + b = (a⊕b) + 2(a∧b)          thm:transporte
  entre átomos      o vai-um do baixo entra como cin do alto — a MESMA lei,
                    um andar acima

FEITO NESTA ETAPA (o andar sozinho, a célula ainda onde estava):
  W16 {baixo, alto} · ula_soma_c (soma de 8 com vai-um à entrada e à saída)
  ula_add16 · ula_sub16 (a + ~b + 1, com o mesmo vai-um a atravessar)
  ula_menor16 (a ordem É o empréstimo do sub) · ula_igual16
  Tudo construído do NAND — não há um `+` do C lá dentro.

  medidor  banco/sql.c: 100 pares varridos nos sítios onde o transporte decide,
           soma/subtracção/ordem/igualdade a bater, o transbordo do andar dito
           em vez de perdido, e o transporte a atravessar em 40 deles.
           E O GUME é o sítio onde a ULA de BAIXO pára: 255+1 dá 0 nela e 256
           aqui. Sem esse caso, uma soma que ignorasse o vai-um passava em tudo
           o que não atravessa.
  gume     o vai-um a não atravessar → 49 de 100 caem · o transbordo perdido →
           19 · a ordem só pelo átomo alto → 12

O QUE FALTA PARA A CÉLULA CRESCER (e é trabalho próprio, não é este)
--------------------------------------------------------------------
A aritmética está provada; falta o CAMINHO DO DADO:
  · a célula passa a ocupar DOIS Words — `S_LINHAS + 2·(i·ncols + j)` —, um
    para o numerador (baixo,alto) e outro para o denominador
  · o programa emitido copia DOIS Words por célula (emit_copia × 2)
  · OP_CMP e a comparação do WHERE passam a 16 bits: hoje é um OP_SUB de 8 e o
    bit de sinal por S_MASK; passa a ser o empréstimo do sub de 16
  · 23 sítios com S_LINHAS, 5 com S_DEN, o escritor, o leitor — e os medidores
    que lêem slots à mão (`mem_le(S_LINHAS + 0)`, `+2`, `+4`) mudam de endereço
Enquanto isso não for feito, o que não cabe é RECUSADO (§24) — o que não se pode
é guardar lixo em silêncio.


27. O DADO PASSA A DEZASSEIS BITS — e a metade que falta diz-se
=================================================================
O §26 provou a aritmética do andar; falta o caminho. O caminho tem DUAS metades,
e elas não custam o mesmo:

  a FRONTEIRA   escrever e ler uma célula — barata, e feita agora
  o AVALIADOR   o WHERE, que é POLINOMIAL dentro da ISA: o `S_MT` mascara o `.e`,
                os produtos são de átomo e a comparação é um OP_SUB de 8 com o
                bit de sinal. Alargar isto é alargar toda a avaliação — trio
                próprio, e não este.

FEITO
-----
  S_ALTO = 40960: o byte ALTO de cada célula num plano paralelo, como o
  denominador já vivia em S_DEN. **A Word da linha NÃO MUDA** — e é isso que
  torna a mudança barata: toda a aritmética emitida continua a ler exactamente
  o que sempre leu, e nenhuma das 23 ocorrências de S_LINHAS mexe.

    INSERT (255,1000) · (9,65535)   entram e voltam EXACTOS
    INSERT (65536,·) e (−1,·)       recusados, contados em cel_recusadas

E A METADE QUE FALTA DIZ-SE, EM VEZ DE SE FINGIR
------------------------------------------------
Um WHERE que cite uma coluna com valores acima de 255 é RECUSADO:

    column "grande" holds values above the 8-bit evaluator

contado em `cmp_recusadas`. O valor está guardado inteiro e o SELECT devolve-o;
o que não se faz é comparar METADE dele. Um banco que responde à toa é pior que
um que diz o que não sabe.

  medidor  banco/sql.c: 65535 encosta e volta, 65536 e −1 recusados, o WHERE na
           coluna ESTREITA corre e na LARGA é recusado — os dois lados
  gume     o byte alto não guardado → cai · o leitor sem compor → cai · a
           comparação sem recusar → cai · o envelope de volta a 255 → cai

O TRIO SEGUINTE — e o plano que eu tinha escrito estava ERRADO
--------------------------------------------------------------
Escrevi aqui «o `S_MT` deixa de mascarar o `.e` no caminho das colunas
inteiras», como se o segundo átomo estivesse livre. NÃO ESTÁ. Fui ler o
`emit_mul_zeck` e o `.e` é o REGISTO DE TRABALHO DO MULTIPLICADOR:

  a ISA não tem MUL. `n·x` faz-se por Zeckendorf — todo inteiro é soma de
  Fibonacci não consecutivos — e cada F(k) é `GOLD^(k−1)`, com
  GOLD = (a,b) ↦ (a+b, a). Parte-se de (x, 0) e o `.e` carrega F(k−1)·x.
  O `S_MT` limpa o `.e` ANTES dos deslocamentos e DEPOIS deles, e é por isso
  que ele está lá — não é desperdício, é o estado a ser reposto a zero.

Logo alargar o avaliador NÃO é tirar uma máscara. É:

  · o valor passa a ocupar DOIS Words, e o GOLD tem de deslocar em 16 bits —
    a mesma dobra do §26 aplicada ao rei, não à soma
  · o acumulador cresce para 32: o produto de dois valores de 16 pede-o, e é
    onde a torre volta a dobrar OUTRA VEZ
  · OP_ADD/OP_SUB ficam COMPONENTE A COMPONENTE — o catálogo guarda
    {ncols, nrows} numa Word e o `nrows++` soma 1 ao `.e`; com transporte a
    atravessar, um nrows de 255 a passar a 256 subiria para o ncols. O par e o
    número são leituras DIFERENTES da mesma Word, e por isso pedem instruções
    diferentes (OP_ADD16 e companhia), não uma troca do OP_ADD.

E NÃO SE ACRESCENTAM OS OPCODES ANTES DE HAVER QUEM OS EMITA. Escrevi-os,
liguei-os ao despacho, e tirei-os: sem um `emit` que os use são um ramo que
ninguém corre — a `ula_add16` e companhia ficam, porque essas TÊM utilizador (o
medidor do §26). O andar existe e está provado; o que falta é o caminho, e o
caminho é o avaliador inteiro.


28. A DOBRA DO REI — GOLD em dezasseis bits
=============================================
O §26 dobrou a SOMA (o vai-um atravessa o átomo). Este dobra o REI, que é o que
o avaliador precisa: a ISA não tem MUL, e `n·x` faz-se por ZECKENDORF —

    todo inteiro é soma de Fibonacci NÃO consecutivos, de um único jeito
    F(k) é uma potência do rei:  GOLD = (a,b) ↦ (a+b, a),  σ² = σ + 1
    de (x,0):  (x,x) → (2x,x) → (3x,2x) → (5x,3x) → …   k passos dão F(k+1)·x

Em oito bits o estado do rei É a Word — `a` no `.total`, `b` no `.e` — e é
exactamente por isso que o segundo átomo não está livre (§27).

FEITO
-----
  G16 {a,b}      o estado do rei um andar acima: um PAR de valores de 16 bits
  gold16         (a,b) ↦ (a+b, a), com o `+` a ser `ula_add16` — o vai-um
                 atravessa o átomo, e o que passa do andar DIZ-SE
  fib_vezes16    F(k+1)·x por k deslocamentos, de (x,0)
  mul16_zeck     n·x = Σ F(k_i)·x, o mesmo desenho do `emit_mul_zeck` um andar
                 acima; custo ~log_φ(n), não n

  medidor  o rei É Fibonacci nos quinze primeiros · 387 produtos varridos, 336
           acima de 255 e 17 ACIMA DE 65535 — e todos os que passam do tecto
           dizem-no · E O GUME: 3·100 dá 44 no rei de oito e 300 aqui
  gume     o GOLD sem deslocar (b'=b) → 326 de 387 caem · o Zeckendorf com i em
           vez de i+1 → 312 · o transbordo do gold16 perdido → 2

E UMA ARMADILHA DO MEU PRÓPRIO MEDIDOR, que vale mais que o resto: a primeira
varredura ia até n ≤ 40, x ≤ 300 — produto máximo 12000, LONGE do tecto de
65535. O ramo do transbordo NUNCA CORRIA, e por isso tirar a bandeira do andar
não derrubava nada. Não era guarda redundante nem ramo morto: era o REGIME do
meu varrimento a não conter o defeito. Subiu para 300·300 = 90000, e a mutação
passou a cair. E a exigência ficou `acima > 0` — que o ramo é VISITADO —, não o
17 que hoje sai: pôr ali o número medido era escrever o resultado dentro da
medição.

O QUE FALTA PARA O AVALIADOR (as duas peças que sobram)
--------------------------------------------------------
  · o valor em DOIS Words no caminho emitido, e o GOLD da ISA a deslocar sobre
    esse par — a `gold16` é a lei; falta o `emit`
  · o acumulador em 32: `c_i·x_i` com c_i e x_i de 16 bits pede-o, e é onde a
    torre dobra outra vez (a mesma conta, um andar acima)
  · e OP_ADD/OP_SUB ficam componente a componente — o catálogo guarda
    {ncols, nrows} numa Word e o `nrows++` soma ao `.e`


29. O AVALIADOR NO ANDAR DE CIMA — o WHERE compara dezasseis bits
==================================================================
O §27 guardava dezasseis bits e RECUSAVA compará-los; o §28 provou o rei. Aqui
o átomo LINEAR passa a correr no andar de cima, e o de oito fica intacto ao lado.

COMO, e sem uma instrução nova para montar
------------------------------------------
  emit_valor16   o valor está em dois sítios (o baixo na linha, o alto em
                 S_ALTO). Junta-se numa Word: TROCA leva (alto,0) a (0,alto) e o
                 OP_ADD — COMPONENTE A COMPONENTE, que aqui é exactamente o que
                 se quer — soma-lhe (baixo,0). Fica {baixo, alto}.
  OP_ADD16/SUB16/CMP16   a Word lida como UM número. O OP_ADD fica como estava:
                 o catálogo guarda {ncols, nrows} e o `nrows++` soma ao `.e`.
  emit_mul_zeck16  o rei de 16 NÃO é opcode — GOLD = (a,b) ↦ (a+b, a) sobre dois
                 slots são um ADD16 e uma cópia
  emit_teste16   o sinal é o bit 15: máscara {0, 0x80} em S_MASK16

E O ENVELOPE DA COMPARAÇÃO NÃO É O DO ARMAZENAMENTO
----------------------------------------------------
A célula guarda 0..65535 e o SELECT devolve-o. A comparação decide pelo SINAL da
diferença, logo pede que a FORMA INTEIRA caia em 0..32767. Três coisas ficam de
fora, cada uma com o seu motivo escrito:

    grau >= 2               um produto de 16×16 pede 32 — a torre dobra outra vez
    valor > 32767           o par lê-se NEGATIVO (`50000 < 100` dava verdadeiro)
    2·x com x até 32767     a FORMA estoura, mesmo com a coluna a caber

  E o majorante da forma não soma magnitudes: separa o MÁXIMO do MÍNIMO, senão
  `medio − 100` com medio a 32767 era recusado — e ali a conta dá 32667.

O QUE A LIGAÇÃO PONTA A PONTA APANHOU (outra vez)
--------------------------------------------------
`UPDATE ... SET v = 30000` escrevia só o átomo BAIXO e o alto ficava do valor
ANTERIOR: sobre um v de 1000 dava 29952 — a linha errada no disco, calada.
Nenhuma asserção o via. S_VA guarda o átomo alto do valor do SET e o emitido
copia os dois.

  medidor  cinco consultas certas em valores que o andar de oito não sabia ler,
           os três casos recusados, um coeficiente MAIOR QUE UM que cabe (3·v,
           que é F(4) e três deslocamentos do rei — com c = 1 errar o número de
           passos acertava na mesma), e o UPDATE de 30000 lido de volta
  gume     7 mutações, todas caem: o alto não junto · o ADD16 componente a
           componente · o sinal no bit 7 · o rei com i passos · o limite da forma
           · o envelope assinado · o SET só com o baixo

FALTA
-----
  · grau ≥ 2 sobre colunas largas: `emit_mul` de 16×16 → acumulador de 32
  · e aí o envelope da comparação sobe com ele


30. O PRODUTO, E O ANDAR ESCOLHE-SE PELA FORMA
================================================
O PRODUTO SAI DA CADEIA, NÃO DE UM IDIOMA DE MÁQUINA. Fui ler os papers antes de
escrever, e a última linha do `naturais.tex` thm:transporte diz-o:

    «as duas operações que a iteração usa são a soma e o produto de 𝔽₂, as
     únicas primitivas de um bit; O PRODUTO DE ℕ SEGUE POR DESLOCAMENTO E SOMA»

e o lem:desloc dá a outra metade: multiplicar pelo gerador da dobra É deslocar,
x⊗σ = 2^w·x. Então

    x·y = Σ_i  y_i · (x << i)     com  x<<1 = x + x  (ADD16)  e  y_i = AND

  emit_mul16   dezasseis passos, o `<<` é `x+x` e o bit do multiplicador é um AND
               com a máscara — cada máscara no SEU slot, porque as constantes
               escrevem-se ao COMPILAR e o programa corre depois
  rascunho     o átomo passou de 12 a 48 slots (ATOMO_SLOTS): 4096 + 16·48 =
               4864, e a região seguinte só começa em 33792

E O DEFEITO QUE ISTO DESTAPOU — ANTERIOR A TUDO ISTO
-----------------------------------------------------
O avaliador de oito bits decide pelo bit 7 da diferença, logo a forma
`c0 + Σ c_i·x_i` tem de caber em ±127. Com a = 100 e b = 50 o `a*b` dá 5000, e
ele RESPONDIA À TOA:

    WHERE a*b > 1000   devolvia (3,7) e (150,2) — as duas que NÃO casam —
                       e deixava de fora (100,50), que é a única que casa

Medido no commit anterior E no de antes: o produto estourava em silêncio.
Nenhuma asserção o via porque as tabelas dos medidores têm valores pequenos e o
produto nunca lá passava de 127.

A CORRECÇÃO É A REGRA: o andar escolhe-se pelo que a FORMA precisa, não pelo que
a coluna guarda. O majorante calcula-se em compilação com o maior valor de cada
coluna — separando o máximo do mínimo, que um coeficiente negativo SUBTRAI —, e

    cabe em ±127     → o andar de oito, byte a byte como sempre foi
    cabe em ±32767   → o andar de dezasseis
    o resto          → RECUSADO, com o motivo escrito

  medidor  as três consultas com produto dão agora a única linha certa, e a
           quarta (`a > 50`, que cabe em oito) continua no andar velho — as duas
           metades medem-se
  gume     o andar escolhido pela COLUNA → cai · o produto sem deslocar → cai ·
           o bit do multiplicador não testado (soma sempre) → cai

O ESTADO DO BANCO
-----------------
  várias tabelas por base · nomes de coluna · valores 0..65535 exactos ·
  SELECT/INSERT/UPDATE/DELETE com WHERE em dezasseis bits, produto incluído ·
  tudo o que não cabe é recusado com o motivo · porta FEBE com fachada libpq
