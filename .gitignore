# --- LaTeX build ---
*.aux
*.log
*.out
*.toc
*.fls
*.synctex.gz

# PDFs gerados dos papers (pdflatex regenera dos .tex). Os quatro da raiz foram apagados
# em 03/08 — regeneram-se com `pdflatex <p>.tex`, e o deploy fá-lo sozinho.
/teoria.pdf
/tiffany.pdf
/microprocessador.pdf
/viveiro.pdf

# banco/: os programas que guardam e servem dado (a ISA, a MMU, o SQL, a fita, as torres).
# Só o fonte entra; os binários compilados e os ficheiros de dados ficam fora.
banco/*
!banco/*.c
!banco/*.sql
!banco/*.sh
!banco/apps/
!banco/apps/*.erg
!banco/*.py
!banco/*.md

# tools/: só o fonte entra (os binários C compilados, sem extensão, ficam fora)
tools/*
!tools/*.c
!tools/*.md
!tools/*.sh
# os .js de tools/ estavam DE FORA e ninguem reparou: o `tools/*` apanha-os e a lista de
# excepcoes nomeia .c, .md, .sh e .py — nao .js. O compara.js viveu um dia inteiro so' no
# disco, e num clone limpo nao existiria. Um medidor que nao esta' no repo nao e' medidor.
!tools/*.js
!tools/*.py
!tools/atestados.txt
!tools/NAO-MEDIDORES.txt
/morfico.pdf

# tatoeba/ SAIU DO PROJETO em 03/08. Os 17 medidores dependiam de corpus que nunca esteve
# no repositório (pares.tsv, por.tsv, lexico.txt, corpus.txt — todos ignorados), logo NENHUM
# era reproduzível num clone limpo: a bateria dava-os por verdes por causa de ficheiros que
# só existiam numa máquina. A validação passou para tools/, que corre sem dados externos.
# Os dados ficam em disco para quem os quiser; o repositório não os carrega.
/tatoeba/

# BINÁRIOS NÃO SOBEM NO GIT — regra do Aarão, 01/08.
# Os PDFs são gerados (pdflatex) ou distribuídos pelo S3/R2, e o app baixa de lá.
# Subi o catalogo.pdf por descuido no commit da separação; sai aqui.
/catalogo.pdf
/fracoes/fracoes-continuas.pdf
/fracoes/realizacoes.pdf
/realizacoes.pdf
/fracoes-continuas.pdf
/enredo.pdf
/livro.pdf
/multiplicacao.pdf
/casl-propagation.pdf
/assets/arvore_metais.pdf   # figura órfã: nenhum .tex a inclui e nada a regenera

# As tabelas da régua (tatoeba/regua.c): DADOS gerados do corpus, ~128 MB. Estavam
# versionadas na raiz e duplicadas em três diretórios — 27 ficheiros — porque o regua.c
# escrevia no diretório de trabalho. Agora ele ancora-as ao corpus (tatoeba/tabelas/), e
# elas ficam fora do git como qualquer outro binário gerado.
/regua_*.bin
tools/regua_*.bin
tatoeba/tabelas/
# e as tabelas soltas de antes da ancoragem (bairro_*, centro_*, lexico_*, marcas_*), caso
# alguma versão antiga volte a correr:
tatoeba/*.bin

# O banco da torre: corpus em disco. Dados, não código — e grande.
# Vive na RAIZ e só aí: o fita.c sobe até ao .git para o achar. Havia uma segunda cópia
# inteira em tools/.torre (18 GB, byte a byte igual) porque a bateria corre de tools/ e o
# caminho era relativo ao cwd. As duas linhas abaixo apanham-na se voltar.
/.torre/
**/.torre/
!tools/*.service
!tools/*.timer

# eval.txt: recados que trocamos ali, não é do repositório
/eval.txt

# a semente do tecido: os pares que a assistente aprende (texto, entra no git)
!tools/tecido_semente.tsv

# o paper das frações: o PDF gera-se no deploy
/fracoes/*.pdf
/fracoes-continuas.pdf

# Fontes de leitura (livros de terceiros, com direitos). Este repositório é
# PÚBLICO e espelhado na Patria — os livros ficam no disco, e o que sobe é o
# que ELES nos fizeram pensar, com a citação e a referência. Nunca o ficheiro.
/amit/
/baden/
*.epub
*.mobi
Bhagavad-Gita-Krishna.pdf
o-tao-da-fisica-fritjof-capra.pdf
biblia.pdf
alcorao.pdf

# ─── DADOS PESSOAIS — NUNCA SUBIR (o repo é público) ────────────────────────
# A pasta curriculo/ tem documentos com CPF, RG, conta bancária, nome dos pais
# e data de nascimento. Só sobem os ficheiros do CV público (fontes + PDF).
curriculo/*
!curriculo/cv.tex
!curriculo/cv.pdf
!curriculo/cv-executivo.tex
!curriculo/cv-parcerias.tex
!curriculo/cv-academico.tex
!curriculo/montar-cv.py
"Imagem colada.png"
tests/dados/
banco/dados/
dados/

# O hinário "Nova Dimensão" é obra religiosa de terceiro (Santo Daime), usada como
# BASE do terceiro acto — o enredo dá-lhe crédito e NÃO reproduz o texto dos hinos.
# Redistribuí-lo no repo público seria outra coisa: publicar a obra em si, sem
# autorização de quem a guarda. Fica de fora até haver decisão explícita.
/nova-dimensão/

# ─── SEGREDOS — nunca, em circunstância nenhuma, neste repositório ───────────
# Este repo é PÚBLICO e é servido por dumb HTTP em goldenkingdom.patriatechnology.com.
# Uma chave privada commitada aqui fica exposta para sempre — o histórico do git
# guarda-a mesmo depois de apagada, e o clone público distribui-a.
# As chaves e o .env vivem em segredo/ e NUNCA entram no git.
/segredo/
.env
.env.*
*.pem
*.key
id_rsa*
id_ed25519*

# o MODELO entra (nao tem segredo nenhum); o .env real, nunca
!.env.exemplo

# PDFs gerados dos papers próprios (pdflatex regenera do .tex)
/papers/*.pdf
/papers/*.aux
/papers/*.log
/papers/*.out

# --- AS GRAVAÇÕES, QUE SÃO O QUE ENVELHECE EM SILÊNCIO ---------------------------------
#
# 13,5 MB de derivados estavam versionados: os PDFs que o tradutor compõe ao clicar, e os
# binários dos medidores. Nada disso é fonte — sai da fonte, e sai igual sempre que se pedir.
#
# E não é arrumação: é o mesmo defeito que o `vite.config.js` já combate no PDF servido. Um
# derivado guardado deixa de acompanhar a fonte e ninguém dá por isso — o `tests/tex` que
# estava aqui tinha sido construído de uma versão anterior do `tex.c`, e um medidor passava
# por estar verde a medir um binário que já não era o do repositório.
tests/.compoe_*.pdf
tests/*.pdf
tests/backend_ttf
tests/biblioteca
tests/cards
tests/converte
tests/desenha
tests/design
tests/dourada_banco
tests/dourada_bidual
tests/dourada_lei2
tests/dual_spline_ttf
tests/escala_dourada
tests/escala_espaco
tests/estrela_emite
tests/fonte_banco
tests/fonte_corpo
tests/hilbert_bidual
tests/latex_corpo
tests/letra_assinatura
tests/pdf_corpo
tests/tex
tests/ttf_corpo
tests/corpo_unico
tests/dissipa_pagina
tests/dourada_escala
tests/grau
tests/medida
tests/wasm_sobe
/corpo-estelar.pdf
/papers/*.pdf
tests/assinatura_tradutor
tests/config_corpo
tests/disco_wasm
tests/dtc_viveiro
tests/estado_caos
tests/lyapunov_refletido
tests/milenio_trial
tests/nove_periodico
tests/relogio_curva
tests/luz_periodo
tests/lean_controlo
tests/lean_convencao
tests/lean_cruz
# conecthus: só a fonte; os PDF (o gerado e o paper de referência) ficam fora do repo público
conecthus/*.pdf
/tests/tex.asm
/tests/estelar_completo
/tests/move
/tests/reversao
