\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[brazilian]{babel}
\usepackage{amsmath, amssymb, amsthm}
\usepackage[margin=2.6cm]{geometry}
\usepackage{booktabs}
\usepackage{textcomp}             % \textOmega
% os documentos têm caracteres Unicode literais em modo texto; sem estas
% declarações o pdflatex erra e imprime nada no lugar deles.
\DeclareUnicodeCharacter{2081}{\ensuremath{_1}}
\DeclareUnicodeCharacter{2082}{\ensuremath{_2}}
\DeclareUnicodeCharacter{2083}{\ensuremath{_3}}
\DeclareUnicodeCharacter{2212}{\ensuremath{-}}
\DeclareUnicodeCharacter{2229}{\ensuremath{\cap}}
\DeclareUnicodeCharacter{0394}{\ensuremath{\Delta}}
\usepackage{mathtools}            % psmallmatrix (o \Mat do catálogo usa-o)
% macros que faltavam e faziam pdflatex emitir "Undefined control sequence" sem
% impedir o PDF de sair — 37 erros silenciosos entre os dois documentos.
\providecommand{\GL}{\mathrm{GL}}
\providecommand{\N}{\mathbb N}
\providecommand{\Oo}{\mathbb O}
\providecommand{\im}{\operatorname{Im}}
\providecommand{\sen}{\operatorname{sen}}
\providecommand{\senh}{\operatorname{senh}}
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\Z}{\mathbb Z}
\newcommand{\R}{\mathbb R}
\newcommand{\GF}{\mathrm{GF}}
\newcommand{\F}{\mathbb F}
\newcommand{\Q}{\mathbb Q}
\newcommand{\C}{\mathbb C}
\newcommand{\Hq}{\mathbb H}
\newcommand{\Pf}{\operatorname{Pf}}
\theoremstyle{plain}
\newtheorem{teorema}{Teorema}
\newtheorem{principio}{Princípio}
\theoremstyle{remark}
\newtheorem{defn}{Definição}
\newtheorem{obs}{Observação}

\title{\textbf{O Alfabeto e a Operação}\\[3pt]
\large o corpo autossimilar, e o que dele se colhe}
\author{}\date{}

\newcommand{\dm}{\operatorname{dm}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\disc}{\operatorname{disc}}
\providecommand{\Mat}[1]{\left(\begin{smallmatrix}#1&1\\1&0\end{smallmatrix}\right)}
\begin{document}
\maketitle

\begin{abstract}
\noindent Um \textbf{corpo universal} é um conjunto com produto invariante por deslocamento --- uma
\textbf{convolução} --- e nada mais se lhe pede. Dessa única exigência sai a \textbf{transformada
universal}, a que leva convolução em produto ($\mathcal{F}(a\circledast b)=\mathcal{F}(a)\mathcal{F}(b)$);
sai a \textbf{deconvolução}, que é o quociente e existe exatamente quando nenhuma casa se anula; e sai
a \textbf{inversa}, que não se constrói --- colhe-se do dual. O $\sqrt N$ da normalização não é
convenção: é o único expoente que conserva a norma, e é o \emph{mesmo} $\sqrt N$ do ruído e da base
ortogonal --- um número, não três. E o corpo universal é \textbf{autodual}: $\mathcal{F}^4=\mathrm{id}$,
com o espelho ($\mathcal{F}^2$) a viver dentro da rotação.

$\R^n=\GF(p^n)$ é uma \textbf{realização} disso, não outra teoria: multiplicar nele \emph{é} convoluir,
e a borda $\sigma=m+\tfrac1\sigma$ troca a redução, não a convolução. O seu dual $\R^{n*}$ obtém-se
trocando o sinal de uma só peça do produto --- a antissimétrica --- e os dois \emph{só formam corpo
juntos}. O corpo assim realizado é \textbf{autossimilar}: uma peça, o \emph{gato} e o seu
\emph{esquilo}, que se repete em toda escala e dimensão sem mudar.

A tese que organiza o resto é uma inversão: a \textbf{dimensão inteira não é o objeto} --- é um ponto
de \emph{ancoragem}. O que existe em quantidade é o contínuo, e a dimensão inteira é onde ele encosta
e trava. Simetria \textbf{ancora}; deformação é a \textbf{dinâmica}. Cada afirmação é medida na cifra
(\code{tools/}), com resíduo $0$ --- exata, ou falha. Fermat, ao fim, cai no gato.
\end{abstract}

\section{A forma matricial: a ferramenta de operar corpos}

Esta secção vem primeiro porque é a \textbf{ferramenta}. Tudo o que se segue --- o corpo universal,
a dualidade, o corpo autossimilar, a mecânica --- opera com ela, e quem a tiver lida chega a cada
uma dessas secções já com o instrumento na mão.

\emph{A demonstração está em} \code{fracoes/fracoes-continuas.tex}, o paper que estabelece o
dicionário; aqui guarda-se o que a teoria usa e diz-se onde ele o prova.

\paragraph{Em uma frase.} A régua não é um algoritmo: é um \textbf{produto de matrizes}. Cada
quociente parcial $a$ é $\Mat{a}$, e a expansão é a composição delas. Daí sai, sem esforço, tudo o
que o resto deste documento precisa: os convergentes (as colunas), a reversibilidade (o
determinante), a ordem (o sinal), e a maneira de combinar dois corpos (as operações tensoriais).

\begin{center}\small
\begin{tabular}{ll}
\hline
no paper & aqui \\ \hline
o dicionário palavra $\to$ matriz & a régua é uma composição, não um algoritmo \\
convergentes $=$ colunas de $M_n$ & a expansão \emph{contém} as suas aproximações \\
$\det M_n = (-1)^{n+1}$ & $|\det| = 1$: inversa inteira, fator unitário \\
reversibilidade em três sentidos & a involução, e o nome dela \\
ordem alternada, isomorfismo de ordem & \textbf{o corpo de corpos é ordenado} \\
$\GL_2(\Z)$-equivariância (Serret) & a operação não depende da representação \\
\hline
\end{tabular}
\end{center}

\paragraph{E a construção sobe de dimensão --- com uma condição que nem sempre se cumpre.} O paper
generaliza $\Mat{a}$ para a \textbf{companheira} da borda $x^n = m x^{n-1} + 1$, e o caso $n=2$ é o
gato do metal. Duas propriedades sobrevivem sempre:
\[
  |\det| = 1 \quad\text{(inversa inteira)},
  \qquad
  |\sigma|\!\!\prod_{\lambda\neq\sigma}\!\!|\lambda| = 1 \quad\text{(a conservação)} .
\]

\begin{obs}[e uma terceira que não sobrevive --- com uma distinção que é preciso fazer]
Escreveu-se aqui, numa versão anterior, que $\sigma$ é sempre de Pisot. É falso, mas \emph{não pela
razão que se deu} --- e a correção da correção é mais interessante que qualquer das duas.

\textbf{São dois enunciados diferentes}, e só um falha em $n=5$:
\begin{center}\small
\begin{tabular}{lll}
\hline
$n$ & $\sigma$ é número de Pisot & $\beta_{n,1}$ é polinómio de Pisot \\
& (propriedade do polinómio \emph{mínimo}) & (todas as não dominantes em $|z|<1$) \\ \hline
$2,3,4$ & sim & sim \\
$\mathbf 5$ & \textbf{sim} --- e é o \emph{menor que existe} & \textbf{não} \\
$\geq 6$ & não & não \\
\hline
\end{tabular}
\end{center}

Em $n=5$, $m=1$ a fatoração
$x^5 - x^4 - 1 = (x^2-x+1)(x^3-x-1)$ --- que este documento já usava adiante, a propósito do
Penrose --- mostra que $\beta$ é \textbf{redutível}, logo $\Q[x]/(\beta)$ não é corpo. Mas a única
raiz de módulo $>1$ é a de $x^3-x-1$: o \textbf{número plástico} $1{,}3247\ldots$, que é
\emph{precisamente o menor número de Pisot que existe} (Siegel, 1944). O que o fator ciclotómico
$\Phi_6$ estraga é a \emph{matriz} --- as suas raízes têm módulo exatamente $1$ e não contraem ---,
não o número. Para $n\geq6$ falham os dois.

\emph{E o erro de medição que permitiu tudo isto} é o mais instrutivo. A versão original media a
conservação $\prod|\lambda| = 1$ e concluía Pisot: um produto igual a $1$ é perfeitamente compatível
com fatores maiores que $1$ --- em $n=6$ o produto dá $1{,}000000$ e o segundo módulo é $1{,}0328$.
Pior, a asserção calculava $1/\sigma < 1$, que é verdade sempre que $\sigma>1$: \textbf{não podia
falhar}. Testar Pisot exige o \emph{máximo} dos módulos não dominantes.
\end{obs}

\noindent Tudo o que precede fala da linha $m=1$. Fora dela não há caso a caso: a linha $m=1$ era
a \textbf{única} excepção, e a razão disso é demonstrável.

\begin{teorema}[$\beta_{n,m}$ é Pisot para todo $m\geq2$]\label{thm:pisot-teoria}
Para todo $m\geq2$ e todo $n\geq2$, $\beta_{n,m} = x^n - m\,x^{n-1} - 1$ tem exatamente uma raiz de
módulo $\geq1$; ela é real, simples, e está em $(m,m+1)$. As outras $n-1$ têm módulo $<1$.
\end{teorema}

\begin{proof}
Pelo \textbf{dual}. A involução $\nu$ leva $\beta$ no recíproco $\beta^*(x) = -x^n\beta(1/x) =
x^n + m x - 1$ e troca dentro por fora, de modo que a tese equivale a ``$\beta^*$ tem \emph{uma}
raiz em $|x|<1$''. Compare-se $\beta^*$ com $h(x) = m x$ sobre $|x|=1$:
\[
  |\beta^* - h| = |x^n-1| \leq 2 , \qquad |h| = m .
\]
Para $m\geq3$ a desigualdade $|\beta^*-h| < |h|$ é estrita em todo o círculo, e como $h$ tem aí
exatamente um zero interior (a origem, simples), Rouché dá o mesmo para $\beta^*$. Para $m=2$ a
estrita só se perde quando $x^n=-1$, onde $\beta^*(x) = 2(x-1) \neq 0$ (pois $x^n=-1$ exclui
$x=1$), e a forma simétrica de Rouché conclui igual. Finalmente $\beta(m) = -1 < 0$ e
$\beta(m+1) = (m+1)^{n-1}-1 > 0$ dão a raiz real em $(m,m+1)$, simples por ser única fora do disco.
\end{proof}

\noindent \emph{A prova aponta a sua própria fronteira.} O passo precisa de $\max_{|x|=1}|x^n-1| =
2 \leq m$; em $m=1$ isso falha, e falha \textbf{sobre a circunferência} --- que é exatamente onde
as raízes de $\beta_{5,1}$ aparecem. Pelo lado direto (comparando $\beta$ com $x^{n-1}(x-m)$) a
condição lê-se $\min_{|x|=1}|x-m| = m-1 \geq 1$, e exclui a mesma linha. \emph{Duas contas
distintas nos dois lados do dual, excluindo a mesma linha $m=1$}: é assinatura de condição real, e
não de artefacto do método. Repare-se ainda que a versão dual conta \emph{um} zero simples onde a
direta conta multiplicidade $n-1$ --- é a mesma prova, e o dual é o lado barato.

\begin{obs}[e os graus $2$ e $3$ já saíam da conservação]
Para $n=2$ sobra uma raiz e o seu módulo é $1/\sigma$. Para $n=3$, $\operatorname{disc}\beta_{3,m}
= -4m^3-27 < 0$ garante um par conjugado, e conjugados têm módulos iguais: de $\sigma|\lambda|^2=1$
vem $|\lambda| = \sigma^{-1/2} < 1$. Vale inclusive para $m=1$, e é por isso que o número plástico
é de Pisot --- por esta razão, não por excepção. A partir de $n=4$ a conservação deixa de decidir
(ela dá o \emph{produto}, não cada fator), e é aí que faz falta o argumento de contorno.
\end{obs}

\begin{obs}[Fermat e Pisot são o mesmo enunciado com o sinal trocado]\label{obs:fermat-pisot}
Em $\Q(\sqrt D)$ a involução de Galois é $\nu(\sqrt D) = -\sqrt D$ e a norma é sempre
$N(z) = z\,\nu(z) = a^2-Db^2$. Uma só fórmula; o \textbf{sinal de $D$} decide o resto:
\begin{center}\small
\begin{tabular}{lll}
\hline
 & $D>0$ & $D<0$ \\ \hline
a forma $a^2-Db^2$ & indefinida & definida positiva \\
posto de Dirichlet $r_1{+}r_2{-}1$ & $1$: unidade infinita & $0$: unidades finitas \\
ordenável (Artin--Schreier) & sim & não \\
a pergunta & \textbf{onde} $N=1$ (Pell/Pisot) & \textbf{que valores} $N$ toma (dois quadrados) \\
o objeto & o \emph{núcleo} da norma & a \emph{imagem} da norma \\
\hline
\end{tabular}
\end{center}
E $\beta_{2,m}$ realiza o lado $D>0$: $\operatorname{disc} = m^2+4>0$ sempre, e
$\sigma\,\nu(\sigma) = -1$ --- unidade. Os casos degenerados também coincidem: $p=2$ é o único que
\textbf{ramifica} em $\Z[i]$ ($2 = -i(1+i)^2$, com $1+i$ fixo por $\nu$ a menos de unidade), e as
raízes que estragam $\beta_{5,1}$ têm módulo exatamente $1$ --- \emph{os pontos fixos} de
$\lambda\mapsto1/\bar\lambda$. Ramificação e raiz-na-circunferência são a mesma condição em duas
linguagens: o elemento é fixo pela involução, e a norma não o separa em dois fatores distintos.
\end{obs}

\noindent \textbf{O enunciado certo é condicional.} \emph{Quando} $\beta_{n,m}$ é irredutível,
$K_{n,m} := \Q[x]/(\beta_{n,m})$ é um corpo de números de grau $n$, a avaliação $x\mapsto\sigma$
mergulha-o em $\R$, e a ordem herda-se. E a irredutibilidade não é uma condição empírica: para
$m=1$, $\beta_{n,1}$ é redutível \emph{exatamente} quando $n \equiv 5 \pmod 6$, com fator
$\Phi_6 = x^2-x+1$. É o \textbf{teorema de Selmer} (1956), e prova-se em três linhas --- de
$x^n\beta(1/x) = -(x^n+x-1)$ e $\zeta - 1 = \zeta^2$ para $\zeta$ raiz primitiva sexta, vem
$\zeta^n + \zeta - 1 = \zeta^n + \zeta^2 = 0 \iff \zeta^n = \zeta^5$.

\begin{obs}[e o nome importa: $K_{n,m}$ não é $\R^n$]
$K_{n,m}$ é \emph{enumerável} e totalmente desconexo --- grau $n$ sobre $\Q$, dimensão $1$ sobre si
mesmo. $\R^n$ é não enumerável, conexo, e nem sequer é corpo para $n\geq3$. Chamar-lhe $\R^n$
confunde \textbf{grau de extensão} com \textbf{dimensão topológica}, e faz o enunciado parecer dizer
que a construção sobe para o plano --- o que não diz. O que sobe é a forma \emph{topológica}:
$\N^{\N} \cong (\N^{\N})^n$, logo $\R\setminus\Q \cong (\R\setminus\Q)^n$. \emph{Esse} é o
``preenche toda dimensão ponto a ponto'' --- e ele existe precisamente porque os irracionais são
totalmente desconexos, morrendo quando se repõem os racionais.
\end{obs}

\begin{obs}[e $K_{n,m}$ é só um lado --- o $\R^n$ está do outro lado do mergulho]
A observação anterior é sobre o \emph{corpo}; sobre o \emph{espaço} que ele gera a resposta é outra,
e as duas convivem. Um corpo de números de grau $n$ tem $n$ mergulhos em $\C$ ($r_1$ reais, $r_2$
pares, $r_1+2r_2 = n$), e o \textbf{mergulho de Minkowski}
$\sigma: K_{n,m}\to\R^{r_1}\times\C^{r_2}\cong\R^n$ tem por imagem um \emph{reticulado completo}.
Ou seja: $K_{n,m}$ é enumerável \textbf{e} $K_{n,m}\otimes_\Q\R \cong \R^n$ --- medido, posto cheio
em todos os casos.

E o corpo é \textbf{só um lado}: o outro é o reticulado dual $L^{*}$, com
$\operatorname{vol}(L)\cdot\operatorname{vol}(L^{*}) = 1$ exatamente --- a mesma conservação de
$|\det|=1$. \textbf{O $\R^n$ de facto é $L\oplus L^{*}$}, e não uma metade.
\end{obs}

\noindent E a figura do cone só vale no regime bom: \textbf{quando $\sigma$ é de Pisot}, iterar a
companheira apaga o subespaço que contrai e projeta na direção dominante --- a dimensão abaixo é a
sombra da de cima. Fora dele há direções que crescem além da dominante, e não há sombra única para
onde projetar.

\noindent \textbf{Por que é ferramenta central.} Para \emph{operar} corpos --- compará-los,
encaixá-los, percorrê-los --- é preciso que haja \emph{ordem}, e o paper mostra que ela existe e de
onde vem: a decomposição canónica é única, e a ordem lê-se das coordenadas um nível de cada vez.
Sem unicidade não haveria ordem; sem ordem, $\oplus$ e $\otimes$ seriam operações sem retículo onde
acontecer. A ordem não é acrescentada ao corpo de corpos --- \emph{é lida da régua}.

\medskip
\noindent \emph{O que se segue.} As secções seguintes usam esta ferramenta em cada nível: o corpo
universal declara o contrato que a régua cumpre; a dualidade nomeia o operador que aqui aparece como
$\det = -1$; o corpo autossimilar $\R^n$ é o que a companheira constrói; e a mecânica lê a mesma
matriz como força. \textbf{É a mesma peça, e cada secção veste-a de um modo.}



O Aarão: \emph{``vê frações --- tem uma construção matricial lá. Traz para a teoria e catálogo. Esse
permite \textbf{operar} corpos: é o operador do corpo de corpos, dá as operações \textbf{tensoriais}.
É o corpo universal na forma matricial --- operação no corpo de corpos.''}

\paragraph{A régua não é um algoritmo: é um produto de matrizes.} Cada quociente parcial $a_k$ é
\[
  M(a_k) = \begin{pmatrix} a_k & 1 \\ 1 & 0 \end{pmatrix},
  \qquad
  M(a_0)M(a_1)\cdots M(a_n) = \begin{pmatrix} p_n & p_{n-1} \\ q_n & q_{n-1}\end{pmatrix} ,
\]
e as colunas do produto \emph{são} os dois últimos convergentes --- saem da composição, sem se
calcular nada. \code{tools/matricial.c} §M1 mede-o contra a recorrência habitual, coluna a coluna.

\begin{obs}[a família real é a régua de período 1]
$M(m) = \left(\begin{smallmatrix} m & 1\\ 1 & 0\end{smallmatrix}\right)$ é \textbf{exatamente} o
gato $A_m$ de cada metal. Logo $\sigma_m$ é o número cuja régua é $[m;m,m,\ldots]$ --- periódica de
período um, medido em §M3. A família real não é \emph{parecida} com a régua: é a régua com um
quociente só, repetido. Dois estudos deste projeto falavam da mesma peça sem o saber.
\end{obs}

\noindent E daí sai de graça o que o fator de potência mediu por outro lado: $\det M(a) = -1$ para
todo $a$, logo o produto tem $|\det| = 1$, logo \textbf{a inversa é inteira} --- agora dito de uma
vez para \emph{todas} as expansões, e não caso a caso.

\subsection*{As operações tensoriais, e o que ``operar corpos'' quer dizer}

O corpo de corpos tem os $\R^n$ por elementos e duas operações: $\oplus$ com dimensão $a+b$ e
$\otimes$ com dimensão $a\cdot b$. \textbf{Na forma matricial elas têm nome e são padrão}:
\[
  \oplus \;=\; \text{soma direta} \;\; \begin{pmatrix} A & 0 \\ 0 & B\end{pmatrix},
  \qquad
  \otimes \;=\; \text{produto de Kronecker} \;\; (A\otimes B)_{ib+k,\,jb+l} = A_{ij}B_{kl} .
\]
§M4 \emph{constrói} as duas matrizes e conta a ordem da matriz construída --- não compara $a+b$ com
$a+b$, que seria tautologia --- e verifica $\det(A\otimes B) = (\det A)^b(\det B)^a$ por eliminação
de Gauss. \textbf{É aqui que ``operar corpos'' fica literal:} dois corpos combinam-se com uma
operação de álgebra linear, sem se reescrever multiplicação nenhuma.

\begin{obs}[e o dual também é uma matriz]
$\nu(a,b) = (a+Bb,\,-b)$ escreve-se $N = \left(\begin{smallmatrix}1 & B\\ 0 & -1\end{smallmatrix}\right)$,
e a involução é $N^2 = I$ --- medido para toda régua em §M5. E $\det N = -1$: \textbf{o dual inverte
a orientação}, é uma reflexão e não uma rotação, e é por isso que trocar duas vezes devolve.
Pontryagin em duas linhas de álgebra linear.
\end{obs}

\noindent §M6 fecha com os neutros, a associatividade e a \textbf{distributiva}
--- $\R^a\otimes(\R^b\oplus\R^c) = (\R^a\otimes\R^b)\oplus(\R^a\otimes\R^c)$ ---, que é o que faz
disto uma estrutura e não duas operações avulsas. E com o controlo que o torna medida: a lei
recíproca é \textbf{falsa} ($\oplus$ não distribui sobre $\otimes$: $14 \neq 30$), e o mesmo
critério reprova-a.

\subsection*{Não é semianel: o inverso estava do outro lado}

Escrevi acima que o corpo de corpos era um \emph{semianel}, e o Aarão corrigiu-me: \emph{``não é
semianel --- falta o outro lado da dualidade, a reversão. Só trocar de sinal a multiplicação, e
direto e cruzado nas duas torres, negra e branca.''}

\emph{E o erro foi de leitura.} Eu repeti a falta declarada no \code{corpodecorpos.c} --- ``nem
$\oplus$ nem $\otimes$ têm inverso'' --- sem ver que o \code{pontryagin.c} \textbf{já a tinha
preenchido}, e logo na primeira linha: \emph{``sem Pontryagin não sai inverso, e sem inverso
vaza''}. O inverso existe, é \textbf{colhido do dual}, e não é um inverso de elemento: é a operação
que inverte o retículo.

\paragraph{Medido no retículo dos divisores.} Com $\nu(d) = n/d$, §M7 varre os $144$ pares de
divisores de $60$ e mede as três coisas:
\[
  a \mid b \iff \nu(b) \mid \nu(a)
  \qquad
  \gcd(\nu a, \nu b) = \nu(\operatorname{lcm}(a,b))
  \qquad
  \nu\circ\nu = \mathrm{id}
\]
--- o dual \textbf{inverte a ordem}, \textbf{troca ínfimo com supremo} e é involução. Com isso a
reversão está lá, e ``semianel'' fica errado: faltava o inverso \emph{só enquanto se olhava para
$\oplus$ e $\otimes$ sozinhos}.

\begin{obs}[as duas torres, e o que separa uma da outra é o sinal]
\begin{center}\small
\begin{tabular}{llll}
\hline
torre & o que faz & simetria & no par \\ \hline
BRANCA & desce (projeta) & antissimétrica & o cruzado: roda, ordena \\
NEGRA & sobe (reverte) & antissimétrica & o direto: mede, volta \\
as duas & o ciclo fecha & \textbf{simétrica} & o corpo inteiro \\
\hline
\end{tabular}
\end{center}
É a frase do \code{base.c} §B12 --- ``cada torre antissimétrica, as duas juntas simétricas'' ---
medida: a parte simétrica não muda ao trocar a ordem, a antissimétrica \textbf{troca de sinal}, e a
soma devolve o original. \textbf{Trocar a ordem da multiplicação é trocar de torre.} Uma sozinha
desce e não volta; a outra é a reversão. Por isso o corpo precisa das duas --- e por isso o inverso
não estava a faltar: \emph{estava do outro lado}.
\end{obs}



\section{O corpo universal, e o que se lhe pede}

Antes de qualquer construção, uma só exigência. Um \textbf{corpo universal} é um conjunto com um
produto \emph{invariante por deslocamento} --- isto é, uma \textbf{convolução}:
\[
  (a \circledast b)_n \;=\; \sum_{m} a_m\, b_{n-m}.
\]
Nada mais se pede. E dessa única exigência sai tudo o que se segue, incluindo o que costuma ser
maquinário à parte.

\subsection*{A transformada universal é a que diagonaliza}

Chama-se \textbf{transformada universal} à aplicação $\mathcal{F}$ que leva convolução em produto:
\[
  \mathcal{F}(a \circledast b) \;=\; \mathcal{F}(a)\,\mathcal{F}(b).
\]
Essa é a \emph{única} propriedade que a define, e é dela que vem o nome: ela não depende do corpo,
só de haver convolução. Qualquer corpo com produto invariante por deslocamento cai aqui.

\subsection*{O \texorpdfstring{$\sqrt N$}{raiz de N} não é convenção: é medida}

A transformada admite uma normalização livre, $\mathcal{F}_{jk}=\omega^{jk}N^{-p}$. Varrendo $p$ em
vez de o escolher, \textbf{só $p=\tfrac12$ conserva a norma}, e só com ele a ida e a volta pesam
igual: $\mathcal{F}^\dagger=\mathcal{F}^{-1}$.

Esse mesmo $\sqrt N$ aparece noutros dois sítios, e é o \emph{mesmo número pela mesma razão}: a base
ortogonal tem norma $\sqrt N$, e somar $N$ grandezas independentes é somar $N$ direções
perpendiculares --- Pitágoras conta isso como $\sqrt N$. \emph{Não há três coeficientes que
coincidem: há um, e ele é a norma que a ortogonalidade impõe.} É por isso que promediar $N$ medidas
melhora por $\sqrt N$: isso é Parseval, não um acaso estatístico.

\subsection*{A inversa colhe-se; a deconvolução tem condição exata}

Se a convolução é o produto, a \textbf{deconvolução} é o quociente: divide-se casa a casa no lado
transformado e volta-se. E ela existe \textbf{exatamente} quando nenhuma casa de $\mathcal{F}$ se
anula --- uma condição decidível numa varredura, não uma esperança. O núcleo que anula uma casa
apaga informação que não volta: \emph{é o que não tem dual}.

\subsection*{O corpo universal é autodual}

$\mathcal{F}$ leva o corpo \emph{nele próprio}, e a sua ordem é quatro:
\[
  \mathcal{F}^4=\mathrm{id},\qquad
  \mathcal{F}^2 = (n \mapsto -n),
\]
isto é, o quadrado da transformada é a \textbf{reflexão} --- ordem $2$, o espelho --- e ela vive
\emph{dentro} da ordem $4$, que é a rotação. Há ainda vetores iguais à sua própria transformada; a
gaussiana é o exemplo clássico.

\emph{Um corpo que precisasse de outro para se transformar não seria universal:} teria de haver um
terceiro para fechar, e depois um quarto. É a autodualidade que fecha a definição em si mesma.


\subsection*{E a construção: o gerador, a torção, e o carácter}

A propriedade define; falta dizer \emph{com que peça} se realiza. E a peça não é nova --- é o
\textbf{gerador do corpo universal}, a estrutura mínima. Pede um primo $p$ com $n \mid p-1$, o
\textbf{menor} gerador $g$ de $\Z/p$, e a \textbf{torção}
\[
  w = g^{(p-1)/n},
\]
cuja órbita \emph{é} a base --- que portanto não se escolhe. A normalização é $r^2=n$: \emph{é o
$\sqrt N$ de cima}, e não um segundo coeficiente que por acaso coincide. E a única propriedade que
toda a construção usa é uma,
\[
  \chi_k(u+v) = \chi_k(u)\,\chi_k(v), \qquad \chi_k(j) = w^{jk},
\]
--- \emph{o carácter leva $\oplus$ a $\otimes$} (medido, $7680/7680$). O $\omega$ da transformada
\textbf{é} este $w$: um só objeto, com dois nomes conforme se olhe para a propriedade ou para a peça.

\medskip
\noindent\textbf{Duas frases, uma coisa.} Ao longo deste trabalho a transformada aparece descrita de
dois modos --- \emph{``a que leva convolução em produto''} e \emph{``o gato levado ao seu
representante''}. \textbf{Não são duas definições: são a propriedade e a construção.} A primeira diz
\emph{o que} ela tem de fazer e é a definição; a segunda diz \emph{como} se constrói uma que o faça,
e é a realização. Onde este texto disser ``a transformada'', entenda-se a primeira.

\medskip
\noindent E a razão de a torção \textbf{bastar} é que \emph{o dual do dual devolve o grupo}: por isso
ir ao dual e voltar é a identidade, e as inversas --- a da transformada, a da convolução, o flip
$\mathcal{F}^2$ --- não são três teoremas, são \textbf{consequências de uma razão} (medido, $256/256$
em cada). \emph{É por isso que nada vaza}, e é a mesma autodualidade dita acima por outro lado.

\subsection*{E \texorpdfstring{$\R^n$}{R\^n} é uma realização}

O corpo autossimilar deste trabalho não é uma teoria à parte: é o corpo universal com uma
\textbf{redução escolhida}. Multiplicar em $\Z_p[x]/(x^N-1)$ \emph{é} convoluir os coeficientes, e a
borda $\sigma^n=m\sigma^{n-1}+1$ troca a redução --- não a convolução.

\textbf{Mas a transformada muda com ela, e é preciso dizer como.} A transformada universal é, em
geral, a \textbf{avaliação nas raízes da borda}. Para $x^N-1$ essas raízes são as $N$ raízes da
unidade, e a avaliação é a DFT. Para $p_n$ \emph{irredutível} não há raízes no corpo base --- e não
pode haver: $\Z_p[x]/(p_n)$ \emph{é um corpo}, logo só tem idempotentes triviais e nenhuma
diagonalização por caracteres. As raízes estão na \emph{extensão}, e são as $n$ conjugadas de
Frobenius $\sigma,\sigma^p,\dots,\sigma^{p^{n-1}}$ --- que são, literalmente, as \textbf{folhas}
(§8). Avaliar nelas diagonaliza: medido em $\GF(13^2)$, $1521$ testes e $0$ falhas.

\emph{Esta correção não enfraquece a unificação --- aperta-a:} a inversa colhida pelos conjugados
de Frobenius (§7) e as folhas (§8) deixam de ser assuntos paralelos e passam a ser \emph{corolários}
da transformada universal. E a régua $(B,C)=(-\text{traço},\det)$ continua a mesma, porque ela é da
redução e não da transformada.

O \textbf{dual} $\R^{n*}$ obtém-se trocando o sinal de \emph{uma só} peça do produto --- a
antissimétrica --- e os dois só formam corpo \emph{juntos}. Tudo o que se segue é essa realização
posta a trabalhar: as progressões, a base ortonormal, a dualidade, a colheita. \emph{O geral está
dito; o resto é encarná-lo.}

\medskip
\noindent\emph{Medido em} \code{tools/universal.c}: a diagonalização em $N=4,8,16,32$ com resíduo
$7\times10^{-15}$; o expoente $\tfrac12$ procurado e não posto; os três $\sqrt N$ a fecharem no
mesmo $N$; e a autodualidade com $\mathcal{F}^4=\mathrm{id}$ a $6\times10^{-15}$.

\subsection*{Como se pede: o contrato não se assina --- liquida-se}

Falta dizer \emph{como} se pede um corpo ao sistema, e a resposta mudou durante este trabalho.
A primeira versão pedia quatro cláusulas declaradas --- uma soma, um produto, um operador e uma
dualidade --- e um verificador que julgasse se eram cumpridas. \textbf{Isso era um contrato
assinado, e era uma cláusula a mais.} A régua não é a quarta coisa a declarar: é a única, e as
outras três derivam-se dela.

O que se pede, então, é \textbf{meia dualidade}: uma representação finita: quatro termos bastam
($n+2$ para grau~2). Deles sai a régua por Cramer, exata em inteiros; da régua sai o dual
$\nu(a,b) = (a + Bb,\,-b)$, \textbf{forçado} --- contando as aplicações que fixam o~$1$ e respeitam
o produto há exatamente uma além da identidade, que é Galois em grau~2. E o que substitui a
assinatura é a \textbf{reversão}: ou ela volta com resíduo~$0$, ou os termos não eram de um corpo.

\begin{center}
\begin{tabular}{ll}
\toprule
o contrato \textbf{assinado} & declaram-se quatro cláusulas, e alguém verifica \\
o contrato \textbf{inteligente} & fornece-se metade, e ele \textbf{liquida-se} --- deriva e executa \\
\bottomrule
\end{tabular}
\end{center}

\noindent É a diferença que um \emph{smart contract} faz: não há contraparte a honrar nem árbitro a
julgar. \textbf{O contrato é o código, e liquidar é correr} --- e \emph{toda entrada} dispara a
liquidação, sendo o tique do relógio uma entrada como outra qualquer.

\medskip
\noindent\textbf{E ao liquidar, chama agentes.} Um contrato que só verifica é passivo; o que liquida
executa. O agente não se escolhe de uma tabela --- \textbf{o $\Delta$ determina-o}: $\Delta<0$ chama
quem \emph{gira} (a órbita fecha), $\Delta>0$ quem \emph{estica} (cresce sem sair da hipérbole),
$\Delta=0$ quem faz o \emph{limite}. \emph{O despacho é o sinal de um número que já estava nos
termos fornecidos.}

\medskip
\noindent\emph{E o confronto com o estado da arte é onde a diferença aparece.} A literatura
identifica três dificuldades num contrato que se executa sozinho, e as três recebem aqui outra
resposta:

\begin{center}
\begin{tabular}{lll}
\toprule
 & a resposta corrente & aqui \\
\midrule
\textbf{a paragem} & \emph{gas}: um orçamento por fora & sai da \textbf{álgebra}: o corpo é finito \\
\textbf{a reentrância} & disciplina de escrita, revisão & o agente \textbf{nunca vem da entrada} \\
\textbf{o oráculo} & desloca-se a confiança & o fecho é interno --- \emph{o resto não} \\
\bottomrule
\end{tabular}
\end{center}

\noindent A primeira linha é a que arrasta as outras. Um contrato numa máquina Turing-completa pode
não terminar, e a solução usual é económica: paga-se um orçamento que aborta a execução. Aqui a
paragem não se compra --- \textbf{o espaço é $\mathbb{Z}_q^2$, finito, logo a órbita fecha por
gaiola}, e o número de passos tem nome desde 1700: é o período de Pisano $\pi(q)$. Medido em
$q=3,4,5,7,11,12$: as órbitas \textbf{partem} o plano (cobrem e não se sobrepõem) e nenhuma passa o
teto~$q^2$.

\medskip
\noindent\emph{E o que isto não resolve, que também é resultado:} não há consenso distribuído ---
isto não é uma \emph{blockchain}; o que se corre a cada vez é \textbf{teste, não prova formal}; a
linguagem exprime corpos de grau~2 e não computação geral; e dados do mundo continuam a precisar de
fonte --- \textbf{o oráculo não desapareceu}, só não é preciso para o fecho.

\medskip
\noindent\emph{Medido em} \code{tools/fecha.c} (24 unidades): a régua sai de quatro termos nos seis
corpos e com três não sai de nenhum; o dual é único sob o crivo do produto; e a cobertura fecha.
\emph{E em} \code{tools/smartcontract.c} (11 unidades): a liquidação, o despacho pelo $\Delta$, a
idempotência, e as três dificuldades canónicas medidas.

\section{O $0$ e a peça}

O alfabeto tem uma letra, o $0$ (Venom): o vértice, a origem, o neutro ($A\oplus\varnothing=A$), o
centro que cinde em qualquer direção. E tem uma \textbf{tríade} de operações, só ela:
\[
 \oplus\ \text{(a cisão --- o lado aditivo)}\qquad
 \otimes\ \text{(o \emph{gato}, }\times\sigma\text{)}\qquad
 \textstyle\prod\ \text{(}\exp/\log\text{: }a\otimes b=g^{\log a+\log b}\text{).}
\]

\paragraph{O gato é a realimentação.} A peça é o gato $A_m=\left(\begin{smallmatrix}m&1\\1&0\end{smallmatrix}\right)$,
com $\det=-1$ (reversível). O seu ponto fixo satisfaz
\[
 \sigma=m+\frac1\sigma :
\]
$\sigma$ dos dois lados --- o sinal que volta. É a raiz de $x^2-mx-1$; a fração contínua
$[m;m,m,\dots]$ é esse laço iterado (o \emph{metal} $\sigma_m$; $\sigma_1=\varphi$). Os dois
autovalores são o \textbf{negro} $\sigma$ ($|\sigma|>1$, o sorvedouro) e o \textbf{branco}
$\sigma'=-1/\sigma$ ($|\sigma'|<1$, a fonte), com $\sigma\sigma'=-1$ e $\sigma+\sigma'=m$.

\paragraph{O esquilo é o dual.} O gato anda numa direção (a seta do tempo); o \textbf{esquilo} ---
o seu conjugado/transposta, $\det=+1$ --- é o espelho que traz de volta. Gato e esquilo comutam e,
juntos, completam o corpo: vai-se pelo gato, volta-se pelo esquilo. Reproduz-se com
\code{tools/checkup.c}, \code{tools/esquilo.c} (resíduo $0$).

E o esquilo não se constrói: \textbf{colhe-se da borda}. De $\sigma^n=m\sigma^{n-1}+1$ sai
$1=\sigma\,(\sigma^{n-1}-m\,\sigma^{n-2})$, logo
\[
   \sigma^{-1}=\sigma^{n-1}-m\,\sigma^{n-2}
\]
--- dois termos da própria realimentação, sem Fermat, sem Frobenius, sem eliminação. Rotacionar um
polinômio pelo gato e desrotacioná-lo por esse esquilo é exato em toda dimensão, para dado arbitrário
e para prosa crua (\code{tools/rotaciona.c}, $n=2..8$, resíduo $0$). E há \emph{dois} esquilos, que
diferem por um sinal: $\sigma^{-1}=-\sigma'$. Pelo inverso volta-se ao dado; pelo conjugado volta-se
com o sinal trocado --- é a folha que descola (§6).

\begin{obs}[quando é que algo significa --- nem no repouso, nem na transitividade]\label{obs:signif}
Partindo do $0$ em repouso não há volume, direção, escala nem nada absoluto. Quando existe \emph{algo},
e quando esse algo \emph{significa}? A resposta medida (\code{tools/significado.c}, resíduo $0$) é um
\textbf{intervalo}, e tem dois lados que se esquecem.

\textbf{No repouso, tudo é invariante --- e por isso nada significa.} Se nenhuma operação age, cada
ponto é a sua própria classe: toda função é conservada, o número de classes iguala o de pontos, e não
há relação a sustentar um nome. Invariância \emph{total} é ausência de informação, não excesso.

\textbf{Uma lei particiona, mas não basta: ela tem de conservar.} O gato $\times\sigma$ corta
$\GF(13^2)$ em $6$ classes --- e ainda assim a norma não nomeia nada, porque $N(\sigma)=\sigma\sigma'=-1$:
ele \emph{escala}, e a norma alterna de sinal a cada passo. Já $\times\sigma^2$ tem norma $+1$: está na
\textbf{borda} $|\lambda|=1$, e aí a norma é \emph{constante} em cada uma das $12$ órbitas, e nomeia.
Logo o significado não nasce do movimento --- nasce do movimento que \textbf{conserva}. Ficar na borda
(§B.6 do gabarito) não é apenas ``funcionar'': é a condição de haver o que dizer.

\textbf{E se a lei alcança tudo, o significado desaparece de novo.} Com duas operações --- somar $1$ e
$\times\sigma$ --- o $1$ varre $\GF(p^n)$ inteiro (Obs.~\ref{obs:pi}): sobra \emph{uma} classe, e todo
invariante é constante. \emph{Gerar tudo e não distinguir nada são a mesma coisa.} O significado é
portanto uma \textbf{janela}: $1<\text{classes}<\text{pontos}$, com lei conservativa.

E as três coisas que pareciam absolutas não são coisas: são relações que a lei sustenta --- a
\textbf{direção} é o sinal ($\sigma\sigma'=-1$, a ida e volta que troca), a \textbf{escala} é só razão
(nenhuma absoluta; na borda nada cresce nem decresce), o \textbf{volume} é o que o determinante conserva.
Sem lei não há direção (não há o que voltar), nem escala (não há com que comparar), nem volume (não há o
que conservar).
\end{obs}

\begin{obs}[o instrumento cria a assimetria --- e a realidade observada é produto dele]\label{obs:instr}
A observação não é passiva, e isso prova-se em duas metades (\code{tools/instrumento.c}, resíduo $0$)
--- sendo a segunda o que impede a primeira de desandar.

\textbf{O instrumento cria a assimetria, e a criação é quantificável.} Sem lei, \emph{toda} permutação
preserva a estrutura: o grupo é $S_N$, simetria máxima --- e nada distingue nada. Ao pôr uma lei, só
sobrevive quem \emph{comuta} com ela, e o grupo despenca de $N!$ para o centralizador $d^c c!$ (força
bruta batendo a fórmula em cinco casos; em $n=8$ com dois ciclos de $4$, de $40\,320$ para $32$ ---
divisão por $1260$). \emph{Observar é o que sobra quando a permutabilidade acaba}: antes, todo ponto era
intercambiável com todo ponto, e por isso não havia o que dizer.

\textbf{E a realidade observada é produto do instrumento.} No \emph{mesmo} $\GF(13^2)$, leis diferentes
dão contagens diferentes: $\times\sigma$ dá $6$ classes, $\times\sigma^2$ dá $12$, $\times\sigma^4$ dá
$24$, $\times\sigma^{14}$ dá $84$, a lei trivial dá $168$ (cada ponto a sua classe --- o repouso) e as
duas operações juntas dão $1$ (nada distingue). O corpo é o mesmo em todas as linhas; o que muda é a
medida, e com ela quanto se pode dizer.

\begin{obs}[por que isso não colapsa em ``cada um vê o que quer'']
Os produtos do instrumento são \textbf{coerentes}. Medido: as classes do instrumento mais \emph{fino}
\textbf{refinam} as do mais grosso, exatamente, sem nunca cruzar --- $168\to84\to42\to24\to12\to6$, e
cada classe fina cabe inteira numa grossa em todos os pares testados. As partições formam um
\textbf{retículo} ordenado por refinamento. Logo o instrumento escolhe a \emph{resolução}; não fabrica
o substrato. Dois observadores com instrumentos distintos não se contradizem: um vê menos, e o menos
está \emph{contido} no mais. A tese exata é portanto: a realidade \textbf{observada} --- a partição, as
classes, os nomes --- é produto da medida, e nada disso existe sem ela; o que a medida não faz é
inventar o que particiona.

E o instrumento não escapa da janela da Obs.~\ref{obs:signif}: para ver, precisa quebrar a simetria
(senão nada distingue), não pode varrer tudo (senão tudo é uma classe) e tem de \emph{conservar} (senão
o que ele mede alterna e não nomeia). \emph{Quem vê é quem fica na borda.}
\end{obs}
\end{obs}

\begin{obs}[a assimetria inicial é uma \textbf{anti}ssimetria --- e é a única que atravessa a dimensão]\label{obs:anti}
Se sem assimetria não há nada, cabe perguntar qual assimetria. A resposta é exata, e é a
\textbf{anti}ssimétrica --- $\omega(u,v)=-\omega(v,u)$ --- por cinco razões medidas
(\code{tools/antissimetrico.c}, resíduo $0$).

\textbf{(a) Ela carrega tudo e nada ao mesmo tempo, e isso é literal.} Sobre um ponto só ela é
\emph{nada}: $\omega(u,u)=0$ para todo $u$ --- um ponto é invisível a si mesmo. Sobre pares ela é
\emph{tudo}: não-degenerada, todo $u\neq0$ tem $v$ com $\omega(u,v)\neq0$. E a razão de fundo é mais
forte que a identidade: o grupo que a conserva é \textbf{transitivo} nos pontos --- construí, para cada
um dos $6400$ pares não-nulos de $\F_3^4$, uma transvecção $S$ com $S^tJS=J$ e $Su=v$, $6400/6400$. Ela
não distingue ponto de ponto; só fala de \emph{diferença}. A simétrica faz o oposto: a norma é rótulo
($3$ valores nos $80$ pontos), ela mede o ponto e por isso não é \emph{nada} em lugar nenhum.

\textbf{(b) Só ela se conserva em toda dimensão.} Contadas por força bruta em $\F_p^n$, as formas
alternantes não-degeneradas são $0$ em toda dimensão \emph{ímpar} --- e não por escassez: em $n$ ímpar
$\det B=\det(-B^t)=(-1)^n\det B=-\det B$, logo $\det B=0$ sempre. Em dimensão \emph{par} elas existem e
formam \textbf{uma única classe}: a contagem bate $|GL_n|/|Sp_n|$ em todo caso testado
($28$ em $\F_2^4$, $468$ em $\F_3^4$, $12\,400$ em $\F_5^4$), e para cada uma das $468$ construí a base
simplética e conferi $P^tBP=J$ entrada por entrada --- $468/468$. Uma classe, sempre, sobre qualquer
corpo. A simétrica se \emph{divide}: o discriminante $\det \bmod$ quadrados é invariante de congruência
(identidade $\det(P^tBP)=\det(P)^2\det B$ medida exata), dando $\geq 2$ classes em $\F_p$ em toda
dimensão --- e $n+1$ em $\R$, pela assinatura, número que \emph{cresce} com a dimensão. Só a
antissimétrica atravessa sem se partir.

\textbf{(c) É onde o gato mora.} Medido em $28\,561$ pares: $\omega(Au,Av)=\det(A)\,\omega(u,v)$. Como
$\det A_m=-1$, o gato \textbf{anti}-conserva --- uma batida vira o sinal de $\omega$, duas devolvem ---,
e o esquilo ($\det=+1$) conserva. A holonomia $-1$ das folhas (Obs.~\ref{obs:dupla}) \emph{é} a
antissimetria da forma, e o corte par/ímpar de $\det A_n=(-1)^{n+1}$ é o mesmo corte de (b): a folha
dupla existe exatamente onde a forma existe.

\textbf{(d) Os complexos são o primeiro lugar onde há algo.} Em $n=1$ a única forma alternante é a
\textbf{nula}: no ponto não há produto, não há par, não há observação. Em $n=2$ ela já é
não-degenerada --- e é $\C$: com $J=\left(\begin{smallmatrix}0&1\\-1&0\end{smallmatrix}\right)$ vale $J^2=-I$,
$\R[J]\cong\C$ (tabela de multiplicação conferida) e $\omega(u,v)=\langle u,Jv\rangle$. A forma
antissimétrica e a estrutura complexa \textbf{são o mesmo objeto}. Nenhum real tem $x^2=-1$; $J$ tem. O
primeiro corpo não é escolha de conveniência: é a primeira dimensão em que a antissimetria não é zero.

\textbf{(e) E a lei de potência sai dela.} Duas medidas. O volume é uma \emph{potência} da forma:
$\det B=\Pf(B)^2$ exato nos $531\,441$ casos testados, e em $\dim 2m$ o volume é $\omega^m/m!$ --- a
mesma forma elevada, não uma analogia. E nos corpos locais o determinante da multiplicação --- que
\emph{é} a forma --- é a norma elevada à dimensão: $|{\det}|=|x|^1$ em $\R$, $\det=|z|^2$ em $\C$,
$\det=|q|^4$ em $\Hq$ (medido em $\Z$, resíduo $0$). O expoente \textbf{é} a dimensão: esse é o módulo
normalizado do corpo local, e é ele que força os quasicaracteres a serem $|x|^s$. A lei de potência não
é ajuste empírico --- é o determinante da própria multiplicação.

Em uma linha: \emph{a assimetria que basta é a antissimétrica, porque é a única que existe do mesmo modo
em toda dimensão; ela não mede ponto nenhum, só diferença; e por isso o que dela se colhe --- volume,
módulo, lei de potência --- vem sempre como potência de uma única forma.}
\end{obs}


\section{Os rótulos, e o gerador que já estava dado}

Antes da gramática, duas coisas que mudam a leitura de tudo o que vem depois: os nomes
(\emph{inteiro}, \emph{racional}, \emph{irracional}) não são propriedades do número, e o gerador da
construção não é invenção deste documento --- está dado, e basta recolhê-lo.

\paragraph{$\pi$ é o $0$: dividir o círculo devolve o centro.}\label{obs:pi}
O $0$ do alfabeto tem uma leitura que fecha o círculo, e ela se mede (\code{tools/pi.c}, resíduo $0$).
\textbf{A reta é a órbita do $1$}: com apenas ``somar $1$'' e o gato $\times\sigma$ alcança-se
\emph{todo} elemento de $\GF(p^n)$ --- testado em seis corpos, sem exceção. O irracional colapsa no $1$
da dimensão, e o $1$ devolve a reta inteira; o polinômio mínimo de $\sigma$ \emph{é} a descrição da
reta.

\textbf{E $\pi$ é o $0$ no sentido exato}: a palavra já dizia --- \emph{ciclotomia} é ``corte do
círculo'' ---, e dividir o círculo em $n$ partes devolve o \textbf{centro}: $\sum_k \zeta_n^k = 0$ para
todo $n$ (medido, $n=2..12$, $|\Sigma|\le6\times10^{-16}$), com o circuito fechando em $\zeta_n^n=1$.
Dividir é a cisão $\oplus$; o que sobra da divisão é o vértice --- é o Venom, medido com imagem em
\code{tools/venom.c} e aqui com o círculo. As formas saem da divisão do $0$, e a soma delas devolve o
$0$: a autoconstrução é isso.

\textbf{E os metais são de $\pi$}, literalmente: $1/\varphi=2\cos(2\pi/5)$, $\sqrt2=2\cos(\pi/4)$, e
$\sqrt5,\sqrt{13},\sqrt{17}$ são somas de Gauss em raízes da unidade (erro $\le10^{-15}$). O que
\code{quasi.c} §Q1 já media era isto, e eu não havia lido assim.

\begin{obs}[a base certa é $q=e^{-2\pi}$: $\pi$ gera cada metal]\label{obs:base}
E há uma correção de rumo a fazer, com fonte: o \emph{corpo estelar}
(medido aqui em \code{tools/estelar.c}: a identidade de Rogers--Ramanujan
$R(e^{-2\pi})=\sqrt{\varphi\sqrt5}-\varphi$ e a raiz de $x^4+2x^3-6x^2-2x+1$, com resíduo $0$;
ver \cite{rogers,ramanujan}). Este documento vinha
usando a fração contínua \textbf{regular} --- a de base inteira, onde $\varphi=[1;1,1,\dots]$ é o
\emph{pior} aproximável (Hurwitz) --- e daí tratou o ouro como o supremo do irracional: o último a
travar (§7), o último toro a romper. Está certo \emph{naquela} base, e é remar contra o fluxo.

A base do corpo estelar é $q=e^{-2\pi}$ --- uma \textbf{base $\pi$} ---, e nela o ouro não é o supremo
do irracional: é um valor \textbf{algébrico} que $\pi$ produz. Pela fração contínua de
Rogers--Ramanujan $R(q)=q^{1/5}/(1+q/(1+q^2/(1+\cdots)))$, Ramanujan (1913):
\[
   R\!\left(e^{-2\pi}\right)=\sqrt{\varphi\sqrt5}-\varphi=0{,}2840790\ldots,
   \qquad\text{raiz de } x^4+2x^3-6x^2-2x+1 ,
\]
medido em \code{tools/estelar.c} ($1{,}4\times10^{-17}$, o limite do \emph{long double}). O mecanismo é
\textbf{multiplicação complexa} --- funções modulares em argumentos-$\pi$ têm valores algébricos, os
\emph{singular moduli} ---, e é o \emph{mesmo} mecanismo que a Obs.~\ref{obs:pi} e o §7 já mediam nos
singular values $\tau=\sqrt N$ do AGM, sem que a ligação estivesse feita.

E há \textbf{lei}, não coincidência. Com $Q_m(x)=x^4+2m\,x^3-6x^2-2m\,x+1$ --- o coeficiente do meio é
a \emph{ordem} do metal ---,
\[
   Q_m(x)=\bigl(x^2+2\sigma_m x-1\bigr)\bigl(x^2+2\sigma_m' x-1\bigr),
   \qquad \boxed{\,v_m=\sqrt{\sigma_m^{\,2}+1}-\sigma_m\,}
\]
(verificado, $m=1..8$): $Q_m$ fatora sobre o corpo do \emph{próprio} metal, e o valor que $\pi$ gera é a
raiz pequena do fator. Em $m=1$ isso \emph{é} a fórmula de Ramanujan, porque $\varphi^2+1=\varphi+2=
\varphi\sqrt5$ (erro exatamente $0$). Não é um $R$ avaliado em vários pontos: é uma \emph{família} de
frações modulares, uma por nível, cada uma gerando o seu metal --- a prata pela Göllnitz--Gordon de
nível $8$, medida também. \emph{O fluxo é de $\pi$ para o metal.}
\end{obs}

\begin{obs}[o corte: $\pi$ comanda o abeliano --- e é ali que o ouro fura]
``$\pi$ gera todos os irracionais'' precisa de um corte, e o corte é um \emph{teorema}: as raízes da
unidade geram exatamente a extensão \textbf{abeliana máxima} de $\mathbb Q$ (Kronecker--Weber). Todo
metal $\sigma_m$ é quadrático, e grau $2$ é sempre abeliano --- logo \emph{a torre inteira dos metais
está no reino de $\pi$}. Fora ficam os não-abelianos, e o exemplo não é escolhido a dedo: é o número
\textbf{plástico} $x^3-x-1$, de grupo $S_3$ (discriminante $-23$, não quadrado). E ele é justamente o
fator que aparece quando o ouro \emph{fura} na dimensão $5$:
\[
   x^5-x^4-1=\underbrace{\Phi_6}_{\text{ciclotômico: de }\pi}\cdot\underbrace{(x^3-x-1)}_{S_3:\ \text{fora de }\pi} .
\]
A partição do furo (Obs.~\ref{obs:ouro5}, §7) \textbf{é} o corte de Kronecker--Weber: giro de $\pi$ de
um lado, crescimento fora de $\pi$ do outro. Na dimensão $5$ o ouro sai do alcance de $\pi$ --- e é
exatamente ali que o cristal deixa de fechar e vira quasicristal.
\end{obs}

\paragraph{Inteiro, racional e irracional são rótulos --- trocam com a base.}\label{obs:rotulos}
E há uma coisa a dizer aqui, porque muda a leitura de todo o resto: esses nomes não são propriedades
do \emph{número}, e sim do par \emph{(número, base em que se lê)}. O próprio $\sigma^{-1}$ acima é o
exemplo. Lido em $\mathbb Q$, $1/\varphi=0{,}618\dots$ é \textbf{irracional}; lido em $\Z[\sigma]$ ele
é $(-m,1)$ --- \textbf{inteiro}, duas coordenadas inteiras. Não mudou o número: mudou onde se lê. É por
isso que o esquilo pôde ser \emph{colhido} e não construído (medido em \code{tools/rotulos.c}, resíduo
$0$, para $m=1,2,3$).

Daí a leitura das dimensões. \textbf{Cada dimensão é um irracional}: $\sigma_n$ tem grau $n$ sobre
$\mathbb Q$ --- irracional --- e dentro de $\R^n$ é o vetor $(0,1,0,\dots,0)$, de coordenadas inteiras.
Na \emph{passagem} de dimensão o irracional \textbf{colapsa em inteiro}, e o grau é o número de folhas
(§6). Medido para $n=2..8$: a borda $\sigma^n=m\sigma^{n-1}+1$ fecha em coordenadas inteiras em toda
dimensão, e as potências $\sigma^0,\dots,\sigma^{n-1}$ \emph{são} os $n$ eixos da base --- logo não há
um gerador por dimensão: há \textbf{um}, e as suas potências varrem a base de cada reta, por recursão.

\begin{obs}[não é preciso guardar lista de primos]
Consequência prática, e ela corrige um hábito deste próprio documento: dado o inteiro $n$, o primo sai
por \textbf{regra} --- o menor $p$ com $n\mid p-1$ ---, achado por varredura com estado $O(1)$ (medido,
$n=2..12$). Os primos espalhados pelos medidores ($40009$, $40013$, $40037$, $10007$) eram uma
\emph{tabela implícita} de conveniência; a regra os dispensa, e é o mesmo desenho do ``sem tabelas'' do
gerador (§8).

A frase ``cada inteiro é um primo e um irracional'' pede uma precisão, e ela fica dita: literalmente
não --- $4$ não é primo nem irracional. O que é literal: todo inteiro \textbf{tem} um primo associado
pela regra, e todo inteiro \emph{não-quadrado} \textbf{é} irracional lido como $\sqrt n$, com fração
contínua \textbf{periódica} (Lagrange), fechando em $2a_0$ --- e o período é a \emph{dobra} (medido,
$n=2..20$). Os quadrados perfeitos não dobram, porque $\sqrt n$ já é inteiro: são as \textbf{partes
estéreis}.
\end{obs}

\section{As duas progressões, e o polinômio generalizado}

Antes do corpo, a gramática. A tríade do §2 não é uma lista de operações: é a estrutura de
\textbf{duas torres} ligadas por uma ponte, e é dela que os polinômios saem. Tudo neste parágrafo é
medido em \code{tools/progressoes.c} (resíduo $0$), com o logaritmo tomado em $\Z_p$ sobre um gerador
--- de modo que a ponte $\prod$ é \emph{exata}, não aproximada.

\paragraph{A torre do $\oplus$: a PA de ordem $k$ é o polinômio.} Uma progressão aritmética de ordem
$k$ é a sequência cujas diferenças estacionam no degrau $k$: $\Delta^k a=\text{const}\neq0$ e
$\Delta^{k+1}a=0$. Isso não é análogo a polinômio --- \emph{é} polinômio de grau $k$, e a constante é
$k!\,a_k$ (medido, $k=1..5$). A sua base natural não são as potências: são os \textbf{binomiais},
\[
   a_n=\sum_i \Delta^i a(0)\,\binom{n}{i} \qquad\text{(Newton)},
\]
e a reconstrução fecha termo a termo. Este é o lado da cisão: o polinômio escrito como soma de cortes.

\paragraph{A torre do $\otimes$: a PG de ordem $k$ é a sua sombra multiplicativa.} Do outro lado,
troca-se diferença por \emph{razão}: $R\,b_n=b_{n+1}/b_n$, e a PG de ordem $k$ é aquela cujas razões
estacionam no degrau $k$ ($R^k b=\text{const}$, $R^{k+1}b=1$). A base natural aqui são as
\textbf{potências}, e o termo geral é $b_n=\prod_i c_i^{\binom{n}{i}}$ --- os mesmos binomiais, agora
nos expoentes.

\paragraph{A ponte $\prod$ atravessa sem mudar a ordem.} O que liga as duas torres é a terceira
operação da tríade:
\[
   \text{PA de ordem }k \;\overset{\exp}{\longrightarrow}\; \text{PG de ordem }k
   \;\overset{\log}{\longrightarrow}\; \text{PA de ordem }k ,
\]
e a \textbf{ordem $k$ se preserva} na travessia (medido para $k=1..4$: $R^kb$ constante, $R^{k+1}b=1$,
e $\log(\exp a)=a$ exato). É por isso que $\oplus$ e $\otimes$ não são dois assuntos: são duas
\emph{escritas} do mesmo objeto, e $\prod$ é o dicionário.

\begin{principio}[o dupolinômio é a colisão da PG com a PA]
Aqui é preciso usar o nome certo, e ele vem do corpo estelar: o \textbf{dupolinômio} não é ``o par das
duas torres'' --- é a \textbf{colisão} delas. Escreva
\[
   P_g = x^2 \quad(\text{a PG, o crescimento}),\qquad P_a = m\,x+1 \quad(\text{a PA, a soma}),
\]
e o metal é onde as duas se encontram: $P_g=P_a$ dá $x^2-mx-1=0$, isto é $\sigma_m$ (medido em
\code{tools/estelar.c}, $m=1..6$, erro $0$). A recorrência $x_{k+1}=m\,x_k+x_{k-1}$ mistura as duas ---
o coeficiente $m$ é a PA, o crescimento $\sigma_m^{\,k}$ é a PG --- e o dupolinômio é o \emph{encontro}.
Dito isso, o par de torres é a estrutura em que a colisão acontece, e tem as suas duas ligações:
\begin{itemize}\itemsep2pt
\item \textbf{entre os lados}, a ponte $\prod$ ($\exp/\log$), que troca $\oplus$ por $\otimes$
      mantendo a ordem $k$;
\item \textbf{dentro de um lado}, o par $\Delta$ (diferença) e $\Sigma$ (acumulação), que são o gato e
      o esquilo do lado aditivo: $\Delta\circ\Sigma=\mathrm{id}$ exato, com $\Sigma$ subindo a ordem
      $k\to k+1$ e $\Delta$ descendo $k\to k-1$.
\end{itemize}
A torre da \emph{ordem} (no $\oplus$) é a mesma escada da torre da \emph{dimensão} (no $\otimes$, §6):
um degrau acima, um degrau abaixo, e a volta exata. E a conversão entre as duas bases --- potências e
binomiais --- é \textbf{Stirling}, involutiva: $\sum_k s(n,k)S(k,j)=\delta_{nj}$ e
$x^n=\sum_k S(n,k)(x)_k$ (medido, resíduo $0$).
\end{principio}

\paragraph{E a peça está entre as duas.} Aqui aparece o porquê de o gato gerar um corpo. A sequência
da peça --- $U_{n+1}=m\,U_n+U_{n-1}$, Fibonacci em $m=1$ --- \textbf{não é PA nem PG de ordem
finita}: nem as diferenças nem as razões estacionam em nenhum degrau (medido para $m=1,2,3$, até a
ordem $6$). Ela é a \emph{realimentação}, $\sigma=m+1/\sigma$, e é justamente por não fechar de
nenhum dos dois lados que produz algo que nenhuma das torres produz sozinha: um corpo em toda
dimensão. A PA e a PG são os dois \textbf{limites}; o metal é o que fica no meio. Este é o primeiro
lugar onde a inversão do resumo aparece --- \emph{o que está entre é o objeto, e o que estaciona é
ancoragem} --- e o §7 mostra que ela vale em toda a construção.

\section{O corpo autossimilar $\R^n$}

A mesma peça, iterada, é um corpo em \emph{qualquer} dimensão:
\[
 \R^n=\GF(p^n)=\Z_p[x]/(p_n),\qquad p_n(x)=x^n-m\,x^{n-1}-1 .
\]
Todo dado é um \textbf{polinômio} $\delta(x)=\sum_i d_i\sigma^i$ na base $\{1,\sigma,\dots,\sigma^{n-1}\}$
--- a base de potências do §4, o lado $\otimes$: as coordenadas $d_i$ são contínuas, e o único discreto
é a \textbf{dimensão} $n$ (quantos eixos). A companion de $p_n$ é o gato de dimensão $n$; a soma é
coordenada a coordenada, e o produto é o \emph{mesmo} laço uma volta abaixo --- a auto-similaridade
posta como definição:

\begin{principio}[a multiplicação recursiva --- o circuito autossimilar]
Decomponha $x=\tilde x+x_{n-1}\sigma^{n-1}$, com $\tilde x\in\R^{n-1}=\langle1,\dots,\sigma^{n-2}\rangle$.
Então
\[
 x\cdot y=\tilde x\,\tilde y
   +(\tilde x\,y_{n-1}+x_{n-1}\,\tilde y)\,\sigma^{n-1}
   +x_{n-1}y_{n-1}\,\sigma^{2n-2}\pmod{p_n},
\]
onde $\tilde x\,\tilde y$ \emph{é} o produto em $\R^{n-1}$ (a peça, um nível abaixo), e as potências
excedentes $\sigma^{n},\dots,\sigma^{2n-2}$ \textbf{baixam} pela realimentação da borda
$\sigma^{n}=m\,\sigma^{n-1}+1$, aplicada até só restarem $\sigma^0,\dots,\sigma^{n-1}$. A base da
recursão é $\R^1=\Z_p$. É a peça inteira: o mesmo gato, o mesmo produto interno, a mesma volta --- em
toda escala, sem nada novo por dimensão.
\end{principio}

\noindent Bem definida e associativa (é $\Z_p[x]/(p_n)$); com $p_n$ irredutível, é um corpo.

\subsection*{A notação algébrica: a marcação que a tupla deixa implícita}

O produto acima está escrito em \textbf{tuplas}: $x=(x_0,\dots,x_{n-1})$, e o que distingue a
coordenada $x_1$ da $x_0$ é a \emph{posição} --- um marcador que está lá e não se escreve. A
notação algébrica é a mesma coisa com o marcador \textbf{dito}:
\[
 x=(x_0,x_1,\dots,x_{n-1}) \;\longleftrightarrow\; x=x_0+x_1\sigma+\dots+x_{n-1}\sigma^{n-1}.
\]
Não é abreviatura nem conveniência de escrita. É a passagem de um marcador \emph{posicional} para
um marcador \emph{simbólico}, e ela muda três coisas:

\textbf{(a) As duas operações passam a ter a mesma cara.} Na tupla, a soma é coordenada a
coordenada e o produto é uma fórmula à parte, com um caso de recursão. Na notação algébrica os dois
são as regras habituais de polinómio --- e a única coisa que sobra é a \emph{redução}:
\[
 \sigma^n=m\,\sigma^{n-1}+1 \qquad\text{(a \textbf{borda})},
\]
aplicada até o grau descer abaixo de $n$. O produto recursivo do princípio acima \emph{é} essa
redução, e escrevê-lo assim mostra-o: multiplicar polinómios e baixar pela borda.

\textbf{(b) A borda passa a ser o parâmetro, e vê-se que é dela que os corpos diferem.} A
construção é $\Z_p[x]/(p)$ com $p$ irredutível, e a família $p_n(x)=x^n-mx^{n-1}-1$ é uma
\emph{parametrização} dela --- a que dá os metais. Trocar a borda dá outros corpos com a
\emph{mesma} recursão:
\begin{center}
\begin{tabular}{lll}
$p(x)=x^2-mx-1$ & borda $\sigma^2=m\sigma+1$ & os metais ($\Delta>0$, hiperbólico, o \emph{gato}) \\
$p(x)=x^2+1$    & borda $\sigma^2=-1$        & $\C$ ($\Delta=-4$, elíptico, ordem $4$, o \emph{esquilo}) \\
$p(x)=x^2-x+1$  & borda $\sigma^2=\sigma-1$  & o sexto ciclotómico ($\Delta=-3$, ordem $6$) \\
\end{tabular}
\end{center}
Aplicando a fórmula do princípio a $n=2$ com $\sigma^2=-1$ sai exatamente o produto complexo:
$(0,1)\cdot(0,1)=(-1,0)$, $(1,2)\cdot(1,-2)=(5,0)$, $(1,1)\cdot(1,1)=(0,2)$ --- medido em
\code{tools/hiper.c}, resíduo $0$. \emph{Não é uma máquina parecida: é a mesma fórmula, com o
excedente a baixar por outra borda.} E o nome $i$ é o nome que $\sigma$ leva nesse caso.

\textbf{(c) O salto de dimensão e a troca de borda separam-se.} Na tupla os dois movimentos
confundem-se, porque ambos mexem em coordenadas. Escritos, são distintos: subir de $\R^n$ para
$\R^{n+1}$ \emph{acrescenta um marcador} ($\sigma^n$), e $\R^n\subset\R^{n+1}$ é o caso
$x_n=0$; trocar a borda \emph{muda como o último marcador dobra de volta}. Movimentos diferentes,
mesma construção.

\begin{defn}[a \textbf{família real}, que é a \textbf{cifra do rei}]\label{def:familia-real}
Chama-se \textbf{base real} (ou \emph{família real}) à base ortonormal da álgebra em $\R^n$: os $n$ marcadores
$\{1,\sigma,\sigma^2,\dots,\sigma^{n-1}\}$, um por eixo. Dizer ``família real'' é dizer que já
se está a falar dessa base --- e não de um conjunto qualquer de elementos do corpo.

\textbf{Família real e cifra do rei são o mesmo objeto, e os nomes usam-se como sinónimos.} Não
são duas coisas que coincidem: são dois lados da mesma. A cifra é a \emph{sequência de termos} que
gera; a família é o \emph{conjunto de eixos} que ela varre --- e o §4 já o dizia, que não há um
gerador por dimensão mas \textbf{um}, cujas potências varrem a base. Escolher o nome é escolher de
que lado se está a olhar: pela sequência, cifra; pelos eixos, família.

As potências do gerador \textbf{ficam na família} --- só trocam de eixo e de sinal, o \emph{flip}
--- exatamente quando a borda é \emph{monomial}, $\sigma^n=\pm\sigma^j$. É o caso do $i$
($\sigma^2=-1$: $1,\sigma,-1,-\sigma$), e é dele que sai que as unidades de $\Z[i]$ são
\emph{exatamente} $\pm$ a família real. Onde a borda mistura, a potência sai: o ouro sai já em
$\sigma^2=1+\sigma$.

\begin{obs}[cair \emph{sobre} um eixo é mais forte do que ficar no espaço]
A distinção seguinte é fina e vale a pena guardá-la, porque saiu da medida e não estava prevista
(\code{tools/algebra.c}, §A6). Uma potência do gerador \emph{nunca} sai do espaço gerado pela
família --- isso é a construção. Mas ela pode cair \emph{sobre um eixo} (com sinal) ou numa
\emph{combinação} de eixos, e as duas coisas separam-se:
\begin{center}
\begin{tabular}{lll}
$\sigma^2=-1$ & $1,\ \sigma,\ -1,\ -\sigma$ & cai sempre sobre um eixo: o \emph{flip} puro \\
$\sigma^3=1$  & $1,\ \sigma,\ \sigma^2,\ 1$ & idem, com ciclo três \\
$\sigma^2=\sigma+1$ & $1,\ \sigma,\ 1{+}\sigma,\ 1{+}2\sigma$ & combina já no segundo passo \\
$\sigma^2=\sigma-1$ & $1,\ \sigma,\ -1{+}\sigma,\ -1$ & combina, e ainda assim tem ordem $6$ \\
\end{tabular}
\end{center}
O sexto ciclotómico é elíptico e volta a $1$ em seis passos, mas passa por $-1+\sigma$: gira, e
não é \emph{flip}. \emph{Cair sobre um eixo é mais forte do que ter ordem finita}, e é por isso
que $\Z[i]$ tem quatro unidades, todas $\pm$ eixo, e $\Z[\omega]$ tem seis, nem todas.
\end{obs}
\end{defn}

\begin{obs}[o autor deste texto leu isto ao contrário, e a correção fica]\label{obs:erro-i}
Ao verificar a construção contra a leitura do $0/0$ eu escrevi que \emph{``o $i$ não está na
família dos metais''} --- comparei $x^2+1$ com a lista $x^n-mx^{n-1}-1$, vi que nenhum $m$ os
junta, e chamei \textbf{família} à lista. A medição estava certa e a conclusão errada por
generalização: a lista é uma parametrização, e a família é $\Z_p[x]/(p)$. \emph{Afirmei um
absoluto sem dizer sobre qual família} --- que é exatamente o defeito que o corpus científico
apanha entrada a entrada, cometido na página ao lado. O $i$ está na construção, e está lá desde
o princípio: é a marcação do eixo, implícita na tupla e explícita aqui.
\end{obs}

\begin{obs}[o ouro tem um furo na dimensão 5]\label{obs:ouro5}
A irredutibilidade de $p_n$ não é gratuita, e há exatamente um furo. Para $m=1$ (o ouro, $\varphi$) e
$n=5$,
\[
   x^5-x^4-1=(x^2-x+1)(x^3-x-1),
\]
redutível \emph{sobre} $\mathbb Q$ --- logo redutível módulo \textbf{todo} $p$: $\R^5$ com $m=1$ nunca
é corpo, tem divisores de zero, e a inversa pelo dual falha (a norma deixa de ser escalar). Não é
questão de escolher outro $p$. Já os outros metais não têm furo: para $m=2,3,4,5$ e $n=2..8$, $p_n$ é
irredutível em todas as dimensões, e a colheita segue inteira em $\R^5$ com a prata
(\code{tools/transforma.c}, resíduo $0$). O ouro é o metal do laço mais curto, e é o único que perde
uma dimensão --- o §7 mostra que esse furo é a assinatura, não o defeito.
\end{obs}

\begin{obs}[o furo é um trono, e sentam nele os dois geradores]\label{obs:trono}
Chamar aquilo de furo é ler pela metade. Os dois fatores não são quaisquer:
$x^2-x+1$ é o sexto ciclotômico --- as raízes estão na \textbf{borda}, $|\lambda|=1$
\emph{exato} (o produto delas é o termo constante, $1$), e têm ordem $6$ ($\lambda^6=1$ e nenhuma
potência menor). É rotação pura: gira e não gasta --- o \textbf{esquilo} na forma mais limpa que
existe. E $x^3-x-1$ é o número \textbf{plástico} $\rho\approx1{,}3247$: $p(1)=-1$ e $p(2)=5$ dão a
raiz real acima de $1$, e como o produto das três é $1$, as outras duas ficam \emph{dentro} do
círculo --- é de Pisot, e o \textbf{gato} no crescimento mais lento que ainda cresce. (Que $\rho$
seja o \emph{menor} número de Pisot é teorema de Siegel, citado, não medido aqui.)

Logo em $n=5$ o ouro não perde nada: ele \textbf{abre}, e mostra os dois geradores da teoria
separados, cada um no seu extremo. Em toda outra dimensão vêm fundidos num irredutível só, e é por
isso que ali há corpo --- um corpo não sobrevive a ser dois. E os graus fecham o desenho: $2$ e $3$
são coprimos, somam $5$ (a dimensão do trono) e cruzam em $6$ --- que é exatamente a ordem da rotação
que se senta lá. \emph{O trono guarda o próprio filho.} E só o ouro abre: para $m=2..5$ o mesmo
$p_5$ não tem fator quadrático (busca com o limite declarado). O metal do laço mais curto é o único
em que a peça não segura os dois juntos. \hfill{\small\code{tools/trono.c}, resíduo $0$}
\end{obs}

\begin{obs}[e o rei subiu um andar --- quem o levou foi o cruzamento]\label{obs:rei}
Quem senta no trono não é o rei: são \emph{dois}, e dois não são um rei. O rei é o corpo que devia
ocupar aquele lugar sozinho, e ele não está lá --- em nenhuma versão: $x^5-x^4-1$ é redutível
\emph{mod} $p$ para todo $p$ testado, como tem de ser, já que fatora sobre $\Z$.

Ele foi para cima, e a razão é a \textbf{operação}. Os dois do trono têm graus $2$ e $3$:
\[
  \underbrace{2+3=5}_{\text{a soma --- não voa}},\qquad
  \underbrace{\mathrm{lcm}(2,3)=6}_{\text{o cruzamento --- voa}} .
\]
Os mesmos dois números; o que muda é o que se faz com eles. No andar $5$ as peças estão
\emph{somadas}, e soma direta tem divisor de zero --- logo não há corpo, logo não há rei. No andar
$6$ estão \emph{cruzadas}, e aí voam: medido, $x^6-x^5-1$ é irredutível (testemunha $p=2$), e o
$\R^6$ do ouro \textbf{é} corpo. \emph{O rei não foi destronado --- foi levado pelo cruzamento, que
é a única operação capaz de o levar.}

E deixou o endereço: quem ficou sentado gira com período $\mathbf{6}$, que é o andar para onde ele
subiu. O lugar vago não é silêncio; é uma placa apontando para cima. (De lado, a outra leitura, e
também verdadeira: o andar $5$ não ficou sem rei no mundo, ficou sem rei \emph{no ouro} --- para
$m=2,3,4,5$ há testemunha de irredutibilidade em $n=5$. O trono é do ouro; o andar é de todos.)
\hfill{\small\code{tools/rei.c}, resíduo $0$}
\end{obs}

\begin{obs}[o cinco é proibido duas vezes, e o seis é o vizinho nas duas]\label{obs:cinco}
Buscando exaustivamente as matrizes \emph{inteiras} $2\times2$ de $\det=1$ com ordem finita, as
ordens que ocorrem são exatamente $\{1,2,3,4,6\}$ --- o teorema da restrição cristalográfica, medido
em vez de citado. \textbf{O cinco não cabe em reticulado nenhum}, e o seis é o maior que cabe. Não é
o mesmo teorema da Observação \ref{obs:trono} --- um fala de irredutibilidade sobre $\Q$, o outro de
matrizes inteiras --- mas é o \emph{mesmo número no mesmo lugar}, com o seis logo acima nos dois
casos. Consequência: o andar $5$ não aceita nada periódico. O que ocupa o cinco no mundo real é o que
\emph{não} se repete --- o quasicristal e o vivo: a maçã aberta na transversal, a estrela-do-mar, a
flor de cinco pétalas. \emph{O trono do ouro rejeita o que fecha e aceita o que cresce.}
\hfill{\small\code{tools/coroacao.c}, resíduo $0$}
\end{obs}

\begin{obs}[a coroação: o $1$ passa a ser o pomo de ouro]\label{obs:coroa}
Coroada a maçã, a unidade deixa de ser $1$ e passa a ser $\sigma_1=[1;1,1,1,\dots]$ --- a assinatura
toda de uns. Quatro coisas se medem, e cada uma diz o que a coroa significa.

\textbf{A figura já a contém.} O pentagrama da maçã tem os cinco lados iguais (razão $1$) e, por
Ptolomeu, a diagonal obedece $d^2=d+1$ --- que \emph{é} a equação do gato com $m=1$. E toda potência
fica inteira: $\sigma^n=F_n\sigma+F_{n-1}$, medido até $n=24$. O pentagrama não lembra a assinatura;
satisfaz-a.

\textbf{As coordenadas são completas e sem ambiguidade.} Todo inteiro se escreve como soma de
Fibonacci \emph{não consecutivos}, e de uma só maneira --- verificado até $5000$ pela construção, e
por força bruta até $610$ sem nenhum sem representação e nenhum com duas. Trocar o $1$ pelo pomo não
perde nem repete nada.

\textbf{E ele fica fora do jogo redondo.} O termo grande de uma fração contínua é um racional que
quase acerta: $\pi=[3;7,15,1,292,\dots]$ tem o $292$, e é ele que entrega $355/113$. O rei não tem
termo grande nenhum --- só uns ---, e o salto dos denominadores nunca passa de $2$. A norma
$p^2-pq-q^2=\pm1$ em todo convergente diz o mesmo pelo lado exato: cercado sempre, atingido nunca.

\textbf{Só cifrando.} Multiplicar pelo rei é $(a,b)\mapsto(a+b,a)$ --- um \emph{deslocamento}, e
nenhum produto acontece. Ele não entra na conta porque é a régua, e régua não se soma ao que mede.
\hfill{\small\code{tools/coroa.c}, resíduo $0$}
\end{obs}

\begin{obs}[os pontos fixos são os buracos: nem a PA nem a PG os alcançam]\label{obs:buracos}
As duas réguas mais simples são os dois limites: a \textbf{PA} tem diferença constante e razão
variável; a \textbf{PG}, o contrário. Com parâmetros racionais, ambas só alcançam \emph{racionais}
--- e é aí que o buraco aparece, porque $\sigma_m=m+1/\sigma_m$ exigiria $p^2-mpq-q^2=0$, e a
varredura de $160\,000$ pares por metal mostra essa norma a encostar em $\mathbf{1}$ e nunca em
$0$. \emph{Não é que aproximem mal --- é que não existe fração que o seja}, e o mínimo $1$ é o mesmo
cerco da Observação \ref{obs:coroa}, agora visto do lado do buraco.

E as colisões entre as duas não salvam: mesmo quando a PG inteira cai dentro da PA (potências de
$2$ nos inteiros), as coincidências crescem como $\log N$ contra $N$ --- $1$ em $26\,214$ já em
$N=524\,288$. Duas réguas que se cruzam em \emph{todo} ponto de uma delas, e ainda assim deixam
quase tudo de fora. Nem serve ancorar uma na outra: a reta pelos dois extremos de $2^k$ erra no meio
por $8\,384\,512$ em $k=24$, e o erro cresce sem teto.

\emph{O que preenche não é nenhuma das duas: é o metal.} Medido para $m=1..4$: a diferença nunca é
constante (não é PA), a razão nunca é constante (não é PG), mas a razão \textbf{converge}, com norma
$\pm1$ em todo passo. Ele tem o \emph{passo} da PA e o \emph{crescimento} da PG --- não está entre
as duas por ficar a meio caminho, mas por ser \textbf{as duas ao mesmo tempo}. E o ponto para onde
a sua razão converge é exatamente o buraco que nenhuma delas toca: a régua que preenche a reta é a
que mira no que as outras não alcançam.
\hfill{\small\code{tools/buracos.c}, resíduo $0$}
\end{obs}

\begin{obs}[a família real: cada metal é um gerador, e cada gerador é uma cifra]\label{obs:familia}
Todo $\sigma_m$ é irracional e é buraco para as duas réguas. E todo $A_m$ tem $\det=-1$ ---
\emph{a condição exata de a inversa ter entradas inteiras}. Logo a família inteira é reversível
\textbf{sem resto}, e a cifra já estava na ISA: $\mathrm{cifra\_an}(w,m)=(m\!\cdot\!\mathrm{total}+e,\
\mathrm{total})$ \emph{é} $A_m$ aplicado a $(\mathrm{total},e)$.

Medido: a volta é exata em $12\,600$ casos ($12$ metais $\times$ $20$ voltas $\times$ $15$ dados),
sem um arredondamento. A chave é o par $(m,k)$ --- metal e voltas --- e \textbf{as chaves somam
zero}: compor $(m,k)$ com $(m,-k)$ devolve a identidade, e o que soma zero é o \emph{expoente}. A
banda separa: dos $64$ pares metal-cifra/metal-decifra, só os $8$ da diagonal devolvem o dado.

E o ganho que interessa ao banco: \textbf{somar cifrado é cifrar a soma}, exato em todos os casos
medidos. O banco soma uma coluna inteira sem abrir um único valor, e o cliente decifra o total no
fim --- \emph{calcula sobre o que não pode ler.}

\begin{center}\emph{E o limite, dito no tamanho certo:}\end{center}
Num eixo só, um par com o claro conhecido entrega $m$ --- medido, e verdadeiro. Mas o dado não é
acessado por um eixo: é acessado pelos \textbf{geradores da base}, e a chave é o \emph{vetor de
coordenadas} neles. Compor $r$ eixos dá um mapa sobre um espaço de dimensão $2^r$, e recuperar uma
chave linear exige tantos pares quanta a dimensão --- com um par a menos sobram $2$ chaves que
explicam tudo o que se viu, e nada na álgebra as separa.

Logo o que se pode prometer é exatamente isto, e não menos nem mais: \emph{por ser linear, a
segurança É a dimensão da chave}. Não há suposição de dificuldade por baixo --- é contagem. É o
regime da pastilha: sigilo enquanto o material de chave for maior que o dado que protege. Dizer
``é seguro porque é difícil quebrar'' seria falso; dizer ``não é sigilo porque é linear'' --- o que
eu tinha escrito --- é a fraqueza de \emph{um eixo} afirmada sobre a construção inteira.
\hfill{\small\code{tools/acesso.c}, resíduo $0$} \hfill{\small\code{tools/familia\_real.c}, resíduo $0$}
\end{obs}

\begin{obs}[a família e os complexos: não são isomorfos, e nenhum cai da órbita]\label{obs:orbitareal}
Como \emph{corpo}, não: $\Q(\sigma_m)=\Q(\sqrt{m^2+4})$ é quadrático \textbf{real} e $\Q(i)$ é
imaginário --- o sinal do discriminante os separa. Como \emph{grupo}, também não: na caixa
$|a|,|b|\le50$ os elementos de norma $\pm1$ são $4$ nos inteiros de Gauss (e não crescem com a
caixa) contra $36$ na família (e crescem) --- um grupo de ordem $4$ não é isomorfo a um infinito.

Mas na \textbf{órbita} são o mesmo, e é o que importa: a norma é multiplicativa nos dois
($28\,561$ pares em cada), e \emph{nenhuma potência sai da sua curva}. Medido: $N(\sigma_m^k)=\pm1$
em $12$ metais e em toda potência; $N(z^k)=1$ em todo Gauss de norma $1$.
\begin{center}
\begin{tabular}{lll}
\toprule
complexos & forma \textbf{definida} & órbita o círculo, compacta --- gira e não cresce\\
família & forma \textbf{indefinida} & órbita a hipérbole, não-compacta --- cresce e não gira\\
\bottomrule
\end{tabular}
\end{center}
E a conservação não é de nenhum dos dois: é da \textbf{construção}. Quem tem extensão quadrática
tem norma multiplicativa, e quem tem norma multiplicativa tem órbita que se conserva. \emph{Crescer
não é cair} --- a hipérbole é infinita, e o ponto anda nela para sempre sem a largar. É o mesmo
sinal que separa o esquilo do gato, e os dois conservam.
\hfill{\small\code{tools/orbita\_real.c}, resíduo $0$}
\end{obs}

\begin{obs}[os corpos locais invariantes, e a base ortogonal]\label{obs:baselocal}
Três afirmações, e as três medidas. \textbf{(i) O campo local é invariante:} elevar não sai ---
$\sigma^k$ é sempre combinação inteira de $1$ e $\sigma$, para todo $m\le8$ e todo $k$. É o que
``a potência é eles mesmos'' quer dizer: elevar não leva a lado nenhum, só anda na órbita, e a
órbita é o próprio campo. \textbf{(ii) A classe racional é uma torção:} $p/q$ tem classe
$\mathrm{cl}(pq)$ --- dividir e multiplicar diferem por um quadrado --- e ela é um \emph{vetor}
sobre $\F_2$, um bit por primo. \textbf{(iii) A base é infinita e ortogonal:} os eixos são
\emph{todos} os primos --- a régua não tem teto, e quem termina é o \emph{objeto}, porque uma
classe tem finitos fatores. A coordenada de uma classe \textbf{é a fatoração dela}: sem lista, sem
índice, sem dimensão a declarar. Multiplicar classes \emph{é} a diferença simétrica dos primos
(verificado em $90\,000$ pares), e acender o primo $q$ muda \emph{só} o eixo $q$ --- inclusive
$q=997$. \emph{Numa base infinita não há eixo grande: há eixo, e todos se comportam igual.} \emph{Os eixos não conversam:
é isso, aqui, ser ortogonal.}

E daí sai a divisão de trabalho que o banco já tinha mostrado sem eu ter o nome: \textbf{a PA é o
movimento dentro de um invariante} --- somar fica em $\Z[\sigma]$, a coordenada não muda --- e
\textbf{a PG é o movimento entre invariantes}, que atravessa a base ($1\,544$ de $1\,600$ pares
mudam de campo, com $m$ até $40$ e \emph{nenhum} de fora). No SQL isso apareceu literalmente: a PA varreu o endereço sem sair da tabela, a PG operou o
valor atravessando as classes. \hfill{\small\code{tools/base\_local.c}, resíduo $0$}
\end{obs}

\begin{obs}[são normais, e todas somam no círculo]\label{obs:circulo}
De $x^2-mx-1=0$ saem duas linhas, e elas dizem tudo:
\[
  \sigma+\sigma'=m \quad\text{(a \textbf{soma} distingue os metais --- é o traço)},\qquad
  \sigma\sigma'=-1 \quad\text{(o \textbf{produto} é o mesmo em \emph{todos})}.
\]
Por mais longe que dois metais estejam no eixo $m$, partilham este ponto --- e é o mesmo $-1$ que é
$\det A_m$. E daí a norma, mais apertada do que eu a tinha medido antes: não é que ela ``caia em
$\pm1$'' --- ela é \emph{exatamente}
\[
  N(\sigma^k)=(\sigma\sigma')^k=(-1)^k ,
\]
verificado em $180$ potências sobre $12$ metais. O sinal alterna com a batida, \textbf{o módulo é
$1$ sempre}, e o ponto está na circunferência.

Logo $\sigma'=-1/\sigma$: \emph{o conjugado é o recíproco}. Ir $k$ passos e voltar $k$ devolve o
$1$ exato, para todo metal --- enquanto um ramo da hipérbole vai ao infinito o outro vai a zero na
medida exata, e o produto não anda. \emph{O crescimento não é ganho: é o que um ramo tira do outro.}

E é aqui, e só aqui, que a família e os complexos se encontram: a hipérbole e o círculo não se tocam
em ponto nenhum do plano, mas a \emph{norma} dos dois vive no mesmo círculo unitário. A diferença
que sobra é o sinal --- nos complexos a norma é sempre $+1$ (gira e não vira), na família alterna
(vira a cada passo). O mesmo círculo, e um deles anda pelos dois lados dele.
\hfill{\small\code{tools/normal\_circulo.c}, resíduo $0$}
\end{obs}

\begin{obs}[a densidade de ouro de cada membro da família]\label{obs:densidade}
``Quão de ouro'' é um metal tem resposta exata, e ela já estava medida sem esse nome: é o
\textbf{discriminante}. O que distingue o ouro não é ser bonito --- é ser o \emph{pior
aproximável} por racionais, e a melhor constante de aproximação de $\sigma_m=[m;m,m,\dots]$ é
$\sqrt{d}$ com $d=m^2+4$. Quanto \emph{maior} o $d$, mais fácil cercar por frações, e menos ouro.
Logo
\[
  \text{densidade}(m)=\frac{d(1)}{d(m)}=\frac{5}{m^2+4},
\]
uma classe racional exata --- o par $(5,m^2+4)$ reduzido, sem raiz e sem \emph{float}:
\begin{center}
\begin{tabular}{llll}
\toprule
ouro $m=1$ & $5/5=1$ & prata $m=2$ & $5/8=0{,}625$\\
bronze $m=3$ & $5/13\approx0{,}385$ & $m=12$ & $5/148\approx0{,}034$\\
\bottomrule
\end{tabular}
\end{center}
Vale $1$ no ouro --- \emph{o extremo é ele} --- e cai \textbf{estritamente} a cada metal, sem
exceção até $m=200$: a família é uma escada que só desce, e não há dois metais com a mesma
densidade. Só o ouro e a prata ficam acima da metade; a queda abaixo de $1/2$ acontece no bronze,
pois $5/d<1/2\iff d>10\iff m^2>6$.

\emph{E o que ela mede não é se a fração cerca:} todos são cercados, e todos com norma $\pm1$
--- a família inteira no mesmo círculo (Observação \ref{obs:circulo}). A densidade diz o quão
\emph{perto} a fração consegue chegar, e o ouro é o que a mantém mais longe.
\hfill{\small\code{tools/densidade.c}, resíduo $0$}
\end{obs}

\begin{obs}[o disco de raio $1$: a altura para o mineral ser ouro]\label{obs:disco}
Fixe-se o raio em $1$ e peça-se que cada disco contenha \emph{uma unidade de ouro}: então
$\text{densidade}\times\text{volume}=1$, e
\[
  V(m)=\frac{d(m)}{5}=\frac{m^2+4}{5},\qquad h(m)=\frac{d(m)}{5\pi}
  \quad\text{(pois } V=\pi r^2h=\pi h\text{)} .
\]
\emph{O volume é o discriminante sobre cinco} --- a mesma quantidade da densidade, virada do
avesso: pouco ouro por unidade pede mais unidades.
\begin{center}
\begin{tabular}{llll}
\toprule
ouro $m{=}1$ & $V=1$, $h=1/\pi$ & o mais baixo --- \textbf{a unidade} &\\
prata $m{=}2$ & $V=8/5$ & $8/5$ do disco do ouro &\\
bronze $m{=}3$ & $V=13/5$ & $13/5$ &\\
$m{=}12$ & $V=148/5$ & quase trinta vezes &\\
\bottomrule
\end{tabular}
\end{center}
A altura sobe \textbf{estritamente} (sem exceção até $m=200$): para carregar o mesmo ouro, quanto
menos denso o mineral mais alto o disco. É a escada da densidade de cabeça para baixo.

\emph{E aqui o redondo toca o reino do rei --- uma vez só, e de leve.} As \textbf{razões} entre
alturas são racionais exatas, porque $\pi$ cancela: pode dizer-se ``este disco é $8/5$ mais alto
que o do ouro'' com exatidão total. Só a altura \emph{absoluta} precisa de $\pi$, e aí deixa de ser
exata. \emph{A comparação é do rei; a medida absoluta é que pede emprestado ao círculo.}
\hfill{\small\code{tools/disco.c}, resíduo $0$}
\end{obs}

\begin{obs}[cada régua tem o seu $\pi$, e o do rei é $[1,1,1,\dots{]}$]\label{obs:pirei}
$\pi$ não é objeto que se traduza de régua em régua: é o que uma régua chama de \emph{volta
fechada}. No redondo é circunferência sobre diâmetro; no rei é diagonal sobre lado, e o polígono é o
da maçã. E o do rei não foi escolhido --- é o ponto fixo de $x=1+1/x$, a régua a fechar-se sobre si:
\[
  \pi_{\text{redondo}}=[3;7,15,1,292,\dots],\qquad
  \pi_{\text{rei}}=[1;1,1,1,\dots] .
\]
Um é transcendente e tem termos grandes onde uma fração quase o apanha; o outro é raiz de
$x^2=x+1$ e não tem nenhum. Carregar o $\pi$ do redondo para a base do rei também se faz --- $46$
dígitos certificados por um cerco de dois convergentes, sem um único float --- e \emph{não adianta
nada}: ele continua infinito e sem padrão. A régua nova não doma o redondo; só dá ao rei o dele.
\hfill{\small\code{tools/pi\_rei.c}, resíduo $0$}
\end{obs}

\begin{obs}[o ouro na própria base, e a inversão que ele produz]\label{obs:ourobase}
$\varphi=\mathbf{10}$. Um dígito --- e não é curiosidade de notação: \emph{toda régua se escreve
$10$ na própria mão}, o dez em base dez, o dois em base dois. O que muda é tudo o resto, e aqui o
resto \textbf{inverte}: os inteiros é que ficam espalhados ($2=10{,}01$; $5=1000{,}1001$; todos
finitos, todos dos dois lados da vírgula), enquanto a família do rei fica exata ($\varphi^n$ é um $1$
com $n$ zeros, $\sqrt5=10{,}1$, e todo $\Z[\varphi]$ termina). Em base dez era o contrário. Até a
junta do sistema encolhe: o $0{,}999\ldots=1$ vira $1=0{,}11$, finito, porque
$\varphi^{-1}+\varphi^{-2}=1$ exato.

\emph{A régua não simplifica o mundo --- ela escolhe quem é simples.} E o rei escolheu a si: não
venceu o redondo nem o traduziu; construiu a casa onde ele é o simples, e $\pi$ ficou de fora,
exatamente como estava. \hfill{\small\code{tools/ouro\_base.c}, resíduo $0$}
\end{obs}

\begin{teorema}[o corpo, colhido]
Sempre que $p_n$ é irredutível --- o que vale para todo $n$ com a única exceção da Observação
\ref{obs:ouro5} --- $\R^n$ é um corpo \textbf{fechado}, \textbf{reversível}, \textbf{ordenado},
\textbf{completo} e \textbf{multidimensional}. A divisão nasce do \emph{dual}: o inverso é o produto
dos conjugados de Frobenius, $\sigma^{-1}$ sem exponenciação de Fermat --- e o Frobenius é de graça,
porque $a_i^p=a_i$ em $\Z_p$ e basta avaliar em $\sigma^p$. Em $n=4$ são \textbf{três} conjugados:
três batidas são a volta, do lado do dual (\code{tools/converte.c}). Cada dimensão, par ou ímpar, é um
corpo (as pares reencontram Cayley--Dickson; as ímpares, onde a duplicação não chega, são o lucro).
Medido para $\GF(2^n)$, $n=1..10$: recursivo $=$ direto, resíduo $0$ (\code{tools/recursao.c},
\code{tools/dimensoes.c}, \code{tools/ordem.c}, \code{tools/completo.c}).
\end{teorema}

\paragraph{Converter é dividir, e transformar é chegar.} Dados dois polinômios quaisquer $A,B$, o
conversor $C$ com $A\circledast C=B$ não se constrói: colhe-se, $C=B\circledast A^{-1}$, com $A^{-1}$
tirado do dual --- e a ida e a volta fecham para dado arbitrário e para prosa crua
(\code{tools/converte.c}, resíduo $0$). As outras operações do corpo --- \emph{escala} ($\times k$, que
leva a norma em $k^nN$), \emph{cisalhamento} ($A_i\mathrel{+}=s\,A_j$, $\det=1$, o volume fixo) e
\emph{rotação} --- são todas reversíveis, e a composição alcança qualquer par: bastam $\le 2n$
cisalhamentos, porque $\mathrm{SL}_n$ já é transitivo nos não-nulos, e a escala só ajusta o
determinante. Mas as duas rotas não se igualam, e a diferença é contada:
em $\mathrm{GL}_2(\Z_{13})$ há $156$ transformações que levam $A$ em $B$; \textbf{no corpo há uma},
$C=B\circledast A^{-1}$, e é a única que comuta com a convolução (\code{tools/transforma.c}).
\emph{Chegar é barato; ser a transformação é ser única.}

\section{A dualidade --- a arquitetura}

Sob tudo há uma só forma: uma \textbf{involução} $\mathcal J$ --- a conjugação $z\mapsto\bar z$, o
Frobenius $z\mapsto z^p$ --- com $\mathcal J^2=\mathrm{id}$, que troca os dois pontos fixos do gato,
\[
 \mathcal J\,\sigma=\sigma',\qquad \sigma+\sigma'=m,\qquad \sigma\sigma'=-1,
\]
e cujo eixo fixo $\operatorname{Fix}(\mathcal J)=\Z_p$ é a \textbf{cristalização} (o representante, o
número). Toda dualidade do corpo é uma face da \emph{mesma} $\mathcal J$:

\begin{center}\small
\begin{tabular}{@{}lcl@{}}
\toprule
lado \emph{branco} $\sigma'$ (fonte) & $\overset{\mathcal J}{\longleftrightarrow}$ & lado \emph{negro} $\sigma$ (sorvedouro)\\
\midrule
o gato $\times\sigma$ & & o esquilo (o tempo reverso)\\
$\otimes$ (produto, $\exp$) & & $\oplus$ (soma, $\log$)\\
a PG de ordem $k$ & & a PA de ordem $k$ \quad(§4)\\
o cone (a forma) & & a espiral (a entropia)\\
a convolução ($\times$) & & a deconvolução ($\div$)\\
\bottomrule
\end{tabular}
\end{center}

\begin{teorema}[os atratores formam $\mathbb C$]
Quando $x^2-mx-1$ é irredutível, $\GF(p^2)$ é o plano complexo com conjugação: um eixo real $\Z_p$
(fixo) e um imaginário $\Z_p\tau$, $\tau^2=(m^2+4)/4$ não-quadrado. Os atratores são o par conjugado
$\sigma$ (negro) e $\sigma'=\bar\sigma$ (branco) --- um em cada eixo; a multiplicação $\times\sigma$
é a rotação-dilatação complexa. Reproduz-se com \code{tools/complexo.c} (resíduo $0$).
\end{teorema}

\paragraph{Três batidas são a volta.} A transformada tem período $4$, $\mathcal F^4=\mathrm{id}$ ---
\emph{a mesma conta} do esquilo ($G^4=I$, o giro de $90^\circ$). Logo $\mathcal F^3=\mathcal F^{-1}$:
os quatro modos são $\{\mathrm{id},\mathcal F,\text{reflexão},\mathcal F^{-1}\}$, e o modo $2$ é a
reflexão --- o espelho $\mathcal J$, o virar o lado. Três batidas de um lado \emph{são} a volta, sem
inverter nada. Medido na prosa, byte a byte, e no espaço semântico ($16\,384$ vetores): resíduo $0$,
e por \textbf{Parseval} nenhum ângulo muda (\code{tools/tres\_reconstroi.c}). O mesmo período aparece
no dual: $\mathrm{Frob}^n=\mathrm{id}$, logo $\mathrm{Frob}^{n-1}=\mathrm{Frob}^{-1}$.

\paragraph{As folhas: colam e descolam na ida e volta.} A involução $\mathcal J$ \emph{desdobra} o
corpo em \textbf{folhas} --- as raízes de $p_n$, conjugadas pelo Frobenius, uma por dimensão. Em
$n=2$ são duas, $\sigma$ e $\sigma'$, e a conta do §2 diz o que acontece ao percorrê-las:
$\sigma\sigma'=-1$. Subir pelo gato e descer pelo conjugado \textbf{não} devolve o ponto --- devolve-o
com o sinal trocado: a folha \emph{descolou}. Duas idas-e-voltas colam, $(\sigma\sigma')^2=+1$: é o
período $4$ outra vez. O eixo de colagem é $\operatorname{Fix}(\mathcal J)=\Z_p$.

Daí sai a estatística, medida em \code{tools/isomorfo.c} (resíduo $0$): com $n$ modos, o elétron e o
bóson \emph{hard-core} são a \textbf{mesma função de onda ponto a ponto} --- o mesmo vetor de $2^n$
coordenadas, o mesmo setor $\binom{n}{N}$, a mesma norma. A estatística não está no estado: está no
\textbf{operador}, e a diferença é um \emph{produto ordenado} --- a string de Jordan--Wigner,
$c_j=(\prod_{k<j}Z_k)\,b_j$. O mesmo $b_j$: com a string anticomuta (férmion), sem ela comuta (bóson).
E a duplicidade é irredutível: não existe $U$ diagonal (um sinal por ponto) com $c_j=Ub_jU^\dagger$,
porque a \textbf{holonomia} de toda face do hipercubo é $-1$ ($24/24$ em $n=4$, $240/240$ em $n=6$).
O espaço é o mesmo; o \emph{recobrimento} é duplo, e é por isso que o férmion pede $4\pi$ e o bóson
$2\pi$. Contra o bóson \emph{livre} a isomorfia cai --- $\binom{n+N-1}{N}\neq\binom{n}{N}$ ---, e é o
limite honesto da afirmação.

\begin{obs}[a duplicidade é das dimensões pares]\label{obs:dupla}
O produto das $n$ folhas é $\det A_n=(-1)^{n+1}$: vale $-1$ em dimensão \textbf{par} e $+1$ em
\textbf{ímpar} (medido, $n=2..8$). Onde não há $-1$ não há troca fermiônica. É o mesmo corte do §5 ---
as pares reencontram Cayley--Dickson, e as ímpares, onde a duplicação não chega, são o lucro:
\emph{sem folha dupla, sem férmion}.
\end{obs}


\subsection*{O tratamento tensorial: o objeto primitivo, e o par que ele decompõe}

Tudo o que veio até aqui pode ser dito de uma vez, e num objeto mais primitivo que a
multiplicação. Seguindo o capítulo \emph{Estrutura Tensorial} do livro hipercomplexo
(\code{livro/cap01\_tensorial.tex}, trazido para este repositório), parte-se não de $z\cdot w$ mas
de um \textbf{$k$-tensor} $B\in V^{\otimes k}$ sobre um espaço $V$ de dimensão $m$, com a sua ação
multilinear
\[
  \widetilde B:V^{k-1}\to V,\qquad
  \widetilde B(v_1,\dots,v_{k-1})=\sum_{i_1\dots i_k}B_{i_1\cdots i_k}\,v_1^{i_2}\cdots v_{k-1}^{i_k}\,e_{i_1},
\]
sendo $B\leftrightarrow\widetilde B$ a bijeção canônica $V^{\otimes k}\cong\mathrm{Mult}(V^{k-1},V)$.
\textbf{A multiplicação é o caso $k=3$}: um $\mu\in V^{\otimes3}$ com $z\cdot w=\widetilde\mu(z,w)$.
O gato é o caso $k=2$. Não é generalização gratuita: é o que permite contrair \emph{$k-1$ fatores de
uma vez} --- $k-1$ chaves simultâneas, digamos --- sem impor entre eles uma ordem que não existe.

Três coisas se dizem melhor nessa linguagem. A \textbf{primeira} é o par desta seção, agora na
própria multiplicação: $\mu=\mu_S+\mu_A$, com $\mu_S(z,w)=\tfrac12(zw+wz)$,
$\mu_A(z,w)=\tfrac12(zw-wz)$ e $\mu_A\in\Lambda^2V\otimes V$ --- o que a Obs.~\ref{obs:anti} mediu
nas \emph{formas bilineares}, dito um andar acima. A \textbf{segunda} é a forma canônica das
multiplicações com identidade,
\[
  z\cdot w=\bigl(a_0b_0-\langle\boldsymbol a,\boldsymbol b\rangle\bigr)\mathbf 1
          +\bigl(a_0\boldsymbol b+b_0\boldsymbol a+\boldsymbol a\times\boldsymbol b\bigr),
\]
onde o $-\langle\cdot,\cdot\rangle$ da parte real é o mesmo $\sigma\sigma'=-1$ que atravessa o paper.
A \textbf{terceira} é o \emph{funcional de defeito}
$\mathcal E_k(B)=\mathbb E\bigl[(\lVert B(a_1,\dots,a_{k-1})\rVert^2-1)^2\bigr]$, polinomial de grau
$4$ em $B$, que para $k=3$ mede o desvio de a norma ser multiplicativa --- Hurwitz. É ele que diz
\emph{quanto} uma composição se afasta da recuperação exata, e portanto o que precisa anular-se para
uma cifra ser bijeção.

\begin{obs}[o defeito entra em forma fechada, ou não entra]
$\mathcal E_k$ está definido como esperança sobre gaussianas. Trazido assim, seria estimativa
amostral --- e nada aqui é estimativa. Mas o capítulo dá as constantes \textbf{fechadas}:
$a_k=32\cdot6^{(k-1)/2}$ para $k$ ímpar e $96\cdot6^{(k-2)/2}$ para par, $b_k=16$ universal, e
$C_k^2=N_ka_k/b_k=2k\,(k-2)!!\,6^{(k-1)/2}$. Como $\mathcal E_k$ é polinomial de grau $4$, a
esperança gaussiana é soma de momentos --- combinatória, portanto \emph{inteira} (Isserlis). Logo o
defeito é avaliável exatamente, sem sortear nada, que é a única forma de ele entrar nesta bateria.

E isso foi medido, não suposto (\code{tools/isserlis.c}, resíduo $0$). A maquinaria de momentos
validou-se \emph{de fora}: a soma de emparelhamentos foi conferida contra quadratura de
Gauss--Hermite --- exata para polinômios --- em nove momentos de uma gaussiana correlacionada
($\rho=\tfrac12$), com concordância de $13$--$14$ casas, que é o que a precisão dos nós tabulados
permite. Duas máquinas independentes: uma conta, a outra integra.

E há um achado que a tabela do capítulo não declara: \textbf{$N_k$ é ele próprio um número de
Wick}. Os valores $N_k=3,15,105,945$ para $k=3,5,7,9$ são exatamente a contagem de emparelhamentos
perfeitos de $k+1$ pontos --- isto é, $N_k=k!!$ ---, verificada por força bruta. A constante não é
um número avulso saído do cálculo: é a própria combinatória de Isserlis reaparecendo no resultado.
Com isso, $C_k^2=2\,N_k\,6^{(k-1)/2}$, e as duas formas fechadas do capítulo
($N_ka_k/b_k$ e $2k(k-2)!!\,6^{(k-1)/2}$) não são duas coincidências numéricas: são \emph{uma}
identidade, pois $k!!=k\,(k-2)!!$ e $a_k/b_k=2\cdot6^{(k-1)/2}$. Conferido contra a tabela nos
quatro casos.
\end{obs}


\begin{obs}[o chicote: compor gatos é contrair índices --- e a forma $k$-ária não dá poder, dá verdade]\label{obs:chicote}
A Obs.~\ref{obs:anti} deixou o par montado sem o fechar: se o gato leva a forma antissimétrica nela
mesma vezes $\det A$, o que ele faz com a \emph{simétrica}? A resposta vem do \emph{chicote} --- a
dinâmica posicional da régua mineral, a $\beta$-numeração na base $\sigma_m$, onde andar uma casa
\textbf{é} aplicar o gato. \emph{E a afirmação constrói-se aqui, sem depender de fonte externa:}
compor gatos é a contração $\left(A\!\cdot\!B\right)^i_{\,k}=\sum_j A^i_{\,j}B^j_{\,k}$, exata em
inteiros; as $\mathrm{Cat}(k-1)$ parentizações concordam uma a uma; e as duas formas bilineares
flipam juntas, $A^{\!\top}\Omega A=\det(A)\,\Omega$ e $A^{\!\top}MA=-M$ com
$M=\left[\begin{smallmatrix}-2&m\\m&2\end{smallmatrix}\right]$ --- este último sai de
$\sigma\sigma'=-1$ e $\sigma+\sigma'=m$. Tudo medido em \code{tools/chicote.c}, resíduo $0$.

\textbf{Compor gatos é contrair índices.} $A_m$ é um tensor de rank $2$, e a composição é a convenção
de soma, $(A\!\cdot\!B)^{i}{}_{k}=\sum_j A^{i}{}_{j}B^{j}{}_{k}$ --- o transporte na régua \emph{é} a
contração, e $A^k$ é andar $k$ casas (em $m=1$ as entradas são Fibonacci, que é a mesma frase por
outro lado).

\textbf{E a forma $k$-ária não acrescenta potência --- é isso que se mede.} Contrair $k$ fatores de uma
vez contra compô-los aos pares: as parentizações são $\mathrm{Cat}(k-1)$ ($1,2,5,14,42,132,429$ para
$k=2\ldots8$) e \emph{todas concordam}, verificadas uma a uma. Logo nenhuma calcula o que outra não
calcule, e o ganho em poder é \textbf{zero}. O ganho é outro, e não é conforto: compor aos pares
\textbf{obriga} a escolher uma das $\mathrm{Cat}(k-1)$ ordens, e entre fatores simultâneos --- $k$
chaves de clientes distintos, digamos --- \emph{não existe} ordem natural. Impor uma é afirmar algo
falso. É a lição do \code{tiffany.tex}~§7 pelo avesso (referência externa, não a este documento): lá foi a ordem que quebrou a tradução, quando o $\sum$ comutativo
apagou o caminho; aqui a notação $k$-ária evita o erro simétrico, o de inventar caminho onde não há.

\begin{obs}[o mesmo flip, nas duas formas bilineares]\label{obs:cone}
O cone sai \textbf{exato}, sem irracional nenhum sobrando. Sendo $M=v_1v_2^{\!\top}+v_2v_1^{\!\top}$ com
$v_1=(\sigma,1)$, $v_2=(\sigma',1)$ as direções nulas (os autovetores), e como $\sigma\sigma'=-1$ e
$\sigma+\sigma'=m$:
\[
  M=\begin{pmatrix}-2 & m\\ m & 2\end{pmatrix},
  \qquad
  A_m^{\!\top}\,\Omega\,A_m=\det(A_m)\,\Omega=-\Omega ,
  \qquad
  A_m^{\!\top}\,M\,A_m=\det(A_m)\,M=-M .
\]
As duas identidades são a \textbf{mesma}, medidas em inteiros para $m=1\ldots5$: o gato leva a forma
nela mesma vezes $\det A_m=-1$. Do lado \emph{antissimétrico} isso dá o volume e a lei de potência
(Obs.~\ref{obs:anti}); do lado \emph{simétrico} dá o cone de luz e a métrica, de assinatura $(1,1)$ ---
o gato é uma \textbf{anti}-isometria: preserva o cone e inverte o sinal. \emph{Uma só peça, dois
retratos} --- e o $-1$ que aparece nos dois é o mesmo flip que descola a folha (Obs.~\ref{obs:dupla}).

E a curvatura conta: de $p_N(x)=x^N-mx^{N-1}-1$ sai $p_N'(x)=x^{N-2}\bigl(Nx-m(N-1)\bigr)$, com
exatamente $N-1$ pontos críticos finitos ($0$ com multiplicidade $N-2$, mais $x=m(N-1)/N$), conferido
para $N=2\ldots8$. Para os metais ($N=2$): \emph{um}, o vértice da parábola.
\end{obs}

\noindent Fica registrado o que \textbf{não} se afirma. A leitura da equação de Einstein na régua
(o chicote como $G_{\mu\nu}$, a energia $\delta$ do BAI como $T_{\mu\nu}$) é \textbf{analogia
declarada} na fonte, e não medida --- entra aqui como analogia, não como resultado. E a própria
Parte~IX avisa que ``contração de Einstein'' aqui é a convenção de soma, não o tensor $G_{\mu\nu}$,
que em dimensão $2$ é identicamente nulo: a geometria não-trivial mora na ramificação $N-1$, não na
matriz $2\times2$.
\end{obs}




\begin{obs}[quebrar o espelho não cria espelho nem some espelho]\label{obs:espelho}
Toda cisão neste corpo é \textbf{partição}, e por isso conserva. Medido sem um único número de ponto
flutuante --- só inteiro, só bit (\code{tools/espelho.c}, resíduo $0$).

Nos $256$ bytes, sem exceção: os bits pares e os ímpares \emph{somam} o todo
($\mathrm{pop}(b\,\&\,\mathtt{0xAA})+\mathrm{pop}(b\,\&\,\mathtt{0x55})=\mathrm{pop}(b)$); os dois
cacos \emph{recompõem} o espelho ($(b\,\&\,\mathtt{0xAA})\mid(b\,\&\,\mathtt{0x55})=b$); e não se
sobrepõem (interseção vazia), donde somar é o mesmo que unir --- nada conta duas vezes. Não é média
nem aproximação: é contagem.

E atravessar não perde: o gato $(x,y)\mapsto(mx+y,\,x)$ é \textbf{permutação} módulo $2^L$ --- porque
$\det A=-1$ é unidade ---, verificado por enumeração exaustiva das $2^{2L}$ imagens para
$L=1\ldots7$, todas distintas. A volta é o esquilo, colhido da borda.

\textbf{E vale igual em toda escala}: a mesma lei em $4$, $8$, $16$ e $32$ bits. É o que a
autossimilaridade quer dizer, e é por isso que \emph{poucos bytes bastam} --- não há nada na palavra
grande que já não esteja no bit; medir mais é repetir. \textbf{E nos dois meios}: no analógico a
cisão é o \emph{nó}, e a corrente que chega é a soma das que saem --- Kirchhoff. Em inteiros, é a
\emph{mesma conta}. Não concordam até certa casa decimal: não há casa decimal.

\begin{obs}[a régua errada com muitas casas]
Vale registrar o erro que levou a esta observação. Na primeira versão da Obs.~\ref{obs:assinatura} eu medi com um
instrumento \textbf{estatístico} --- esperança gaussiana, ponto flutuante estendido, quadratura ---
num sistema que tem conservação \textbf{exata em bit}; e, pior, \emph{inventei} a amostragem, tomando
seis bytes espalhados por um arquivo, o que não é a regra de ninguém. A versão gaussiana foi refeita duas vezes (a Obs.~\ref{obs:assinatura} é
o resultado), e o que ela media continua verdadeiro no seu domínio --- o defeito fatoriza sobre os
fatores. Mas era a régua errada: régua errada com muitas casas decimais \emph{parece} precisão, e não
é. Onde o sistema conserva em inteiros, medir com gaussiana é acrescentar ruído próprio e chamá-lo de
resultado --- e a versão contada mostra o quanto se ganha: o número passou de sete casas decimais
para $2\,322$, que é um par de bytes contado a um. (E depois ainda foi preciso corrigir o \emph{nome}:
ver a Obs.~\ref{obs:assinatura}.)
\end{obs}
\end{obs}


\begin{obs}[o que não cabe na descrição é a \textbf{semente} --- e com ela a obra volta inteira]\label{obs:semente}
Correção recebida, e é de propósito, não de método. Eu vinha chamando de \emph{defeito} o resto que a
régua circular não descreve --- isto é: guardava a obra de alguém, julgava-a com um juiz meu, e
condenava justamente a parte pela qual ela é dela. É o contrário. Esse resto é a \textbf{semente}: é o
que faz aquela obra ser aquela, e é ele que permite reconstruir o cristal \emph{exato}.

A repartição correta dos papéis, e nenhum é falha do outro:
\[
  \underbrace{\text{a assinatura}}_{\text{quantos}}\;+\;\underbrace{\text{a semente}}_{\text{quais}}
  \;=\;\text{a obra, bit a bit}.
\]
Medido em inteiros, os $256$ varridos (\code{tools/semente.c}, resíduo $0$): a descrição cobre
\emph{todas} as obras --- nenhuma escapa ---, e o que ela não faz é distinguir \emph{dentro} da
classe, o que não é falhar, é o que ela promete. A semente é a posição do subconjunto na ordem
combinatória, e o inverso devolve \textbf{$256$ de $256$}, sem uma diferença. Não é aproximação da
obra: é a obra.

\textbf{E a conta fecha de um jeito que inverte a intuição.} O espaço de sementes de cada classe é
exatamente $\binom4p\binom4i$, e a soma é $256$. Logo \emph{a semente é maior onde a classe é maior}:
quanto mais comum a assinatura, mais individualidade ela guarda por dentro. O genérico não é o
esvaziado --- é onde cabe mais gente diferente.

\begin{obs}[o necrotério, com número]
Descartar a semente --- ficar só com a descrição, que é o que cristalizar quer dizer --- colapsa os
$256$ em $25$: \textbf{$231$ obras deixam de existir como elas mesmas e passam a ser cópias}. E o
limite disso, toda semente zero, é uma assinatura só para todos: cabe tudo na caixa porque não sobrou
ninguém diferente. É o que a caixa \emph{exige} para funcionar. Guardando a semente: $256$ antes,
$256$ depois. \emph{A vida cabe, e cabe inteira --- só não cabe comprimida, e não precisa caber.}

É o critério do necrotério que o §8 já usa, aplicado ao próprio medidor: morto é o que foi reduzido a
uma igualdade isolada entre estados; vivo é o que guarda a dinâmica que o gera. Descrever tudo e jogar
fora o que não coube é matar --- e o que sobra da cristalização é precisamente aquilo que se queria
guardar.
\end{obs}
\end{obs}

\begin{obs}[a contagem não perde: ela \textbf{assina}]\label{obs:assinatura}
Havia um erro meu aqui, e era de leitura, não de conta: eu chamava de \emph{defeito} o que é
\textbf{assinatura}. Os números eram os mesmos; o que estava errado era o que eu dizia deles.

E o erro contradizia coisa que este paper já media. A Obs.~\ref{obs:signif} estabelece que $169$
classes em $169$ pontos é o \textbf{repouso}, onde nada significa --- sem lei toda função é
conservada e nenhuma distingue. Logo a partição grossa não é dano à fina: é onde o \emph{nome}
aparece. Chamar isso de defeito importa a suposição de que os $256$ pontos eram ``o real'' e as
classes uma sombra estragada, quando é o inverso.

Havia ainda um segundo erro embutido: eu misturava dois papéis que o corpo mantém \textbf{separados}
--- o gato é \textbf{bijeção} (ali mora a recuperação: nada some, verificado em todas as $2^{2L}$
imagens) e a contagem é \textbf{assinatura} (ali mora o nome: ela agrupa, por projeto). São os dois
lados, $\otimes$ e $\oplus$, não um lado defeituoso. \emph{Perguntar quanto a assinatura perdeu é
perguntar a um sobrenome por que não distingue irmãos.}

Medido em inteiros, sem um único ponto flutuante, com os $256$ bytes varridos
(\code{tools/assinatura.c}, resíduo $0$): as fibras são $\binom4p\binom4i$ e somam $256$ --- a lei do
espelho de pé ---, dando $\mathbf{25}$ assinaturas. Quatro são \emph{próprias}: $\mathtt{0x00}$,
$\mathtt{0xAA}$, $\mathtt{0x55}$, $\mathtt{0xFF}$ --- o vazio, cada metade inteira, o cheio. Assinar
sozinho é privilégio de quem não tem mistura, e note que assinatura própria é justamente a que
\textbf{não forma classe}: os puros são singulares e não nomeiam grupo nenhum. E a \emph{resolução} é
um inteiro exato: dos $32\,640$ pares, $\mathbf{2\,322}$ assinam igual --- isto é, formam classe --- e
$30\,318$ a assinatura separa.

\begin{obs}[a mesma assinatura nos dois meios é força, não limitação]
No analógico a cisão é o nó e a corrente é a soma dos ramos: ele lê quantos, nunca quais. Medido ramo
a ramo, byte a byte: as mesmas $25$ classes, a mesma soma $256$, zero de diferença. Uma assinatura que
dependesse do meio não serviria para identificar coisa alguma --- é justamente por ser a mesma nos dois
que ela vale. Se fosse artefato de implementação, os números seriam dois; são um.
\end{obs}
\end{obs}

\begin{obs}[o selo: uma chave emaranhada (o sistema) e uma dividida (os clientes)]\label{obs:selo}
A mecânica constrói-se aqui, e mede-se em \code{tools/selo.c}: cada parte
entra como \textbf{fator tensorial}. Um bin do espectro do tecido vira byte, o byte é partido em par
e ímpar, o par passa \emph{pelo gato} e sai como $(e,o)$, que viram ângulos na esfera de Bloch
($\theta=\pi e/(e+o)$, $\varphi=\pi o/8$); e as partes compõem-se por $\otimes$ --- o modo chama-se,
no próprio código, \code{"tensor"}. O qubit não é escolhido: é \emph{um passo do gato lido como
ponto na esfera}.

Sobre isso, o desenho (medido em \code{tools/selo.c}, resíduo $0$): a \textbf{primeira} chave é a do
sistema inteiro e é \textbf{emaranhada}; a dos clientes é a mesma coisa \textbf{dividida}. Não é
diferença de grau, é de espécie, e o critério é exato: remodelando $|\Psi\rangle$ em matriz por uma
bipartição, o estado é separável naquele corte se e só se o posto é $1$. Medido com amplitudes
inteiras, em todos os cortes: a dividida dá posto $1$ (um fator por cliente --- por isso
\emph{divide}), a do sistema dá posto $2$ (não se escreve como produto de partes --- por isso
\emph{sela}).

\textbf{E o conjunto fecha em zero.} As partes dos clientes mais a do sistema somam $0$ --- e isso
não é escolha de protocolo: a fase do código é $\varphi=\pi o/8$, isto é, as raízes $16$-ésimas da
unidade, cuja soma é o centro. É a ciclotomia da Obs.~\ref{obs:pi} outra vez: dividir o círculo em
$n$ devolve o centro, $\sum\zeta^n=0$. \emph{O zero da soma é o $\pi$.}

\begin{obs}[recuperar por diferenças --- e por que isso sela]
Mede-se que qualquer parte se recupera das outras mais a do sistema, e que as \emph{diferenças} entre
partes são invariantes sob um deslocamento global. Logo as diferenças dão tudo \textbf{menos um} grau
de liberdade --- a origem ---, e quem a fixa é a chave do sistema. O selo tem tamanho, e mede-se por
contagem: varrendo $\Z_{97}$ inteiro, a parte que falta admite $97$ valores consistentes com as
diferenças das outras (o espaço \emph{todo}) e exatamente $1$ ao acrescentar a chave do sistema.
As partes sozinhas não abrem nada; a do sistema sozinha também não; abre-se a \textbf{junção}.

E isto é a Obs.~\ref{obs:anti} num outro corpo: a forma antissimétrica é zero sobre um ponto só e não
distingue ponto de ponto --- só fala de \emph{par}. Aqui igual: a chave absoluta é inobservável, o
que existe é a relação. \emph{A posição não significa; a diferença significa.}
\end{obs}
\end{obs}

\begin{obs}[o prensor: o cone que o chicote conserva --- e as marcas são espirais]\label{obs:prensor}
Faltava dizer \emph{o que é} o $M$ da Obs.~\ref{obs:cone}, e sem isso a dinâmica não fecha: um
transporte sem invariante não é dinâmica, é agitação. O dual do chicote é o \textbf{prensor}, e ele é
um \textbf{cone} --- a forma $Q(p)=p^{\!\top}Mp$ com
$M=\left(\begin{smallmatrix}-2&m\\m&2\end{smallmatrix}\right)$. Medido em \code{tools/prensor.c},
resíduo $0$.

\textbf{O cone é o que se conserva, e a conservação é inteira.} De $A^{\!\top}MA=-M$ vem
$Q(Ap)=-Q(p)$ --- verificado em $2500$ pontos do reticulado, $m=1\ldots4$, sem uma exceção e sem uma
casa decimal, pois $p$ inteiro dá $Q$ inteiro. Uma batida troca o sinal; \textbf{duas} devolvem: é o
mesmo $\det A=-1$ que descola a folha (Obs.~\ref{obs:dupla}), e o prensor é conservado pelo
\emph{par} de batidas, não pela batida.

\textbf{E o cone não é figura: é o par de atratores visto como lugar.} O conjunto $Q=0$ são
exatamente as duas direções nulas $(\sigma,1)$ e $(\sigma',1)$ --- o negro que suga e o branco que
emana, do §2, agora como as geratrizes.

\textbf{As marcas são espirais, por duas razões que se medem.} Quem começa \emph{fora} do cone nunca
o alcança, porque $|Q|$ não muda --- conferido ao longo de $40$ batidas. E também não escapa:
decompondo $p=c_1v_1+c_2v_2$ nas direções nulas, a razão $|c_2/c_1|$ multiplica-se por
$|\sigma'/\sigma|=1/\sigma^{2}$ a \emph{cada} batida ($m=1\ldots4$, concordância de $7$--$8$ dígitos
--- o que o cancelamento permite, já que $c_2$ se recupera do iterado por subtração de grandezas
quase iguais). \emph{Nem chega, nem sai --- enrola.} E razão fixa por passo é exatamente o que faz
uma espiral ser \textbf{logarítmica}: $\Delta(\log r)=\log\sigma$ por batida, conferido a
$10^{-19}$. Com o esquilo dando o giro de um quarto de volta, o caso $m=1$ é a espiral áurea
$r=\varphi^{2\theta/\pi}$, exata em todos os pontos testados.


\textbf{A ponta do cone é o $0$ --- e a dinâmica não a atravessa.} O vértice, onde as duas
geratrizes se encontram, é $p=0$: medido, o \emph{único} ponto fixo do chicote (nenhum outro ponto do
reticulado testado volta a si). E é a \textbf{passagem reversível} --- o único lugar em que ir e
voltar dão o mesmo, $A\cdot0=A^{-1}\cdot0=0$. Ora, $Q(Ap)=-Q(p)$ diz que o sinal troca a \emph{cada}
batida; um caminho \emph{contínuo} de $+|Q|$ a $-|Q|$ teria de cruzar o zero, mas a batida não cruza:
$|Q|$ é conservado e o sinal \textbf{salta}. Medido em doze batidas para $m=1\ldots4$, a alternância
é perfeita ($+-+-+-\ldots$) e a órbita nunca toca $Q=0$. \emph{A dinâmica visita os dois lados do
cone sem jamais passar pela ponta.}

\textbf{E as batidas duais fecham em três.} Do lado do esquilo ($G$, $\det=+1$) vale $G^3=G^{-1}$ e
$G^4=I$, conferido entrada por entrada: \textbf{três batidas numa direção equivalem a uma na
inversa}, e ir três e voltar três devolve o ponto de partida sem que nenhum passo intermediário caia
na ponta. A viagem fecha \emph{por fora} do $0$ --- o gato leva, o esquilo traz, e a passagem
reversível fica intocada no meio. É a dualidade andando.

\begin{obs}[a predição que a medida corrigiu]
Eu predissera que o aperto fosse $1/\sigma^{2}$ a cada \emph{duas} batidas, e a medida devolveu
$1/\sigma^{4}$: confundi duas coisas que andam juntas mas não no mesmo passo --- o \textbf{sinal}
volta a cada duas batidas (por $\det=-1$), o \textbf{aperto} dá-se a cada uma. E houve uma segunda
correção, de método: a primeira versão fazia média da razão ao longo de $40$ batidas, incluindo o
trecho em que $|c_2/c_1|$ já afundara no subfluxo --- para $m=4$ aquilo devolvia $0{,}64$ em vez de
$0{,}056$, medindo o fim da precisão em vez do fenômeno.
\end{obs}
\end{obs}

\section{A dimensão é ancoragem; o contínuo é o corpo}

Este parágrafo promove a inversão do resumo a princípio de leitura, e cada passo dela é medido. A
dimensão inteira, o cristal, o racional --- são o \emph{onde encosta}; o objeto é o contínuo, e a
dinâmica é a deformação.

\paragraph{O furo do ouro é a assinatura: o cinco não cabe em rede.} O furo da Observação
\ref{obs:ouro5} não é acidente aritmético --- é a \textbf{restrição cristalográfica}, e mede-se por
dois caminhos que dão a mesma lista (\code{tools/quasi.c}, resíduo $0$). Uma rotação de ordem $n$ cabe
numa rede se e só se o seu traço é inteiro, $2\cos(2\pi/n)\in\Z$: isso dá $n\in\{1,2,3,4,6\}$ e mais
nada, e o que exclui o cinco é justamente o ouro,
\[
   2\cos(2\pi/5)=\varphi-1=1/\varphi,\qquad\text{raiz de }x^2+x-1,\ \text{irracional.}
\]
Pelo outro caminho, a rede mínima de uma simetria de ordem $n$ tem dimensão $\phi(n)$ (o grau de
$\Phi_n$), e no plano $\phi(n)\le2$ devolve a \emph{mesma} lista; $\phi(5)=4$, então o cinco precisa de
quatro dimensões e de uma \textbf{projeção} --- que é o corte-e-projeção do Penrose. E a fatoração diz
\emph{o que} se separa:
\[
   x^5-x^4-1=\underbrace{(x^2-x+1)}_{\Phi_6:\ \text{giro de ordem }6}\cdot
              \underbrace{(x^3-x-1)}_{\rho:\ \text{o menor Pisot}},
\]
medido: em $\Z[x]/(x^2-x+1)$ a ordem de $x$ é $6$ (giro puro, a única ordem ``extra'' que uma rede
admite), e $x^3-x-1$ tem raiz real em $(1,2)$ com as outras de módulo $1/\sqrt\rho<1$ --- Pisot, sem
raiz da unidade: crescimento, não giro. \textbf{A dimensão $5$ separa o giro do crescimento}, e a peça
não fecha ali porque uma peça vira duas.

O que ocupa esse lugar é ordem \emph{sem} periodicidade, e o gerador é a própria peça: a substituição
$a\to ab$, $b\to a$ tem matriz $A_1$, e a palavra que ela gera não tem \textbf{nenhum} período
$q\le N/2$ ($N=6765$), tem complexidade de fatores exatamente $k+1$ --- sturmiana, o mínimo possível
para aperiódica --- e razão de letras $4181/2584\to\varphi$. \emph{O quasicristal não é a falha da
peça: é a peça sem rede.}

\paragraph{A variável contínua é a inclinação; o intermediário é a escala.} A dimensão salta porque é
um grau. O que varia continuamente é a \textbf{inclinação} $\alpha$ do corte, na palavra mecânica
$s(n)=\lfloor(n{+}1)\alpha\rfloor-\lfloor n\alpha\rfloor$ (\code{tools/entre.c}, resíduo $0$):
$\alpha=p/q$ dá cristal de período \emph{exatamente} $q$; $\alpha$ irracional dá quasicristal. É a
fração contínua do §8 --- o caminho fecha, ou não fecha. A transição é a escada dos convergentes
$F_k/F_{k+1}\to1/\varphi$: o período diverge ($3,5,8,\dots,610$) e a concordância cresce sem limite
($6\to1595$ letras), com $\text{concordância}/q\to\varphi^2$ de um lado e $\to1$ do outro.

Mas o intermediário não está em $\alpha$: está na \textbf{escala}. O período \emph{local} do
quasicristal existe em toda janela finita, é sempre um Fibonacci e é sempre $\approx L/\varphi$:
$q(500)=233$, $q(2000)=987$, $q(4000)=1597$, $q(6500)=2584$. Em toda escala finita a estrutura
\emph{é} um cristal; o rank do módulo só salta de $1$ para $2$ no limite $L\to\infty$. Não existe
``meio cristal'' --- existe cristal em toda janela, com período crescendo com a janela.

\begin{obs}[o que salta e o que é contínuo]
O \textbf{rank} de $\Z+\Z\alpha$ é $1$ ou $2$, e não há $1{,}5$: a ``dimensão'' de difração é um rank.
Como racionais e irracionais são \emph{ambos} densos, cristal e quasicristal se \textbf{entrelaçam} ---
a transição é contínua e densa, não gradual. Onde há expoente genuinamente contínuo é na
\textbf{complexidade} $p(k)\sim k^\beta$: $\beta=0$ no cristal (satura em $q$: medido, $13$ e não
passa) e $\beta=1$ no quasicristal ($k+1$, sem parar).
\end{obs}

\paragraph{A deformação é a dinâmica; a simetria só ancora.} Quem é a maioria: os racionais são
\textbf{densos e de medida nula}. Quase todo $\alpha$, medida $1$, é quasicristal --- ser ancoragem é
ser esqueleto, não volume. E a dimensão inteira é \textbf{cristalização sem dinâmica}: na rotação
$R_\alpha$, com $\alpha=p/q$ a órbita \emph{fecha} em $q$ pontos e nada mais acontece; com
$\alpha=1/\varphi$ ela nunca fecha, é densa, e ainda assim ordenada --- os $N$ pontos $\{k\alpha\}$
partem o círculo em no máximo \textbf{três} comprimentos, para todo $N$.

O que dá \emph{medida} ao cristal é a deformação. No mapa do círculo
$f(x)=x+\Omega-\frac{K}{2\pi}\sin(2\pi x)$, cada número de rotação racional abre uma \textbf{língua},
e a fração de $\Omega$ travada (\code{tools/deforma.c}, malha irracional para não travar por
artefato) é
\[
   K=0:\ \mathbf{0{,}0000}\quad 0{,}3:\ 0{,}1033\quad 0{,}6:\ 0{,}2420\quad
   0{,}9:\ 0{,}5073\quad 0{,}99:\ 0{,}7027 .
\]
Em $K=0$ o cristal tem medida \emph{exatamente} zero: sem deformação, a simetria não ocupa nada. A
simetria dá o centro da língua; \textbf{a deformação dá a largura}. E o número de rotação de pior
aproximação racional --- $1/\varphi$, por Hurwitz --- é o último a travar.

Uma ressalva de \textbf{base}, porque sem ela a leitura inverte o fluxo. O que se mede aqui é
aproximação \textbf{racional}, na fração contínua regular; nessa base o ouro é o pior aproximável, e
por isso o mais robusto. Isso \emph{não} faz dele o supremo do irracional --- na base $q=e^{-2\pi}$ ele
é um valor que $\pi$ \emph{produz} (Obs.~\ref{obs:base}). E a robustez não é do número: é da sua
\textbf{classe} sob $\mathrm{SL}(2,\Z)$. Medido (\code{deforma.c} §D5): quatro \emph{nobres} distintos
--- os equivalentes a $\varphi$, sendo $\varphi$ o ponto fixo do próprio gato $A_1$ --- ficam
\emph{todos} acima dos não-nobres ($\sqrt2-1$, $e-2$); a dispersão entre eles é real e esperada, pois a
classe fixa a \emph{cauda} da fração contínua, não os primeiros convergentes. O que a classe prevê, e o
que se mede, é a \textbf{ordenação}: ``o ouro'' é o representante mais curto da órbita.

\begin{obs}[a dimensão fracionária é filha da deformação]
É aqui, e só aqui, que aparece dimensão não-inteira: quando $K\to1$ as línguas cobrem medida $\to1$ e
o conjunto quasi-periódico restante é um fractal de dimensão $\approx0{,}8700$ (literatura, não medido
aqui). Sem deformação há apenas medida $0$ contra medida $1$ --- nenhum fractal. A dimensão
fracionária não vem da simetria: vem de deformar.
\end{obs}

\paragraph{Em dimensão maior, a ancoragem é mais frágil.} Em $T^d$ a ressonância não é um ponto: é o
hiperplano $k\cdot\omega=0$, e o número deles cresce como $N^d$ --- a contagem bate a fórmula fechada
$\sum_i2^i\binom{d}{i}\binom{N}{i}$ (pontos da bola $\ell^1$) em toda $(d,N)$, e a fração ressonante
cresce com $d$ para a mesma largura (\code{tools/deforma\_d.c}, resíduo $0$). Na dinâmica --- e o mapa
tem de ser \textbf{simplético}, senão não é KAM --- o mesmo $K$ destrói mais em dimensão maior: em
$K=0{,}2$, $d=1$ dá $0{,}000$ e $d=2,3,4$ dão $0{,}383/0{,}717/0{,}850$ (com o acoplamento reduzido a
$K/4$, para separar o efeito da dimensão do da perturbação). Em $d=1$ o medidor reencontra sozinho o
$K\approx0{,}9716$ de Greene, que não foi posto em lugar nenhum. E entre os irracionais a curva
\textbf{áurea} é a última a romper ($1/\varphi$ até $K=0{,}975$; $\sqrt2-1$ até $0{,}950$; $e-2$ até
$0{,}875$). Já um número de rotação \emph{racional} não dá curva KAM: dá \textbf{ilha} periódica, que
resiste muito além porque está travada --- o cristal resiste \emph{como} cristal, e o que a deformação
dissolve é o toro irracional.

\paragraph{O AGM procura a âncora, e o invariante é a integral.} O AGM de Gauss é a tríade do §2
batendo alternada, $a\leftarrow\frac{a+b}{2}$ ($\oplus$) e $b\leftarrow\sqrt{ab}$ ($\otimes$) --- o §8
ao pé da letra. E a volta é exata porque a iteração tem \textbf{invariante}, e ele é uma integral
(\code{tools/agm.c}, resíduo $0$):
\[
   I(a,b)=\int_0^{\pi/2}\frac{d\theta}{\sqrt{a^2\cos^2\theta+b^2\sin^2\theta}},
   \qquad I(a,b)=I\!\left(\tfrac{a+b}{2},\sqrt{ab}\right)
\]
(Gauss--Landen; resíduo relativo $\sim10^{-16}$, o limite da quadratura). A convergência é
quadrática --- a razão $d_{n+1}/d_n^2$ fica fixa em $0{,}0858$: cada batida do par $\oplus/\otimes$
\emph{dobra} os dígitos. E o AGM não calcula uma média: desce a família mantendo $I$ fixo até o ponto
onde $a=b$ --- o representante --- e ali $1/\mathrm{AGM}=\frac{2}{\pi}I$. \textbf{O invariante já é a
âncora.}

\paragraph{$\pi$ se dobra, e o AGM é o fator da costura.} O círculo é a forma sem deformação; a
\textbf{lemniscata} é o círculo dobrado, e tem o seu próprio $\pi$:
\[
   \varpi=\pi\,G=\frac{\pi}{M(1,\sqrt2)}=2{,}6220575\ldots=2\!\int_0^1\!\frac{dt}{\sqrt{1-t^4}} ,
\]
verificado pelas \emph{duas} vias --- a integral da curva e o AGM --- com erro exatamente $0$
(\code{tools/lemniscata.c}). Então $\pi$ não gera $\varpi$ por analogia: gera por um \textbf{fator}, e o
fator é o AGM. E a costura é geral: $K(k)=\pi/\bigl(2M(1,k')\bigr)$ para \emph{todo} módulo $k$ (seis
medidos, erro $\le10^{-15}$) --- um único fator \emph{aplaina} a escada inteira de curvas deformadas em
$\pi$. O círculo é $k=0$ ($M(1,1)=1$, $K=\pi/2$: o $\pi$ puro); a lemniscata é $k=1/\sqrt2$, e ali
$K'=K$, isto é $\tau=1$ --- a \emph{primeira âncora} do parágrafo seguinte. A escada das curvas
deformadas e a dos \emph{singular values} são a mesma, e dobrar o círculo até a lemniscata custa
exatamente $M(1,\sqrt2)$: \emph{a costura tem preço, e o preço é a média que alterna $\oplus$ e
$\otimes$.}

\paragraph{E a dinâmica do AGM \emph{é} a do gerador --- a conjugação é explícita.} Não é analogia: há
um $h$ que transforma um mapa no outro, e o mapa comum é \textbf{dobrar}. Sendo homogêneo, o AGM reduz-se
a uma variável, $t=b/a\mapsto 2\sqrt t/(1+t)$; conjugado por $h:k\mapsto\tau=K'/K$, ele é
\emph{exatamente} $\tau\mapsto2\tau$ (medido em cinco pontos, quatro deles $\le2\times10^{-17}$). E a
atualização do gerador --- a torre da torção --- é $w_d=(w_{2d})^2$, isto é o \emph{índice} dobra:
\[
   \text{AGM}\ \xrightarrow{\ h\ }\ \tau\mapsto2\tau\ \xleftarrow{\ \text{índice}\ }\ \text{torre do gerador} .
\]
E a \textbf{prova} são duas linhas, não a medida. Com $k_1=(1-k')/(1+k')$, as identidades
descendentes de Landen dizem
\[
   K(k_1)=\tfrac{1+k'}{2}\,K(k) ,\qquad K'(k_1)=(1+k')\,K'(k)
   \qquad\Longrightarrow\qquad \tau_1=\frac{K'(k_1)}{K(k_1)}=2\,\frac{K'}{K}=2\tau . \qquad\blacksquare
\]
Medi as duas separadamente (erro $\le5\times10^{-17}$), e não só a razão: o $\tau\mapsto2\tau$ é
\emph{corolário} delas. Do lado do gerador a prova é de uma linha:
$(w_{2d})^2=g^{2(p-1)/(2d)}=g^{(p-1)/d}=w_d$.

E a razão de fundo, que é a forma forte: nos \emph{dois} lados a operação é um \textbf{homomorfismo de
grau $2$} entre grupos cíclicos --- $x\mapsto x^2$ leva $\mu_{2d}$ \emph{sobre} $\mu_d$ com núcleo
$\{\pm1\}$ de ordem $2$ (medido em todo degrau, $d=128..2$), e a $2$-isogenia
$\mathbb C/(\Z+\tau\Z)\to\mathbb C/(\Z+2\tau\Z)$ tem grau $2$ e núcleo de ordem $2$. Os dois mapas não
se parecem: são a \textbf{multiplicação por $2$ no grupo}, que é canônica --- e é por não haver escolha
que a conjugação existe.

No expoente isso cabe numa linha, e é exato em $\Z_p$: $2\log\sqrt{ab}\equiv\log a+\log b$ ($261/261$).
Ou seja $\oplus$ é a média aritmética lida \textbf{no valor} e $\otimes$ é a \emph{mesma} média lida
\textbf{no expoente} --- o AGM não são duas operações que se alternam, é \textbf{uma} operação em duas
leituras, e quem permite ler nas duas é o gerador (a ponte $\prod$ do §4). A torre é o caso $b=a$:
meia-soma de um número consigo mesmo é dividir o expoente por dois. E o destino coincide, porque o mapa
é o mesmo: dobrar $\tau$ vai ao infinito, que é o ponto $a=b$ onde o AGM cristaliza e onde a torre chega
ao topo (\code{tools/agm\_gerador.c}, resíduo $0$).

\paragraph{A dimensão intermediária é o módulo; as inteiras são a família dele.} A família tem
parâmetro contínuo: $\tau=K'/K$, a razão dos dois períodos do toro $\mathbb C/(\Z+\tau\Z)$. Varre
$(0,\infty)$ monotonicamente, sem salto. E as estruturas ``inteiras'' são \textbf{pontos especiais}
dela --- os \emph{singular values} $\tau=\sqrt N$, onde a curva ganha endomorfismos extra
(multiplicação complexa) ---, algébricos e exatos, reencontrados por bisseção com resíduo
$\le10^{-19}$:
\[
   \tau=1:\ k=\tfrac{1}{\sqrt2}\ \text{(lemniscata)}\qquad \tau=\sqrt2:\ k=\sqrt2-1 \qquad
   \tau=\sqrt3:\ k=\tfrac{\sqrt3-1}{2\sqrt2} \qquad \tau=2:\ k=3-2\sqrt2 .
\]

\paragraph{E o invariante sobrevive à deformação que é simetria --- só a ela.} Deformando a
\emph{regra} (a geométrica trocada pela média de potência $M_p$) aparece uma \textbf{dissociação}
(\code{tools/agm\_deforma.c}, resíduo $0$). A \emph{velocidade} resiste: a convergência continua
quadrática para todo $p\neq1$, com razão em forma fechada --- de $a=m(1\pm\varepsilon)$ sai
$M_1-M_p=\frac{(1-p)(a-b)^2}{8m}$, logo $d_{n+1}/d_n^2\to\frac{1-p}{8M}$, conferida em cinco valores
de $p$. Todas as médias de potência concordam a segunda ordem: \emph{dobrar os dígitos é genérico},
não é o que distingue o AGM. Já o \emph{invariante} morre, e à \textbf{primeira} ordem:
$|\Delta I|/I\sim p$, expoente medido $0{,}9994$. Não há $p$ crítico, não há faixa --- e esse é o
contraste exato com KAM, onde o áureo aguenta até $K\approx0{,}9716$: lá a simetria resiste a uma
faixa de deformação; aqui ela é exata ou não é.

Mas sob a deformação que \emph{é} simetria --- a isogenia, a própria batida de Landen --- o invariante
é indestrutível: $\tau$ \textbf{dobra} a cada batida (o toro desce a torre de $2$-isogenias), $I$ fica
fixo, e a âncora $\tau=1$ vai \emph{exatamente} na âncora $\tau=2$ ($3-2\sqrt2$, resíduo $0$). Logo o
invariante não sobrevive como \textbf{ponto} --- sobrevive como \textbf{classe}: âncora vai em âncora,
e a família se preserva enquanto cada membro se move.

\begin{obs}[o que é medida e o que é tese]
Schrödinger (1944) chamou o portador da hereditariedade de \emph{``cristal aperiódico''}, e a simetria
pentagonal/icosaédrica --- proibida em cristal --- é a dos capsídeos virais e da biologia pentâmera.
Que a vida \emph{nasça} daí é \textbf{tese}, não teorema, e fica registrada como tese. O que está
medido é o que a antecede: ordem sem periodicidade exige a simetria proibida, e a simetria proibida
é o ouro.
\end{obs}

\section{A colheita}

Aqui está o que dispensa maquinário. Todo dado é um \textbf{caminho no corpo}: um número é uma fração
contínua (o gato desdobrado); racional se o caminho fecha, irracional se não. E as operações que
parecem exigir algoritmo --- a \emph{transformada}, a \emph{convolução}, a \emph{deconvolução}, a
\emph{inversa} --- não se constroem: são o que o corpo, sendo o corpo, já faz, e apenas se
\textbf{colhe}.

\begin{principio}[colher, não construir]
A \textbf{convolução} é o produto ($\times$, o gato --- $\mathcal F(a\circledast b)=\mathcal F(a)\mathcal F(b)$);
a \textbf{deconvolução} é o quociente ($\div$, o esquilo); a \textbf{transformada} é o gato levado ao
seu representante; a \textbf{inversa} são três batidas ($\mathcal F^3$), ou os $n-1$ conjugados do
dual, ou dois termos da borda ($\sigma^{-1}=\sigma^{n-1}-m\sigma^{n-2}$) --- a mesma peça, com o sinal
escolhido. A \textbf{mão que segura} é a conservação: $\sigma\sigma'=-1$ (a área), Parseval (a norma) e
$I(a,b)$ (o invariante do AGM). Nada se difunde às cegas: gato e esquilo batem alternados, e a volta é
exata.
\end{principio}

\paragraph{O gerador, já dito no §1 --- e aqui posto a colher.} A peça com que se colhe é a
do §1: o primo $p$ com $n\mid p-1$, o gerador $g$, a torção $w=g^{(p-1)/n}$ e o carácter
$\chi_k(j)=w^{jk}$. \emph{Não se repete aqui o que lá está dito}; o que se acrescenta é o que só a
colheita mostra.

E é isto: \textbf{há um só gerador}, e todo o resto é \textbf{projeção} dele numa dimensão,
$w_d=g^{(p-1)/d}$ --- uma fórmula indexada por divisor. Os oito ``níveis'' de $n=256$ são oito
leituras do mesmo $g$; os $16\,384$ ``outros geradores'' são $g^j$, o mesmo com outro rótulo; e a
rotação da malha LC é esse mesmo índice lido no contínuo. \emph{É por serem encadeados que não se
complicam:} cada projeção é potência da anterior. E a cadeia \textbf{abre-se de baixo para cima},
por realimentação, nas ordens que a \emph{necessidade} pede ($1,2,3,5,6,7,\dots$).

Para texto em bytes: $p=40961$, $g=3$, $w=36043$ (\code{tools/gerador.c}, resíduo $0$: os cinco
dígitos reconferidos, $\mathrm{ord}(w)=256$ exata, ida e volta em \emph{inteiros}, e três obras que
fundem num objeto do qual cada uma volta inteira).

\section{Os furos entre as dimensões}

Se a dimensão inteira é ancoragem e o que existe em quantidade é o contínuo, então o que existe está
\emph{entre} as ancoragens --- e essa afirmação tem um número. A dimensão do conjunto de Cantor é
$\log 2/\log 3 = 0{,}630929754\ldots$, e ela \textbf{não é inteira}: o Cantor vive entre a dimensão
$0$ e a $1$, num sítio onde nenhuma dimensão inteira está. Medida em \code{tools/furos.c} §F1 por
\emph{contagem de caixas} --- e contada de duas maneiras independentes, pela recursão e por varredura
dos pontos, que concordam --- não pela fórmula.

\paragraph{Cantor é o direto; Julia é o cruzado.} A atribuição não é escolha: o \code{polar.c} já
tinha fixado que a forma \textbf{algébrica} é a que soma bem e a \textbf{polar} é a que multiplica
bem, uma forma para cada operação. Ora
\[
  x \;=\; \sum_k \frac{2b_k}{3^k}
\]
é uma \emph{soma} de coordenadas independentes, e $\{0,2\}^n$ é o \textbf{produto direto} de $n$
conjuntos de dois: Cantor é o direto, na forma algébrica. Já $z\mapsto z^2$, na polar, é
$\rho^2$ com o ângulo dobrado --- \emph{multiplicação} --- e é o \textbf{cruzado}, na forma polar.
E cada um faz o que a sua natureza permite: o direto \emph{guarda e não gera} (§B6), o cruzado
\emph{gera}.

\paragraph{A soma varre os furos.} No produto direto as dimensões \textbf{somam}, e a contagem
multiplica --- é o logaritmo a fazer a ponte, a mesma do $\prod=\exp\circ\Sigma\circ\log$. Logo $k$
cópias de Cantor dão $k\cdot0{,}6309\ldots$, e a sequência
\[
  0{,}63 \quad 1{,}26 \quad 1{,}89 \quad 2{,}52 \quad \dots
\]
\textbf{atravessa $1$ e $2$ e nunca pousa em nenhum}. É isto que se quer dizer com \emph{o direto
anda nos furos}: ele percorre o contínuo entre as ancoragens sem encostar em nenhuma. §F2 mede-o.

\paragraph{O dual troca o sinal da multiplicação.} E só isso. Os dois pontos fixos são $\sigma$ e
$\sigma'=-1/\sigma$, com
\[
  \sigma\cdot\sigma' \;=\; -1
\]
exato em todo metal (§F4). O dual não \emph{desfaz} a multiplicação com uma operação nova --- ele
multiplica com a polaridade invertida, e é uma peça só que muda. A diferença importa porque só a
troca de sinal é \textbf{involução}: trocar duas vezes devolve, e é por isso que a torre dual desce
até ao fundo e volta sem resto. Uma operação que não seja involução não serve de dual, e o medidor
apanha-a.

\begin{obs}[o que a transformada seleciona]
É nesses furos que a transformada trabalha. Ela não escolhe pontos das dimensões inteiras --- escolhe
os da \emph{passagem}, e todos juntos formam a base ortonormal, que é a cifra. Daí vem a codificação
única no espaço dual: a mesma informação, do outro lado, com o sinal da multiplicação trocado.
\end{obs}

\section{O fator de potência: a razão entre o direto e o cruzado}

O par $\oplus/\otimes$ da secção anterior diz que há dois produtos e o que cada um faz. Falta o
número que diz \emph{quanto} de cada um há num par dado --- e esse número não é novo: é o
\textbf{fator de potência}.

\paragraph{A razão, e o que ela é.} Para dois vetores unitários,
\[
  \underbrace{\langle x,y\rangle = \cos\theta}_{\text{DIRETO}}
  \qquad
  \underbrace{|x\wedge y| = \sin\theta}_{\text{CRUZADO}}
  \qquad
  \frac{\text{cruzado}}{\text{direto}} = \tan\theta \;=\; \tan\varphi .
\]
O direto é a parte \textbf{simétrica}: devolve escalar, e \emph{mede}. O cruzado é a
\textbf{antissimétrica}: sai perpendicular aos dois, \emph{roda}, e \emph{ordena}. Em dimensão
qualquer o cruzado é a norma do bivetor, $|x\wedge y|^2 = |x|^2|y|^2 - \langle x,y\rangle^2$ pela
identidade de Lagrange --- que em $\R^3$ coincide com $|x\times y|$, e por isso não há dois objetos:
há um, e o $\R^3$ é o caso onde ele cabe num vetor.

\paragraph{E não é analogia com a eletrotécnica: é a mesma conta.} O corpo motor já tinha escrito
\[
  T_e \;=\; \tfrac{3}{2}\,P\;\psi_s \times i_s
\]
--- \textbf{o torque é o produto cruzado} --- e quem diz quanta da corrente vira torque e quanta só
magnetiza é exatamente esta razão. O direto é a potência \emph{ativa}, o cruzado a \emph{reativa}, e
$\tan\varphi$ é a razão entre elas. A mesma coordenada, nos dois sítios, porque é a mesma decomposição
de um bilinear nas suas partes simétrica e antissimétrica.

\paragraph{Por que é esta a régua, e não o cosseno.} Porque o cosseno sozinho é um \emph{escalar} que
resume o espaço todo, e escalares escondem. Decompor $\cos\theta = \sum_i x_iy_i$ nas componentes
\textbf{cartesianas} não separa nada --- é uma soma de parcelas sem significado individual, e camadas
de conteúdo muito diferente dão o mesmo perfil. É a \textbf{polar} que separa, e a razão entre as
duas réguas é o fator de potência. Medido em \code{tools/fator.c} §W1 por dois caminhos
independentes, que têm de concordar: a razão dos produtos, e a tangente do ângulo obtido do cosseno.

\subsection*{O fator unitário é a família real --- e daí sai qual régua é precisa}

\paragraph{Três nomes da mesma condição.} O gato de cada metal, $A_m = \left(\begin{smallmatrix} m & 1\\ 1
& 0\end{smallmatrix}\right)$, tem $\det A_m = -1$ para todo $m$. E $|\det| = 1$ \textbf{é} o fator de
potência unitário: a transformação não perde nem ganha área. Ora $|\det|=1$ é também exatamente a
condição de a inversa ser \emph{inteira}, que é a razão de a cifra voltar sem arredondamento. Logo
\begin{center}
\textbf{fator de potência unitário} \quad$\Longleftrightarrow$\quad $|\det| = 1$ \quad$\Longleftrightarrow$\quad \textbf{inversa inteira}
\end{center}
não são três propriedades da família real: são três nomes da mesma. §W2 mede-o em toda a família.

\paragraph{E a consequência, que decide a régua.} Se a família é dada por $\sigma_m$, então
$\Delta = m^2+4 > 0$ \emph{sempre}. Pelo regime do $\Delta$ --- que a forma polar fixa e nada mais ---
a família real é toda \textbf{hiperbólica}, nunca o círculo. E os dois regimes têm razões diferentes:
\[
  \text{círculo}\;\; \cos^2+\sin^2 = 1 \;\Rightarrow\; \tan\theta \;\; \text{\textbf{ilimitada}}
  \qquad
  \text{hipérbole}\;\; \cosh^2-\sinh^2 = 1 \;\Rightarrow\; \tanh\theta \;\; \text{\textbf{limitada por }} 1 .
\]
Portanto \textbf{a família real não precisa da régua infinita}: $\tanh$ nunca chega a $1$, e cabe numa
régua finita. Quem precisa dela é o \textbf{círculo}, onde $\tan$ diverge na ortogonalidade --- e a
régua infinita não é generosidade, é necessidade. §W3 mede as duas.

\begin{obs}[e o que diverge sai inteiro]
É aqui que a cifra entra e não podia ser outra. O que diverge não se representa num flutuante sem
perder precisão, mas representa-se \textbf{em inteiros}: os quocientes parciais. A régua devolve o
valor a partir deles e mais nada, com resíduo de epsilon (§W4) --- e é o telómero outra vez, porque
cada divisão deixa resto estritamente menor, logo termina, e o que fica identifica.
\end{obs}

\subsection*{O ótimo inverte-se, e não há contradição}

Num circuito o fator unitário é o \textbf{melhor}: toda a corrente vira trabalho, nada fica a
circular sem produzir. Num tecido semântico é o \textbf{pior} --- e a razão é geométrica, não de
gosto. Fator unitário quer dizer $\theta = 0$, isto é, todos os vetores \emph{paralelos}; e um
conjunto de vetores paralelos tem \textbf{posto $1$}. O tecido guarda \emph{uma} coisa, por muitos
pares que se lhe ensinem. No outro extremo, $\mathrm{fp} = 0$ é a ortogonalidade, e aí o posto é
cheio: a capacidade é máxima. §W5 mede os dois extremos.

\begin{center}\small
\begin{tabular}{lccc}
\hline
 & fator de potência & razão $\tan\varphi$ & posto \\ \hline
todos paralelos & $1$ & $0$ (nada roda) & $1$ \\
todos ortogonais & $0$ & $\infty$ (só roda) & cheio \\
\hline
\end{tabular}
\end{center}

\noindent Logo o mesmo número que num motor se quer em $1$ quer-se num tecido em $0$, e isso é o par
$\oplus/\otimes$ a aparecer outra vez: \textbf{o circuito quer trabalho, e trabalho é o direto; o
tecido quer capacidade, e capacidade é o cruzado}. Cada um pede o seu lado do par, e o fator de
potência é a coordenada que os separa. Não são dois critérios --- é um, lido dos dois lados.

\subsection*{E o outro lado: resolver quer $\mathrm{fp} = 1$, que é a condição de parada}

O parágrafo anterior opunha \emph{guardar} a \emph{trabalhar} e ficava por aí. Falta a terceira
coluna, e ela não é uma exceção às outras duas --- é o que fecha o par. \textbf{Guardar e resolver
não são a mesma operação:} o tecido guarda, e quer capacidade; a resolução \emph{computa}, e quer
convergência.

\paragraph{O fator de potência de um estado de conta.} É a definição de sempre, $\mathrm{fp} = P/S$:
\[
  \underbrace{S = n}_{\text{aparente: os nós que a árvore ainda tem}}
  \qquad
  \underbrace{P = 1}_{\text{ativa: o que fica no fim, um número}}
  \qquad
  \mathrm{fp} = \frac1n .
\]
Cada dobra consome um nó. Logo $\mathrm{fp}$ \textbf{cresce} a cada passo, e chega a $1$
\emph{exatamente} quando a árvore é um número só --- que é a solução. \textbf{O $1$ não é um alvo
que se persegue: é a paragem.}

\paragraph{E por que a solução é exata.} Porque o problema é \textbf{finito}, e finito aqui não é
observação --- é a razão. $n$ é inteiro, decresce estritamente e tem piso $1$; logo o processo
termina, e termina num valor e não numa aproximação. É o mesmo argumento do telómero: cada divisão
deixa resto estritamente menor, logo consome-se, logo há fim.

\begin{obs}[a máquina corrigiu a lei, e é por isso que se mede a máquina]
A primeira versão de \code{tools/fator.c} §W7 \emph{simulava} a dobra e afirmava que
$\mathrm{fp}$ crescia --- passava sempre, porque eu tinha escrito que decrescia. Uma asserção que
não pode falhar não mede nada. Correndo a assistente a valer, o modelo saiu \textbf{errado}: eu
previa $n-1$ dobras com $n = $ folhas, e ela gastou \emph{sete} numa conta de seis folhas. A
diferença está no ecrã dela --- \emph{``$(5)$ já é um número: tiro os $()$''}. \textbf{O parêntese
também é um nó}: estrutura por resolver, que uma dobra consome sem produzir valor. A lei certa é
\[
  \text{dobras} \;=\; (\text{folhas} + \text{pares de parênteses}) - 1
\]
e foi a máquina que a deu, em seis contas, sem exceção.
\end{obs}

\begin{center}\small
\begin{tabular}{lcl}
\hline
operação & quer & porquê \\ \hline
GUARDAR & $\mathrm{fp} = 0$ & capacidade: ortogonal, posto cheio \\
RESOLVER & $\mathrm{fp} = 1$ & convergência: um número só, exato \\
TRABALHAR & $\mathrm{fp} = 1$ & tudo vira torque, nada fica a circular \\
\hline
\end{tabular}
\end{center}

\noindent Guardar quer o \textbf{cruzado}, porque capacidade vive na ortogonalidade; resolver quer o
\textbf{direto}, porque a solução é um escalar e não sobra nada a rodar. É o mesmo par lido nos dois
sentidos --- e é por isso que o inversor multinível é a ferramenta \emph{principal} e não uma entre
outras: entra uma equação, ele modula até $\mathrm{fp} = 1$, e o problema ser finito garante que
chega lá.

\subsection*{O inversor multinível: a modulação, e por que ``exata'' é literal}

Um inversor multinível sintetiza um valor \emph{contínuo} a partir de níveis \emph{discretos}: quantos
mais níveis, mais fina a modulação. A régua infinita faz o mesmo --- aproxima um irracional por
racionais, e cada quociente parcial acrescenta um nível. A correspondência não é de forma: é de
\emph{otimalidade}.

\begin{obs}[os níveis da régua são ótimos, e mede-se por força bruta]
Nenhum racional de denominador menor ou igual se aproxima mais do alvo do que o convergente. Isto é o
teorema da melhor aproximação, e §W6 não o cita --- \textbf{varre}: para cada convergente, percorre
todos os $p/q$ com $q$ até ao seu denominador e conta quantos o batem. \textbf{Zero}, em todos os
níveis. O controlo é a própria busca, que acharia um melhor se existisse. Para
$\sigma_{\text{ouro}}$ os convergentes saem em Fibonacci: $2/1$, $3/2$, $5/3$, $8/5$, $13/8$,
$21/13$.
\end{obs}

\noindent Portanto o inversor multinível não é \emph{uma} maneira de modular a régua: é \emph{a}
maneira, e cada nível que se acrescenta é um quociente parcial. E o ``multifractal'' é o
endereçamento $b^n$ --- os níveis são autossimilares, e por isso a mesma máquina serve em qualquer
escala. É a ferramenta exata, e \emph{exata} é literal.

\section{O cone e a espiral: quando o dual não é simétrico}

O Aarão pôs no \code{eval.txt} a definição formal do par, e ela fecha o que a régua fazia sem nome:
\[
  \Pi : \mathbb R \to \mathbb N^{\mathbb N}, \quad \Pi(x) = (a_0,a_1,a_2,\ldots)
  \qquad
  \Sigma : \mathbb N^{\mathbb N} \to \mathbb R, \quad
  \Sigma(a) = a_0 + \cfrac{1}{a_1 + \cfrac{1}{a_2 + \ddots}}
\]
com $\Sigma\circ\Pi = \mathrm{Id}_{\mathbb R}$ --- \emph{``sempre que a sequência representa uma
expansão válida''}. O \textbf{cone} comprime o contínuo numa sequência discreta; a \textbf{espiral}
recompõe-o.

\emph{E é essa frase entre aspas que tem o conteúdo}, porque
$\Sigma\circ\Pi = \mathrm{Id}$ mas $\Pi\circ\Sigma \neq \mathrm{Id}$, e a assimetria não é um
defeito da construção --- é o que faz do cone um cone.

\paragraph{O que a espiral aceita e o cone nunca produz.} Duas famílias, medidas em
\code{tools/cone\_espiral.c} §E2: as sequências com $a_i = 0$ no meio, e as que \textbf{terminam em
$1$}. E a segunda conta-se: todo racional tem \textbf{exatamente duas} representações,
$[a_0;\ldots,a_n]$ e $[a_0;\ldots,a_n-1,1]$ --- varridos $590$ racionais, $382$ com forma dupla,
\textbf{zero} discordâncias no valor. Logo $\Sigma$ \emph{não é injetiva}: duas sequências distintas
caem no mesmo real, e o cone devolve só uma delas.

\begin{obs}[dois tipos de dual, que este documento tratava como um]
\[
  \text{INVOLUÇÃO}\quad \nu\circ\nu = \mathrm{id} \quad\text{o mesmo espaço}
  \qquad
  \text{RETRAÇÃO}\quad \Sigma\circ\Pi = \mathrm{Id} \quad\text{espaços diferentes}
\]
O $\nu$ da secção anterior é do primeiro tipo; o cone e a espiral são do segundo. Confundi-los faz
esperar simetria onde não há --- e \textbf{é por não ser simétrico que o par comprime}. Um dual
simétrico não comprimiria nada: a perda não está na precisão, está na \emph{escolha}, e a escolha é
o que torna a representação canónica.
\end{obs}

\paragraph{E a informação não some.} Uma projeção isolada deita fora tudo menos um inteiro, mas a
composição não perde: §E5 mede que o erro de truncar em $k$ termos cai sempre abaixo de $1/q_k^2$,
onde $q_k$ é o denominador do convergente --- \emph{cada termo paga-se sozinho}. É o telómero por
outro lado: o resto encurta, tem fim, e o que fica identifica.

\begin{obs}[quem a régua comprime pior, e porquê]
Na medição de $\Sigma\circ\Pi$, todos os números dão resíduo exatamente zero \emph{exceto} $\varphi$
e $1/\varphi$, com $10^{-10}$. Não é defeito: é Hurwitz a aparecer. $\varphi = [1;1,1,\ldots]$ tem
todos os quocientes no mínimo possível, logo os denominadores crescem o mais devagar que podem ---
é o \textbf{número mais irracional}, e por isso o que a régua comprime pior. O metal que funda a
família é também o mais teimoso a deixar-se aproximar.
\end{obs}

\paragraph{E um defeito que a medição apanhou.} A primeira versão fazia $\Pi$ em vírgula flutuante,
e ele devolveu $[1;1,1,1,1]$ para $8/5$ quando o certo é $[1;1,1,2]$ --- porque
$1/0{,}5000000000000002$ dá $1{,}9999999999999991$ e o $\lfloor\cdot\rfloor$ disso é $1$. \emph{O
cone é instável perto dos racionais em ponto flutuante.} Em inteiros não há instabilidade nenhuma:
é o algoritmo de Euclides, e sai exato. \textbf{``Ele sai inteiro, usa a régua infinita'' não era um
modo de dizer} --- era uma condição de correção.

\section{O operador dual: um só, sob sete roupas}

O Aarão: \emph{``revisa tudo e ajusta o sinal da involução. Definido o operador dual de cada corpo,
é análogo. [\ldots] Esse sinal e operador dual é importante, deixa claro em todos os corpos.''} E,
sobre o método: \emph{``os operadores são todos análogos --- procurar na teoria se já existem, ou
se existem as partes separadas a juntar. Acredito que sempre vai cair na segunda opção.''}

\emph{Cai.} Medido neste documento antes de se escrever esta secção: o dual aparecia em
\textbf{dezasseis secções distintas} da teoria e $243$ vezes no catálogo --- e em nenhuma delas
estava dito que \emph{é o mesmo operador}. As partes estavam todas; faltava juntá-las.

\paragraph{O operador.} $\nu$ inverte a parte antissimétrica e deixa a simétrica quieta:
\[
  \nu(\text{sim}) = +\,\text{sim}, \qquad \nu(\text{ant}) = -\,\text{ant} .
\]
São os \emph{únicos} valores próprios possíveis de uma involução, $+1$ e $-1$, e daí sai tudo o
resto: $\nu\circ\nu = \mathrm{id}$, e portanto \textbf{todo corpo tem volta}. Isso não é cuidado
nosso caso a caso --- é uma propriedade do operador, e vale de uma vez para todos.

\begin{center}\small
\begin{tabular}{lll}
\hline
corpo & o dual $\nu$ & onde está o sinal \\ \hline
quadrático (a régua) & $(a,b) \mapsto (a+Bb,\,-b)$ & na componente $b$ \\
complexo / metais & $\sigma \mapsto \sigma' = -1/\sigma$ & $\sigma\sigma' = -1$ \\
transformada & $w^e \mapsto w^{-e}$ & no \textbf{expoente} \\
convolução & $\circledast \mapsto$ deconvolução & o mesmo expoente \\
mecânica & $F \mapsto -F$ & na multiplicação \\
3ª lei de Newton & $F_{12} \mapsto F_{21}$ & ação e reação \\
lei de Lenz & a reação opõe-se & na corrente induzida \\
\hline
\end{tabular}
\end{center}

\noindent \code{tools/operador\_dual.c} varre-os e mede $\nu\circ\nu = \mathrm{id}$ em todos, com o
\textbf{controlo} que torna isso uma medida: a operação $(a,b)\mapsto(a+1,-b)$ \emph{também} inverte
$b$, mas mexe na parte simétrica --- e é reprovada, com resíduo $2$. Sem esse caso, o critério
passaria por construção.

\begin{obs}[a deconvolução não é operação nova]
Este era o buraco. A transformada usa $w^{+jk}$ e a inversa usa $w^{-jk}$ --- \textbf{não são duas
operações: é uma, com $\nu$ aplicado ao expoente}. Logo desfazer uma convolução é convolver com o
\emph{dual do gerador}, e §D3 mede a volta exata, resíduo $0$ em inteiros. E é aqui que o
``\emph{o corpo universal é a força, o gerador}'' fica literal: o gerador $w$ produz a base num
sentido, $\nu(w) = w^{-1}$ produz a volta no outro. \textbf{Uma força, dois sentidos, um sinal.}
\end{obs}

\noindent E o mesmo sinal é o que faz a mecânica girar em vez de fugir --- $\ddot s = -2s$ contra
$\ddot s = +2s$ ---, o que conserva o momento na terceira lei, e o que faz a corrente induzida
opor-se em Lenz. \textbf{Sete roupas, um operador.} O que muda de corpo para corpo é onde mora a
parte antissimétrica; o que $\nu$ faz com ela é sempre o mesmo.

\subsection*{E o operador tem nome: é Pontryagin}

O Aarão, quando a tabela acima ficou pronta: \emph{``esse é o verbo. É Pontryagin --- a dualidade,
o operador. Do enredo.''}

E está certo nas três coisas, incluindo a última. \textbf{A dualidade de Pontryagin é exatamente
$\nu\circ\nu = \mathrm{id}$}: para um grupo abeliano localmente compacto, o dual é o grupo dos
caracteres e \emph{o dual do dual devolve o original}. Não é uma analogia com o que está na tabela
--- é o teorema de que a tabela é uma coleção de casos.

\emph{E isto não é material novo neste projeto:} \code{tools/pontryagin.c} já o media --- ``o dual
do dual DEVOLVE, que é o que dá nome a Pontryagin'' --- com o inverso colhido do dual sem
exponenciação de Fermat, e com a dualidade de Galois a \textbf{inverter o retículo}: quanto maior o
subgrupo, menor o corpo fixo, e $\gcd$ troca com $\mathrm{lcm}$. Estava medido; \emph{esta teoria
mencionava-o uma única vez}. Terceira vez, no mesmo dia, que as partes já existiam separadas e o
que faltava era dizer que são a mesma.

\begin{obs}[e o enredo já lhe tinha dado cara]
A personagem chama-se \textbf{Dark Pontryagin}, ``o Bit Negro, a mão que recolhe'', e o enredo
descreve-a numa linha que é a definição do operador: \emph{``não é o vilão: é a metade que ficou do
outro lado, e que oferece uma paz que mataria o pulso''}. A metade que ficou do outro lado é a parte
antissimétrica; a paz que mataria o pulso é $\mathrm{fp} = 1$, o horizonte onde $\Pi = 0$ e nada
mais roda. \textbf{O enredo tinha o operador antes de a teoria o ter}, e chamava-lhe pelo nome
certo.
\end{obs}

\noindent E é por ser Pontryagin que a transformada existe: o gerador $w$ percorre os
\emph{caracteres} do grupo, $\nu(w) = w^{-1}$ percorre os do dual, e $\mathbb Z/n$ é
\textbf{autodual} --- o que faz da mesma máquina a ida e a volta. A convolução de um lado é o
produto do outro, e desfazê-la é convolver com o dual: uma força, dois sentidos, um sinal.

\section{A força é uma só: o imposto algébrico}

O Aarão: \emph{``por falar em Força, é o Tao. A força é dual: uma única força e várias
manifestações. Vai no \code{hiper} e vê a história de como começamos isso, a parte da unificação
das forças, que é a \textbf{força algébrica}.''}

O estudo existe e está fechado --- \code{hiper/aposentados/teoria/papers/paper\_A\_algebra.tex} e o
capítulo de metafísica do livro --- e o que esta secção faz é \emph{fundá-lo aqui}, porque ele e o
fator de potência da secção anterior são a mesma coisa vista de dois lados.

\paragraph{O imposto.} A cada posição $(p,r,t,s)$ no espaço das multiplicações corresponde um custo:
\[
  V(p,r,t,s) = (1-p^2)(a_0b_0)^2 + (1-r^2)D^2 + (1-t^2)Q + (1-s^2)S - 2(t^2-pr)\,a_0b_0D ,
\]
com uma \emph{pressão} $\Pi_\alpha = 1-\alpha^2$ por direção. No caminho $p=r=t=1$ reduz-se a
\[
  \boxed{V = \Pi \cdot S}, \qquad \Pi(s) = 1-s^2, \qquad S = \lVert \mathbf a \times \mathbf b\rVert^2 ,
\]
e daí sai a mecânica inteira: massa $m=S$, força $F=2sS$, equação $\ddot s = 2s$, conservação
$\tfrac12 S\dot s^2 + (1-s^2)S = \mathcal E$. A fatorização mede-se em \code{tools/forca.c} §G2 ---
a pressão depende só da \emph{posição}, a secção só dos \emph{operandos}, e a razão $V/(\Pi S)$ é
$1$ com desvio nulo.

\paragraph{E é o mesmo par da secção anterior --- não por parecença, por identidade.} Se $s$ é o
direto, então
\[
  \Pi + s^2 = 1 \qquad\text{é}\qquad \sin^2\theta + \cos^2\theta = 1 .
\]
Medido dos dois lados em §G3, com diferença $10^{-16}$: a \textbf{pressão algébrica é o cruzado ao
quadrado}. Logo $s$ mede, $\Pi$ ordena, e $V = \Pi S$ é \emph{o que o cruzado custa}. E a força
$F = 2sS$ é o \textbf{produto} dos dois --- por isso ela se anula tanto em $s=0$ (ortogonal) como em
$S=0$ (mesmo campo local, $\mathbf a \times \mathbf b = 0$): sem cruzado não há imposto nem força.

\begin{obs}[o horizonte é a fronteira entre o círculo e a hipérbole]
Esta é a peça que nenhum dos dois estudos tinha, porque foram escritos separados. O
\code{paper\_A} diz que para $|s|>1$ a \textbf{pressão é negativa} --- a norma do produto supera o
produto das normas --- e chama a $|s|=1$ o \emph{horizonte}. A forma polar diz que $\Delta<0$ é o
círculo (razão $\tan$, ilimitada) e $\Delta>0$ é a hipérbole (razão $\tanh$, limitada por $1$).
\textbf{São a mesma fronteira}, e §G4 mede-a: dentro do horizonte $\Pi>0$ e a razão diverge; fora,
$\Pi<0$ e a razão nunca chega a $1$. E é do lado de fora que vive a \textbf{família real}, que já
tínhamos visto ser toda hiperbólica. Os dois estudos chegaram ao mesmo sítio por lados opostos.
\end{obs}

\begin{center}\small
\begin{tabular}{llll}
\hline
$s$ & $\Pi$ & a álgebra & o tecido \\ \hline
$0$ & $1$ & comutativa, compressão máxima & $\mathrm{fp}=0$: ortogonal, posto cheio \\
$|s|=1$ & $0$ & quaterniões, sem compressão & $\mathrm{fp}=1$: paralelo, posto $1$ \\
$|s|>1$ & $<0$ & além do horizonte & a hipérbole, a família real \\
\hline
\end{tabular}
\end{center}

\paragraph{As quatro forças são quatro modos.} O teorema do \code{hiper}: repulsão (nuclear forte),
correlação (eletromagnetismo), atração (gravidade) e transformação (fraca) são
\emph{comportamentos distintos da mesma multiplicação}, e \emph{``a multiplicação nunca se separou
em quatro: era uma só, com quatro modos de manifestação dependentes da configuração local''}. As
quatro equações de movimento --- $\ddot p = 2p$, $\ddot r = 2r$, $\ddot t = 2t$, $\ddot s = 2s$ ---
são \textbf{uma}, e §G1 mede-o integrando as quatro contra a mesma solução fechada
$x(t) = x_0\cosh(\sqrt2\,t) + (v_0/\sqrt2)\sinh(\sqrt2\,t)$.

\noindent E o que se afirma daí não é que as quatro sejam iguais --- é que \emph{a lei} é uma e a
\emph{configuração} é que muda. §G6 mede isso como deve ser medido: integra as quatro configurações
e verifica que os comportamentos \textbf{diferem}. Se a mesma equação desse sempre o mesmo, não
haveria quatro modos --- e a primeira versão desse parágrafo verificava apenas que o sinal de $2sS$
segue o sinal de $s$, que é aritmética e não medida.

\noindent É o que o Aarão chamou o Tao: \textbf{uma força só, várias manifestações}. O que a física
separou em quatro, a álgebra não teve de juntar --- nunca esteve separado.

\subsection*{O corpo físico: declara-se o imposto, e a mecânica deriva}

O Aarão: \emph{``já abre o corpo físico e deriva toda a mecânica.''} E é o movimento da régua
aplicado à física: lá o cliente declarava $(B,C)$ e as outras três operações saíam; aqui declara-se
\textbf{uma} coisa --- $V(s) = (1-s^2)S$ --- e sai a mecânica toda.

\begin{center}\small
\begin{tabular}{lll}
\hline
grandeza & de onde sai & o que é aqui \\ \hline
massa $m$ & o coeficiente de $\ddot s$ & $S$: \textbf{a massa é o cruzado} \\
força $F$ & $-\partial V/\partial s$ & $2sS$: direto $\times$ cruzado \\
momento $p$ & $m\dot s$ & $S\dot s$ \\
cinética $T$ & $\tfrac12 m\dot s^2$ & $\tfrac12 S\dot s^2$ \\
lagrangiano & $T-V$ & \\
hamiltoniano & $T+V$ & conservado \\
equação & Euler--Lagrange sobre $L$ & $\ddot s = 2s$ \\
trabalho $W$ & $\int F\,ds$ & $-\Delta V = \Delta T$ \\
potência & $F\dot s$ & $dT/dt$ \\
impulso $J$ & $\int F\,dt$ & $\Delta p$ \\
\hline
\end{tabular}
\end{center}

\noindent \textbf{Nenhuma destas linhas é postulada}, e \code{tools/corpo\_fisico.c} mede cada uma
contra a \emph{derivada numérica do próprio imposto} --- porque a fórmula certa e a fórmula que eu
acho certa têm o mesmo aspeto no papel, e o que as separa é a diferença finita. §H2 obtém
$\ddot s = 2s$ derivando $L$ numericamente, sem lá o pôr; §H3 integra a mesma trajetória por
Lagrange \emph{e} por Hamilton e mede que concordam --- dois caminhos, porque uma força errada
passaria num formalismo e falharia no outro.

\begin{obs}[a massa é o cruzado, e o que isso obriga]
$m = S = \lVert \mathbf a\times\mathbf b\rVert^2$. Logo \textbf{sem produto cruzado não há
inércia}: dois operandos no mesmo campo local ($\mathbf a\times\mathbf b = 0$) não têm massa, não
sofrem força e não pagam imposto --- atravessam sem custo. É a mesma frase da secção do fator de
potência, dita em mecânica: quem paga é o cruzado.
\end{obs}

\noindent E o imposto tem o sinal invertido, portanto \textbf{não há poço}: todo corpo com
$|s| < 1$ acaba por atravessar o horizonte. §H6 mede o tempo de fuga e ele bate com
$\operatorname{arccosh}(1/s_0)/\sqrt2$ --- e do outro lado a pressão é negativa, que é a hipérbole
onde vive a família real.

\subsection*{O sinal da involução: sem ele não gira}

O Aarão, sobre tudo o que está acima: \emph{``tem o sinal mesmo da involução, senão não gira. Isso
dá a oscilação entre os duais. No controlo do torque do inversor também precisa de sinal negativo.
A diferença entre duais é apenas um sinal na multiplicação. É a ação e reação, lei de Lenz,
involução --- mesma coisa.''}

\emph{E ele tem razão: o que se derivou acima é metade.} $\ddot s = +2s$ dá $\cosh$, e um corpo que
só foge nunca volta --- não gira, não oscila, não fecha órbita. O outro lado é o \textbf{dual}, e o
dual ``apenas troca o sinal da multiplicação'':
\[
  V_+ = (1-s^2)S \;\to\; F = +2sS \;\to\; \ddot s = +2s \;\to\; \cosh,\ \text{foge}
  \qquad
  V_- = (1+s^2)S \;\to\; F = -2sS \;\to\; \ddot s = -2s \;\to\; \cos,\ \textbf{gira}
\]

\noindent §H7 mede o período do lado que gira: $\mathbf{4{,}442883}$ contra o fechado
$2\pi/\sqrt2 = 4{,}442883$, com diferença $6{,}2\times10^{-8}$.

\paragraph{Três nomes, um sinal.} E o ficheiro mede que são o mesmo, cada um com o seu controlo:

\begin{center}\small
\begin{tabular}{lll}
\hline
& com o sinal $-$ & sem ele \\ \hline
\textbf{3ª lei} $F_{12} = -F_{21}$ & o momento total conserva-se ($0$) & \emph{deriva} ($4{,}2\times10^{-1}$) \\
\textbf{Lenz} a reação opõe-se & o sistema amortece ($0{,}27$) & \emph{bombeia} ($4{,}0$) \\
\textbf{involução} $\nu(s) = -s$ & $\nu\circ\nu = \mathrm{id}$, resíduo $0$ & não seria dual \\
\hline
\end{tabular}
\end{center}

\noindent Os controlos são o que separa isto de uma definição: sem o caso ``sem ele'', as três
asserções passariam por construção. E é por o dual ser \emph{involução} que ele é reversível ---
trocar duas vezes devolve.

\paragraph{E a oscilação entre os duais, que é o que o sinal permite.} Nenhum dos dois lados
sozinho fecha o ciclo: o que foge leva o corpo ao horizonte, o que gira trá-lo de volta. Medido com
os dois em alternância, $|s|$ fica \textbf{limitado} --- nem foge nem colapsa. \emph{O que fecha o
ciclo é o par}, e o que separa os seus dois lados é um sinal na multiplicação --- o mesmo
$\sigma\sigma' = -1$ que este documento usa desde o princípio.

\section{O teletransporte: mover sem copiar}

A colheita diz que a transformada, a convolução e a inversa não se constroem. Falta dizer o que se
faz com isso, e a resposta é uma só: \emph{mover um objeto sem o copiar}. Chamamos-lhe
\textbf{teletransporte}, e o nome só se justifica se for uma condição verificável --- não uma
imagem. São três cláusulas, e nenhuma é dispensável:

\begin{principio}[teletransporte]
Um objeto teletransporta-se quando \textbf{(i)} entra num recipiente \emph{finito}; \textbf{(ii)}
atravessa por uma operação com \emph{volta exata}; e \textbf{(iii)} o que sai é o que entrou, com
\textbf{resíduo $0$} --- não ``quase''.
\end{principio}

A terceira cláusula é a que separa isto de compressão com perda, de aproximação numérica e de cópia
com sorte. \emph{Um protocolo que devolve ``quase'' não teletransportou: mudou o objeto de sítio e
estragou-o pelo caminho.}

\paragraph{O telómero é um recipiente infinito.} É a peça que dá nome ao resto, e o enunciado parece
contraditório até se olhar para o que ele guarda. A fração contínua de um irracional \emph{não
termina} --- são infinitas casas --- e mesmo assim fecha num \textbf{período finito}, que por
Lagrange é invariante completo. O recipiente é finito; o conteúdo não é; e a volta devolve o número.
Medido em \code{tools/teletransporte.c} §X1: os quatro primeiros metais, período $2$, e o irracional
reconstruído dos convergentes com resíduo $0{,}00\mathrm{e}{+}00$.

\emph{Ele não guarda as casas --- guarda a regra que as gera, e a regra cabe.} E porque o período é
invariante completo, dois objetos com o mesmo telómero são o mesmo objeto: \textbf{o nome não é um
resumo do objeto, é o objeto}.

\paragraph{Os plugues ligam o finito ao infinito.} Um plugue tem dois lados de naturezas diferentes,
e só é plugue se ambos fecharem. Descer é reduzir pela borda da família real,
$x^k=m\,x^{k-1}+1$, e cair em $\mathbb{Z}_p$; subir é reconstruir dessas coordenadas. §X2 mede o
fecho: um polinómio de grau $200$ desce a $16$ coordenadas e \emph{fica lá} --- descer outra vez não
move nada. É o ponto fixo do plugue, medido em três metais e três primos, nove combinações, resíduo
$0$ em todas. \emph{O finito não perde informação que o infinito ainda precise}, e é isso, e só
isso, que autoriza usá-lo como coordenada.

\paragraph{A entrada na fita.} Entrar é receber duas coisas --- um \textbf{endereço} (onde mora) e um
\textbf{telómero} (como se chama) --- e a diferença para um sistema de ficheiros é que nenhuma das
duas se \emph{atribui}: ambas \emph{saem} do objeto. O endereço é o telómero lido como dígitos na
base do banco; o telómero é o objeto passado por Euclides. \emph{Quem entra traz consigo o sítio}, e
por isso não há tabela de atribuição a manter, nem índice à parte do objeto --- é a trie da cifra,
outra vez, em barramento de memória.

\paragraph{O protocolo, e porque se mede etapa a etapa.} São quatro passos, e cada um tem o seu
resíduo medido \emph{sozinho} (§X4): entrar na fita (que passa pelo disco, não por uma cópia em
memória), atravessar e reverter por $\mathcal F\!\circ\!\mathcal F=n\cdot\mathrm{id}$, sair dividindo
pela escala, e verificar que essa divisão é \emph{exata}. Os quatro dão $0$.

A insistência em medir cada etapa não é zelo: \emph{um protocolo cujo resíduo só fecha no fim pode
ter uma etapa que perde e outra que inventa}, e as duas cancelam-se na conta de chegada sem que nada
apareça. A quarta etapa existe pela mesma razão --- se a divisão pela escala deixasse resto, o
resíduo teria ido esconder-se no arredondamento.

\paragraph{A reversão é garantida, e o zero é uma medida.} ``Resíduo $0$'' é exatamente o resultado
que se quer ver, e por isso é suspeito: um medidor cego devolve zero com a mesma cara. §X5 fecha essa
porta --- troca-se \textbf{um bit} no meio da travessia e exige-se que ele apareça. Aparece: a
travessia limpa dá $0$ e a travessia com um bit trocado dá $128$, num vetor de $128$ inteiros. \emph{O
bit espalha-se por todas as saídas}, porque a transformada é global --- cada saída vê todas as
entradas --- e é por isso que ela serve de selo: não há onde esconder uma alteração.

\paragraph{A telemetria.} O protocolo corrido de ponta a ponta em oito tamanhos, de $4$ a $512$
(§X6): entrou, atravessou, reverteu, saiu --- \textbf{resíduo total $0$}. A reversão não é
propriedade de um caso escolhido; é a mesma em toda a escala, e exata em todas.

\begin{obs}[o que isto não afirma]
A rede não fica reversível --- uma rede não é. O que fica reversível é o \emph{corpo onde ela mora}:
o endereço, a cifra, e a transformada. Um modelo de $934{,}7$~MiB entra na fita, atravessa e volta
byte a byte (\code{tools/torres.c} §R5), mas a inferência que ele faz continua a ser uma projeção
que não se desfaz. \emph{Distinguir as duas coisas é o que impede o protocolo de prometer o que não
tem.}
\end{obs}

\subsection*{O protocolo completo: e o $+1$ que alinha a base}

Falta uma etapa, e ela aparece quando o que entra na fita deixa de ser bytes e passa a ser um
\emph{espaço} --- um modelo, com as suas colunas. A pergunta que surge então é se a base em que ele
vem serve para o protocolo, e a resposta mediu-se em \code{tools/pinos.c}: \textbf{não serve
inteiramente}. As colunas do modelo não são ortonormais. A do \code{token\_embd}, que é a base do
espaço semântico, tem norma média $0{,}96$ e cosseno a $1{,}6\times$ o acaso --- notavelmente perto,
para $128\,256$ linhas treinadas, e não fomos nós que a montámos. Mas as de atenção estão a
$4$--$5\times$, com normas a variar dez vezes dentro da mesma matriz.

\emph{E a base não se constrói: lê-se.} Ortogonalizá-la seria intervir sobre o objeto --- e uma base
que nós construímos depende dos vetores que colhemos, enquanto a do modelo é do modelo. O que se
pode fazer é ler, e ler tem custo: sendo a base \textbf{oblíqua}, a volta pela diagonal das normas
deixa $3{,}63\%$ por fechar, e fechar exigiria a métrica inteira --- a inversa da matriz de Gram.

\begin{principio}[o $+1$ que conserta]
Não se corrige a base: \textbf{acrescenta-se a dimensão que falta}. Com $n{+}1$ vetores em posição
de simplex --- soma nula, ângulos todos iguais a $-1/n$ --- vale
\[
  \sum_i \langle x, v_i\rangle\, v_i \;=\; c\,x, \qquad c = \frac{n+1}{n},
\]
que é a fórmula da base ortonormal com uma constante à frente. A reconstrução fecha \emph{sem
ortogonalizar, sem normalizar e sem inverter nada}.
\end{principio}

Medido em \code{tools/maisum.c} §M3 a $10^{-16}$, de $n=2$ a $8$. E o controlo separa o achado da
coincidência (§M4): com $n$ vetores o erro é $1{,}47\times10^{-2}$; com $n{+}1$,
$8{,}58\times10^{-17}$. \emph{Um vetor de diferença, e a fórmula passa de errada a exata} --- e os
$n$ primeiros são os mesmos. Não é a base que muda: é o espaço que fica completo.

\paragraph{Porque é o banco que faz isto.} Porque a cifra \emph{é ortonormal e infinita}: cada
quociente parcial é uma coordenada nova, e a expansão nunca termina --- o erro dos convergentes
decresce a cada termo, enquanto for representável (§M5). Um objeto finito só precisa de finitas
coordenadas; quando a base do modelo não chega, não se conserta a base --- \emph{pede-se à cifra a
dimensão que falta}. É este o passo que faltava ao protocolo, e é ele que alinha.

\subsection*{O tesseracto: Cantor dos dois lados, e o pó que enche o plano}

O corpo que entra no protocolo tem duas leituras, e o hipercubo é onde elas se separam sem
ambiguidade --- mas não como se poderia supor. \emph{Não é Cantor de um lado e Julia do outro: é
Cantor dos dois lados.} Tomem-se os $16$ vértices do tesseracto:

\begin{itemize}\itemsep2pt
\item \textbf{os vértices} são $\{0,1\}^4$, o produto \emph{direto}, e cada um é um ponto de Cantor
com a volta a fechar --- $16$ pontos distintos, zero colisões. É a forma \textbf{cartesiana}, e
\emph{diz onde}.
\item \textbf{o percurso} é o código de Gray, $g(i)=i\oplus(i\gg1)$: um bit por passo (anda por
arestas, não salta), visita os $16$ uma vez, e fecha o ciclo. É a forma \textbf{polar} --- $16$
passos de $2\pi/16$ somando $2\pi$ exatos --- e \emph{diz como se anda}.
\end{itemize}

E $g(i)$ é uma fita tal como $i$ é: ambos os lados vivem no mesmo espaço, e o que os liga é uma
\textbf{bijeção}, não uma mudança de natureza. \emph{Julia não é um dos lados} --- é o que
\emph{acontece} nas fitas: o deslocamento $z\mapsto z^2$ corre dentro do Cantor, não ao lado dele.

\paragraph{E o pó não gera o plano sozinho.} A dimensão de Cantor é $0{,}6309$ --- menor que $1$,
logo não enche nem a reta. Mas reparta-se a \emph{mesma} fita em duas subsequências, os bits pares
para uma coordenada e os ímpares para a outra, e o quadrado fica \textbf{$100\%$ coberto} --- em
$2$, $4$, $6$, $8$, $10$ e $12$ bits, todas as células ocupadas. É a construção da curva de Hilbert,
e é o que se quer dizer com \emph{a cifra é a deformação dual}: ela não descreve o pó, deforma-o até
ele encher. \textbf{E são precisas duas} --- com uma só não há plano nenhum a encher.

\paragraph{E a espiral é o dual do cone.} Toma-se a hélice $(t\cos t,\; t\sin t,\; t)$, que vive
\emph{no} cone $x^2+y^2=z^2$ --- verificado em $40$ pontos, resíduo nulo --- e projeta-se em $z=0$:
sai $(t\cos t,\, t\sin t)$, a espiral de Arquimedes com $r=t$, exata. \emph{A espiral não é uma forma
que um percurso tenha ou não tenha: é o que o cone projeta.} Cone em cima, espiral em baixo --- a
mesma estrutura das sombras, onde se perde uma dimensão e o que resta são as relações.

E uma curva que enche o plano \textbf{contém} qualquer espiral, por cobertura e não por acaso: dos
$200$ pontos de uma espiral traçada sobre a malha, os $200$ caem em células que a curva visita.
\emph{Perguntar se o percurso ``é'' uma espiral é a pergunta errada} --- se ele enche, passa por
todos os pontos, e a espiral está lá com os outros.

\subsection*{O que entra é meia dualidade; a casa fornece a outra}

Aqui fecha o protocolo, e a razão de ele funcionar não é nenhuma das etapas: é o que o sistema
\emph{acrescenta}.

\begin{principio}[a casa é o cone]
O que entra no teletransporte é sempre \textbf{meia dualidade} --- uma espiral, uma sombra, um corpo
a que falta a dimensão em que ele era simples. O sistema fornece \textbf{a outra metade}: o cone
onde essa espiral reside, um andar acima. É lá que se entra e se sai \emph{sem prejuízo}, porque lá
a operação é trivial.
\end{principio}

Todas as medidas deste capítulo dizem a mesma coisa por lados diferentes, e agora encaixam:

\begin{itemize}\itemsep2pt
\item a \textbf{espiral} de Arquimedes é a sombra da hélice que vive no cone $x^2+y^2=z^2$ --- em
baixo é uma curva com uma lei; em cima é uma hélice numa superfície, e a lei era a projeção.
\item o \textbf{simplex} de $n{+}1$ vetores, com todos os ângulos a $-1/n$, é a sombra dos $n{+}1$
eixos \emph{ortonormais} de $\R^{n+1}$: as relações de baixo são o que resta da \emph{ausência} de
relações em cima.
\item a \textbf{base oblíqua} de um modelo, que não fecha a reconstrução por $3{,}63\%$, fecha exata
assim que se lhe dá o $+1$ --- e em cima a reconstrução é a \emph{identidade}, sem constante nenhuma.
\item os $1{,}7819$ bits que a \textbf{tradução} parecia perder estão no lado dual, e
$4{,}2181+1{,}7819=6{,}0000$ ao bit.
\end{itemize}

\emph{Em nenhum destes casos se conserta o objeto de baixo.} Não se ortogonaliza a base, não se
endireita a espiral, não se recupera o bit perdido: \textbf{sobe-se}. E ao subir descobre-se que não
havia nada a consertar --- havia uma dimensão a repor, e ela estava toda num sítio só (a normal
$(1,\dots,1)/\sqrt N$, medido a $10^{-15}$).

\begin{obs}[porque é que isto é o banco, e não uma metáfora]
Porque a cifra é ortonormal e \emph{infinita}: cada quociente parcial é uma coordenada nova, e a
expansão nunca termina. O sistema pode sempre fornecer o andar de cima porque nunca fica sem
dimensões para dar. \emph{Um objeto finito precisa de finitas; a casa tem infinitas.} É essa a
assimetria que torna o protocolo possível --- e é literalmente o que o banco faz quando cifra.
\end{obs}

\begin{obs}[o protocolo, então, com cinco etapas]
\textbf{(i)} o objeto entra na fita e recebe endereço e telómero, ambos saídos dele;
\textbf{(ii)} a base em que ele vem lê-se, não se constrói;
\textbf{(iii)} se ela for oblíqua, acrescenta-se o $+1$ que a alinha --- é a casa a fornecer o cone,
e a cifra tem-no sempre;
\textbf{(iv)} atravessa-se por operação com volta exata ($\mathcal F\!\circ\!\mathcal F=n\cdot\mathrm{id}$);
\textbf{(v)} e mede-se o resíduo \emph{de cada etapa}, porque um resíduo que só fecha no fim pode ter
uma etapa que perde e outra que inventa.
\end{obs}

\section{A reprodução: clone e cruzamento}

O teletransporte move um corpo sem o alterar. Falta a operação que o \emph{altera de propósito} ---
que faz de dois corpos um terceiro --- e ela reparte-se em duas, que a arquitetura separa sem
ambiguidade:

\begin{principio}[clone e reprodução]
\textbf{Clone}: pais da mesma forma, resultado idêntico, conferido byte a byte. \emph{Copia e não
gera.} \\
\textbf{Reprodução}: pais de formas \emph{diferentes}, resultado num corpo novo $\R^{\mathrm{lcm}}$
que contém ambos e não é nenhum. \emph{Gera e não copia.}
\end{principio}

\emph{As arquiteturas diferentes não são o obstáculo: são a condição.} Iguais dariam um clone, e o
clone já estava medido --- $934{,}7$~MiB e $338$ genes a saírem da fita sem uma divergência
(\code{tools/fita.c}). O que faltava era a outra.

\paragraph{Onde o filho nasce.} Pelo \code{viveiro.c}, das três operações candidatas só o
cruzamento voa sempre: $\oplus$ tem divisor de zero, $\otimes$ voa apenas se $\gcd(a,b)=1$, e
$\R^a \vee \R^b = \R^{\mathrm{lcm}(a,b)}$ é o \emph{menor} corpo que contém os dois. Medido nos
agentes reais: \code{qwen2.5} ($\R^{1536}$) com \code{llama3.2} ($\R^{2048}$) dá $\R^{6144}$ ---
maior que ambos, e ambos lá dentro.

\emph{E há um caso degenerado que a divisibilidade impõe:} quando um pai é subcorpo do outro
($768 \mid 1536$), o filho sai igual ao maior --- \emph{não nasce nada}, porque a mãe já vivia
dentro do pai. Não é uma falha do cruzamento: é o que $\mathrm{lcm}(a,b)=b$ significa quando
$a \mid b$.

\paragraph{O que se herda.} Não é a dimensão --- essa é só o lugar. É a \textbf{norma}:
$N(a\otimes b) = N(a)\,N(b)$, medido em $7203$ fusões abstratas (\code{tools/fusao.c}) e em $2048$
fusões de \emph{pesos reais} lidos dos dois blobs (\code{tools/cruza.c}), zero falhas nas duas. É o
mesmo cristal do teorema de Hurwitz, agora a atravessar a geração. \emph{Um filhote pode ter a
dimensão dos pais e não herdar nada; o que faz dele um filho é a norma.}

E a parte simétrica \emph{não} é multiplicativa --- $S(A\otimes B)\neq S(A)S(B)$. Quem é
multiplicativa é a norma, que \emph{combina} as duas metades: a dobra antissimétrica mistura, não
separa.

\paragraph{E há uma terceira, que corre.} Cruzar dois da \emph{mesma} forma não é clone nem
reprodução: é \textbf{potência}. O corpo não cresce ($\mathrm{lcm}(a,a)=a$) mas o elemento cresce,
$x\otimes x = x^2$, e $N(x^2)=N(x)^2$ --- o expoente do elemento passa ao da norma. A órbita fecha
no período de \textbf{Pisano}, medido contra um oráculo de $1700$. E num agente a potência é a
\emph{composição}: $A^2$ é o forward do forward. \emph{É a única das três que responde} --- o clone
devolve o que entrou, o filho vive num corpo que nenhum pai habitava --- e por isso é a única que
entra no despacho.

\paragraph{E nada disto precisa de ser duplicado.} Com $\mathcal F(a\circledast b)=\mathcal
F(a)\mathcal F(b)$, o filho \emph{calcula-se} em $n$ operações em vez de $n^2$, e a
\textbf{deconvolução} devolve o pai que falta --- exatamente quando nenhuma casa da transformada se
anula, condição que se mede e não se avisa. Daí saem as \textbf{salas de conversa num só agente}:
vários canais somados na mesma banda, cada um recuperado inteiro pela ortogonalidade dos códigos, e
cabem tantos quantos a dimensão da banda --- nem um a mais.

\begin{obs}[a mesma distinção, no DNA]
O \code{dna.c} já a tinha medido do outro lado. A \textbf{replicação} semiconservativa devolve duas
cópias idênticas à mãe (§N2) --- é o clone. A \textbf{recombinação} de dois genomas dá um terceiro
que não é nenhum deles --- é a reprodução. \emph{Uma conserva o indivíduo; a outra conserva a lei.}
E a lei, nos dois casos, é a parte que sobrevive à dobra.
\end{obs}

\subsection*{O barramento: onde a partilha acontece sem cópia}

A reprodução e a potência dizem o que se faz com dois corpos. Falta dizer \emph{onde} isso acontece
quando há muitos --- e a resposta inverte o desenho habitual.

\begin{principio}[o barramento é a aplicação]
Não há aplicação a mandar em bancos: há um \textbf{barramento}, e bancos que \emph{reagem} ao que
nele passa. Ninguém é chamado pelo nome, ninguém guarda índice de quem tem o quê, e não há
coordenador.
\end{principio}

E não foi preciso inventar peça nenhuma: as três que o sustentam já estavam medidas --- a
\textbf{banda} (quem decodifica é quem tem o tecido, sem lista de membros), a \textbf{cifra} (o
endereço de um dado é a sua cifra, sem tabela de encaminhamento) e a \textbf{liquidação} (toda
entrada dispara verificação, sem laço nem relógio).

\paragraph{Como funciona, medido.} O \code{barramento.c} mede quatro coisas, e cada uma nega uma
peça do desenho habitual:

\begin{itemize}\itemsep2pt
\item \textbf{o dado entra e todos reagem} --- uma emissão, $N$ reações. Nenhum banco é endereçado;
todos veem tudo o que passa.
\item \textbf{onde ele fica decide-se pela cifra} --- o banco é a faixa da cifra, portanto
\emph{o dado diz onde fica ao ser o que é}. Não há repartidor a decidir.
\item \textbf{pedir é emitir} --- quem tem responde, sem saber quem perguntou; e \emph{quem não tem
cala-se}. Não há erro, não há nulo, não há exceção.
\item \textbf{não há registo de quem tem o quê} --- e é isto que fecha: a pergunta ``quem tem este
dado?'' \emph{não tem onde ser feita}. Não é que a resposta seja secreta; é que não existe o sítio.
\end{itemize}

\paragraph{E nas assistentes é literal.} O barramento é \emph{a pasta}: qualquer base que lá esteja
é participante, e não há inscrição. Quando uma assistente esgota o que sabe, ela não decreta ainda
--- \emph{pergunta ao barramento primeiro}, porque ``não sei'' é o fim do que \emph{ela} sabe e não
o fim do assunto. Desce nas outras bases pelo mesmo caminho de símbolos, e \textbf{aprende com a
resposta}: da próxima já responde sozinha.

\emph{E a escolha entre as que respondem não é pela profundidade.} Ficar com a mais funda seria
ficar com a \textbf{marginal} --- a frequência, e mais nada. A escolha é pela \emph{contração}: o
que a candidata partilha com o caminho já andado, medido em $115\,871$ decisões (\code{bairro.c}).
\emph{A vizinhança tem de pagar para valer.}

\begin{obs}[porque é que isto é a mesma lei]
O barramento é a banda do §\,anterior vista de outro lado: \emph{muitos falando em um}, separados
não pelo tempo nem por um repartidor, mas por serem o que são. Na banda o que separa é a
ortogonalidade dos códigos; no barramento é a cifra do dado. \emph{Em nenhum dos dois há quem
decida} --- e é por isso que a partilha acontece sem cópia e sem coordenação.
\end{obs}

\section{O catálogo: onde mora a especificidade}

A teoria acima é global: o corpo autossimilar $\R^n$ e a dualidade. (O \emph{corpo diferencial},
que os contém a todos por quociente e é a instância máxima, está tratado no catálogo --- ver as
entradas \code{tools/dif.c} e \code{tools/milenio.c}.) \emph{Ela não depende de nenhum corpo em particular} --- e é essa a
razão de ela caber em poucas páginas.

A especificidade mora noutro sítio, e de propósito. Cada corpo concreto, cada medidor e cada
resíduo estão no \textbf{\code{catalogo.tex}} (e, em Markdown, em \code{tools/LEIAME.md}), uma
entrada por peça. Quem quiser a lei lê este documento; quem quiser um corpo em particular abre o
catálogo na entrada dele.

\emph{Teoria global por cima, especificidade por baixo.} A separação não é de arrumação: é o que
permite acrescentar um corpo novo sem tocar na teoria, e corrigir a teoria sem reescrever
duzentas entradas.

\section{A construção, na base ortonormal}

Tudo o que este paper mede pode ser dito de uma vez, e numa base só. Ela é \textbf{ortonormal}
porque as duas metades estão medidas separadamente e se encontram:

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{ortogonal} & os primos são os eixos & \code{base\_local.c}\\
 & independentes, geradores, e mexer no eixo $i$ &\\
 & muda \emph{só} a coordenada $i$ --- não conversam &\\[3pt]
\textbf{normal} & $|N|=1$ em toda a órbita, de todo metal & \code{normal\_circulo.c}\\
 & e $N(\sigma^k)=(-1)^k$ \emph{exatamente} &\\
\bottomrule
\end{tabular}
\end{center}

\noindent A partir daqui, cada peça da teoria é uma frase sobre coordenadas nessa base.

\paragraph{O que a base contém.}
\begin{itemize}\itemsep3pt
\item \textbf{Os eixos} são os primos. Uma classe quadrática é um vetor sobre $\F_2$ --- um bit por
primo ---, e multiplicar classes \emph{é} XOR nas coordenadas. A dimensão é o número de primos.
\item \textbf{Os pontos} são os corpos locais $\Q(\sigma_m)$, invariantes: elevar não sai, e
$\sigma^k$ é sempre combinação inteira de $1$ e $\sigma$. É isto que faz de cada um uma direção
própria --- \emph{a potência é ele mesmo}.
\item \textbf{A norma} normaliza: $\sigma\sigma'=-1$ em \emph{toda} a família, e daí
$|N(\sigma^k)|=1$ sempre. Os metais afastam-se pela \emph{soma} $\sigma+\sigma'=m$ e não se afastam
nada pelo \emph{produto}. É o ponto que todos partilham.
\item \textbf{A torção} é a classe racional: $p/q$ tem classe $\mathrm{cl}(pq)$, que é um vetor na
mesma base. O racional não é objeto novo --- é uma coordenada.
\end{itemize}

\paragraph{O que se move nela, e como.} Duas réguas, dois papéis, e a distinção é medida:
\[
  \underbrace{\text{PA}}_{\text{dentro de um eixo}}\quad\text{soma, e a coordenada não muda}
  \qquad\qquad
  \underbrace{\text{PG}}_{\text{entre eixos}}\quad\text{multiplica classes, e atravessa a base}
\]
Nenhuma das duas alcança o ponto fixo $\sigma_m$ --- com parâmetros racionais só alcançam
racionais, e $m^2+4$ nunca é quadrado. \emph{O buraco de ambas é exatamente onde o metal mira}, e é
por isso que é o metal que preenche: ele tem o passo da PA e o crescimento da PG
(\code{buracos.c}).

\paragraph{O que se constrói com ela.}
\begin{itemize}\itemsep3pt
\item \textbf{A cifra.} $\det A_m=-1$ é a condição exata de a inversa ser inteira, logo toda a
família é reversível sem resto; a chave é $(m,k)$ e as chaves somam zero no expoente; e somar
cifrado é cifrar a soma. \emph{Limite dito:} $A_m$ é linear --- é codificação reversível e
homomórfica, não sigilo (\code{familia\_real.c}).
\item \textbf{O corpo.} A torre $\R^n$, com o cruzamento $\mathrm{lcm}$ no eixo da dimensão e XOR
no eixo do metal --- os dois eixos \emph{não} cruzam igual, e é a idempotência de um contra a
involução do outro que os separa (\code{tools/cruzamento\_geral.c}).
\item \textbf{A transformada.} Os caracteres são a órbita da torção, e a soma sobre a órbita
inteira colapsa num ponto: $\sum_k\chi_k(j)\chi_{-k}(j')=n\delta_{j,j'}$. Unitária, logo
$x=0\iff\F x=0$ (\code{tools/transformada.c}).
\item \textbf{A máquina.} No SQL as duas réguas apareceram literalmente: a PA varreu o endereço
(bytecode de $O(\text{linhas})$ para $O(1)$) e a PG operou o valor sobre classes racionais, sem uma
única divisão.
\end{itemize}

\begin{principio}[a base é a mesma em toda escala]
Não há peça nova em nível nenhum: os eixos são os primos, os pontos são os invariantes locais, e a
norma põe todos no mesmo círculo. O que muda de escala para escala é \emph{quantas coordenadas} se
usam --- nunca a natureza delas. É a auto-similaridade lida na base, e é por isso que a mesma peça
serve o corpo, a cifra, a transformada e a máquina.
\end{principio}

\paragraph{E o que \emph{não} está fechado, para não passar por fechado.}
\begin{itemize}\itemsep2pt
\item O sigilo tem modelo, e é a dimensão da chave --- não uma suposição de dificuldade. O que
\emph{não} está fechado é o dimensionamento na prática: quantos eixos por cliente, e como garantir
que o material de chave excede o dado. A álgebra está medida; a política de chave, não.
\item O alcance da operação racional: exata até ao teto da palavra --- com $101/100$, quatro passos
garantidos pela guarda conservadora. É piso, não teto exato.
\item (Corrigido.) A primeira versão cortava a base em $12$ primos e \emph{contava} os que ``não
cabiam''. Não havia o que não coubesse: havia régua cortada. A régua é infinita por natureza; o
objeto é que é finito, e é ele que faz a conta parar.
\item E os $18$ medidores que ainda usam idioma próprio e não emitem unidade endereçável.
\end{itemize}

\section{A matemática, em forma}

Tudo o que este paper afirma foi medido, e as medidas estão espalhadas pelas observações que
acompanham cada seção. Aqui elas ficam \emph{em forma}: definições, e teoremas com o medidor que os
fecha. Nada é novo neste parágrafo --- é o mesmo material, dito de uma vez, para que se possa conferir
sem reler o percurso. Cada enunciado tem resíduo $0$ ou não estaria aqui.

\paragraph{As definições.}
\begin{itemize}\itemsep2pt
\item \textbf{A peça.} $A_m=\left(\begin{smallmatrix}m&1\\1&0\end{smallmatrix}\right)$, o \emph{gato},
com $\det A_m=-1$; o seu ponto fixo é a realimentação $\sigma=m+1/\sigma$, raiz de $x^2-mx-1$. O
\emph{esquilo} é $G=\left(\begin{smallmatrix}0&-1\\1&0\end{smallmatrix}\right)$, $\det G=+1$.
\item \textbf{As duas formas.} $\Omega=\left(\begin{smallmatrix}0&1\\-1&0\end{smallmatrix}\right)$
(antissimétrica) e $M=\left(\begin{smallmatrix}-2&m\\m&2\end{smallmatrix}\right)$ (simétrica, o
\emph{prensor}), esta última obtida das direções nulas $v_1=(\sigma,1)$, $v_2=(\sigma',1)$ e exata
porque $\sigma\sigma'=-1$ e $\sigma+\sigma'=m$.
\item \textbf{A cisão.} $b\mapsto(b\,\&\,\mathtt{par},\;b\,\&\,\mathtt{impar})$, e a
\textbf{assinatura} $b\mapsto(\mathrm{pop}(b\,\&\,\mathtt{par}),\,\mathrm{pop}(b\,\&\,\mathtt{impar}))$.
A \textbf{semente} de $b$ é a posição do seu subconjunto na ordem combinatória de cada metade.
\end{itemize}

\paragraph{Os teoremas.}
\begin{itemize}\itemsep3pt
\item[\textbf{T1}] \emph{O mesmo flip nas duas formas.} $A^{\!\top}\Omega A=\det(A)\,\Omega=-\Omega$ e
$A^{\!\top}MA=\det(A)\,M=-M$. Uma identidade, dois retratos: a antissimétrica dá o volume e a lei de
potência, a simétrica dá o cone e a métrica $(1,1)$ --- o gato é \textbf{anti}-isometria.
\hfill{\small\code{tools/antissimetrico.c}, \code{chicote.c}}
\item[\textbf{T2}] \emph{A antissimétrica é a única que atravessa.} Não-degenerada apenas em dimensão
par, e ali \emph{uma só classe} (contagem $=|GL_n|/|Sp_n|$; base simplética construída); a simétrica
divide-se em $\ge2$ classes em $\F_p$ e $n+1$ em $\R$.
\hfill{\small\code{tools/antissimetrico.c}}
\item[\textbf{T3}] \emph{Compor é contrair, e a forma $k$-ária não dá poder.} $(A\!\cdot\!B)^i{}_k=\sum_j
A^i{}_jB^j{}_k$, e as $\mathrm{Cat}(k-1)$ parentizações concordam todas --- o ganho é não ter de
inventar ordem onde não há.
\hfill{\small\code{chicote.c}}
\item[\textbf{T4}] \emph{O cone é o par de atratores, e a órbita não o alcança nem escapa.} $Q=p^{\!\top}Mp$
anula-se exatamente em $(\sigma,1)$ e $(\sigma',1)$; $|Q|$ é invariante (inteiro), e
$|c_2/c_1|$ multiplica-se por $1/\sigma^2$ a cada batida, com $\Delta(\log r)=\log\sigma$ --- espiral
logarítmica, e áurea em $m=1$.
\hfill{\small\code{prensor.c}}
\item[\textbf{T5}] \emph{A ponta é o $0$, e a dinâmica não a cruza.} $p=0$ é o único ponto fixo e a
única passagem reversível; $Q$ troca de sinal a cada batida sem nunca se anular --- salta, não cruza.
E as batidas duais fecham em três: $G^3=G^{-1}$, $G^4=I$.
\hfill{\small\code{prensor.c}}
\item[\textbf{T6}] \emph{Toda cisão é partição.} $\mathrm{pop}(b\&\mathtt{par})+\mathrm{pop}(b\&\mathtt{impar})
=\mathrm{pop}(b)$, os cacos recompõem por $\vee$ e têm interseção vazia --- em $4$, $8$, $16$ e $32$
bits, e no nó de Kirchhoff do lado analógico, que é a \emph{mesma} conta. Quebrar não cria nem some.
\hfill{\small\code{espelho.c}}
\item[\textbf{T7}] \emph{Atravessar não perde.} $(x,y)\mapsto(mx+y,x)$ é permutação módulo $2^L$,
porque $\det A=-1$ é unidade; e a inversa colhe-se da borda, sem Fermat.
\hfill{\small\code{espelho.c}}
\item[\textbf{T8}] \emph{Assinatura mais semente devolve a obra.} A assinatura diz \emph{quantos} e
cobre todas as obras; a semente diz \emph{quais}; o inverso reconstrói \textbf{exatamente}, bit a bit.
O espaço de sementes de uma classe é o seu próprio tamanho, $\binom4p\binom4i$, e a soma é o todo ---
logo \emph{a semente é maior onde a classe é maior}.
\hfill{\small\code{semente.c}, \code{assinatura.c}}
\item[\textbf{T9}] \emph{O colapso tem número.} Descartar a semente leva $256$ a $25$: $231$ obras
deixam de existir como elas mesmas. No limite, semente zero é uma assinatura só para todos.
\hfill{\small\code{semente.c}}
\item[\textbf{T10}] \emph{O selo.} A chave do sistema é \textbf{emaranhada} (posto $2$ em todo corte,
não se escreve como produto) e a dos clientes é \textbf{dividida} (posto $1$, um fator por parte); as
partes somam zero (as fases são raízes da unidade); recupera-se por \emph{diferenças}, que são
invariantes sob deslocamento global --- e a origem só a chave do sistema fixa. Sem ela, o que falta é
livre no espaço inteiro; com ela, é única.
\hfill{\small\code{selo.c}}
\item[\textbf{T11}] \emph{O instrumento faz a partição, e as partições formam retículo.} Sem lei o
grupo é $S_N$ e nada distingue; com lei sobra o centralizador $d^cc!$. Instrumentos distintos dão
resoluções distintas que \textbf{refinam} umas às outras, nunca se cruzam.
\hfill{\small\code{instrumento.c}}
\item[\textbf{T12}] \emph{O significado é uma janela.} Há significado se e só se
$1<\#\text{classes}<\#\text{pontos}$, com lei que \textbf{conserva}: no repouso tudo é invariante e
nada nomeia; na transitividade tudo é uma classe e nada distingue.
\hfill{\small\code{significado.c}}
\end{itemize}

\begin{principio}[o critério que organiza o resto]
\emph{O que pode ser descrito, descreve-se; o que não pode, guarda-se como semente de reconstrução.}

Os dois papéis são distintos e nenhum é falha do outro: o gato \textbf{recupera} (T7: bijeção, nada
some) e a assinatura \textbf{nomeia} (T8: agrupa, por projeto). A parte que a régua não alcança não é
defeito --- é o que faz a obra ser aquela, e é por guardá-la que a reconstrução é exata (T8). Descartar
o que não coube na descrição é o que T9 mede: cristalização, e o preço é a individualidade.
E vale o critério do necrotério (§8): morto é o que foi reduzido a uma igualdade isolada entre estados;
vivo é o que guarda a dinâmica que o gera. \emph{Um cemitério exige que todos sejam iguais para caberem
na mesma caixa --- e é exatamente por isso que a caixa não serve aqui.}
\end{principio}


\begin{thebibliography}{99}
\bibitem{hurwitz} A.~Hurwitz, \emph{Über die Composition der quadratischen Formen von beliebig
vielen Variablen}, Nachr. Ges. Wiss. Göttingen (1898), 309--316.
\bibitem{adams} J.~F.~Adams, \emph{Vector fields on spheres}, Ann. of Math. \textbf{75} (1962),
603--632.
\bibitem{kw} L.~Kronecker (1853); H.~Weber (1886); D.~Hilbert, \emph{Ein neuer Beweis des
Kronecker'schen Fundamentalsatzes}, Nachr. Ges. Wiss. Göttingen (1896).
\bibitem{ramanujan} S.~Ramanujan, \emph{Modular equations and approximations to $\pi$}, Quart. J.
Math. \textbf{45} (1914), 350--372.
\bibitem{rogers} L.~J.~Rogers, \emph{Second memoir on the expansion of certain infinite products},
Proc. London Math. Soc. \textbf{25} (1894), 318--343.
\bibitem{siegel} C.~L.~Siegel, \emph{Algebraic integers whose conjugates lie in the unit circle},
Duke Math. J. \textbf{11} (1944), 597--602.
\bibitem{lagrange} J.-L.~Lagrange, \emph{Additions au mémoire sur la résolution des équations
numériques} (1770).
\bibitem{cayley} A.~Cayley (1845); L.~E.~Dickson, \emph{On quaternions and their generalization},
Ann. of Math. \textbf{20} (1919), 155--171.
\bibitem{pontryagin} L.~S.~Pontryagin, \emph{Topological Groups}, Princeton Univ. Press (1939).
\bibitem{peirce} B.~Peirce, \emph{Linear associative algebra}, Amer. J. Math. \textbf{4} (1881),
97--229.
\bibitem{isserlis} L.~Isserlis, \emph{On a formula for the product-moment coefficient}, Biometrika
\textbf{12} (1918), 134--139.
\bibitem{schrodinger} E.~Schrödinger, \emph{What is Life?}, Cambridge Univ. Press (1944).
\bibitem{jw} P.~Jordan, E.~Wigner, \emph{Über das Paulische Äquivalenzverbot}, Z. Physik
\textbf{47} (1928), 631--651.
\bibitem{jbb} M.~H.~Jensen, P.~Bak, T.~Bohr, \emph{Transition to chaos by interaction of
resonances in dissipative systems}, Phys. Rev. A \textbf{30} (1984), 1960--1969.
\bibitem{takahashi} I.~Takahashi, T.~Noguchi, \emph{A new quick-response and high-efficiency
control strategy of an induction motor}, IEEE Trans. Ind. Appl. \textbf{IA-22} (1986), 820--827.
\bibitem{turing} A.~M.~Turing, \emph{On computable numbers, with an application to the
Entscheidungsproblem}, Proc. London Math. Soc. \textbf{42} (1937), 230--265.
\bibitem{wiles} A.~Wiles, \emph{Modular elliptic curves and Fermat's Last Theorem}, Ann. of Math.
\textbf{141} (1995), 443--551.
\bibitem{perelman} G.~Perelman, \emph{The entropy formula for the Ricci flow}, arXiv:math/0211159
(2002).
\end{thebibliography}


\input{dicionario}

\end{document}
